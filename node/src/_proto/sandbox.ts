// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.29.6
// source: sandbox.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "openshell.sandbox.v1";

/** Scope that currently controls a setting. */
export enum SettingScope {
  SETTING_SCOPE_UNSPECIFIED = 0,
  SETTING_SCOPE_SANDBOX = 1,
  SETTING_SCOPE_GLOBAL = 2,
  UNRECOGNIZED = -1,
}

export function settingScopeFromJSON(object: any): SettingScope {
  switch (object) {
    case 0:
    case "SETTING_SCOPE_UNSPECIFIED":
      return SettingScope.SETTING_SCOPE_UNSPECIFIED;
    case 1:
    case "SETTING_SCOPE_SANDBOX":
      return SettingScope.SETTING_SCOPE_SANDBOX;
    case 2:
    case "SETTING_SCOPE_GLOBAL":
      return SettingScope.SETTING_SCOPE_GLOBAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SettingScope.UNRECOGNIZED;
  }
}

export function settingScopeToJSON(object: SettingScope): string {
  switch (object) {
    case SettingScope.SETTING_SCOPE_UNSPECIFIED:
      return "SETTING_SCOPE_UNSPECIFIED";
    case SettingScope.SETTING_SCOPE_SANDBOX:
      return "SETTING_SCOPE_SANDBOX";
    case SettingScope.SETTING_SCOPE_GLOBAL:
      return "SETTING_SCOPE_GLOBAL";
    case SettingScope.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Source used for the policy payload in GetSandboxConfigResponse. */
export enum PolicySource {
  POLICY_SOURCE_UNSPECIFIED = 0,
  POLICY_SOURCE_SANDBOX = 1,
  POLICY_SOURCE_GLOBAL = 2,
  UNRECOGNIZED = -1,
}

export function policySourceFromJSON(object: any): PolicySource {
  switch (object) {
    case 0:
    case "POLICY_SOURCE_UNSPECIFIED":
      return PolicySource.POLICY_SOURCE_UNSPECIFIED;
    case 1:
    case "POLICY_SOURCE_SANDBOX":
      return PolicySource.POLICY_SOURCE_SANDBOX;
    case 2:
    case "POLICY_SOURCE_GLOBAL":
      return PolicySource.POLICY_SOURCE_GLOBAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PolicySource.UNRECOGNIZED;
  }
}

export function policySourceToJSON(object: PolicySource): string {
  switch (object) {
    case PolicySource.POLICY_SOURCE_UNSPECIFIED:
      return "POLICY_SOURCE_UNSPECIFIED";
    case PolicySource.POLICY_SOURCE_SANDBOX:
      return "POLICY_SOURCE_SANDBOX";
    case PolicySource.POLICY_SOURCE_GLOBAL:
      return "POLICY_SOURCE_GLOBAL";
    case PolicySource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Sandbox security policy configuration. */
export interface SandboxPolicy {
  /** Policy version. */
  version: number;
  /** Filesystem access policy. */
  filesystem?:
    | FilesystemPolicy
    | undefined;
  /** Landlock configuration. */
  landlock?:
    | LandlockPolicy
    | undefined;
  /** Process execution policy. */
  process?:
    | ProcessPolicy
    | undefined;
  /** Network access policies keyed by name (e.g. "claude_code", "gitlab"). */
  networkPolicies: { [key: string]: NetworkPolicyRule };
}

export interface SandboxPolicy_NetworkPoliciesEntry {
  key: string;
  value?: NetworkPolicyRule | undefined;
}

/** Filesystem access policy. */
export interface FilesystemPolicy {
  /** Automatically include the workdir as read-write. */
  includeWorkdir: boolean;
  /** Read-only directory allow list. */
  readOnly: string[];
  /** Read-write directory allow list. */
  readWrite: string[];
}

/** Landlock policy configuration. */
export interface LandlockPolicy {
  /** Compatibility mode (e.g. "best_effort", "hard_requirement"). */
  compatibility: string;
}

/** Process execution policy. */
export interface ProcessPolicy {
  /** User name to run the sandboxed process as. */
  runAsUser: string;
  /** Group name to run the sandboxed process as. */
  runAsGroup: string;
}

/** A named network access policy rule. */
export interface NetworkPolicyRule {
  /** Human-readable name for this policy rule. */
  name: string;
  /** Allowed endpoint (host:port) pairs. */
  endpoints: NetworkEndpoint[];
  /** Allowed binary identities. */
  binaries: NetworkBinary[];
}

/** A network endpoint (host + port) with optional L7 inspection config. */
export interface NetworkEndpoint {
  /**
   * Hostname or host glob pattern. Exact match is case-insensitive.
   * Glob patterns use "." as delimiter: "*.example.com" matches a single
   * subdomain label, "**.example.com" matches across labels.
   */
  host: string;
  /**
   * Single port (backwards compat). Use `ports` for multiple ports.
   * Mutually exclusive with `ports` — if both are set, `ports` takes precedence.
   */
  port: number;
  /** Application protocol for L7 inspection: "rest", "websocket", "graphql", "sql", or "" (L4-only). */
  protocol: string;
  /** TLS handling: "terminate" or "passthrough" (default). */
  tls: string;
  /** Enforcement mode: "enforce" or "audit" (default). */
  enforcement: string;
  /**
   * Access preset shorthand: "read-only", "read-write", "full".
   * Mutually exclusive with rules.
   */
  access: string;
  /** Explicit L7 rules (mutually exclusive with access). */
  rules: L7Rule[];
  /**
   * Allowed resolved IP addresses or CIDR ranges for this endpoint.
   * When non-empty, the SSRF internal-IP check is replaced by an allowlist check:
   *   - If host is also set: domain must resolve to an IP in this list.
   *   - If host is empty: any domain is allowed as long as it resolves to an IP in this list.
   * Supports exact IPs ("10.0.5.20") and CIDR notation ("10.0.5.0/24").
   * Loopback (127.0.0.0/8) and link-local (169.254.0.0/16) are always blocked
   * regardless of this field.
   */
  allowedIps: string[];
  /**
   * Multiple ports. When non-empty, this endpoint covers all listed ports.
   * If `port` is set and `ports` is empty, `port` is normalized to `ports: [port]`.
   * If both are set, `ports` takes precedence.
   */
  ports: number[];
  /**
   * Explicit L7 deny rules. When present, requests matching any deny rule
   * are blocked even if they match an allow rule or access preset.
   * Deny rules take precedence over allow rules.
   */
  denyRules: L7DenyRule[];
  /**
   * When true, percent-encoded '/' (%2F) is preserved in path segments
   * rather than rejected by the L7 path canonicalizer. Required for
   * upstreams like GitLab that embed %2F in namespaced resource paths.
   * Defaults to false (strict).
   */
  allowEncodedSlash: boolean;
  /**
   * GraphQL persisted-query behavior for hash-only/saved-query requests:
   * "deny" (default) or "allow_registered".
   */
  persistedQueries: string;
  /**
   * Trusted GraphQL persisted-query registry keyed by hash or service-specific ID.
   * Only used when persisted_queries is "allow_registered".
   */
  graphqlPersistedQueries: { [key: string]: GraphqlOperation };
  /**
   * Maximum GraphQL request body bytes to buffer for inspection.
   * Defaults to 65536 when unset.
   */
  graphqlMaxBodyBytes: number;
  /**
   * Optional HTTP path glob that scopes this L7 endpoint on shared host:port APIs.
   * Example: use path "/graphql" for protocol "graphql" and "/repos/**" for
   * protocol "rest" when both surfaces live under api.example.com:443.
   * Empty means all paths.
   */
  path: string;
  /**
   * When true on a "rest" endpoint, OpenShell rewrites credential placeholders
   * inside client-to-server WebSocket text messages after an allowed HTTP 101
   * upgrade. Defaults to false.
   */
  websocketCredentialRewrite: boolean;
  /**
   * When true on a "rest" endpoint, OpenShell rewrites credential placeholders
   * inside supported textual HTTP request bodies before forwarding upstream.
   * Defaults to false.
   */
  requestBodyCredentialRewrite: boolean;
}

export interface NetworkEndpoint_GraphqlPersistedQueriesEntry {
  key: string;
  value?: GraphqlOperation | undefined;
}

/** Trusted GraphQL operation classification. */
export interface GraphqlOperation {
  /** Operation type: "query", "mutation", or "subscription". */
  operationType: string;
  /** Operation name, if known. */
  operationName: string;
  /** Root field names selected by the operation. */
  fields: string[];
}

/**
 * An L7 deny rule that blocks specific requests.
 * Mirrors L7Allow — same fields, same matching semantics, inverted effect.
 * Deny rules are evaluated after allow rules and take precedence.
 */
export interface L7DenyRule {
  /** HTTP method (REST): GET, POST, etc. or "*" for any. */
  method: string;
  /** URL path glob pattern (REST): "/repos/* /pulls/* /reviews", "**" for any. */
  path: string;
  /** SQL command (SQL): SELECT, INSERT, etc. or "*" for any. */
  command: string;
  /**
   * Query parameter matcher map (REST).
   * Same semantics as L7Allow.query.
   */
  query: { [key: string]: L7QueryMatcher };
  /** GraphQL operation type: "query", "mutation", "subscription", or "*" for any. */
  operationType: string;
  /** GraphQL operation name glob. "*" matches any operation name. */
  operationName: string;
  /**
   * GraphQL root field globs. Deny rules match when any selected root field
   * matches any configured glob.
   */
  fields: string[];
}

export interface L7DenyRule_QueryEntry {
  key: string;
  value?: L7QueryMatcher | undefined;
}

/** An L7 policy rule (allow-only). */
export interface L7Rule {
  allow?: L7Allow | undefined;
}

/** Allowed action definition for L7 rules. */
export interface L7Allow {
  /** HTTP method (REST): GET, POST, etc. or "*" for any. */
  method: string;
  /** URL path glob pattern (REST): "/repos/**", "**" for any. */
  path: string;
  /** SQL command (SQL): SELECT, INSERT, etc. or "*" for any. */
  command: string;
  /**
   * Query parameter matcher map (REST).
   * Key is the decoded query parameter name (case-sensitive).
   * Value supports either a single glob (`glob`) or a list (`any`).
   */
  query: { [key: string]: L7QueryMatcher };
  /** GraphQL operation type: "query", "mutation", "subscription", or "*" for any. */
  operationType: string;
  /** GraphQL operation name glob. "*" matches any operation name. */
  operationName: string;
  /**
   * GraphQL root field globs. Allow rules match only when every selected root
   * field matches one of the configured globs. Omit to match all fields.
   */
  fields: string[];
}

export interface L7Allow_QueryEntry {
  key: string;
  value?: L7QueryMatcher | undefined;
}

/** Query value matcher for one query parameter key. */
export interface L7QueryMatcher {
  /** Single glob pattern. */
  glob: string;
  /** Any-of glob patterns. */
  any: string[];
}

/** A binary identity for network policy matching. */
export interface NetworkBinary {
  path: string;
  /**
   * Deprecated: the harness concept has been removed. This field is ignored.
   *
   * @deprecated
   */
  harness: boolean;
}

/** Request to get sandbox settings by sandbox ID. */
export interface GetSandboxConfigRequest {
  /** The sandbox ID. */
  sandboxId: string;
}

/** Request to get gateway-global settings. */
export interface GetGatewayConfigRequest {
}

/** Response containing gateway-global settings. */
export interface GetGatewayConfigResponse {
  /**
   * Gateway-global settings map excluding the reserved policy key.
   * Registered keys without a configured value are returned with an empty SettingValue.
   */
  settings: { [key: string]: SettingValue };
  /** Monotonically increasing revision for gateway-global settings. */
  settingsRevision: number;
}

export interface GetGatewayConfigResponse_SettingsEntry {
  key: string;
  value?: SettingValue | undefined;
}

/** Type-aware setting value for sandbox/gateway settings. */
export interface SettingValue {
  value?:
    | { $case: "stringValue"; stringValue: string }
    | { $case: "boolValue"; boolValue: boolean }
    | { $case: "intValue"; intValue: number }
    | { $case: "bytesValue"; bytesValue: Buffer }
    | undefined;
}

/** Effective setting value and the scope it was resolved from. */
export interface EffectiveSetting {
  value?: SettingValue | undefined;
  scope: SettingScope;
}

/** Response containing effective sandbox settings and policy. */
export interface GetSandboxConfigResponse {
  /** The sandbox policy configuration. */
  policy?:
    | SandboxPolicy
    | undefined;
  /** Current policy version (monotonically increasing per sandbox). */
  version: number;
  /** SHA-256 hash of the serialized policy payload. */
  policyHash: string;
  /**
   * Effective settings resolved for this sandbox, excluding the reserved policy key.
   * Registered keys without a configured value are returned with an empty EffectiveSetting.value.
   */
  settings: { [key: string]: EffectiveSetting };
  /** Fingerprint for effective config (policy + settings). Changes when any effective input changes. */
  configRevision: number;
  /** Source of the policy payload for this response. */
  policySource: PolicySource;
  /**
   * When policy_source is GLOBAL, the version of the global policy revision.
   * Zero when no global policy is active or when policy_source is SANDBOX.
   */
  globalPolicyVersion: number;
  /**
   * Fingerprint for provider credential inputs attached to this sandbox.
   * Changes when attached provider names or attached provider records change.
   */
  providerEnvRevision: number;
}

export interface GetSandboxConfigResponse_SettingsEntry {
  key: string;
  value?: EffectiveSetting | undefined;
}

function createBaseSandboxPolicy(): SandboxPolicy {
  return { version: 0, filesystem: undefined, landlock: undefined, process: undefined, networkPolicies: {} };
}

export const SandboxPolicy: MessageFns<SandboxPolicy> = {
  encode(message: SandboxPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.filesystem !== undefined) {
      FilesystemPolicy.encode(message.filesystem, writer.uint32(18).fork()).join();
    }
    if (message.landlock !== undefined) {
      LandlockPolicy.encode(message.landlock, writer.uint32(26).fork()).join();
    }
    if (message.process !== undefined) {
      ProcessPolicy.encode(message.process, writer.uint32(34).fork()).join();
    }
    globalThis.Object.entries(message.networkPolicies).forEach(([key, value]: [string, NetworkPolicyRule]) => {
      SandboxPolicy_NetworkPoliciesEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filesystem = FilesystemPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.landlock = LandlockPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.process = ProcessPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = SandboxPolicy_NetworkPoliciesEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.networkPolicies[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxPolicy {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      filesystem: isSet(object.filesystem) ? FilesystemPolicy.fromJSON(object.filesystem) : undefined,
      landlock: isSet(object.landlock) ? LandlockPolicy.fromJSON(object.landlock) : undefined,
      process: isSet(object.process) ? ProcessPolicy.fromJSON(object.process) : undefined,
      networkPolicies: isObject(object.networkPolicies)
        ? (globalThis.Object.entries(object.networkPolicies) as [string, any][]).reduce(
          (acc: { [key: string]: NetworkPolicyRule }, [key, value]: [string, any]) => {
            acc[key] = NetworkPolicyRule.fromJSON(value);
            return acc;
          },
          {},
        )
        : isObject(object.network_policies)
        ? (globalThis.Object.entries(object.network_policies) as [string, any][]).reduce(
          (acc: { [key: string]: NetworkPolicyRule }, [key, value]: [string, any]) => {
            acc[key] = NetworkPolicyRule.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: SandboxPolicy): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.filesystem !== undefined) {
      obj.filesystem = FilesystemPolicy.toJSON(message.filesystem);
    }
    if (message.landlock !== undefined) {
      obj.landlock = LandlockPolicy.toJSON(message.landlock);
    }
    if (message.process !== undefined) {
      obj.process = ProcessPolicy.toJSON(message.process);
    }
    if (message.networkPolicies) {
      const entries = globalThis.Object.entries(message.networkPolicies) as [string, NetworkPolicyRule][];
      if (entries.length > 0) {
        obj.networkPolicies = {};
        entries.forEach(([k, v]) => {
          obj.networkPolicies[k] = NetworkPolicyRule.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxPolicy>, I>>(base?: I): SandboxPolicy {
    return SandboxPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxPolicy>, I>>(object: I): SandboxPolicy {
    const message = createBaseSandboxPolicy();
    message.version = object.version ?? 0;
    message.filesystem = (object.filesystem !== undefined && object.filesystem !== null)
      ? FilesystemPolicy.fromPartial(object.filesystem)
      : undefined;
    message.landlock = (object.landlock !== undefined && object.landlock !== null)
      ? LandlockPolicy.fromPartial(object.landlock)
      : undefined;
    message.process = (object.process !== undefined && object.process !== null)
      ? ProcessPolicy.fromPartial(object.process)
      : undefined;
    message.networkPolicies = (globalThis.Object.entries(object.networkPolicies ?? {}) as [string, NetworkPolicyRule][])
      .reduce((acc: { [key: string]: NetworkPolicyRule }, [key, value]: [string, NetworkPolicyRule]) => {
        if (value !== undefined) {
          acc[key] = NetworkPolicyRule.fromPartial(value);
        }
        return acc;
      }, {});
    return message;
  },
};

function createBaseSandboxPolicy_NetworkPoliciesEntry(): SandboxPolicy_NetworkPoliciesEntry {
  return { key: "", value: undefined };
}

export const SandboxPolicy_NetworkPoliciesEntry: MessageFns<SandboxPolicy_NetworkPoliciesEntry> = {
  encode(message: SandboxPolicy_NetworkPoliciesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      NetworkPolicyRule.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxPolicy_NetworkPoliciesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxPolicy_NetworkPoliciesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = NetworkPolicyRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxPolicy_NetworkPoliciesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? NetworkPolicyRule.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: SandboxPolicy_NetworkPoliciesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = NetworkPolicyRule.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxPolicy_NetworkPoliciesEntry>, I>>(
    base?: I,
  ): SandboxPolicy_NetworkPoliciesEntry {
    return SandboxPolicy_NetworkPoliciesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxPolicy_NetworkPoliciesEntry>, I>>(
    object: I,
  ): SandboxPolicy_NetworkPoliciesEntry {
    const message = createBaseSandboxPolicy_NetworkPoliciesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? NetworkPolicyRule.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseFilesystemPolicy(): FilesystemPolicy {
  return { includeWorkdir: false, readOnly: [], readWrite: [] };
}

export const FilesystemPolicy: MessageFns<FilesystemPolicy> = {
  encode(message: FilesystemPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.includeWorkdir !== false) {
      writer.uint32(8).bool(message.includeWorkdir);
    }
    for (const v of message.readOnly) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.readWrite) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilesystemPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilesystemPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.includeWorkdir = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.readOnly.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.readWrite.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilesystemPolicy {
    return {
      includeWorkdir: isSet(object.includeWorkdir)
        ? globalThis.Boolean(object.includeWorkdir)
        : isSet(object.include_workdir)
        ? globalThis.Boolean(object.include_workdir)
        : false,
      readOnly: globalThis.Array.isArray(object?.readOnly)
        ? object.readOnly.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.read_only)
        ? object.read_only.map((e: any) => globalThis.String(e))
        : [],
      readWrite: globalThis.Array.isArray(object?.readWrite)
        ? object.readWrite.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.read_write)
        ? object.read_write.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: FilesystemPolicy): unknown {
    const obj: any = {};
    if (message.includeWorkdir !== false) {
      obj.includeWorkdir = message.includeWorkdir;
    }
    if (message.readOnly?.length) {
      obj.readOnly = message.readOnly;
    }
    if (message.readWrite?.length) {
      obj.readWrite = message.readWrite;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilesystemPolicy>, I>>(base?: I): FilesystemPolicy {
    return FilesystemPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilesystemPolicy>, I>>(object: I): FilesystemPolicy {
    const message = createBaseFilesystemPolicy();
    message.includeWorkdir = object.includeWorkdir ?? false;
    message.readOnly = object.readOnly?.map((e) => e) || [];
    message.readWrite = object.readWrite?.map((e) => e) || [];
    return message;
  },
};

function createBaseLandlockPolicy(): LandlockPolicy {
  return { compatibility: "" };
}

export const LandlockPolicy: MessageFns<LandlockPolicy> = {
  encode(message: LandlockPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.compatibility !== "") {
      writer.uint32(10).string(message.compatibility);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LandlockPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLandlockPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.compatibility = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LandlockPolicy {
    return { compatibility: isSet(object.compatibility) ? globalThis.String(object.compatibility) : "" };
  },

  toJSON(message: LandlockPolicy): unknown {
    const obj: any = {};
    if (message.compatibility !== "") {
      obj.compatibility = message.compatibility;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LandlockPolicy>, I>>(base?: I): LandlockPolicy {
    return LandlockPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LandlockPolicy>, I>>(object: I): LandlockPolicy {
    const message = createBaseLandlockPolicy();
    message.compatibility = object.compatibility ?? "";
    return message;
  },
};

function createBaseProcessPolicy(): ProcessPolicy {
  return { runAsUser: "", runAsGroup: "" };
}

export const ProcessPolicy: MessageFns<ProcessPolicy> = {
  encode(message: ProcessPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runAsUser !== "") {
      writer.uint32(10).string(message.runAsUser);
    }
    if (message.runAsGroup !== "") {
      writer.uint32(18).string(message.runAsGroup);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runAsUser = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.runAsGroup = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessPolicy {
    return {
      runAsUser: isSet(object.runAsUser)
        ? globalThis.String(object.runAsUser)
        : isSet(object.run_as_user)
        ? globalThis.String(object.run_as_user)
        : "",
      runAsGroup: isSet(object.runAsGroup)
        ? globalThis.String(object.runAsGroup)
        : isSet(object.run_as_group)
        ? globalThis.String(object.run_as_group)
        : "",
    };
  },

  toJSON(message: ProcessPolicy): unknown {
    const obj: any = {};
    if (message.runAsUser !== "") {
      obj.runAsUser = message.runAsUser;
    }
    if (message.runAsGroup !== "") {
      obj.runAsGroup = message.runAsGroup;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessPolicy>, I>>(base?: I): ProcessPolicy {
    return ProcessPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessPolicy>, I>>(object: I): ProcessPolicy {
    const message = createBaseProcessPolicy();
    message.runAsUser = object.runAsUser ?? "";
    message.runAsGroup = object.runAsGroup ?? "";
    return message;
  },
};

function createBaseNetworkPolicyRule(): NetworkPolicyRule {
  return { name: "", endpoints: [], binaries: [] };
}

export const NetworkPolicyRule: MessageFns<NetworkPolicyRule> = {
  encode(message: NetworkPolicyRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.endpoints) {
      NetworkEndpoint.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.binaries) {
      NetworkBinary.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicyRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkPolicyRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.endpoints.push(NetworkEndpoint.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binaries.push(NetworkBinary.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkPolicyRule {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      endpoints: globalThis.Array.isArray(object?.endpoints)
        ? object.endpoints.map((e: any) => NetworkEndpoint.fromJSON(e))
        : [],
      binaries: globalThis.Array.isArray(object?.binaries)
        ? object.binaries.map((e: any) => NetworkBinary.fromJSON(e))
        : [],
    };
  },

  toJSON(message: NetworkPolicyRule): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.endpoints?.length) {
      obj.endpoints = message.endpoints.map((e) => NetworkEndpoint.toJSON(e));
    }
    if (message.binaries?.length) {
      obj.binaries = message.binaries.map((e) => NetworkBinary.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetworkPolicyRule>, I>>(base?: I): NetworkPolicyRule {
    return NetworkPolicyRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetworkPolicyRule>, I>>(object: I): NetworkPolicyRule {
    const message = createBaseNetworkPolicyRule();
    message.name = object.name ?? "";
    message.endpoints = object.endpoints?.map((e) => NetworkEndpoint.fromPartial(e)) || [];
    message.binaries = object.binaries?.map((e) => NetworkBinary.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNetworkEndpoint(): NetworkEndpoint {
  return {
    host: "",
    port: 0,
    protocol: "",
    tls: "",
    enforcement: "",
    access: "",
    rules: [],
    allowedIps: [],
    ports: [],
    denyRules: [],
    allowEncodedSlash: false,
    persistedQueries: "",
    graphqlPersistedQueries: {},
    graphqlMaxBodyBytes: 0,
    path: "",
    websocketCredentialRewrite: false,
    requestBodyCredentialRewrite: false,
  };
}

export const NetworkEndpoint: MessageFns<NetworkEndpoint> = {
  encode(message: NetworkEndpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(16).uint32(message.port);
    }
    if (message.protocol !== "") {
      writer.uint32(26).string(message.protocol);
    }
    if (message.tls !== "") {
      writer.uint32(34).string(message.tls);
    }
    if (message.enforcement !== "") {
      writer.uint32(42).string(message.enforcement);
    }
    if (message.access !== "") {
      writer.uint32(50).string(message.access);
    }
    for (const v of message.rules) {
      L7Rule.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.allowedIps) {
      writer.uint32(66).string(v!);
    }
    writer.uint32(74).fork();
    for (const v of message.ports) {
      writer.uint32(v);
    }
    writer.join();
    for (const v of message.denyRules) {
      L7DenyRule.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.allowEncodedSlash !== false) {
      writer.uint32(88).bool(message.allowEncodedSlash);
    }
    if (message.persistedQueries !== "") {
      writer.uint32(98).string(message.persistedQueries);
    }
    globalThis.Object.entries(message.graphqlPersistedQueries).forEach(([key, value]: [string, GraphqlOperation]) => {
      NetworkEndpoint_GraphqlPersistedQueriesEntry.encode({ key: key as any, value }, writer.uint32(106).fork()).join();
    });
    if (message.graphqlMaxBodyBytes !== 0) {
      writer.uint32(112).uint32(message.graphqlMaxBodyBytes);
    }
    if (message.path !== "") {
      writer.uint32(122).string(message.path);
    }
    if (message.websocketCredentialRewrite !== false) {
      writer.uint32(128).bool(message.websocketCredentialRewrite);
    }
    if (message.requestBodyCredentialRewrite !== false) {
      writer.uint32(136).bool(message.requestBodyCredentialRewrite);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkEndpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkEndpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tls = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.enforcement = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.access = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.rules.push(L7Rule.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.allowedIps.push(reader.string());
          continue;
        }
        case 9: {
          if (tag === 72) {
            message.ports.push(reader.uint32());

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ports.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.denyRules.push(L7DenyRule.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.allowEncodedSlash = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.persistedQueries = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          const entry13 = NetworkEndpoint_GraphqlPersistedQueriesEntry.decode(reader, reader.uint32());
          if (entry13.value !== undefined) {
            message.graphqlPersistedQueries[entry13.key] = entry13.value;
          }
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.graphqlMaxBodyBytes = reader.uint32();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.websocketCredentialRewrite = reader.bool();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.requestBodyCredentialRewrite = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkEndpoint {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      tls: isSet(object.tls) ? globalThis.String(object.tls) : "",
      enforcement: isSet(object.enforcement) ? globalThis.String(object.enforcement) : "",
      access: isSet(object.access) ? globalThis.String(object.access) : "",
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => L7Rule.fromJSON(e)) : [],
      allowedIps: globalThis.Array.isArray(object?.allowedIps)
        ? object.allowedIps.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_ips)
        ? object.allowed_ips.map((e: any) => globalThis.String(e))
        : [],
      ports: globalThis.Array.isArray(object?.ports) ? object.ports.map((e: any) => globalThis.Number(e)) : [],
      denyRules: globalThis.Array.isArray(object?.denyRules)
        ? object.denyRules.map((e: any) => L7DenyRule.fromJSON(e))
        : globalThis.Array.isArray(object?.deny_rules)
        ? object.deny_rules.map((e: any) => L7DenyRule.fromJSON(e))
        : [],
      allowEncodedSlash: isSet(object.allowEncodedSlash)
        ? globalThis.Boolean(object.allowEncodedSlash)
        : isSet(object.allow_encoded_slash)
        ? globalThis.Boolean(object.allow_encoded_slash)
        : false,
      persistedQueries: isSet(object.persistedQueries)
        ? globalThis.String(object.persistedQueries)
        : isSet(object.persisted_queries)
        ? globalThis.String(object.persisted_queries)
        : "",
      graphqlPersistedQueries: isObject(object.graphqlPersistedQueries)
        ? (globalThis.Object.entries(object.graphqlPersistedQueries) as [string, any][]).reduce(
          (acc: { [key: string]: GraphqlOperation }, [key, value]: [string, any]) => {
            acc[key] = GraphqlOperation.fromJSON(value);
            return acc;
          },
          {},
        )
        : isObject(object.graphql_persisted_queries)
        ? (globalThis.Object.entries(object.graphql_persisted_queries) as [string, any][]).reduce(
          (acc: { [key: string]: GraphqlOperation }, [key, value]: [string, any]) => {
            acc[key] = GraphqlOperation.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      graphqlMaxBodyBytes: isSet(object.graphqlMaxBodyBytes)
        ? globalThis.Number(object.graphqlMaxBodyBytes)
        : isSet(object.graphql_max_body_bytes)
        ? globalThis.Number(object.graphql_max_body_bytes)
        : 0,
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      websocketCredentialRewrite: isSet(object.websocketCredentialRewrite)
        ? globalThis.Boolean(object.websocketCredentialRewrite)
        : isSet(object.websocket_credential_rewrite)
        ? globalThis.Boolean(object.websocket_credential_rewrite)
        : false,
      requestBodyCredentialRewrite: isSet(object.requestBodyCredentialRewrite)
        ? globalThis.Boolean(object.requestBodyCredentialRewrite)
        : isSet(object.request_body_credential_rewrite)
        ? globalThis.Boolean(object.request_body_credential_rewrite)
        : false,
    };
  },

  toJSON(message: NetworkEndpoint): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.tls !== "") {
      obj.tls = message.tls;
    }
    if (message.enforcement !== "") {
      obj.enforcement = message.enforcement;
    }
    if (message.access !== "") {
      obj.access = message.access;
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => L7Rule.toJSON(e));
    }
    if (message.allowedIps?.length) {
      obj.allowedIps = message.allowedIps;
    }
    if (message.ports?.length) {
      obj.ports = message.ports.map((e) => Math.round(e));
    }
    if (message.denyRules?.length) {
      obj.denyRules = message.denyRules.map((e) => L7DenyRule.toJSON(e));
    }
    if (message.allowEncodedSlash !== false) {
      obj.allowEncodedSlash = message.allowEncodedSlash;
    }
    if (message.persistedQueries !== "") {
      obj.persistedQueries = message.persistedQueries;
    }
    if (message.graphqlPersistedQueries) {
      const entries = globalThis.Object.entries(message.graphqlPersistedQueries) as [string, GraphqlOperation][];
      if (entries.length > 0) {
        obj.graphqlPersistedQueries = {};
        entries.forEach(([k, v]) => {
          obj.graphqlPersistedQueries[k] = GraphqlOperation.toJSON(v);
        });
      }
    }
    if (message.graphqlMaxBodyBytes !== 0) {
      obj.graphqlMaxBodyBytes = Math.round(message.graphqlMaxBodyBytes);
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.websocketCredentialRewrite !== false) {
      obj.websocketCredentialRewrite = message.websocketCredentialRewrite;
    }
    if (message.requestBodyCredentialRewrite !== false) {
      obj.requestBodyCredentialRewrite = message.requestBodyCredentialRewrite;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetworkEndpoint>, I>>(base?: I): NetworkEndpoint {
    return NetworkEndpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetworkEndpoint>, I>>(object: I): NetworkEndpoint {
    const message = createBaseNetworkEndpoint();
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.protocol = object.protocol ?? "";
    message.tls = object.tls ?? "";
    message.enforcement = object.enforcement ?? "";
    message.access = object.access ?? "";
    message.rules = object.rules?.map((e) => L7Rule.fromPartial(e)) || [];
    message.allowedIps = object.allowedIps?.map((e) => e) || [];
    message.ports = object.ports?.map((e) => e) || [];
    message.denyRules = object.denyRules?.map((e) => L7DenyRule.fromPartial(e)) || [];
    message.allowEncodedSlash = object.allowEncodedSlash ?? false;
    message.persistedQueries = object.persistedQueries ?? "";
    message.graphqlPersistedQueries =
      (globalThis.Object.entries(object.graphqlPersistedQueries ?? {}) as [string, GraphqlOperation][]).reduce(
        (acc: { [key: string]: GraphqlOperation }, [key, value]: [string, GraphqlOperation]) => {
          if (value !== undefined) {
            acc[key] = GraphqlOperation.fromPartial(value);
          }
          return acc;
        },
        {},
      );
    message.graphqlMaxBodyBytes = object.graphqlMaxBodyBytes ?? 0;
    message.path = object.path ?? "";
    message.websocketCredentialRewrite = object.websocketCredentialRewrite ?? false;
    message.requestBodyCredentialRewrite = object.requestBodyCredentialRewrite ?? false;
    return message;
  },
};

function createBaseNetworkEndpoint_GraphqlPersistedQueriesEntry(): NetworkEndpoint_GraphqlPersistedQueriesEntry {
  return { key: "", value: undefined };
}

export const NetworkEndpoint_GraphqlPersistedQueriesEntry: MessageFns<NetworkEndpoint_GraphqlPersistedQueriesEntry> = {
  encode(
    message: NetworkEndpoint_GraphqlPersistedQueriesEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      GraphqlOperation.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkEndpoint_GraphqlPersistedQueriesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkEndpoint_GraphqlPersistedQueriesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = GraphqlOperation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkEndpoint_GraphqlPersistedQueriesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? GraphqlOperation.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: NetworkEndpoint_GraphqlPersistedQueriesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = GraphqlOperation.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetworkEndpoint_GraphqlPersistedQueriesEntry>, I>>(
    base?: I,
  ): NetworkEndpoint_GraphqlPersistedQueriesEntry {
    return NetworkEndpoint_GraphqlPersistedQueriesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetworkEndpoint_GraphqlPersistedQueriesEntry>, I>>(
    object: I,
  ): NetworkEndpoint_GraphqlPersistedQueriesEntry {
    const message = createBaseNetworkEndpoint_GraphqlPersistedQueriesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? GraphqlOperation.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseGraphqlOperation(): GraphqlOperation {
  return { operationType: "", operationName: "", fields: [] };
}

export const GraphqlOperation: MessageFns<GraphqlOperation> = {
  encode(message: GraphqlOperation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operationType !== "") {
      writer.uint32(10).string(message.operationType);
    }
    if (message.operationName !== "") {
      writer.uint32(18).string(message.operationName);
    }
    for (const v of message.fields) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GraphqlOperation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraphqlOperation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operationType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operationName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fields.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GraphqlOperation {
    return {
      operationType: isSet(object.operationType)
        ? globalThis.String(object.operationType)
        : isSet(object.operation_type)
        ? globalThis.String(object.operation_type)
        : "",
      operationName: isSet(object.operationName)
        ? globalThis.String(object.operationName)
        : isSet(object.operation_name)
        ? globalThis.String(object.operation_name)
        : "",
      fields: globalThis.Array.isArray(object?.fields) ? object.fields.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GraphqlOperation): unknown {
    const obj: any = {};
    if (message.operationType !== "") {
      obj.operationType = message.operationType;
    }
    if (message.operationName !== "") {
      obj.operationName = message.operationName;
    }
    if (message.fields?.length) {
      obj.fields = message.fields;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GraphqlOperation>, I>>(base?: I): GraphqlOperation {
    return GraphqlOperation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GraphqlOperation>, I>>(object: I): GraphqlOperation {
    const message = createBaseGraphqlOperation();
    message.operationType = object.operationType ?? "";
    message.operationName = object.operationName ?? "";
    message.fields = object.fields?.map((e) => e) || [];
    return message;
  },
};

function createBaseL7DenyRule(): L7DenyRule {
  return { method: "", path: "", command: "", query: {}, operationType: "", operationName: "", fields: [] };
}

export const L7DenyRule: MessageFns<L7DenyRule> = {
  encode(message: L7DenyRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.command !== "") {
      writer.uint32(26).string(message.command);
    }
    globalThis.Object.entries(message.query).forEach(([key, value]: [string, L7QueryMatcher]) => {
      L7DenyRule_QueryEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.operationType !== "") {
      writer.uint32(42).string(message.operationType);
    }
    if (message.operationName !== "") {
      writer.uint32(50).string(message.operationName);
    }
    for (const v of message.fields) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7DenyRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7DenyRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = L7DenyRule_QueryEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.query[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.operationType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.operationName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.fields.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7DenyRule {
    return {
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      query: isObject(object.query)
        ? (globalThis.Object.entries(object.query) as [string, any][]).reduce(
          (acc: { [key: string]: L7QueryMatcher }, [key, value]: [string, any]) => {
            acc[key] = L7QueryMatcher.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      operationType: isSet(object.operationType)
        ? globalThis.String(object.operationType)
        : isSet(object.operation_type)
        ? globalThis.String(object.operation_type)
        : "",
      operationName: isSet(object.operationName)
        ? globalThis.String(object.operationName)
        : isSet(object.operation_name)
        ? globalThis.String(object.operation_name)
        : "",
      fields: globalThis.Array.isArray(object?.fields)
        ? object.fields.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: L7DenyRule): unknown {
    const obj: any = {};
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.query) {
      const entries = globalThis.Object.entries(message.query) as [string, L7QueryMatcher][];
      if (entries.length > 0) {
        obj.query = {};
        entries.forEach(([k, v]) => {
          obj.query[k] = L7QueryMatcher.toJSON(v);
        });
      }
    }
    if (message.operationType !== "") {
      obj.operationType = message.operationType;
    }
    if (message.operationName !== "") {
      obj.operationName = message.operationName;
    }
    if (message.fields?.length) {
      obj.fields = message.fields;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7DenyRule>, I>>(base?: I): L7DenyRule {
    return L7DenyRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7DenyRule>, I>>(object: I): L7DenyRule {
    const message = createBaseL7DenyRule();
    message.method = object.method ?? "";
    message.path = object.path ?? "";
    message.command = object.command ?? "";
    message.query = (globalThis.Object.entries(object.query ?? {}) as [string, L7QueryMatcher][]).reduce(
      (acc: { [key: string]: L7QueryMatcher }, [key, value]: [string, L7QueryMatcher]) => {
        if (value !== undefined) {
          acc[key] = L7QueryMatcher.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.operationType = object.operationType ?? "";
    message.operationName = object.operationName ?? "";
    message.fields = object.fields?.map((e) => e) || [];
    return message;
  },
};

function createBaseL7DenyRule_QueryEntry(): L7DenyRule_QueryEntry {
  return { key: "", value: undefined };
}

export const L7DenyRule_QueryEntry: MessageFns<L7DenyRule_QueryEntry> = {
  encode(message: L7DenyRule_QueryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      L7QueryMatcher.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7DenyRule_QueryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7DenyRule_QueryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = L7QueryMatcher.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7DenyRule_QueryEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? L7QueryMatcher.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: L7DenyRule_QueryEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = L7QueryMatcher.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7DenyRule_QueryEntry>, I>>(base?: I): L7DenyRule_QueryEntry {
    return L7DenyRule_QueryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7DenyRule_QueryEntry>, I>>(object: I): L7DenyRule_QueryEntry {
    const message = createBaseL7DenyRule_QueryEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? L7QueryMatcher.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseL7Rule(): L7Rule {
  return { allow: undefined };
}

export const L7Rule: MessageFns<L7Rule> = {
  encode(message: L7Rule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allow !== undefined) {
      L7Allow.encode(message.allow, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7Rule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7Rule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.allow = L7Allow.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7Rule {
    return { allow: isSet(object.allow) ? L7Allow.fromJSON(object.allow) : undefined };
  },

  toJSON(message: L7Rule): unknown {
    const obj: any = {};
    if (message.allow !== undefined) {
      obj.allow = L7Allow.toJSON(message.allow);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7Rule>, I>>(base?: I): L7Rule {
    return L7Rule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7Rule>, I>>(object: I): L7Rule {
    const message = createBaseL7Rule();
    message.allow = (object.allow !== undefined && object.allow !== null)
      ? L7Allow.fromPartial(object.allow)
      : undefined;
    return message;
  },
};

function createBaseL7Allow(): L7Allow {
  return { method: "", path: "", command: "", query: {}, operationType: "", operationName: "", fields: [] };
}

export const L7Allow: MessageFns<L7Allow> = {
  encode(message: L7Allow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.command !== "") {
      writer.uint32(26).string(message.command);
    }
    globalThis.Object.entries(message.query).forEach(([key, value]: [string, L7QueryMatcher]) => {
      L7Allow_QueryEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.operationType !== "") {
      writer.uint32(42).string(message.operationType);
    }
    if (message.operationName !== "") {
      writer.uint32(50).string(message.operationName);
    }
    for (const v of message.fields) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7Allow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7Allow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = L7Allow_QueryEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.query[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.operationType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.operationName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.fields.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7Allow {
    return {
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      query: isObject(object.query)
        ? (globalThis.Object.entries(object.query) as [string, any][]).reduce(
          (acc: { [key: string]: L7QueryMatcher }, [key, value]: [string, any]) => {
            acc[key] = L7QueryMatcher.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      operationType: isSet(object.operationType)
        ? globalThis.String(object.operationType)
        : isSet(object.operation_type)
        ? globalThis.String(object.operation_type)
        : "",
      operationName: isSet(object.operationName)
        ? globalThis.String(object.operationName)
        : isSet(object.operation_name)
        ? globalThis.String(object.operation_name)
        : "",
      fields: globalThis.Array.isArray(object?.fields)
        ? object.fields.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: L7Allow): unknown {
    const obj: any = {};
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.query) {
      const entries = globalThis.Object.entries(message.query) as [string, L7QueryMatcher][];
      if (entries.length > 0) {
        obj.query = {};
        entries.forEach(([k, v]) => {
          obj.query[k] = L7QueryMatcher.toJSON(v);
        });
      }
    }
    if (message.operationType !== "") {
      obj.operationType = message.operationType;
    }
    if (message.operationName !== "") {
      obj.operationName = message.operationName;
    }
    if (message.fields?.length) {
      obj.fields = message.fields;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7Allow>, I>>(base?: I): L7Allow {
    return L7Allow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7Allow>, I>>(object: I): L7Allow {
    const message = createBaseL7Allow();
    message.method = object.method ?? "";
    message.path = object.path ?? "";
    message.command = object.command ?? "";
    message.query = (globalThis.Object.entries(object.query ?? {}) as [string, L7QueryMatcher][]).reduce(
      (acc: { [key: string]: L7QueryMatcher }, [key, value]: [string, L7QueryMatcher]) => {
        if (value !== undefined) {
          acc[key] = L7QueryMatcher.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.operationType = object.operationType ?? "";
    message.operationName = object.operationName ?? "";
    message.fields = object.fields?.map((e) => e) || [];
    return message;
  },
};

function createBaseL7Allow_QueryEntry(): L7Allow_QueryEntry {
  return { key: "", value: undefined };
}

export const L7Allow_QueryEntry: MessageFns<L7Allow_QueryEntry> = {
  encode(message: L7Allow_QueryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      L7QueryMatcher.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7Allow_QueryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7Allow_QueryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = L7QueryMatcher.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7Allow_QueryEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? L7QueryMatcher.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: L7Allow_QueryEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = L7QueryMatcher.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7Allow_QueryEntry>, I>>(base?: I): L7Allow_QueryEntry {
    return L7Allow_QueryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7Allow_QueryEntry>, I>>(object: I): L7Allow_QueryEntry {
    const message = createBaseL7Allow_QueryEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? L7QueryMatcher.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseL7QueryMatcher(): L7QueryMatcher {
  return { glob: "", any: [] };
}

export const L7QueryMatcher: MessageFns<L7QueryMatcher> = {
  encode(message: L7QueryMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.glob !== "") {
      writer.uint32(10).string(message.glob);
    }
    for (const v of message.any) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7QueryMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7QueryMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.glob = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.any.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7QueryMatcher {
    return {
      glob: isSet(object.glob) ? globalThis.String(object.glob) : "",
      any: globalThis.Array.isArray(object?.any) ? object.any.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: L7QueryMatcher): unknown {
    const obj: any = {};
    if (message.glob !== "") {
      obj.glob = message.glob;
    }
    if (message.any?.length) {
      obj.any = message.any;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7QueryMatcher>, I>>(base?: I): L7QueryMatcher {
    return L7QueryMatcher.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7QueryMatcher>, I>>(object: I): L7QueryMatcher {
    const message = createBaseL7QueryMatcher();
    message.glob = object.glob ?? "";
    message.any = object.any?.map((e) => e) || [];
    return message;
  },
};

function createBaseNetworkBinary(): NetworkBinary {
  return { path: "", harness: false };
}

export const NetworkBinary: MessageFns<NetworkBinary> = {
  encode(message: NetworkBinary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.harness !== false) {
      writer.uint32(16).bool(message.harness);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkBinary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkBinary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.harness = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkBinary {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      harness: isSet(object.harness) ? globalThis.Boolean(object.harness) : false,
    };
  },

  toJSON(message: NetworkBinary): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.harness !== false) {
      obj.harness = message.harness;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetworkBinary>, I>>(base?: I): NetworkBinary {
    return NetworkBinary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetworkBinary>, I>>(object: I): NetworkBinary {
    const message = createBaseNetworkBinary();
    message.path = object.path ?? "";
    message.harness = object.harness ?? false;
    return message;
  },
};

function createBaseGetSandboxConfigRequest(): GetSandboxConfigRequest {
  return { sandboxId: "" };
}

export const GetSandboxConfigRequest: MessageFns<GetSandboxConfigRequest> = {
  encode(message: GetSandboxConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxConfigRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
    };
  },

  toJSON(message: GetSandboxConfigRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxConfigRequest>, I>>(base?: I): GetSandboxConfigRequest {
    return GetSandboxConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxConfigRequest>, I>>(object: I): GetSandboxConfigRequest {
    const message = createBaseGetSandboxConfigRequest();
    message.sandboxId = object.sandboxId ?? "";
    return message;
  },
};

function createBaseGetGatewayConfigRequest(): GetGatewayConfigRequest {
  return {};
}

export const GetGatewayConfigRequest: MessageFns<GetGatewayConfigRequest> = {
  encode(_: GetGatewayConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetGatewayConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetGatewayConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetGatewayConfigRequest {
    return {};
  },

  toJSON(_: GetGatewayConfigRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetGatewayConfigRequest>, I>>(base?: I): GetGatewayConfigRequest {
    return GetGatewayConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetGatewayConfigRequest>, I>>(_: I): GetGatewayConfigRequest {
    const message = createBaseGetGatewayConfigRequest();
    return message;
  },
};

function createBaseGetGatewayConfigResponse(): GetGatewayConfigResponse {
  return { settings: {}, settingsRevision: 0 };
}

export const GetGatewayConfigResponse: MessageFns<GetGatewayConfigResponse> = {
  encode(message: GetGatewayConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.settings).forEach(([key, value]: [string, SettingValue]) => {
      GetGatewayConfigResponse_SettingsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    if (message.settingsRevision !== 0) {
      writer.uint32(16).uint64(message.settingsRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetGatewayConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetGatewayConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GetGatewayConfigResponse_SettingsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.settings[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.settingsRevision = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetGatewayConfigResponse {
    return {
      settings: isObject(object.settings)
        ? (globalThis.Object.entries(object.settings) as [string, any][]).reduce(
          (acc: { [key: string]: SettingValue }, [key, value]: [string, any]) => {
            acc[key] = SettingValue.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      settingsRevision: isSet(object.settingsRevision)
        ? globalThis.Number(object.settingsRevision)
        : isSet(object.settings_revision)
        ? globalThis.Number(object.settings_revision)
        : 0,
    };
  },

  toJSON(message: GetGatewayConfigResponse): unknown {
    const obj: any = {};
    if (message.settings) {
      const entries = globalThis.Object.entries(message.settings) as [string, SettingValue][];
      if (entries.length > 0) {
        obj.settings = {};
        entries.forEach(([k, v]) => {
          obj.settings[k] = SettingValue.toJSON(v);
        });
      }
    }
    if (message.settingsRevision !== 0) {
      obj.settingsRevision = Math.round(message.settingsRevision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetGatewayConfigResponse>, I>>(base?: I): GetGatewayConfigResponse {
    return GetGatewayConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetGatewayConfigResponse>, I>>(object: I): GetGatewayConfigResponse {
    const message = createBaseGetGatewayConfigResponse();
    message.settings = (globalThis.Object.entries(object.settings ?? {}) as [string, SettingValue][]).reduce(
      (acc: { [key: string]: SettingValue }, [key, value]: [string, SettingValue]) => {
        if (value !== undefined) {
          acc[key] = SettingValue.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.settingsRevision = object.settingsRevision ?? 0;
    return message;
  },
};

function createBaseGetGatewayConfigResponse_SettingsEntry(): GetGatewayConfigResponse_SettingsEntry {
  return { key: "", value: undefined };
}

export const GetGatewayConfigResponse_SettingsEntry: MessageFns<GetGatewayConfigResponse_SettingsEntry> = {
  encode(message: GetGatewayConfigResponse_SettingsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      SettingValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetGatewayConfigResponse_SettingsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetGatewayConfigResponse_SettingsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = SettingValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetGatewayConfigResponse_SettingsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? SettingValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: GetGatewayConfigResponse_SettingsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = SettingValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetGatewayConfigResponse_SettingsEntry>, I>>(
    base?: I,
  ): GetGatewayConfigResponse_SettingsEntry {
    return GetGatewayConfigResponse_SettingsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetGatewayConfigResponse_SettingsEntry>, I>>(
    object: I,
  ): GetGatewayConfigResponse_SettingsEntry {
    const message = createBaseGetGatewayConfigResponse_SettingsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? SettingValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseSettingValue(): SettingValue {
  return { value: undefined };
}

export const SettingValue: MessageFns<SettingValue> = {
  encode(message: SettingValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.value?.$case) {
      case "stringValue":
        writer.uint32(10).string(message.value.stringValue);
        break;
      case "boolValue":
        writer.uint32(16).bool(message.value.boolValue);
        break;
      case "intValue":
        writer.uint32(24).int64(message.value.intValue);
        break;
      case "bytesValue":
        writer.uint32(34).bytes(message.value.bytesValue);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SettingValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettingValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = { $case: "stringValue", stringValue: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = { $case: "boolValue", boolValue: reader.bool() };
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.value = { $case: "intValue", intValue: longToNumber(reader.int64()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.value = { $case: "bytesValue", bytesValue: Buffer.from(reader.bytes()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SettingValue {
    return {
      value: isSet(object.stringValue)
        ? { $case: "stringValue", stringValue: globalThis.String(object.stringValue) }
        : isSet(object.string_value)
        ? { $case: "stringValue", stringValue: globalThis.String(object.string_value) }
        : isSet(object.boolValue)
        ? { $case: "boolValue", boolValue: globalThis.Boolean(object.boolValue) }
        : isSet(object.bool_value)
        ? { $case: "boolValue", boolValue: globalThis.Boolean(object.bool_value) }
        : isSet(object.intValue)
        ? { $case: "intValue", intValue: globalThis.Number(object.intValue) }
        : isSet(object.int_value)
        ? { $case: "intValue", intValue: globalThis.Number(object.int_value) }
        : isSet(object.bytesValue)
        ? { $case: "bytesValue", bytesValue: Buffer.from(bytesFromBase64(object.bytesValue)) }
        : isSet(object.bytes_value)
        ? { $case: "bytesValue", bytesValue: Buffer.from(bytesFromBase64(object.bytes_value)) }
        : undefined,
    };
  },

  toJSON(message: SettingValue): unknown {
    const obj: any = {};
    if (message.value?.$case === "stringValue") {
      obj.stringValue = message.value.stringValue;
    } else if (message.value?.$case === "boolValue") {
      obj.boolValue = message.value.boolValue;
    } else if (message.value?.$case === "intValue") {
      obj.intValue = Math.round(message.value.intValue);
    } else if (message.value?.$case === "bytesValue") {
      obj.bytesValue = base64FromBytes(message.value.bytesValue);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SettingValue>, I>>(base?: I): SettingValue {
    return SettingValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SettingValue>, I>>(object: I): SettingValue {
    const message = createBaseSettingValue();
    switch (object.value?.$case) {
      case "stringValue": {
        if (object.value?.stringValue !== undefined && object.value?.stringValue !== null) {
          message.value = { $case: "stringValue", stringValue: object.value.stringValue };
        }
        break;
      }
      case "boolValue": {
        if (object.value?.boolValue !== undefined && object.value?.boolValue !== null) {
          message.value = { $case: "boolValue", boolValue: object.value.boolValue };
        }
        break;
      }
      case "intValue": {
        if (object.value?.intValue !== undefined && object.value?.intValue !== null) {
          message.value = { $case: "intValue", intValue: object.value.intValue };
        }
        break;
      }
      case "bytesValue": {
        if (object.value?.bytesValue !== undefined && object.value?.bytesValue !== null) {
          message.value = { $case: "bytesValue", bytesValue: object.value.bytesValue };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseEffectiveSetting(): EffectiveSetting {
  return { value: undefined, scope: 0 };
}

export const EffectiveSetting: MessageFns<EffectiveSetting> = {
  encode(message: EffectiveSetting, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== undefined) {
      SettingValue.encode(message.value, writer.uint32(10).fork()).join();
    }
    if (message.scope !== 0) {
      writer.uint32(16).int32(message.scope);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectiveSetting {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffectiveSetting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = SettingValue.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.scope = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EffectiveSetting {
    return {
      value: isSet(object.value) ? SettingValue.fromJSON(object.value) : undefined,
      scope: isSet(object.scope) ? settingScopeFromJSON(object.scope) : 0,
    };
  },

  toJSON(message: EffectiveSetting): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = SettingValue.toJSON(message.value);
    }
    if (message.scope !== 0) {
      obj.scope = settingScopeToJSON(message.scope);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EffectiveSetting>, I>>(base?: I): EffectiveSetting {
    return EffectiveSetting.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EffectiveSetting>, I>>(object: I): EffectiveSetting {
    const message = createBaseEffectiveSetting();
    message.value = (object.value !== undefined && object.value !== null)
      ? SettingValue.fromPartial(object.value)
      : undefined;
    message.scope = object.scope ?? 0;
    return message;
  },
};

function createBaseGetSandboxConfigResponse(): GetSandboxConfigResponse {
  return {
    policy: undefined,
    version: 0,
    policyHash: "",
    settings: {},
    configRevision: 0,
    policySource: 0,
    globalPolicyVersion: 0,
    providerEnvRevision: 0,
  };
}

export const GetSandboxConfigResponse: MessageFns<GetSandboxConfigResponse> = {
  encode(message: GetSandboxConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policy !== undefined) {
      SandboxPolicy.encode(message.policy, writer.uint32(10).fork()).join();
    }
    if (message.version !== 0) {
      writer.uint32(16).uint32(message.version);
    }
    if (message.policyHash !== "") {
      writer.uint32(26).string(message.policyHash);
    }
    globalThis.Object.entries(message.settings).forEach(([key, value]: [string, EffectiveSetting]) => {
      GetSandboxConfigResponse_SettingsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.configRevision !== 0) {
      writer.uint32(40).uint64(message.configRevision);
    }
    if (message.policySource !== 0) {
      writer.uint32(48).int32(message.policySource);
    }
    if (message.globalPolicyVersion !== 0) {
      writer.uint32(56).uint32(message.globalPolicyVersion);
    }
    if (message.providerEnvRevision !== 0) {
      writer.uint32(64).uint64(message.providerEnvRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.policy = SandboxPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = GetSandboxConfigResponse_SettingsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.settings[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.configRevision = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.policySource = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.globalPolicyVersion = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.providerEnvRevision = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxConfigResponse {
    return {
      policy: isSet(object.policy) ? SandboxPolicy.fromJSON(object.policy) : undefined,
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
      settings: isObject(object.settings)
        ? (globalThis.Object.entries(object.settings) as [string, any][]).reduce(
          (acc: { [key: string]: EffectiveSetting }, [key, value]: [string, any]) => {
            acc[key] = EffectiveSetting.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      configRevision: isSet(object.configRevision)
        ? globalThis.Number(object.configRevision)
        : isSet(object.config_revision)
        ? globalThis.Number(object.config_revision)
        : 0,
      policySource: isSet(object.policySource)
        ? policySourceFromJSON(object.policySource)
        : isSet(object.policy_source)
        ? policySourceFromJSON(object.policy_source)
        : 0,
      globalPolicyVersion: isSet(object.globalPolicyVersion)
        ? globalThis.Number(object.globalPolicyVersion)
        : isSet(object.global_policy_version)
        ? globalThis.Number(object.global_policy_version)
        : 0,
      providerEnvRevision: isSet(object.providerEnvRevision)
        ? globalThis.Number(object.providerEnvRevision)
        : isSet(object.provider_env_revision)
        ? globalThis.Number(object.provider_env_revision)
        : 0,
    };
  },

  toJSON(message: GetSandboxConfigResponse): unknown {
    const obj: any = {};
    if (message.policy !== undefined) {
      obj.policy = SandboxPolicy.toJSON(message.policy);
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    if (message.settings) {
      const entries = globalThis.Object.entries(message.settings) as [string, EffectiveSetting][];
      if (entries.length > 0) {
        obj.settings = {};
        entries.forEach(([k, v]) => {
          obj.settings[k] = EffectiveSetting.toJSON(v);
        });
      }
    }
    if (message.configRevision !== 0) {
      obj.configRevision = Math.round(message.configRevision);
    }
    if (message.policySource !== 0) {
      obj.policySource = policySourceToJSON(message.policySource);
    }
    if (message.globalPolicyVersion !== 0) {
      obj.globalPolicyVersion = Math.round(message.globalPolicyVersion);
    }
    if (message.providerEnvRevision !== 0) {
      obj.providerEnvRevision = Math.round(message.providerEnvRevision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxConfigResponse>, I>>(base?: I): GetSandboxConfigResponse {
    return GetSandboxConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxConfigResponse>, I>>(object: I): GetSandboxConfigResponse {
    const message = createBaseGetSandboxConfigResponse();
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? SandboxPolicy.fromPartial(object.policy)
      : undefined;
    message.version = object.version ?? 0;
    message.policyHash = object.policyHash ?? "";
    message.settings = (globalThis.Object.entries(object.settings ?? {}) as [string, EffectiveSetting][]).reduce(
      (acc: { [key: string]: EffectiveSetting }, [key, value]: [string, EffectiveSetting]) => {
        if (value !== undefined) {
          acc[key] = EffectiveSetting.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.configRevision = object.configRevision ?? 0;
    message.policySource = object.policySource ?? 0;
    message.globalPolicyVersion = object.globalPolicyVersion ?? 0;
    message.providerEnvRevision = object.providerEnvRevision ?? 0;
    return message;
  },
};

function createBaseGetSandboxConfigResponse_SettingsEntry(): GetSandboxConfigResponse_SettingsEntry {
  return { key: "", value: undefined };
}

export const GetSandboxConfigResponse_SettingsEntry: MessageFns<GetSandboxConfigResponse_SettingsEntry> = {
  encode(message: GetSandboxConfigResponse_SettingsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      EffectiveSetting.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxConfigResponse_SettingsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxConfigResponse_SettingsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = EffectiveSetting.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxConfigResponse_SettingsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? EffectiveSetting.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: GetSandboxConfigResponse_SettingsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = EffectiveSetting.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxConfigResponse_SettingsEntry>, I>>(
    base?: I,
  ): GetSandboxConfigResponse_SettingsEntry {
    return GetSandboxConfigResponse_SettingsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxConfigResponse_SettingsEntry>, I>>(
    object: I,
  ): GetSandboxConfigResponse_SettingsEntry {
    const message = createBaseGetSandboxConfigResponse_SettingsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? EffectiveSetting.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
