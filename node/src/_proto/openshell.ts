// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.29.6
// source: openshell.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type ClientWritableStream,
  type handleBidiStreamingCall,
  type handleClientStreamingCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { ObjectMeta, Provider } from "./datamodel";
import { Struct } from "./google/protobuf/struct";
import {
  GetGatewayConfigRequest,
  GetGatewayConfigResponse,
  GetSandboxConfigRequest,
  GetSandboxConfigResponse,
  L7DenyRule,
  L7Rule,
  NetworkBinary,
  NetworkEndpoint,
  NetworkPolicyRule,
  SandboxPolicy,
  SettingValue,
} from "./sandbox";

export const protobufPackage = "openshell.v1";

/**
 * High-level sandbox lifecycle phase derived by the gateway.
 *
 * Clients should rely on this normalized lifecycle summary for readiness and
 * deletion decisions instead of interpreting raw conditions.
 */
export enum SandboxPhase {
  SANDBOX_PHASE_UNSPECIFIED = 0,
  SANDBOX_PHASE_PROVISIONING = 1,
  SANDBOX_PHASE_READY = 2,
  SANDBOX_PHASE_ERROR = 3,
  SANDBOX_PHASE_DELETING = 4,
  SANDBOX_PHASE_UNKNOWN = 5,
  UNRECOGNIZED = -1,
}

export function sandboxPhaseFromJSON(object: any): SandboxPhase {
  switch (object) {
    case 0:
    case "SANDBOX_PHASE_UNSPECIFIED":
      return SandboxPhase.SANDBOX_PHASE_UNSPECIFIED;
    case 1:
    case "SANDBOX_PHASE_PROVISIONING":
      return SandboxPhase.SANDBOX_PHASE_PROVISIONING;
    case 2:
    case "SANDBOX_PHASE_READY":
      return SandboxPhase.SANDBOX_PHASE_READY;
    case 3:
    case "SANDBOX_PHASE_ERROR":
      return SandboxPhase.SANDBOX_PHASE_ERROR;
    case 4:
    case "SANDBOX_PHASE_DELETING":
      return SandboxPhase.SANDBOX_PHASE_DELETING;
    case 5:
    case "SANDBOX_PHASE_UNKNOWN":
      return SandboxPhase.SANDBOX_PHASE_UNKNOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SandboxPhase.UNRECOGNIZED;
  }
}

export function sandboxPhaseToJSON(object: SandboxPhase): string {
  switch (object) {
    case SandboxPhase.SANDBOX_PHASE_UNSPECIFIED:
      return "SANDBOX_PHASE_UNSPECIFIED";
    case SandboxPhase.SANDBOX_PHASE_PROVISIONING:
      return "SANDBOX_PHASE_PROVISIONING";
    case SandboxPhase.SANDBOX_PHASE_READY:
      return "SANDBOX_PHASE_READY";
    case SandboxPhase.SANDBOX_PHASE_ERROR:
      return "SANDBOX_PHASE_ERROR";
    case SandboxPhase.SANDBOX_PHASE_DELETING:
      return "SANDBOX_PHASE_DELETING";
    case SandboxPhase.SANDBOX_PHASE_UNKNOWN:
      return "SANDBOX_PHASE_UNKNOWN";
    case SandboxPhase.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ProviderCredentialRefreshStrategy {
  PROVIDER_CREDENTIAL_REFRESH_STRATEGY_UNSPECIFIED = 0,
  PROVIDER_CREDENTIAL_REFRESH_STRATEGY_STATIC = 1,
  PROVIDER_CREDENTIAL_REFRESH_STRATEGY_EXTERNAL = 2,
  PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_REFRESH_TOKEN = 3,
  PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_CLIENT_CREDENTIALS = 4,
  PROVIDER_CREDENTIAL_REFRESH_STRATEGY_GOOGLE_SERVICE_ACCOUNT_JWT = 5,
  UNRECOGNIZED = -1,
}

export function providerCredentialRefreshStrategyFromJSON(object: any): ProviderCredentialRefreshStrategy {
  switch (object) {
    case 0:
    case "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_UNSPECIFIED":
      return ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_UNSPECIFIED;
    case 1:
    case "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_STATIC":
      return ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_STATIC;
    case 2:
    case "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_EXTERNAL":
      return ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_EXTERNAL;
    case 3:
    case "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_REFRESH_TOKEN":
      return ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_REFRESH_TOKEN;
    case 4:
    case "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_CLIENT_CREDENTIALS":
      return ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_CLIENT_CREDENTIALS;
    case 5:
    case "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_GOOGLE_SERVICE_ACCOUNT_JWT":
      return ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_GOOGLE_SERVICE_ACCOUNT_JWT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProviderCredentialRefreshStrategy.UNRECOGNIZED;
  }
}

export function providerCredentialRefreshStrategyToJSON(object: ProviderCredentialRefreshStrategy): string {
  switch (object) {
    case ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_UNSPECIFIED:
      return "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_UNSPECIFIED";
    case ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_STATIC:
      return "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_STATIC";
    case ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_EXTERNAL:
      return "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_EXTERNAL";
    case ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_REFRESH_TOKEN:
      return "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_REFRESH_TOKEN";
    case ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_CLIENT_CREDENTIALS:
      return "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_OAUTH2_CLIENT_CREDENTIALS";
    case ProviderCredentialRefreshStrategy.PROVIDER_CREDENTIAL_REFRESH_STRATEGY_GOOGLE_SERVICE_ACCOUNT_JWT:
      return "PROVIDER_CREDENTIAL_REFRESH_STRATEGY_GOOGLE_SERVICE_ACCOUNT_JWT";
    case ProviderCredentialRefreshStrategy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Stable provider profile categories used by clients for grouping and filtering. */
export enum ProviderProfileCategory {
  PROVIDER_PROFILE_CATEGORY_UNSPECIFIED = 0,
  PROVIDER_PROFILE_CATEGORY_OTHER = 1,
  PROVIDER_PROFILE_CATEGORY_INFERENCE = 2,
  PROVIDER_PROFILE_CATEGORY_AGENT = 3,
  PROVIDER_PROFILE_CATEGORY_SOURCE_CONTROL = 4,
  PROVIDER_PROFILE_CATEGORY_MESSAGING = 5,
  PROVIDER_PROFILE_CATEGORY_DATA = 6,
  PROVIDER_PROFILE_CATEGORY_KNOWLEDGE = 7,
  UNRECOGNIZED = -1,
}

export function providerProfileCategoryFromJSON(object: any): ProviderProfileCategory {
  switch (object) {
    case 0:
    case "PROVIDER_PROFILE_CATEGORY_UNSPECIFIED":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_UNSPECIFIED;
    case 1:
    case "PROVIDER_PROFILE_CATEGORY_OTHER":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_OTHER;
    case 2:
    case "PROVIDER_PROFILE_CATEGORY_INFERENCE":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_INFERENCE;
    case 3:
    case "PROVIDER_PROFILE_CATEGORY_AGENT":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_AGENT;
    case 4:
    case "PROVIDER_PROFILE_CATEGORY_SOURCE_CONTROL":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_SOURCE_CONTROL;
    case 5:
    case "PROVIDER_PROFILE_CATEGORY_MESSAGING":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_MESSAGING;
    case 6:
    case "PROVIDER_PROFILE_CATEGORY_DATA":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_DATA;
    case 7:
    case "PROVIDER_PROFILE_CATEGORY_KNOWLEDGE":
      return ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_KNOWLEDGE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProviderProfileCategory.UNRECOGNIZED;
  }
}

export function providerProfileCategoryToJSON(object: ProviderProfileCategory): string {
  switch (object) {
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_UNSPECIFIED:
      return "PROVIDER_PROFILE_CATEGORY_UNSPECIFIED";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_OTHER:
      return "PROVIDER_PROFILE_CATEGORY_OTHER";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_INFERENCE:
      return "PROVIDER_PROFILE_CATEGORY_INFERENCE";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_AGENT:
      return "PROVIDER_PROFILE_CATEGORY_AGENT";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_SOURCE_CONTROL:
      return "PROVIDER_PROFILE_CATEGORY_SOURCE_CONTROL";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_MESSAGING:
      return "PROVIDER_PROFILE_CATEGORY_MESSAGING";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_DATA:
      return "PROVIDER_PROFILE_CATEGORY_DATA";
    case ProviderProfileCategory.PROVIDER_PROFILE_CATEGORY_KNOWLEDGE:
      return "PROVIDER_PROFILE_CATEGORY_KNOWLEDGE";
    case ProviderProfileCategory.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Policy load status. */
export enum PolicyStatus {
  POLICY_STATUS_UNSPECIFIED = 0,
  /** POLICY_STATUS_PENDING - Server received the update; sandbox has not yet loaded it. */
  POLICY_STATUS_PENDING = 1,
  /** POLICY_STATUS_LOADED - Sandbox successfully applied this policy version. */
  POLICY_STATUS_LOADED = 2,
  /** POLICY_STATUS_FAILED - Sandbox attempted to apply but failed; LKG policy remains active. */
  POLICY_STATUS_FAILED = 3,
  /** POLICY_STATUS_SUPERSEDED - A newer version was persisted before the sandbox loaded this one. */
  POLICY_STATUS_SUPERSEDED = 4,
  UNRECOGNIZED = -1,
}

export function policyStatusFromJSON(object: any): PolicyStatus {
  switch (object) {
    case 0:
    case "POLICY_STATUS_UNSPECIFIED":
      return PolicyStatus.POLICY_STATUS_UNSPECIFIED;
    case 1:
    case "POLICY_STATUS_PENDING":
      return PolicyStatus.POLICY_STATUS_PENDING;
    case 2:
    case "POLICY_STATUS_LOADED":
      return PolicyStatus.POLICY_STATUS_LOADED;
    case 3:
    case "POLICY_STATUS_FAILED":
      return PolicyStatus.POLICY_STATUS_FAILED;
    case 4:
    case "POLICY_STATUS_SUPERSEDED":
      return PolicyStatus.POLICY_STATUS_SUPERSEDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PolicyStatus.UNRECOGNIZED;
  }
}

export function policyStatusToJSON(object: PolicyStatus): string {
  switch (object) {
    case PolicyStatus.POLICY_STATUS_UNSPECIFIED:
      return "POLICY_STATUS_UNSPECIFIED";
    case PolicyStatus.POLICY_STATUS_PENDING:
      return "POLICY_STATUS_PENDING";
    case PolicyStatus.POLICY_STATUS_LOADED:
      return "POLICY_STATUS_LOADED";
    case PolicyStatus.POLICY_STATUS_FAILED:
      return "POLICY_STATUS_FAILED";
    case PolicyStatus.POLICY_STATUS_SUPERSEDED:
      return "POLICY_STATUS_SUPERSEDED";
    case PolicyStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Service status enum. */
export enum ServiceStatus {
  SERVICE_STATUS_UNSPECIFIED = 0,
  SERVICE_STATUS_HEALTHY = 1,
  SERVICE_STATUS_DEGRADED = 2,
  SERVICE_STATUS_UNHEALTHY = 3,
  UNRECOGNIZED = -1,
}

export function serviceStatusFromJSON(object: any): ServiceStatus {
  switch (object) {
    case 0:
    case "SERVICE_STATUS_UNSPECIFIED":
      return ServiceStatus.SERVICE_STATUS_UNSPECIFIED;
    case 1:
    case "SERVICE_STATUS_HEALTHY":
      return ServiceStatus.SERVICE_STATUS_HEALTHY;
    case 2:
    case "SERVICE_STATUS_DEGRADED":
      return ServiceStatus.SERVICE_STATUS_DEGRADED;
    case 3:
    case "SERVICE_STATUS_UNHEALTHY":
      return ServiceStatus.SERVICE_STATUS_UNHEALTHY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ServiceStatus.UNRECOGNIZED;
  }
}

export function serviceStatusToJSON(object: ServiceStatus): string {
  switch (object) {
    case ServiceStatus.SERVICE_STATUS_UNSPECIFIED:
      return "SERVICE_STATUS_UNSPECIFIED";
    case ServiceStatus.SERVICE_STATUS_HEALTHY:
      return "SERVICE_STATUS_HEALTHY";
    case ServiceStatus.SERVICE_STATUS_DEGRADED:
      return "SERVICE_STATUS_DEGRADED";
    case ServiceStatus.SERVICE_STATUS_UNHEALTHY:
      return "SERVICE_STATUS_UNHEALTHY";
    case ServiceStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Health check request. */
export interface HealthRequest {
}

/** Health check response. */
export interface HealthResponse {
  /** Service status. */
  status: ServiceStatus;
  /** Service version. */
  version: string;
}

/**
 * Public sandbox resource exposed by the OpenShell API.
 *
 * This is the canonical gateway-owned view of a sandbox. It merges user intent
 * (`spec`) with gateway-managed metadata and status derived from internal
 * compute-driver observations.
 *
 * Note: The `namespace` field has been removed from the public API. It remains
 * in the internal `DriverSandbox` message as a compute-driver implementation detail.
 */
export interface Sandbox {
  /** Kubernetes-style metadata (id, name, labels, timestamps, resource version). */
  metadata?:
    | ObjectMeta
    | undefined;
  /** Desired sandbox configuration submitted through the API. */
  spec?:
    | SandboxSpec
    | undefined;
  /** Latest user-facing observed status derived by the gateway. */
  status?:
    | SandboxStatus
    | undefined;
  /** Gateway-derived lifecycle summary. */
  phase: SandboxPhase;
  /** Currently active policy version (updated when sandbox reports loaded). */
  currentPolicyVersion: number;
}

/** Desired sandbox configuration provided through the public API. */
export interface SandboxSpec {
  /** Log level exposed to processes running inside the sandbox. */
  logLevel: string;
  /** Environment variables injected into the sandbox runtime. */
  environment: { [key: string]: string };
  /** Container or VM template used to provision the sandbox. */
  template?:
    | SandboxTemplate
    | undefined;
  /** Required sandbox policy configuration. */
  policy?:
    | SandboxPolicy
    | undefined;
  /** Provider names to attach to this sandbox. */
  providers: string[];
  /** Request NVIDIA GPU resources for this sandbox. */
  gpu: boolean;
  /**
   * Optional PCI BDF address (e.g. "0000:2d:00.0") or device index
   * (e.g. "0", "1"). When empty with gpu=true, the driver assigns the
   * first available GPU.
   */
  gpuDevice: string;
}

export interface SandboxSpec_EnvironmentEntry {
  key: string;
  value: string;
}

/** Public sandbox template mapped onto compute-driver template inputs. */
export interface SandboxTemplate {
  /** Fully-qualified OCI image reference used to boot the sandbox. */
  image: string;
  /** Optional runtime class name requested from the compute platform. */
  runtimeClassName: string;
  /** Optional agent socket path exposed to the workload. */
  agentSocket: string;
  /** Labels applied to compute-platform resources for this sandbox. */
  labels: { [key: string]: string };
  /** Annotations applied to compute-platform resources for this sandbox. */
  annotations: { [key: string]: string };
  /** Additional environment variables injected by the template. */
  environment: { [key: string]: string };
  /** Platform-specific compute resource requirements and limits. */
  resources?:
    | { [key: string]: any }
    | undefined;
  /** Optional platform-specific volume claim templates. */
  volumeClaimTemplates?:
    | { [key: string]: any }
    | undefined;
  /**
   * Enable Kubernetes user namespace isolation (hostUsers: false).
   * When true, container UID 0 maps to a non-root host UID and capabilities
   * become namespaced. Requires Kubernetes 1.33+ with user namespace support
   * available (beta through 1.35, GA in 1.36+) and a supporting runtime.
   * When unset, the cluster-wide default is used.
   */
  userNamespaces?: boolean | undefined;
}

export interface SandboxTemplate_LabelsEntry {
  key: string;
  value: string;
}

export interface SandboxTemplate_AnnotationsEntry {
  key: string;
  value: string;
}

export interface SandboxTemplate_EnvironmentEntry {
  key: string;
  value: string;
}

/**
 * User-facing sandbox status derived by the gateway from compute-driver observations.
 *
 * Lifecycle summary is exposed separately as `Sandbox.phase`. Public status does
 * not embed driver-only flags such as `deleting`.
 */
export interface SandboxStatus {
  /** Compute-platform sandbox object name. */
  sandboxName: string;
  /** Name of the agent pod or equivalent runtime instance. */
  agentPod: string;
  /** File descriptor or endpoint for reaching the agent service, when available. */
  agentFd: string;
  /** File descriptor or endpoint for reaching the sandbox service, when available. */
  sandboxFd: string;
  /** Latest user-facing readiness and lifecycle conditions. */
  conditions: SandboxCondition[];
}

/** User-facing sandbox condition derived from driver-native conditions. */
export interface SandboxCondition {
  /** Condition class, typically mirroring the underlying platform condition type. */
  type: string;
  /** Condition status value such as `True`, `False`, or `Unknown`. */
  status: string;
  /** Short machine-readable reason associated with the condition. */
  reason: string;
  /** Human-readable condition message. */
  message: string;
  /** Timestamp reported by the underlying platform for the last transition. */
  lastTransitionTime: string;
}

/** Public platform event exposed on the sandbox watch stream. */
export interface PlatformEvent {
  /** Event timestamp in milliseconds since epoch. */
  timestampMs: number;
  /** Event source (e.g. "kubernetes", "docker", "process"). */
  source: string;
  /** Event type/severity (e.g. "Normal", "Warning"). */
  type: string;
  /** Short reason code (e.g. "Started", "Pulled", "Failed"). */
  reason: string;
  /** Human-readable event message. */
  message: string;
  /** Optional metadata as key-value pairs. */
  metadata: { [key: string]: string };
}

export interface PlatformEvent_MetadataEntry {
  key: string;
  value: string;
}

/** Create sandbox request. */
export interface CreateSandboxRequest {
  spec?:
    | SandboxSpec
    | undefined;
  /** Optional user-supplied sandbox name. When empty the server generates one. */
  name: string;
  /** Optional labels for the sandbox (key-value metadata). */
  labels: { [key: string]: string };
}

export interface CreateSandboxRequest_LabelsEntry {
  key: string;
  value: string;
}

/** Get sandbox request. */
export interface GetSandboxRequest {
  /** Sandbox name (canonical lookup key). */
  name: string;
}

/** List sandboxes request. */
export interface ListSandboxesRequest {
  limit: number;
  offset: number;
  /** Optional label selector for filtering (format: "key1=value1,key2=value2"). */
  labelSelector: string;
}

/** List providers attached to a sandbox request. */
export interface ListSandboxProvidersRequest {
  /** Sandbox name (canonical lookup key). */
  sandboxName: string;
}

/** Attach provider to sandbox request. */
export interface AttachSandboxProviderRequest {
  /** Sandbox name (canonical lookup key). */
  sandboxName: string;
  /** Provider name to attach. */
  providerName: string;
  /**
   * Expected resource version for optimistic concurrency control.
   * If 0, the server uses the current version (backward compatibility).
   * If non-zero, the server validates that the sandbox's current resource_version
   * matches this value before applying the mutation, returning ABORTED on mismatch.
   */
  expectedResourceVersion: number;
}

/** Detach provider from sandbox request. */
export interface DetachSandboxProviderRequest {
  /** Sandbox name (canonical lookup key). */
  sandboxName: string;
  /** Provider name to detach. */
  providerName: string;
  /**
   * Expected resource version for optimistic concurrency control.
   * If 0, the server uses the current version (backward compatibility).
   * If non-zero, the server validates that the sandbox's current resource_version
   * matches this value before applying the mutation, returning ABORTED on mismatch.
   */
  expectedResourceVersion: number;
}

/** Delete sandbox request. */
export interface DeleteSandboxRequest {
  /** Sandbox name (canonical lookup key). */
  name: string;
}

/** Sandbox response. */
export interface SandboxResponse {
  sandbox?: Sandbox | undefined;
}

/** List sandboxes response. */
export interface ListSandboxesResponse {
  sandboxes: Sandbox[];
}

/** List providers attached to a sandbox response. */
export interface ListSandboxProvidersResponse {
  providers: Provider[];
}

/** Attach provider to sandbox response. */
export interface AttachSandboxProviderResponse {
  sandbox?:
    | Sandbox
    | undefined;
  /** True when the provider was newly attached. False means it was already attached. */
  attached: boolean;
}

/** Detach provider from sandbox response. */
export interface DetachSandboxProviderResponse {
  sandbox?:
    | Sandbox
    | undefined;
  /** True when the provider was removed. False means it was not attached. */
  detached: boolean;
}

/** Delete sandbox response. */
export interface DeleteSandboxResponse {
  deleted: boolean;
}

/** Create SSH session request. */
export interface CreateSshSessionRequest {
  /** Sandbox id. */
  sandboxId: string;
}

/**
 * Create SSH session response.
 *
 * Fields are interpolated into an SSH `ProxyCommand` string that OpenSSH
 * executes through `/bin/sh -c` on the caller's workstation. Servers MUST
 * uphold the charset contract below; clients MUST reject responses that
 * violate it. The client's own escaping provides defense-in-depth, but
 * narrow charsets close injection vectors at the trust boundary.
 */
export interface CreateSshSessionResponse {
  /** Sandbox id. [A-Za-z0-9._-]{1,128}. */
  sandboxId: string;
  /**
   * Session token for the gateway tunnel. URL-safe ASCII
   * ([A-Za-z0-9._~+/=-]) up to 4096 bytes. No shell metacharacters or
   * whitespace.
   */
  token: string;
  /**
   * Gateway host for SSH proxy connection. IPv4 address, bracketed IPv6
   * address, or DNS hostname (Punycode-encoded for IDN). Alphanumeric plus
   * `.-:[]` only, up to 253 bytes.
   */
  gatewayHost: string;
  /** Gateway port for SSH proxy connection. Must be in range 1..=65535. */
  gatewayPort: number;
  /** Gateway scheme. Must be exactly "http" or "https". */
  gatewayScheme: string;
  /** Optional host key fingerprint. If non-empty, [A-Za-z0-9:+/=-] only. */
  hostKeyFingerprint: string;
  /** Expiry timestamp in milliseconds since epoch. 0 means no expiry. */
  expiresAtMs: number;
}

/** Request to expose an HTTP service running inside a sandbox. */
export interface ExposeServiceRequest {
  /** Sandbox name. */
  sandbox: string;
  /** Service name within the sandbox. */
  service: string;
  /** Loopback TCP port inside the sandbox. */
  targetPort: number;
  /** Whether to print/use the browser-facing service URL. */
  domain: boolean;
}

/** Request to fetch an exposed sandbox service endpoint. */
export interface GetServiceRequest {
  /** Sandbox name. */
  sandbox: string;
  /** Service name within the sandbox. Empty selects the unnamed endpoint. */
  service: string;
}

/** Request to list exposed sandbox service endpoints. */
export interface ListServicesRequest {
  /** Optional sandbox name. Empty lists endpoints for all sandboxes. */
  sandbox: string;
  /** Page size. Zero uses the server default. */
  limit: number;
  /** Page offset. */
  offset: number;
}

/** Response containing exposed sandbox service endpoints. */
export interface ListServicesResponse {
  services: ServiceEndpointResponse[];
}

/** Request to delete an exposed sandbox service endpoint. */
export interface DeleteServiceRequest {
  /** Sandbox name. */
  sandbox: string;
  /** Service name within the sandbox. Empty selects the unnamed endpoint. */
  service: string;
}

/** Response for deleting an exposed sandbox service endpoint. */
export interface DeleteServiceResponse {
  /** True when an endpoint existed and was deleted. */
  deleted: boolean;
}

/** Persisted sandbox service endpoint. */
export interface ServiceEndpoint {
  /** Kubernetes-style metadata. */
  metadata?:
    | ObjectMeta
    | undefined;
  /** Sandbox object ID. */
  sandboxId: string;
  /** Sandbox name. */
  sandboxName: string;
  /** Service name within the sandbox. */
  serviceName: string;
  /** Loopback TCP port inside the sandbox. */
  targetPort: number;
  /** Whether browser-facing service routing is enabled for this endpoint. */
  domain: boolean;
}

/** Response containing a service endpoint and, when available, its local URL. */
export interface ServiceEndpointResponse {
  endpoint?: ServiceEndpoint | undefined;
  url: string;
}

/** Revoke SSH session request. */
export interface RevokeSshSessionRequest {
  /** Session token to revoke. */
  token: string;
}

/** Revoke SSH session response. */
export interface RevokeSshSessionResponse {
  /** True when a session was revoked. */
  revoked: boolean;
}

/** Execute command request. */
export interface ExecSandboxRequest {
  /** Sandbox id. */
  sandboxId: string;
  /** Command and arguments. */
  command: string[];
  /** Optional working directory. */
  workdir: string;
  /** Optional environment overrides. */
  environment: { [key: string]: string };
  /** Optional timeout in seconds. 0 means no timeout. */
  timeoutSeconds: number;
  /** Optional stdin payload passed to the command. */
  stdin: Buffer;
  /** Request a pseudo-terminal for the remote command. */
  tty: boolean;
  /** Initial terminal columns (used when tty=true, 0 = use default). */
  cols: number;
  /** Initial terminal rows (used when tty=true, 0 = use default). */
  rows: number;
}

export interface ExecSandboxRequest_EnvironmentEntry {
  key: string;
  value: string;
}

/** One stdout chunk from a sandbox exec. */
export interface ExecSandboxStdout {
  data: Buffer;
}

/** One stderr chunk from a sandbox exec. */
export interface ExecSandboxStderr {
  data: Buffer;
}

/** Final exit status for a sandbox exec. */
export interface ExecSandboxExit {
  exitCode: number;
}

/** One event in a sandbox exec stream. */
export interface ExecSandboxEvent {
  payload?: { $case: "stdout"; stdout: ExecSandboxStdout } | { $case: "stderr"; stderr: ExecSandboxStderr } | {
    $case: "exit";
    exit: ExecSandboxExit;
  } | undefined;
}

/** Initial frame for one TCP forward stream. */
export interface TcpForwardInit {
  /** Sandbox id. */
  sandboxId: string;
  /** Optional service identifier for audit/correlation. */
  serviceId: string;
  /** Target the gateway should request from the supervisor. */
  target?:
    | { $case: "ssh"; ssh: SshRelayTarget }
    | { $case: "tcp"; tcp: TcpRelayTarget }
    | undefined;
  /**
   * Optional target-specific authorization token. SSH targets use this as the
   * short-lived SSH session token issued by CreateSshSession.
   */
  authorizationToken: string;
}

/** A single frame on the CLI-to-gateway TCP forward stream. */
export interface TcpForwardFrame {
  payload?: { $case: "init"; init: TcpForwardInit } | { $case: "data"; data: Buffer } | undefined;
}

/** Client-to-server message for interactive exec. */
export interface ExecSandboxInput {
  payload?:
    | //
    /** First message: exec request metadata. */
    { $case: "start"; start: ExecSandboxRequest }
    | //
    /** Subsequent messages: raw stdin bytes. */
    { $case: "stdin"; stdin: Buffer }
    | //
    /** Terminal window size change. */
    { $case: "resize"; resize: ExecSandboxWindowResize }
    | undefined;
}

/** Terminal window resize event for interactive exec. */
export interface ExecSandboxWindowResize {
  cols: number;
  rows: number;
}

/** SSH session record stored in persistence. */
export interface SshSession {
  /** Kubernetes-style metadata (id, name, labels, timestamps, resource version). */
  metadata?:
    | ObjectMeta
    | undefined;
  /** Sandbox id. */
  sandboxId: string;
  /** Session token. */
  token: string;
  /**
   * Expiry timestamp in milliseconds since epoch. 0 means no expiry
   * (backward-compatible default for sessions created before this field existed).
   */
  expiresAtMs: number;
  /** Revoked flag. */
  revoked: boolean;
}

/** Watch sandbox request. */
export interface WatchSandboxRequest {
  /** Sandbox id. */
  id: string;
  /** Stream sandbox status snapshots. */
  followStatus: boolean;
  /** Stream openshell-server process logs correlated to this sandbox. */
  followLogs: boolean;
  /** Stream platform events correlated to this sandbox. */
  followEvents: boolean;
  /** Replay the last N log lines (best-effort) before following. */
  logTailLines: number;
  /** Replay the last N platform events (best-effort) before following. */
  eventTail: number;
  /** Stop streaming once the sandbox reaches a terminal phase (READY or ERROR). */
  stopOnTerminal: boolean;
  /**
   * Only include log lines with timestamp >= this value (milliseconds since epoch).
   * 0 means no time filter. Applies to both tail replay and live streaming.
   */
  logSinceMs: number;
  /** Filter by log source (e.g. "gateway", "sandbox"). Empty means all sources. */
  logSources: string[];
  /** Minimum log level to include (e.g. "INFO", "WARN", "ERROR"). Empty means all levels. */
  logMinLevel: string;
}

/** One event in a sandbox watch stream. */
export interface SandboxStreamEvent {
  payload?:
    | //
    /** Latest sandbox snapshot. */
    { $case: "sandbox"; sandbox: Sandbox }
    | //
    /** One server log line/event. */
    { $case: "log"; log: SandboxLogLine }
    | //
    /** One platform event. */
    { $case: "event"; event: PlatformEvent }
    | //
    /** Warning from the server (e.g. missed messages due to lag). */
    { $case: "warning"; warning: SandboxStreamWarning }
    | //
    /** Draft policy update notification. */
    { $case: "draftPolicyUpdate"; draftPolicyUpdate: DraftPolicyUpdate }
    | undefined;
}

/** Log line correlated to a sandbox. */
export interface SandboxLogLine {
  sandboxId: string;
  timestampMs: number;
  level: string;
  target: string;
  message: string;
  /**
   * Log source: "gateway" (server-side) or "sandbox" (supervisor).
   * Empty is treated as "gateway" for backward compatibility.
   */
  source: string;
  /** Structured key-value fields from the tracing event (e.g. dst_host, action). */
  fields: { [key: string]: string };
}

export interface SandboxLogLine_FieldsEntry {
  key: string;
  value: string;
}

export interface SandboxStreamWarning {
  message: string;
}

/** Create provider request. */
export interface CreateProviderRequest {
  provider?: Provider | undefined;
}

/** Get provider request. */
export interface GetProviderRequest {
  name: string;
}

/** List providers request. */
export interface ListProvidersRequest {
  limit: number;
  offset: number;
}

/** Update provider request. */
export interface UpdateProviderRequest {
  provider?:
    | Provider
    | undefined;
  /**
   * Optional per-credential expiry timestamps to merge into the provider.
   * A zero value removes the expiry for that credential.
   */
  credentialExpiresAtMs: { [key: string]: number };
}

export interface UpdateProviderRequest_CredentialExpiresAtMsEntry {
  key: string;
  value: number;
}

/** Delete provider request. */
export interface DeleteProviderRequest {
  name: string;
}

/** Provider response. */
export interface ProviderResponse {
  provider?: Provider | undefined;
}

/** List providers response. */
export interface ListProvidersResponse {
  providers: Provider[];
}

/** List provider type profiles request. */
export interface ListProviderProfilesRequest {
  limit: number;
  offset: number;
}

/** Fetch provider type profile request. */
export interface GetProviderProfileRequest {
  id: string;
}

/** Provider profile payload with optional source metadata for diagnostics. */
export interface ProviderProfileImportItem {
  profile?: ProviderProfile | undefined;
  source: string;
}

/** Provider profile validation diagnostic. */
export interface ProviderProfileDiagnostic {
  source: string;
  profileId: string;
  field: string;
  message: string;
  severity: string;
}

/** Provider credential declaration. */
export interface ProviderProfileCredential {
  name: string;
  description: string;
  envVars: string[];
  required: boolean;
  authStyle: string;
  headerName: string;
  queryParam: string;
  refresh?: ProviderCredentialRefresh | undefined;
}

export interface ProviderCredentialRefreshMaterial {
  name: string;
  description: string;
  required: boolean;
  secret: boolean;
}

export interface ProviderCredentialRefresh {
  strategy: ProviderCredentialRefreshStrategy;
  tokenUrl: string;
  scopes: string[];
  refreshBeforeSeconds: number;
  maxLifetimeSeconds: number;
  material: ProviderCredentialRefreshMaterial[];
}

export interface ProviderCredentialRefreshStatus {
  providerName: string;
  providerId: string;
  credentialKey: string;
  strategy: ProviderCredentialRefreshStrategy;
  status: string;
  expiresAtMs: number;
  nextRefreshAtMs: number;
  lastRefreshAtMs: number;
  lastError: string;
}

export interface StoredProviderCredentialRefreshState {
  metadata?: ObjectMeta | undefined;
  providerId: string;
  providerName: string;
  credentialKey: string;
  strategy: ProviderCredentialRefreshStrategy;
  material: { [key: string]: string };
  secretMaterialKeys: string[];
  expiresAtMs: number;
  nextRefreshAtMs: number;
  lastRefreshAtMs: number;
  status: string;
  lastError: string;
  tokenUrl: string;
  scopes: string[];
  refreshBeforeSeconds: number;
  maxLifetimeSeconds: number;
}

export interface StoredProviderCredentialRefreshState_MaterialEntry {
  key: string;
  value: string;
}

export interface GetProviderRefreshStatusRequest {
  provider: string;
  credentialKey: string;
}

export interface GetProviderRefreshStatusResponse {
  credentials: ProviderCredentialRefreshStatus[];
}

export interface ConfigureProviderRefreshRequest {
  provider: string;
  credentialKey: string;
  strategy: ProviderCredentialRefreshStrategy;
  material: { [key: string]: string };
  secretMaterialKeys: string[];
  expiresAtMs?: number | undefined;
}

export interface ConfigureProviderRefreshRequest_MaterialEntry {
  key: string;
  value: string;
}

export interface ConfigureProviderRefreshResponse {
  status?: ProviderCredentialRefreshStatus | undefined;
}

export interface RotateProviderCredentialRequest {
  provider: string;
  credentialKey: string;
}

export interface RotateProviderCredentialResponse {
  status?: ProviderCredentialRefreshStatus | undefined;
}

export interface DeleteProviderRefreshRequest {
  provider: string;
  credentialKey: string;
}

export interface DeleteProviderRefreshResponse {
  deleted: boolean;
}

/** Provider type profile metadata exposed to clients. */
export interface ProviderProfile {
  id: string;
  displayName: string;
  description: string;
  category: ProviderProfileCategory;
  credentials: ProviderProfileCredential[];
  endpoints: NetworkEndpoint[];
  binaries: NetworkBinary[];
  inferenceCapable: boolean;
}

/** Stored custom provider profile object. */
export interface StoredProviderProfile {
  metadata?: ObjectMeta | undefined;
  profile?: ProviderProfile | undefined;
}

/** Provider profile response. */
export interface ProviderProfileResponse {
  profile?: ProviderProfile | undefined;
}

/** List provider profiles response. */
export interface ListProviderProfilesResponse {
  profiles: ProviderProfile[];
}

/** Import custom provider profiles request. */
export interface ImportProviderProfilesRequest {
  profiles: ProviderProfileImportItem[];
}

/** Import custom provider profiles response. */
export interface ImportProviderProfilesResponse {
  diagnostics: ProviderProfileDiagnostic[];
  profiles: ProviderProfile[];
  imported: boolean;
}

/** Lint provider profiles request. */
export interface LintProviderProfilesRequest {
  profiles: ProviderProfileImportItem[];
}

/** Lint provider profiles response. */
export interface LintProviderProfilesResponse {
  diagnostics: ProviderProfileDiagnostic[];
  valid: boolean;
}

/** Delete provider response. */
export interface DeleteProviderResponse {
  deleted: boolean;
}

/** Delete custom provider profile request. */
export interface DeleteProviderProfileRequest {
  id: string;
}

/** Delete custom provider profile response. */
export interface DeleteProviderProfileResponse {
  deleted: boolean;
}

/** Get sandbox provider environment request. */
export interface GetSandboxProviderEnvironmentRequest {
  /** The sandbox ID. */
  sandboxId: string;
}

/** Get sandbox provider environment response. */
export interface GetSandboxProviderEnvironmentResponse {
  /** Provider credential environment variables. */
  environment: { [key: string]: string };
  /** Fingerprint for the provider credential inputs that produced environment. */
  providerEnvRevision: number;
  /** Expiration timestamps for returned environment variables. */
  credentialExpiresAtMs: { [key: string]: number };
}

export interface GetSandboxProviderEnvironmentResponse_EnvironmentEntry {
  key: string;
  value: string;
}

export interface GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry {
  key: string;
  value: number;
}

/** Update sandbox policy request. */
export interface UpdateConfigRequest {
  /**
   * Sandbox name (canonical lookup key). Required for sandbox-scoped updates.
   * Not required when `global=true`.
   */
  name: string;
  /**
   * The new policy to apply.
   *
   * Sandbox scope (`global=false`):
   * - only network_policies and inference fields may differ from create-time
   *   policy; static fields must match version 1.
   *
   * Global scope (`global=true`):
   * - applies to all sandboxes in full (no merge).
   */
  policy?:
    | SandboxPolicy
    | undefined;
  /** Optional single setting key to mutate. */
  settingKey: string;
  /** Setting value for upsert operations. */
  settingValue?:
    | SettingValue
    | undefined;
  /**
   * Delete the setting key from scope.
   * Sandbox-scoped deletes are rejected; only global delete is supported.
   */
  deleteSetting: boolean;
  /** Apply mutation at gateway-global scope. */
  global: boolean;
  /** Batched incremental policy merge operations. Sandbox-scoped only. */
  mergeOperations: PolicyMergeOperation[];
  /**
   * Expected resource version for optimistic concurrency control (sandbox-scoped only).
   * If 0, the server uses the current version (backward compatibility).
   * If non-zero, the server validates that the sandbox's current resource_version
   * matches this value before applying the mutation, returning ABORTED on mismatch.
   * Ignored for global-scoped updates.
   */
  expectedResourceVersion: number;
}

export interface PolicyMergeOperation {
  operation?:
    | { $case: "addRule"; addRule: AddNetworkRule }
    | { $case: "removeEndpoint"; removeEndpoint: RemoveNetworkEndpoint }
    | { $case: "removeRule"; removeRule: RemoveNetworkRule }
    | { $case: "addDenyRules"; addDenyRules: AddDenyRules }
    | { $case: "addAllowRules"; addAllowRules: AddAllowRules }
    | { $case: "removeBinary"; removeBinary: RemoveNetworkBinary }
    | undefined;
}

export interface AddNetworkRule {
  ruleName: string;
  rule?: NetworkPolicyRule | undefined;
}

export interface RemoveNetworkEndpoint {
  ruleName: string;
  host: string;
  port: number;
}

export interface RemoveNetworkRule {
  ruleName: string;
}

export interface AddDenyRules {
  host: string;
  port: number;
  denyRules: L7DenyRule[];
}

export interface AddAllowRules {
  host: string;
  port: number;
  rules: L7Rule[];
}

export interface RemoveNetworkBinary {
  ruleName: string;
  binaryPath: string;
}

/** Update sandbox policy response. */
export interface UpdateConfigResponse {
  /** Assigned policy version (monotonically increasing per sandbox). */
  version: number;
  /** SHA-256 hash of the serialized policy payload. */
  policyHash: string;
  /** Settings revision for the scope that was modified. */
  settingsRevision: number;
  /** True when a setting delete operation removed an existing key. */
  deleted: boolean;
}

/** Get sandbox policy status request. */
export interface GetSandboxPolicyStatusRequest {
  /** Sandbox name (canonical lookup key). Ignored when global is true. */
  name: string;
  /** The specific policy version to query. 0 means latest. */
  version: number;
  /** Query global policy revisions instead of a sandbox-scoped one. */
  global: boolean;
}

/** Get sandbox policy status response. */
export interface GetSandboxPolicyStatusResponse {
  /** The queried policy revision. */
  revision?:
    | SandboxPolicyRevision
    | undefined;
  /** The currently active (loaded) policy version for this sandbox. */
  activeVersion: number;
}

/** List sandbox policies request. */
export interface ListSandboxPoliciesRequest {
  /** Sandbox name (canonical lookup key). Ignored when global is true. */
  name: string;
  limit: number;
  offset: number;
  /** List global policy revisions instead of sandbox-scoped ones. */
  global: boolean;
}

/** List sandbox policies response. */
export interface ListSandboxPoliciesResponse {
  revisions: SandboxPolicyRevision[];
}

/** Report policy load status (called by sandbox runtime after reload attempt). */
export interface ReportPolicyStatusRequest {
  /** Sandbox id. */
  sandboxId: string;
  /** The policy version that was attempted. */
  version: number;
  /** Load result status. */
  status: PolicyStatus;
  /** Error message if status is FAILED. */
  loadError: string;
}

/** Report policy status response. */
export interface ReportPolicyStatusResponse {
}

/** A versioned policy revision with metadata. */
export interface SandboxPolicyRevision {
  /** Policy version (monotonically increasing per sandbox). */
  version: number;
  /** SHA-256 hash of the serialized policy payload. */
  policyHash: string;
  /** Load status of this revision. */
  status: PolicyStatus;
  /** Error message if status is FAILED. */
  loadError: string;
  /** Milliseconds since epoch when this revision was created. */
  createdAtMs: number;
  /** Milliseconds since epoch when this revision was loaded by the sandbox. */
  loadedAtMs: number;
  /** The full policy (only populated when explicitly requested). */
  policy?: SandboxPolicy | undefined;
}

/** Get sandbox logs request (one-shot fetch). */
export interface GetSandboxLogsRequest {
  /** Sandbox id. */
  sandboxId: string;
  /** Maximum number of log lines to return. 0 means use default (2000). */
  lines: number;
  /** Only include logs with timestamp >= this value (ms since epoch). 0 means no filter. */
  sinceMs: number;
  /** Filter by log source (e.g. "gateway", "sandbox"). Empty means all sources. */
  sources: string[];
  /** Minimum log level to include (e.g. "INFO", "WARN", "ERROR"). Empty means all levels. */
  minLevel: string;
}

/** Batch of log lines pushed from sandbox to server. */
export interface PushSandboxLogsRequest {
  /** The sandbox ID. */
  sandboxId: string;
  /** Log lines to ingest. */
  logs: SandboxLogLine[];
}

/** Push sandbox logs response. */
export interface PushSandboxLogsResponse {
}

/** Get sandbox logs response. */
export interface GetSandboxLogsResponse {
  /** Log lines in chronological order. */
  logs: SandboxLogLine[];
  /** Total number of lines in the server's buffer for this sandbox. */
  bufferTotal: number;
}

/** Envelope for supervisor-to-gateway messages on the ConnectSupervisor stream. */
export interface SupervisorMessage {
  payload?:
    | { $case: "hello"; hello: SupervisorHello }
    | { $case: "heartbeat"; heartbeat: SupervisorHeartbeat }
    | { $case: "relayOpenResult"; relayOpenResult: RelayOpenResult }
    | { $case: "relayClose"; relayClose: RelayClose }
    | undefined;
}

/** Envelope for gateway-to-supervisor messages on the ConnectSupervisor stream. */
export interface GatewayMessage {
  payload?:
    | { $case: "sessionAccepted"; sessionAccepted: SessionAccepted }
    | { $case: "sessionRejected"; sessionRejected: SessionRejected }
    | { $case: "heartbeat"; heartbeat: GatewayHeartbeat }
    | { $case: "relayOpen"; relayOpen: RelayOpen }
    | { $case: "relayClose"; relayClose: RelayClose }
    | undefined;
}

/** Supervisor identifies itself and the sandbox it manages. */
export interface SupervisorHello {
  /** Sandbox ID this supervisor manages. */
  sandboxId: string;
  /** Supervisor instance ID (e.g. boot id or process epoch). */
  instanceId: string;
}

/** Gateway accepts the supervisor session. */
export interface SessionAccepted {
  /** Gateway-assigned session ID for this connection. */
  sessionId: string;
  /** Recommended heartbeat interval in seconds. */
  heartbeatIntervalSecs: number;
}

/** Gateway rejects the supervisor session. */
export interface SessionRejected {
  /** Human-readable rejection reason. */
  reason: string;
}

/** Supervisor heartbeat. */
export interface SupervisorHeartbeat {
}

/** Gateway heartbeat. */
export interface GatewayHeartbeat {
}

/**
 * Gateway requests the supervisor to open a relay channel.
 *
 * On receiving this, the supervisor should initiate a RelayStream RPC to
 * the gateway, sending a RelayInit in the first RelayFrame to associate
 * the new HTTP/2 stream with the pending relay slot. The supervisor
 * bridges that stream to the requested local target.
 */
export interface RelayOpen {
  /** Gateway-allocated channel identifier (UUID). */
  channelId: string;
  /**
   * Target the supervisor should dial inside the sandbox.
   * If absent, supervisors treat the relay as SSH for compatibility.
   */
  target?:
    | { $case: "ssh"; ssh: SshRelayTarget }
    | { $case: "tcp"; tcp: TcpRelayTarget }
    | undefined;
  /** Optional service identifier for audit/correlation. */
  serviceId: string;
}

/** Built-in SSH relay target. */
export interface SshRelayTarget {
}

/** TCP target dialed by the supervisor from inside the sandbox. */
export interface TcpRelayTarget {
  /** Phase 1 accepts loopback only: 127.0.0.1, ::1, or localhost. */
  host: string;
  /** Target port. Must fit in u16 and be non-zero. */
  port: number;
}

/** Initial RelayStream frame sent by the supervisor to claim a pending relay. */
export interface RelayInit {
  /** Gateway-allocated channel identifier (UUID). */
  channelId: string;
}

/**
 * A single frame on the RelayStream RPC.
 *
 * The supervisor MUST send `init` as the first frame. All subsequent frames
 * in either direction carry raw bytes in `data`.
 */
export interface RelayFrame {
  payload?: { $case: "init"; init: RelayInit } | { $case: "data"; data: Buffer } | undefined;
}

/** Supervisor reports the result of a relay open request. */
export interface RelayOpenResult {
  /** Channel identifier from the RelayOpen request. */
  channelId: string;
  /** True if the relay was successfully established. */
  success: boolean;
  /** Error message if success is false. */
  error: string;
}

/** Either side requests closure of a relay channel. */
export interface RelayClose {
  /** Channel identifier to close. */
  channelId: string;
  /** Optional reason for closure. */
  reason: string;
}

/** Observed HTTP method+path pattern from L7 inspection. */
export interface L7RequestSample {
  /** HTTP method: GET, POST, PUT, DELETE, etc. */
  method: string;
  /** HTTP path: /v1/models, /repos/myorg/issues */
  path: string;
  /** L7 decision: "audit" or "deny" (allowed requests not collected). */
  decision: string;
  /** Number of times this (method, path) was observed. */
  count: number;
}

/** Structured denial summary from sandbox aggregator. */
export interface DenialSummary {
  /** Sandbox ID that produced this summary. */
  sandboxId: string;
  /** Denied destination host. */
  host: string;
  /** Denied destination port. */
  port: number;
  /** Binary that attempted the connection. */
  binary: string;
  /** Process ancestor chain. */
  ancestors: string[];
  /** Denial reason from OPA evaluation. */
  denyReason: string;
  /** First denial timestamp (ms since epoch). */
  firstSeenMs: number;
  /** Most recent denial timestamp (ms since epoch). */
  lastSeenMs: number;
  /** Number of denials in the current window. */
  count: number;
  /** Events dropped during aggregator cooldown. */
  suppressedCount: number;
  /** Cumulative lifetime count (never resets). */
  totalCount: number;
  /** Distinct cmdline strings observed (sanitized of credentials). */
  sampleCmdlines: string[];
  /** SHA-256 of the binary for audit trail. */
  binarySha256: string;
  /** True if emitted by stale-flush rather than threshold. */
  persistent: boolean;
  /** Denial category: "l4_deny", "l7_deny", "l7_audit", "ssrf". */
  denialStage: string;
  /** Observed HTTP request patterns (from L7 inspection). */
  l7RequestSamples: L7RequestSample[];
  /** True if L7 inspection was active during observation window. */
  l7InspectionActive: boolean;
}

/** A proposed policy rule with rationale and approval status. */
export interface PolicyChunk {
  /** Unique chunk identifier. */
  id: string;
  /** Approval status: "pending", "approved", "rejected". */
  status: string;
  /** Proposed network_policies map key. */
  ruleName: string;
  /** The proposed network policy rule. */
  proposedRule?:
    | NetworkPolicyRule
    | undefined;
  /** Human-readable explanation of why this rule is proposed. */
  rationale: string;
  /** Security concerns flagged by analysis (empty if none). */
  securityNotes: string;
  /** Analysis confidence (0.0-1.0). 0 for mechanistic mode. */
  confidence: number;
  /** IDs of denial summaries that led to this chunk. */
  denialSummaryIds: string[];
  /** Creation timestamp (ms since epoch). */
  createdAtMs: number;
  /** When the user approved/rejected (ms since epoch). 0 if undecided. */
  decidedAtMs: number;
  /** Recommendation stage: "initial" or "refined" (progressive L7 visibility). */
  stage: string;
  /** For stage="refined": the initial chunk this replaces. */
  supersedesChunkId: string;
  /** How many times this endpoint has been seen across denial flush cycles. */
  hitCount: number;
  /** First time this endpoint was proposed (ms since epoch). */
  firstSeenMs: number;
  /** Most recent time this endpoint was re-proposed (ms since epoch). */
  lastSeenMs: number;
  /** Binary path that triggered the denial (denormalized for display convenience). */
  binary: string;
  /**
   * Validation verdict from gateway-side static checks (prover output).
   * Free-form summary string for human consumption in the inbox card.
   * Empty until the prover has run for this chunk.
   */
  validationResult: string;
  /**
   * Operator-supplied free-form text accompanying a rejection. Populated
   * when the reviewer rejects via `RejectDraftChunkRequest.reason`; surfaced
   * back to the in-sandbox agent so it can revise the proposal.
   * Empty for non-rejected chunks.
   */
  rejectionReason: string;
}

/** Notification that the draft policy was updated. */
export interface DraftPolicyUpdate {
  /** Current draft version. */
  draftVersion: number;
  /** Number of new chunks added in this update. */
  newChunks: number;
  /** Total pending chunks awaiting approval. */
  totalPending: number;
  /** Brief description of what changed. */
  summary: string;
}

/** Submit analysis results from sandbox to gateway. */
export interface SubmitPolicyAnalysisRequest {
  /** Aggregated denial summaries. */
  summaries: DenialSummary[];
  /** Proposed policy chunks (validated by sandbox OPA engine). */
  proposedChunks: PolicyChunk[];
  /**
   * Analysis mode. `mechanistic` is the observation-driven path from the
   * denial aggregator — chunks targeting the same host|port|binary fold
   * into one row with hit_count incremented. `agent_authored` is an
   * intentional proposal from an in-sandbox agent — each submission lands
   * as its own chunk so the redraft-after-rejection loop has a stable id
   * to watch. Other values are treated as agent-style (no dedup) so a new
   * mode does not silently collapse proposals.
   */
  analysisMode: string;
  /** Sandbox name. */
  name: string;
}

export interface SubmitPolicyAnalysisResponse {
  /** Number of chunks accepted by the gateway. */
  acceptedChunks: number;
  /** Number of chunks rejected by gateway validation. */
  rejectedChunks: number;
  /** Reasons for each rejected chunk. */
  rejectionReasons: string[];
  /**
   * Server-assigned chunk IDs for the accepted chunks, in submission order.
   * Agents use these to watch proposal state via policy.local's
   * GET /v1/proposals/{id} and /wait endpoints.
   */
  acceptedChunkIds: string[];
}

/** Get draft policy for a sandbox. */
export interface GetDraftPolicyRequest {
  /** Sandbox name. */
  name: string;
  /** Optional status filter: "pending", "approved", "rejected", or "" for all. */
  statusFilter: string;
}

export interface GetDraftPolicyResponse {
  /** Draft policy chunks. */
  chunks: PolicyChunk[];
  /** LLM-generated summary of all analysis (empty in mechanistic mode). */
  rollingSummary: string;
  /** Current draft version. */
  draftVersion: number;
  /** When the last analysis completed (ms since epoch). */
  lastAnalyzedAtMs: number;
}

/** Approve a single draft chunk. */
export interface ApproveDraftChunkRequest {
  /** Sandbox name. */
  name: string;
  /** Chunk ID to approve. */
  chunkId: string;
}

export interface ApproveDraftChunkResponse {
  /** New policy version after merge. */
  policyVersion: number;
  /** SHA-256 hash of the new policy. */
  policyHash: string;
}

/** Reject a single draft chunk. */
export interface RejectDraftChunkRequest {
  /** Sandbox name. */
  name: string;
  /** Chunk ID to reject. */
  chunkId: string;
  /** Optional reason for rejection (fed to LLM context in future analysis). */
  reason: string;
}

export interface RejectDraftChunkResponse {
}

/** Approve all pending chunks. */
export interface ApproveAllDraftChunksRequest {
  /** Sandbox name. */
  name: string;
  /** Include chunks with security_notes (default false: skips them). */
  includeSecurityFlagged: boolean;
}

export interface ApproveAllDraftChunksResponse {
  /** New policy version after merge. */
  policyVersion: number;
  /** SHA-256 hash of the new policy. */
  policyHash: string;
  /** Number of chunks approved. */
  chunksApproved: number;
  /** Number of chunks skipped (security-flagged). */
  chunksSkipped: number;
}

/** Edit a pending chunk in-place. */
export interface EditDraftChunkRequest {
  /** Sandbox name. */
  name: string;
  /** Chunk ID to edit. */
  chunkId: string;
  /** The modified rule (replaces existing proposed_rule). */
  proposedRule?: NetworkPolicyRule | undefined;
}

export interface EditDraftChunkResponse {
}

/** Reverse an approval (remove merged rule from active policy). */
export interface UndoDraftChunkRequest {
  /** Sandbox name. */
  name: string;
  /** Chunk ID to undo. */
  chunkId: string;
}

export interface UndoDraftChunkResponse {
  /** New policy version after removal. */
  policyVersion: number;
  /** SHA-256 hash of the updated policy. */
  policyHash: string;
}

/** Clear all pending draft chunks for a sandbox. */
export interface ClearDraftChunksRequest {
  /** Sandbox name. */
  name: string;
}

export interface ClearDraftChunksResponse {
  /** Number of chunks cleared. */
  chunksCleared: number;
}

/** Get decision history for a sandbox's draft policy. */
export interface GetDraftHistoryRequest {
  /** Sandbox name. */
  name: string;
}

export interface DraftHistoryEntry {
  /** Event timestamp (ms since epoch). */
  timestampMs: number;
  /**
   * Event type: "denial_detected", "analysis_cycle", "approved",
   * "rejected", "edited", "undone", "cleared".
   */
  eventType: string;
  /** Human-readable description. */
  description: string;
  /** Associated chunk ID (if applicable). */
  chunkId: string;
}

export interface GetDraftHistoryResponse {
  /** Chronological decision history. */
  entries: DraftHistoryEntry[];
}

/** Stored payload for a policy revision row in the generic objects table. */
export interface PolicyRevisionPayload {
  /** Serialized policy contents. */
  policy?:
    | SandboxPolicy
    | undefined;
  /** Deterministic hash of the policy payload. */
  hash: string;
  /** Load error reported by the sandbox, if any. */
  loadError: string;
  /** When the policy version was reported as loaded (ms since epoch). 0 if unset. */
  loadedAtMs: number;
}

/** Stored payload for a draft policy chunk row in the generic objects table. */
export interface DraftChunkPayload {
  /** Proposed network_policies map key. */
  ruleName: string;
  /** Proposed network policy rule. */
  proposedRule?:
    | NetworkPolicyRule
    | undefined;
  /** Human-readable explanation of why this rule is proposed. */
  rationale: string;
  /** Security concerns flagged by analysis (empty if none). */
  securityNotes: string;
  /** Analysis confidence (0.0-1.0). 0 for mechanistic mode. */
  confidence: number;
  /** When the user approved/rejected (ms since epoch). 0 if undecided. */
  decidedAtMs: number;
  /** Denormalized endpoint host for dedup and display. */
  host: string;
  /** Denormalized endpoint port for dedup and display. */
  port: number;
  /** Binary path that triggered the denial. */
  binary: string;
  /** Current draft version for the owning sandbox. */
  draftVersion: number;
  /**
   * Gateway prover verdict for this chunk; empty until prover runs.
   * Mirrors PolicyChunk.validation_result.
   */
  validationResult: string;
  /**
   * Operator-supplied free-form rejection text; empty for non-rejected
   * chunks. Mirrors PolicyChunk.rejection_reason.
   */
  rejectionReason: string;
}

/** Internal stored policy revision row materialized from the generic objects table. */
export interface StoredPolicyRevision {
  id: string;
  sandboxId: string;
  version: number;
  policyPayload: Buffer;
  policyHash: string;
  status: string;
  loadError?: string | undefined;
  createdAtMs: number;
  loadedAtMs?: number | undefined;
}

/** Internal stored draft chunk row materialized from the generic objects table. */
export interface StoredDraftChunk {
  id: string;
  sandboxId: string;
  draftVersion: number;
  status: string;
  ruleName: string;
  proposedRule: Buffer;
  rationale: string;
  securityNotes: string;
  confidence: number;
  createdAtMs: number;
  decidedAtMs?: number | undefined;
  host: string;
  port: number;
  binary: string;
  hitCount: number;
  firstSeenMs: number;
  lastSeenMs: number;
  /** Gateway prover verdict; empty until the prover runs. See PolicyChunk. */
  validationResult: string;
  /** Operator-supplied free-form rejection text. See PolicyChunk. */
  rejectionReason: string;
}

function createBaseHealthRequest(): HealthRequest {
  return {};
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(_: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HealthRequest {
    return {};
  },

  toJSON(_: HealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthRequest>, I>>(base?: I): HealthRequest {
    return HealthRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthRequest>, I>>(_: I): HealthRequest {
    const message = createBaseHealthRequest();
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { status: 0, version: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      status: isSet(object.status) ? serviceStatusFromJSON(object.status) : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = serviceStatusToJSON(message.status);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthResponse>, I>>(base?: I): HealthResponse {
    return HealthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthResponse>, I>>(object: I): HealthResponse {
    const message = createBaseHealthResponse();
    message.status = object.status ?? 0;
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseSandbox(): Sandbox {
  return { metadata: undefined, spec: undefined, status: undefined, phase: 0, currentPolicyVersion: 0 };
}

export const Sandbox: MessageFns<Sandbox> = {
  encode(message: Sandbox, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.spec !== undefined) {
      SandboxSpec.encode(message.spec, writer.uint32(18).fork()).join();
    }
    if (message.status !== undefined) {
      SandboxStatus.encode(message.status, writer.uint32(26).fork()).join();
    }
    if (message.phase !== 0) {
      writer.uint32(32).int32(message.phase);
    }
    if (message.currentPolicyVersion !== 0) {
      writer.uint32(40).uint32(message.currentPolicyVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Sandbox {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandbox();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = ObjectMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.spec = SandboxSpec.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = SandboxStatus.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.phase = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.currentPolicyVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Sandbox {
    return {
      metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
      spec: isSet(object.spec) ? SandboxSpec.fromJSON(object.spec) : undefined,
      status: isSet(object.status) ? SandboxStatus.fromJSON(object.status) : undefined,
      phase: isSet(object.phase) ? sandboxPhaseFromJSON(object.phase) : 0,
      currentPolicyVersion: isSet(object.currentPolicyVersion)
        ? globalThis.Number(object.currentPolicyVersion)
        : isSet(object.current_policy_version)
        ? globalThis.Number(object.current_policy_version)
        : 0,
    };
  },

  toJSON(message: Sandbox): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = ObjectMeta.toJSON(message.metadata);
    }
    if (message.spec !== undefined) {
      obj.spec = SandboxSpec.toJSON(message.spec);
    }
    if (message.status !== undefined) {
      obj.status = SandboxStatus.toJSON(message.status);
    }
    if (message.phase !== 0) {
      obj.phase = sandboxPhaseToJSON(message.phase);
    }
    if (message.currentPolicyVersion !== 0) {
      obj.currentPolicyVersion = Math.round(message.currentPolicyVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Sandbox>, I>>(base?: I): Sandbox {
    return Sandbox.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Sandbox>, I>>(object: I): Sandbox {
    const message = createBaseSandbox();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ObjectMeta.fromPartial(object.metadata)
      : undefined;
    message.spec = (object.spec !== undefined && object.spec !== null)
      ? SandboxSpec.fromPartial(object.spec)
      : undefined;
    message.status = (object.status !== undefined && object.status !== null)
      ? SandboxStatus.fromPartial(object.status)
      : undefined;
    message.phase = object.phase ?? 0;
    message.currentPolicyVersion = object.currentPolicyVersion ?? 0;
    return message;
  },
};

function createBaseSandboxSpec(): SandboxSpec {
  return {
    logLevel: "",
    environment: {},
    template: undefined,
    policy: undefined,
    providers: [],
    gpu: false,
    gpuDevice: "",
  };
}

export const SandboxSpec: MessageFns<SandboxSpec> = {
  encode(message: SandboxSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logLevel !== "") {
      writer.uint32(10).string(message.logLevel);
    }
    globalThis.Object.entries(message.environment).forEach(([key, value]: [string, string]) => {
      SandboxSpec_EnvironmentEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    if (message.template !== undefined) {
      SandboxTemplate.encode(message.template, writer.uint32(50).fork()).join();
    }
    if (message.policy !== undefined) {
      SandboxPolicy.encode(message.policy, writer.uint32(58).fork()).join();
    }
    for (const v of message.providers) {
      writer.uint32(66).string(v!);
    }
    if (message.gpu !== false) {
      writer.uint32(72).bool(message.gpu);
    }
    if (message.gpuDevice !== "") {
      writer.uint32(82).string(message.gpuDevice);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logLevel = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = SandboxSpec_EnvironmentEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.environment[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.template = SandboxTemplate.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.policy = SandboxPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.providers.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.gpu = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.gpuDevice = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxSpec {
    return {
      logLevel: isSet(object.logLevel)
        ? globalThis.String(object.logLevel)
        : isSet(object.log_level)
        ? globalThis.String(object.log_level)
        : "",
      environment: isObject(object.environment)
        ? (globalThis.Object.entries(object.environment) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      template: isSet(object.template) ? SandboxTemplate.fromJSON(object.template) : undefined,
      policy: isSet(object.policy) ? SandboxPolicy.fromJSON(object.policy) : undefined,
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => globalThis.String(e))
        : [],
      gpu: isSet(object.gpu) ? globalThis.Boolean(object.gpu) : false,
      gpuDevice: isSet(object.gpuDevice)
        ? globalThis.String(object.gpuDevice)
        : isSet(object.gpu_device)
        ? globalThis.String(object.gpu_device)
        : "",
    };
  },

  toJSON(message: SandboxSpec): unknown {
    const obj: any = {};
    if (message.logLevel !== "") {
      obj.logLevel = message.logLevel;
    }
    if (message.environment) {
      const entries = globalThis.Object.entries(message.environment) as [string, string][];
      if (entries.length > 0) {
        obj.environment = {};
        entries.forEach(([k, v]) => {
          obj.environment[k] = v;
        });
      }
    }
    if (message.template !== undefined) {
      obj.template = SandboxTemplate.toJSON(message.template);
    }
    if (message.policy !== undefined) {
      obj.policy = SandboxPolicy.toJSON(message.policy);
    }
    if (message.providers?.length) {
      obj.providers = message.providers;
    }
    if (message.gpu !== false) {
      obj.gpu = message.gpu;
    }
    if (message.gpuDevice !== "") {
      obj.gpuDevice = message.gpuDevice;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxSpec>, I>>(base?: I): SandboxSpec {
    return SandboxSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxSpec>, I>>(object: I): SandboxSpec {
    const message = createBaseSandboxSpec();
    message.logLevel = object.logLevel ?? "";
    message.environment = (globalThis.Object.entries(object.environment ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.template = (object.template !== undefined && object.template !== null)
      ? SandboxTemplate.fromPartial(object.template)
      : undefined;
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? SandboxPolicy.fromPartial(object.policy)
      : undefined;
    message.providers = object.providers?.map((e) => e) || [];
    message.gpu = object.gpu ?? false;
    message.gpuDevice = object.gpuDevice ?? "";
    return message;
  },
};

function createBaseSandboxSpec_EnvironmentEntry(): SandboxSpec_EnvironmentEntry {
  return { key: "", value: "" };
}

export const SandboxSpec_EnvironmentEntry: MessageFns<SandboxSpec_EnvironmentEntry> = {
  encode(message: SandboxSpec_EnvironmentEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxSpec_EnvironmentEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxSpec_EnvironmentEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxSpec_EnvironmentEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SandboxSpec_EnvironmentEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxSpec_EnvironmentEntry>, I>>(base?: I): SandboxSpec_EnvironmentEntry {
    return SandboxSpec_EnvironmentEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxSpec_EnvironmentEntry>, I>>(object: I): SandboxSpec_EnvironmentEntry {
    const message = createBaseSandboxSpec_EnvironmentEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSandboxTemplate(): SandboxTemplate {
  return {
    image: "",
    runtimeClassName: "",
    agentSocket: "",
    labels: {},
    annotations: {},
    environment: {},
    resources: undefined,
    volumeClaimTemplates: undefined,
    userNamespaces: undefined,
  };
}

export const SandboxTemplate: MessageFns<SandboxTemplate> = {
  encode(message: SandboxTemplate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== "") {
      writer.uint32(10).string(message.image);
    }
    if (message.runtimeClassName !== "") {
      writer.uint32(18).string(message.runtimeClassName);
    }
    if (message.agentSocket !== "") {
      writer.uint32(26).string(message.agentSocket);
    }
    globalThis.Object.entries(message.labels).forEach(([key, value]: [string, string]) => {
      SandboxTemplate_LabelsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    globalThis.Object.entries(message.annotations).forEach(([key, value]: [string, string]) => {
      SandboxTemplate_AnnotationsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    globalThis.Object.entries(message.environment).forEach(([key, value]: [string, string]) => {
      SandboxTemplate_EnvironmentEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    if (message.resources !== undefined) {
      Struct.encode(Struct.wrap(message.resources), writer.uint32(58).fork()).join();
    }
    if (message.volumeClaimTemplates !== undefined) {
      Struct.encode(Struct.wrap(message.volumeClaimTemplates), writer.uint32(74).fork()).join();
    }
    if (message.userNamespaces !== undefined) {
      writer.uint32(80).bool(message.userNamespaces);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxTemplate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxTemplate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.runtimeClassName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.agentSocket = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = SandboxTemplate_LabelsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.labels[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = SandboxTemplate_AnnotationsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.annotations[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = SandboxTemplate_EnvironmentEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.environment[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.resources = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.volumeClaimTemplates = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.userNamespaces = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxTemplate {
    return {
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      runtimeClassName: isSet(object.runtimeClassName)
        ? globalThis.String(object.runtimeClassName)
        : isSet(object.runtime_class_name)
        ? globalThis.String(object.runtime_class_name)
        : "",
      agentSocket: isSet(object.agentSocket)
        ? globalThis.String(object.agentSocket)
        : isSet(object.agent_socket)
        ? globalThis.String(object.agent_socket)
        : "",
      labels: isObject(object.labels)
        ? (globalThis.Object.entries(object.labels) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      annotations: isObject(object.annotations)
        ? (globalThis.Object.entries(object.annotations) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      environment: isObject(object.environment)
        ? (globalThis.Object.entries(object.environment) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      resources: isObject(object.resources) ? object.resources : undefined,
      volumeClaimTemplates: isObject(object.volumeClaimTemplates)
        ? object.volumeClaimTemplates
        : isObject(object.volume_claim_templates)
        ? object.volume_claim_templates
        : undefined,
      userNamespaces: isSet(object.userNamespaces)
        ? globalThis.Boolean(object.userNamespaces)
        : isSet(object.user_namespaces)
        ? globalThis.Boolean(object.user_namespaces)
        : undefined,
    };
  },

  toJSON(message: SandboxTemplate): unknown {
    const obj: any = {};
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.runtimeClassName !== "") {
      obj.runtimeClassName = message.runtimeClassName;
    }
    if (message.agentSocket !== "") {
      obj.agentSocket = message.agentSocket;
    }
    if (message.labels) {
      const entries = globalThis.Object.entries(message.labels) as [string, string][];
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    if (message.annotations) {
      const entries = globalThis.Object.entries(message.annotations) as [string, string][];
      if (entries.length > 0) {
        obj.annotations = {};
        entries.forEach(([k, v]) => {
          obj.annotations[k] = v;
        });
      }
    }
    if (message.environment) {
      const entries = globalThis.Object.entries(message.environment) as [string, string][];
      if (entries.length > 0) {
        obj.environment = {};
        entries.forEach(([k, v]) => {
          obj.environment[k] = v;
        });
      }
    }
    if (message.resources !== undefined) {
      obj.resources = message.resources;
    }
    if (message.volumeClaimTemplates !== undefined) {
      obj.volumeClaimTemplates = message.volumeClaimTemplates;
    }
    if (message.userNamespaces !== undefined) {
      obj.userNamespaces = message.userNamespaces;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxTemplate>, I>>(base?: I): SandboxTemplate {
    return SandboxTemplate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxTemplate>, I>>(object: I): SandboxTemplate {
    const message = createBaseSandboxTemplate();
    message.image = object.image ?? "";
    message.runtimeClassName = object.runtimeClassName ?? "";
    message.agentSocket = object.agentSocket ?? "";
    message.labels = (globalThis.Object.entries(object.labels ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.annotations = (globalThis.Object.entries(object.annotations ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.environment = (globalThis.Object.entries(object.environment ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.resources = object.resources ?? undefined;
    message.volumeClaimTemplates = object.volumeClaimTemplates ?? undefined;
    message.userNamespaces = object.userNamespaces ?? undefined;
    return message;
  },
};

function createBaseSandboxTemplate_LabelsEntry(): SandboxTemplate_LabelsEntry {
  return { key: "", value: "" };
}

export const SandboxTemplate_LabelsEntry: MessageFns<SandboxTemplate_LabelsEntry> = {
  encode(message: SandboxTemplate_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxTemplate_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxTemplate_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxTemplate_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SandboxTemplate_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxTemplate_LabelsEntry>, I>>(base?: I): SandboxTemplate_LabelsEntry {
    return SandboxTemplate_LabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxTemplate_LabelsEntry>, I>>(object: I): SandboxTemplate_LabelsEntry {
    const message = createBaseSandboxTemplate_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSandboxTemplate_AnnotationsEntry(): SandboxTemplate_AnnotationsEntry {
  return { key: "", value: "" };
}

export const SandboxTemplate_AnnotationsEntry: MessageFns<SandboxTemplate_AnnotationsEntry> = {
  encode(message: SandboxTemplate_AnnotationsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxTemplate_AnnotationsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxTemplate_AnnotationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxTemplate_AnnotationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SandboxTemplate_AnnotationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxTemplate_AnnotationsEntry>, I>>(
    base?: I,
  ): SandboxTemplate_AnnotationsEntry {
    return SandboxTemplate_AnnotationsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxTemplate_AnnotationsEntry>, I>>(
    object: I,
  ): SandboxTemplate_AnnotationsEntry {
    const message = createBaseSandboxTemplate_AnnotationsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSandboxTemplate_EnvironmentEntry(): SandboxTemplate_EnvironmentEntry {
  return { key: "", value: "" };
}

export const SandboxTemplate_EnvironmentEntry: MessageFns<SandboxTemplate_EnvironmentEntry> = {
  encode(message: SandboxTemplate_EnvironmentEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxTemplate_EnvironmentEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxTemplate_EnvironmentEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxTemplate_EnvironmentEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SandboxTemplate_EnvironmentEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxTemplate_EnvironmentEntry>, I>>(
    base?: I,
  ): SandboxTemplate_EnvironmentEntry {
    return SandboxTemplate_EnvironmentEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxTemplate_EnvironmentEntry>, I>>(
    object: I,
  ): SandboxTemplate_EnvironmentEntry {
    const message = createBaseSandboxTemplate_EnvironmentEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSandboxStatus(): SandboxStatus {
  return { sandboxName: "", agentPod: "", agentFd: "", sandboxFd: "", conditions: [] };
}

export const SandboxStatus: MessageFns<SandboxStatus> = {
  encode(message: SandboxStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxName !== "") {
      writer.uint32(10).string(message.sandboxName);
    }
    if (message.agentPod !== "") {
      writer.uint32(18).string(message.agentPod);
    }
    if (message.agentFd !== "") {
      writer.uint32(26).string(message.agentFd);
    }
    if (message.sandboxFd !== "") {
      writer.uint32(34).string(message.sandboxFd);
    }
    for (const v of message.conditions) {
      SandboxCondition.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentPod = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.agentFd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sandboxFd = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.conditions.push(SandboxCondition.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxStatus {
    return {
      sandboxName: isSet(object.sandboxName)
        ? globalThis.String(object.sandboxName)
        : isSet(object.sandbox_name)
        ? globalThis.String(object.sandbox_name)
        : "",
      agentPod: isSet(object.agentPod)
        ? globalThis.String(object.agentPod)
        : isSet(object.agent_pod)
        ? globalThis.String(object.agent_pod)
        : "",
      agentFd: isSet(object.agentFd)
        ? globalThis.String(object.agentFd)
        : isSet(object.agent_fd)
        ? globalThis.String(object.agent_fd)
        : "",
      sandboxFd: isSet(object.sandboxFd)
        ? globalThis.String(object.sandboxFd)
        : isSet(object.sandbox_fd)
        ? globalThis.String(object.sandbox_fd)
        : "",
      conditions: globalThis.Array.isArray(object?.conditions)
        ? object.conditions.map((e: any) => SandboxCondition.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SandboxStatus): unknown {
    const obj: any = {};
    if (message.sandboxName !== "") {
      obj.sandboxName = message.sandboxName;
    }
    if (message.agentPod !== "") {
      obj.agentPod = message.agentPod;
    }
    if (message.agentFd !== "") {
      obj.agentFd = message.agentFd;
    }
    if (message.sandboxFd !== "") {
      obj.sandboxFd = message.sandboxFd;
    }
    if (message.conditions?.length) {
      obj.conditions = message.conditions.map((e) => SandboxCondition.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxStatus>, I>>(base?: I): SandboxStatus {
    return SandboxStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxStatus>, I>>(object: I): SandboxStatus {
    const message = createBaseSandboxStatus();
    message.sandboxName = object.sandboxName ?? "";
    message.agentPod = object.agentPod ?? "";
    message.agentFd = object.agentFd ?? "";
    message.sandboxFd = object.sandboxFd ?? "";
    message.conditions = object.conditions?.map((e) => SandboxCondition.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSandboxCondition(): SandboxCondition {
  return { type: "", status: "", reason: "", message: "", lastTransitionTime: "" };
}

export const SandboxCondition: MessageFns<SandboxCondition> = {
  encode(message: SandboxCondition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    if (message.lastTransitionTime !== "") {
      writer.uint32(42).string(message.lastTransitionTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxCondition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxCondition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastTransitionTime = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxCondition {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      lastTransitionTime: isSet(object.lastTransitionTime)
        ? globalThis.String(object.lastTransitionTime)
        : isSet(object.last_transition_time)
        ? globalThis.String(object.last_transition_time)
        : "",
    };
  },

  toJSON(message: SandboxCondition): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.lastTransitionTime !== "") {
      obj.lastTransitionTime = message.lastTransitionTime;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxCondition>, I>>(base?: I): SandboxCondition {
    return SandboxCondition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxCondition>, I>>(object: I): SandboxCondition {
    const message = createBaseSandboxCondition();
    message.type = object.type ?? "";
    message.status = object.status ?? "";
    message.reason = object.reason ?? "";
    message.message = object.message ?? "";
    message.lastTransitionTime = object.lastTransitionTime ?? "";
    return message;
  },
};

function createBasePlatformEvent(): PlatformEvent {
  return { timestampMs: 0, source: "", type: "", reason: "", message: "", metadata: {} };
}

export const PlatformEvent: MessageFns<PlatformEvent> = {
  encode(message: PlatformEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestampMs !== 0) {
      writer.uint32(8).int64(message.timestampMs);
    }
    if (message.source !== "") {
      writer.uint32(18).string(message.source);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(42).string(message.message);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      PlatformEvent_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlatformEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlatformEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestampMs = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = PlatformEvent_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlatformEvent {
    return {
      timestampMs: isSet(object.timestampMs)
        ? globalThis.Number(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? globalThis.Number(object.timestamp_ms)
        : 0,
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: PlatformEvent): unknown {
    const obj: any = {};
    if (message.timestampMs !== 0) {
      obj.timestampMs = Math.round(message.timestampMs);
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlatformEvent>, I>>(base?: I): PlatformEvent {
    return PlatformEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlatformEvent>, I>>(object: I): PlatformEvent {
    const message = createBasePlatformEvent();
    message.timestampMs = object.timestampMs ?? 0;
    message.source = object.source ?? "";
    message.type = object.type ?? "";
    message.reason = object.reason ?? "";
    message.message = object.message ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBasePlatformEvent_MetadataEntry(): PlatformEvent_MetadataEntry {
  return { key: "", value: "" };
}

export const PlatformEvent_MetadataEntry: MessageFns<PlatformEvent_MetadataEntry> = {
  encode(message: PlatformEvent_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlatformEvent_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlatformEvent_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlatformEvent_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PlatformEvent_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlatformEvent_MetadataEntry>, I>>(base?: I): PlatformEvent_MetadataEntry {
    return PlatformEvent_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlatformEvent_MetadataEntry>, I>>(object: I): PlatformEvent_MetadataEntry {
    const message = createBasePlatformEvent_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateSandboxRequest(): CreateSandboxRequest {
  return { spec: undefined, name: "", labels: {} };
}

export const CreateSandboxRequest: MessageFns<CreateSandboxRequest> = {
  encode(message: CreateSandboxRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.spec !== undefined) {
      SandboxSpec.encode(message.spec, writer.uint32(10).fork()).join();
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    globalThis.Object.entries(message.labels).forEach(([key, value]: [string, string]) => {
      CreateSandboxRequest_LabelsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSandboxRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSandboxRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.spec = SandboxSpec.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = CreateSandboxRequest_LabelsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.labels[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSandboxRequest {
    return {
      spec: isSet(object.spec) ? SandboxSpec.fromJSON(object.spec) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      labels: isObject(object.labels)
        ? (globalThis.Object.entries(object.labels) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: CreateSandboxRequest): unknown {
    const obj: any = {};
    if (message.spec !== undefined) {
      obj.spec = SandboxSpec.toJSON(message.spec);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.labels) {
      const entries = globalThis.Object.entries(message.labels) as [string, string][];
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSandboxRequest>, I>>(base?: I): CreateSandboxRequest {
    return CreateSandboxRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSandboxRequest>, I>>(object: I): CreateSandboxRequest {
    const message = createBaseCreateSandboxRequest();
    message.spec = (object.spec !== undefined && object.spec !== null)
      ? SandboxSpec.fromPartial(object.spec)
      : undefined;
    message.name = object.name ?? "";
    message.labels = (globalThis.Object.entries(object.labels ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCreateSandboxRequest_LabelsEntry(): CreateSandboxRequest_LabelsEntry {
  return { key: "", value: "" };
}

export const CreateSandboxRequest_LabelsEntry: MessageFns<CreateSandboxRequest_LabelsEntry> = {
  encode(message: CreateSandboxRequest_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSandboxRequest_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSandboxRequest_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSandboxRequest_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateSandboxRequest_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSandboxRequest_LabelsEntry>, I>>(
    base?: I,
  ): CreateSandboxRequest_LabelsEntry {
    return CreateSandboxRequest_LabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSandboxRequest_LabelsEntry>, I>>(
    object: I,
  ): CreateSandboxRequest_LabelsEntry {
    const message = createBaseCreateSandboxRequest_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetSandboxRequest(): GetSandboxRequest {
  return { name: "" };
}

export const GetSandboxRequest: MessageFns<GetSandboxRequest> = {
  encode(message: GetSandboxRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetSandboxRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxRequest>, I>>(base?: I): GetSandboxRequest {
    return GetSandboxRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxRequest>, I>>(object: I): GetSandboxRequest {
    const message = createBaseGetSandboxRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseListSandboxesRequest(): ListSandboxesRequest {
  return { limit: 0, offset: 0, labelSelector: "" };
}

export const ListSandboxesRequest: MessageFns<ListSandboxesRequest> = {
  encode(message: ListSandboxesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limit !== 0) {
      writer.uint32(8).uint32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    if (message.labelSelector !== "") {
      writer.uint32(26).string(message.labelSelector);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSandboxesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSandboxesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.labelSelector = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSandboxesRequest {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      labelSelector: isSet(object.labelSelector)
        ? globalThis.String(object.labelSelector)
        : isSet(object.label_selector)
        ? globalThis.String(object.label_selector)
        : "",
    };
  },

  toJSON(message: ListSandboxesRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.labelSelector !== "") {
      obj.labelSelector = message.labelSelector;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSandboxesRequest>, I>>(base?: I): ListSandboxesRequest {
    return ListSandboxesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSandboxesRequest>, I>>(object: I): ListSandboxesRequest {
    const message = createBaseListSandboxesRequest();
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.labelSelector = object.labelSelector ?? "";
    return message;
  },
};

function createBaseListSandboxProvidersRequest(): ListSandboxProvidersRequest {
  return { sandboxName: "" };
}

export const ListSandboxProvidersRequest: MessageFns<ListSandboxProvidersRequest> = {
  encode(message: ListSandboxProvidersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxName !== "") {
      writer.uint32(10).string(message.sandboxName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSandboxProvidersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSandboxProvidersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSandboxProvidersRequest {
    return {
      sandboxName: isSet(object.sandboxName)
        ? globalThis.String(object.sandboxName)
        : isSet(object.sandbox_name)
        ? globalThis.String(object.sandbox_name)
        : "",
    };
  },

  toJSON(message: ListSandboxProvidersRequest): unknown {
    const obj: any = {};
    if (message.sandboxName !== "") {
      obj.sandboxName = message.sandboxName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSandboxProvidersRequest>, I>>(base?: I): ListSandboxProvidersRequest {
    return ListSandboxProvidersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSandboxProvidersRequest>, I>>(object: I): ListSandboxProvidersRequest {
    const message = createBaseListSandboxProvidersRequest();
    message.sandboxName = object.sandboxName ?? "";
    return message;
  },
};

function createBaseAttachSandboxProviderRequest(): AttachSandboxProviderRequest {
  return { sandboxName: "", providerName: "", expectedResourceVersion: 0 };
}

export const AttachSandboxProviderRequest: MessageFns<AttachSandboxProviderRequest> = {
  encode(message: AttachSandboxProviderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxName !== "") {
      writer.uint32(10).string(message.sandboxName);
    }
    if (message.providerName !== "") {
      writer.uint32(18).string(message.providerName);
    }
    if (message.expectedResourceVersion !== 0) {
      writer.uint32(24).uint64(message.expectedResourceVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachSandboxProviderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachSandboxProviderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.providerName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expectedResourceVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachSandboxProviderRequest {
    return {
      sandboxName: isSet(object.sandboxName)
        ? globalThis.String(object.sandboxName)
        : isSet(object.sandbox_name)
        ? globalThis.String(object.sandbox_name)
        : "",
      providerName: isSet(object.providerName)
        ? globalThis.String(object.providerName)
        : isSet(object.provider_name)
        ? globalThis.String(object.provider_name)
        : "",
      expectedResourceVersion: isSet(object.expectedResourceVersion)
        ? globalThis.Number(object.expectedResourceVersion)
        : isSet(object.expected_resource_version)
        ? globalThis.Number(object.expected_resource_version)
        : 0,
    };
  },

  toJSON(message: AttachSandboxProviderRequest): unknown {
    const obj: any = {};
    if (message.sandboxName !== "") {
      obj.sandboxName = message.sandboxName;
    }
    if (message.providerName !== "") {
      obj.providerName = message.providerName;
    }
    if (message.expectedResourceVersion !== 0) {
      obj.expectedResourceVersion = Math.round(message.expectedResourceVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttachSandboxProviderRequest>, I>>(base?: I): AttachSandboxProviderRequest {
    return AttachSandboxProviderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttachSandboxProviderRequest>, I>>(object: I): AttachSandboxProviderRequest {
    const message = createBaseAttachSandboxProviderRequest();
    message.sandboxName = object.sandboxName ?? "";
    message.providerName = object.providerName ?? "";
    message.expectedResourceVersion = object.expectedResourceVersion ?? 0;
    return message;
  },
};

function createBaseDetachSandboxProviderRequest(): DetachSandboxProviderRequest {
  return { sandboxName: "", providerName: "", expectedResourceVersion: 0 };
}

export const DetachSandboxProviderRequest: MessageFns<DetachSandboxProviderRequest> = {
  encode(message: DetachSandboxProviderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxName !== "") {
      writer.uint32(10).string(message.sandboxName);
    }
    if (message.providerName !== "") {
      writer.uint32(18).string(message.providerName);
    }
    if (message.expectedResourceVersion !== 0) {
      writer.uint32(24).uint64(message.expectedResourceVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetachSandboxProviderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetachSandboxProviderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.providerName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expectedResourceVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetachSandboxProviderRequest {
    return {
      sandboxName: isSet(object.sandboxName)
        ? globalThis.String(object.sandboxName)
        : isSet(object.sandbox_name)
        ? globalThis.String(object.sandbox_name)
        : "",
      providerName: isSet(object.providerName)
        ? globalThis.String(object.providerName)
        : isSet(object.provider_name)
        ? globalThis.String(object.provider_name)
        : "",
      expectedResourceVersion: isSet(object.expectedResourceVersion)
        ? globalThis.Number(object.expectedResourceVersion)
        : isSet(object.expected_resource_version)
        ? globalThis.Number(object.expected_resource_version)
        : 0,
    };
  },

  toJSON(message: DetachSandboxProviderRequest): unknown {
    const obj: any = {};
    if (message.sandboxName !== "") {
      obj.sandboxName = message.sandboxName;
    }
    if (message.providerName !== "") {
      obj.providerName = message.providerName;
    }
    if (message.expectedResourceVersion !== 0) {
      obj.expectedResourceVersion = Math.round(message.expectedResourceVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetachSandboxProviderRequest>, I>>(base?: I): DetachSandboxProviderRequest {
    return DetachSandboxProviderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetachSandboxProviderRequest>, I>>(object: I): DetachSandboxProviderRequest {
    const message = createBaseDetachSandboxProviderRequest();
    message.sandboxName = object.sandboxName ?? "";
    message.providerName = object.providerName ?? "";
    message.expectedResourceVersion = object.expectedResourceVersion ?? 0;
    return message;
  },
};

function createBaseDeleteSandboxRequest(): DeleteSandboxRequest {
  return { name: "" };
}

export const DeleteSandboxRequest: MessageFns<DeleteSandboxRequest> = {
  encode(message: DeleteSandboxRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSandboxRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSandboxRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSandboxRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: DeleteSandboxRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSandboxRequest>, I>>(base?: I): DeleteSandboxRequest {
    return DeleteSandboxRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSandboxRequest>, I>>(object: I): DeleteSandboxRequest {
    const message = createBaseDeleteSandboxRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseSandboxResponse(): SandboxResponse {
  return { sandbox: undefined };
}

export const SandboxResponse: MessageFns<SandboxResponse> = {
  encode(message: SandboxResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== undefined) {
      Sandbox.encode(message.sandbox, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = Sandbox.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxResponse {
    return { sandbox: isSet(object.sandbox) ? Sandbox.fromJSON(object.sandbox) : undefined };
  },

  toJSON(message: SandboxResponse): unknown {
    const obj: any = {};
    if (message.sandbox !== undefined) {
      obj.sandbox = Sandbox.toJSON(message.sandbox);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxResponse>, I>>(base?: I): SandboxResponse {
    return SandboxResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxResponse>, I>>(object: I): SandboxResponse {
    const message = createBaseSandboxResponse();
    message.sandbox = (object.sandbox !== undefined && object.sandbox !== null)
      ? Sandbox.fromPartial(object.sandbox)
      : undefined;
    return message;
  },
};

function createBaseListSandboxesResponse(): ListSandboxesResponse {
  return { sandboxes: [] };
}

export const ListSandboxesResponse: MessageFns<ListSandboxesResponse> = {
  encode(message: ListSandboxesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sandboxes) {
      Sandbox.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSandboxesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSandboxesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxes.push(Sandbox.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSandboxesResponse {
    return {
      sandboxes: globalThis.Array.isArray(object?.sandboxes)
        ? object.sandboxes.map((e: any) => Sandbox.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListSandboxesResponse): unknown {
    const obj: any = {};
    if (message.sandboxes?.length) {
      obj.sandboxes = message.sandboxes.map((e) => Sandbox.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSandboxesResponse>, I>>(base?: I): ListSandboxesResponse {
    return ListSandboxesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSandboxesResponse>, I>>(object: I): ListSandboxesResponse {
    const message = createBaseListSandboxesResponse();
    message.sandboxes = object.sandboxes?.map((e) => Sandbox.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListSandboxProvidersResponse(): ListSandboxProvidersResponse {
  return { providers: [] };
}

export const ListSandboxProvidersResponse: MessageFns<ListSandboxProvidersResponse> = {
  encode(message: ListSandboxProvidersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.providers) {
      Provider.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSandboxProvidersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSandboxProvidersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providers.push(Provider.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSandboxProvidersResponse {
    return {
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => Provider.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListSandboxProvidersResponse): unknown {
    const obj: any = {};
    if (message.providers?.length) {
      obj.providers = message.providers.map((e) => Provider.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSandboxProvidersResponse>, I>>(base?: I): ListSandboxProvidersResponse {
    return ListSandboxProvidersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSandboxProvidersResponse>, I>>(object: I): ListSandboxProvidersResponse {
    const message = createBaseListSandboxProvidersResponse();
    message.providers = object.providers?.map((e) => Provider.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAttachSandboxProviderResponse(): AttachSandboxProviderResponse {
  return { sandbox: undefined, attached: false };
}

export const AttachSandboxProviderResponse: MessageFns<AttachSandboxProviderResponse> = {
  encode(message: AttachSandboxProviderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== undefined) {
      Sandbox.encode(message.sandbox, writer.uint32(10).fork()).join();
    }
    if (message.attached !== false) {
      writer.uint32(16).bool(message.attached);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachSandboxProviderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachSandboxProviderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = Sandbox.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.attached = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachSandboxProviderResponse {
    return {
      sandbox: isSet(object.sandbox) ? Sandbox.fromJSON(object.sandbox) : undefined,
      attached: isSet(object.attached) ? globalThis.Boolean(object.attached) : false,
    };
  },

  toJSON(message: AttachSandboxProviderResponse): unknown {
    const obj: any = {};
    if (message.sandbox !== undefined) {
      obj.sandbox = Sandbox.toJSON(message.sandbox);
    }
    if (message.attached !== false) {
      obj.attached = message.attached;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttachSandboxProviderResponse>, I>>(base?: I): AttachSandboxProviderResponse {
    return AttachSandboxProviderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttachSandboxProviderResponse>, I>>(
    object: I,
  ): AttachSandboxProviderResponse {
    const message = createBaseAttachSandboxProviderResponse();
    message.sandbox = (object.sandbox !== undefined && object.sandbox !== null)
      ? Sandbox.fromPartial(object.sandbox)
      : undefined;
    message.attached = object.attached ?? false;
    return message;
  },
};

function createBaseDetachSandboxProviderResponse(): DetachSandboxProviderResponse {
  return { sandbox: undefined, detached: false };
}

export const DetachSandboxProviderResponse: MessageFns<DetachSandboxProviderResponse> = {
  encode(message: DetachSandboxProviderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== undefined) {
      Sandbox.encode(message.sandbox, writer.uint32(10).fork()).join();
    }
    if (message.detached !== false) {
      writer.uint32(16).bool(message.detached);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetachSandboxProviderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetachSandboxProviderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = Sandbox.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.detached = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetachSandboxProviderResponse {
    return {
      sandbox: isSet(object.sandbox) ? Sandbox.fromJSON(object.sandbox) : undefined,
      detached: isSet(object.detached) ? globalThis.Boolean(object.detached) : false,
    };
  },

  toJSON(message: DetachSandboxProviderResponse): unknown {
    const obj: any = {};
    if (message.sandbox !== undefined) {
      obj.sandbox = Sandbox.toJSON(message.sandbox);
    }
    if (message.detached !== false) {
      obj.detached = message.detached;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetachSandboxProviderResponse>, I>>(base?: I): DetachSandboxProviderResponse {
    return DetachSandboxProviderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetachSandboxProviderResponse>, I>>(
    object: I,
  ): DetachSandboxProviderResponse {
    const message = createBaseDetachSandboxProviderResponse();
    message.sandbox = (object.sandbox !== undefined && object.sandbox !== null)
      ? Sandbox.fromPartial(object.sandbox)
      : undefined;
    message.detached = object.detached ?? false;
    return message;
  },
};

function createBaseDeleteSandboxResponse(): DeleteSandboxResponse {
  return { deleted: false };
}

export const DeleteSandboxResponse: MessageFns<DeleteSandboxResponse> = {
  encode(message: DeleteSandboxResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== false) {
      writer.uint32(8).bool(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSandboxResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSandboxResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSandboxResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false };
  },

  toJSON(message: DeleteSandboxResponse): unknown {
    const obj: any = {};
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSandboxResponse>, I>>(base?: I): DeleteSandboxResponse {
    return DeleteSandboxResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSandboxResponse>, I>>(object: I): DeleteSandboxResponse {
    const message = createBaseDeleteSandboxResponse();
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseCreateSshSessionRequest(): CreateSshSessionRequest {
  return { sandboxId: "" };
}

export const CreateSshSessionRequest: MessageFns<CreateSshSessionRequest> = {
  encode(message: CreateSshSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSshSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSshSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSshSessionRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
    };
  },

  toJSON(message: CreateSshSessionRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSshSessionRequest>, I>>(base?: I): CreateSshSessionRequest {
    return CreateSshSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSshSessionRequest>, I>>(object: I): CreateSshSessionRequest {
    const message = createBaseCreateSshSessionRequest();
    message.sandboxId = object.sandboxId ?? "";
    return message;
  },
};

function createBaseCreateSshSessionResponse(): CreateSshSessionResponse {
  return {
    sandboxId: "",
    token: "",
    gatewayHost: "",
    gatewayPort: 0,
    gatewayScheme: "",
    hostKeyFingerprint: "",
    expiresAtMs: 0,
  };
}

export const CreateSshSessionResponse: MessageFns<CreateSshSessionResponse> = {
  encode(message: CreateSshSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.token !== "") {
      writer.uint32(18).string(message.token);
    }
    if (message.gatewayHost !== "") {
      writer.uint32(26).string(message.gatewayHost);
    }
    if (message.gatewayPort !== 0) {
      writer.uint32(32).uint32(message.gatewayPort);
    }
    if (message.gatewayScheme !== "") {
      writer.uint32(42).string(message.gatewayScheme);
    }
    if (message.hostKeyFingerprint !== "") {
      writer.uint32(58).string(message.hostKeyFingerprint);
    }
    if (message.expiresAtMs !== 0) {
      writer.uint32(64).int64(message.expiresAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSshSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSshSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gatewayHost = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.gatewayPort = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.gatewayScheme = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.hostKeyFingerprint = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.expiresAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSshSessionResponse {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      gatewayHost: isSet(object.gatewayHost)
        ? globalThis.String(object.gatewayHost)
        : isSet(object.gateway_host)
        ? globalThis.String(object.gateway_host)
        : "",
      gatewayPort: isSet(object.gatewayPort)
        ? globalThis.Number(object.gatewayPort)
        : isSet(object.gateway_port)
        ? globalThis.Number(object.gateway_port)
        : 0,
      gatewayScheme: isSet(object.gatewayScheme)
        ? globalThis.String(object.gatewayScheme)
        : isSet(object.gateway_scheme)
        ? globalThis.String(object.gateway_scheme)
        : "",
      hostKeyFingerprint: isSet(object.hostKeyFingerprint)
        ? globalThis.String(object.hostKeyFingerprint)
        : isSet(object.host_key_fingerprint)
        ? globalThis.String(object.host_key_fingerprint)
        : "",
      expiresAtMs: isSet(object.expiresAtMs)
        ? globalThis.Number(object.expiresAtMs)
        : isSet(object.expires_at_ms)
        ? globalThis.Number(object.expires_at_ms)
        : 0,
    };
  },

  toJSON(message: CreateSshSessionResponse): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.gatewayHost !== "") {
      obj.gatewayHost = message.gatewayHost;
    }
    if (message.gatewayPort !== 0) {
      obj.gatewayPort = Math.round(message.gatewayPort);
    }
    if (message.gatewayScheme !== "") {
      obj.gatewayScheme = message.gatewayScheme;
    }
    if (message.hostKeyFingerprint !== "") {
      obj.hostKeyFingerprint = message.hostKeyFingerprint;
    }
    if (message.expiresAtMs !== 0) {
      obj.expiresAtMs = Math.round(message.expiresAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSshSessionResponse>, I>>(base?: I): CreateSshSessionResponse {
    return CreateSshSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSshSessionResponse>, I>>(object: I): CreateSshSessionResponse {
    const message = createBaseCreateSshSessionResponse();
    message.sandboxId = object.sandboxId ?? "";
    message.token = object.token ?? "";
    message.gatewayHost = object.gatewayHost ?? "";
    message.gatewayPort = object.gatewayPort ?? 0;
    message.gatewayScheme = object.gatewayScheme ?? "";
    message.hostKeyFingerprint = object.hostKeyFingerprint ?? "";
    message.expiresAtMs = object.expiresAtMs ?? 0;
    return message;
  },
};

function createBaseExposeServiceRequest(): ExposeServiceRequest {
  return { sandbox: "", service: "", targetPort: 0, domain: false };
}

export const ExposeServiceRequest: MessageFns<ExposeServiceRequest> = {
  encode(message: ExposeServiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== "") {
      writer.uint32(10).string(message.sandbox);
    }
    if (message.service !== "") {
      writer.uint32(18).string(message.service);
    }
    if (message.targetPort !== 0) {
      writer.uint32(24).uint32(message.targetPort);
    }
    if (message.domain !== false) {
      writer.uint32(32).bool(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExposeServiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExposeServiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.targetPort = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.domain = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExposeServiceRequest {
    return {
      sandbox: isSet(object.sandbox) ? globalThis.String(object.sandbox) : "",
      service: isSet(object.service) ? globalThis.String(object.service) : "",
      targetPort: isSet(object.targetPort)
        ? globalThis.Number(object.targetPort)
        : isSet(object.target_port)
        ? globalThis.Number(object.target_port)
        : 0,
      domain: isSet(object.domain) ? globalThis.Boolean(object.domain) : false,
    };
  },

  toJSON(message: ExposeServiceRequest): unknown {
    const obj: any = {};
    if (message.sandbox !== "") {
      obj.sandbox = message.sandbox;
    }
    if (message.service !== "") {
      obj.service = message.service;
    }
    if (message.targetPort !== 0) {
      obj.targetPort = Math.round(message.targetPort);
    }
    if (message.domain !== false) {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExposeServiceRequest>, I>>(base?: I): ExposeServiceRequest {
    return ExposeServiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExposeServiceRequest>, I>>(object: I): ExposeServiceRequest {
    const message = createBaseExposeServiceRequest();
    message.sandbox = object.sandbox ?? "";
    message.service = object.service ?? "";
    message.targetPort = object.targetPort ?? 0;
    message.domain = object.domain ?? false;
    return message;
  },
};

function createBaseGetServiceRequest(): GetServiceRequest {
  return { sandbox: "", service: "" };
}

export const GetServiceRequest: MessageFns<GetServiceRequest> = {
  encode(message: GetServiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== "") {
      writer.uint32(10).string(message.sandbox);
    }
    if (message.service !== "") {
      writer.uint32(18).string(message.service);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.service = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetServiceRequest {
    return {
      sandbox: isSet(object.sandbox) ? globalThis.String(object.sandbox) : "",
      service: isSet(object.service) ? globalThis.String(object.service) : "",
    };
  },

  toJSON(message: GetServiceRequest): unknown {
    const obj: any = {};
    if (message.sandbox !== "") {
      obj.sandbox = message.sandbox;
    }
    if (message.service !== "") {
      obj.service = message.service;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetServiceRequest>, I>>(base?: I): GetServiceRequest {
    return GetServiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServiceRequest>, I>>(object: I): GetServiceRequest {
    const message = createBaseGetServiceRequest();
    message.sandbox = object.sandbox ?? "";
    message.service = object.service ?? "";
    return message;
  },
};

function createBaseListServicesRequest(): ListServicesRequest {
  return { sandbox: "", limit: 0, offset: 0 };
}

export const ListServicesRequest: MessageFns<ListServicesRequest> = {
  encode(message: ListServicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== "") {
      writer.uint32(10).string(message.sandbox);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(24).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListServicesRequest {
    return {
      sandbox: isSet(object.sandbox) ? globalThis.String(object.sandbox) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: ListServicesRequest): unknown {
    const obj: any = {};
    if (message.sandbox !== "") {
      obj.sandbox = message.sandbox;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListServicesRequest>, I>>(base?: I): ListServicesRequest {
    return ListServicesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServicesRequest>, I>>(object: I): ListServicesRequest {
    const message = createBaseListServicesRequest();
    message.sandbox = object.sandbox ?? "";
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseListServicesResponse(): ListServicesResponse {
  return { services: [] };
}

export const ListServicesResponse: MessageFns<ListServicesResponse> = {
  encode(message: ListServicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.services) {
      ServiceEndpointResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServicesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.services.push(ServiceEndpointResponse.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListServicesResponse {
    return {
      services: globalThis.Array.isArray(object?.services)
        ? object.services.map((e: any) => ServiceEndpointResponse.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListServicesResponse): unknown {
    const obj: any = {};
    if (message.services?.length) {
      obj.services = message.services.map((e) => ServiceEndpointResponse.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListServicesResponse>, I>>(base?: I): ListServicesResponse {
    return ListServicesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServicesResponse>, I>>(object: I): ListServicesResponse {
    const message = createBaseListServicesResponse();
    message.services = object.services?.map((e) => ServiceEndpointResponse.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeleteServiceRequest(): DeleteServiceRequest {
  return { sandbox: "", service: "" };
}

export const DeleteServiceRequest: MessageFns<DeleteServiceRequest> = {
  encode(message: DeleteServiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandbox !== "") {
      writer.uint32(10).string(message.sandbox);
    }
    if (message.service !== "") {
      writer.uint32(18).string(message.service);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteServiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteServiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandbox = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.service = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteServiceRequest {
    return {
      sandbox: isSet(object.sandbox) ? globalThis.String(object.sandbox) : "",
      service: isSet(object.service) ? globalThis.String(object.service) : "",
    };
  },

  toJSON(message: DeleteServiceRequest): unknown {
    const obj: any = {};
    if (message.sandbox !== "") {
      obj.sandbox = message.sandbox;
    }
    if (message.service !== "") {
      obj.service = message.service;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteServiceRequest>, I>>(base?: I): DeleteServiceRequest {
    return DeleteServiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteServiceRequest>, I>>(object: I): DeleteServiceRequest {
    const message = createBaseDeleteServiceRequest();
    message.sandbox = object.sandbox ?? "";
    message.service = object.service ?? "";
    return message;
  },
};

function createBaseDeleteServiceResponse(): DeleteServiceResponse {
  return { deleted: false };
}

export const DeleteServiceResponse: MessageFns<DeleteServiceResponse> = {
  encode(message: DeleteServiceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== false) {
      writer.uint32(8).bool(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteServiceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteServiceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteServiceResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false };
  },

  toJSON(message: DeleteServiceResponse): unknown {
    const obj: any = {};
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteServiceResponse>, I>>(base?: I): DeleteServiceResponse {
    return DeleteServiceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteServiceResponse>, I>>(object: I): DeleteServiceResponse {
    const message = createBaseDeleteServiceResponse();
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseServiceEndpoint(): ServiceEndpoint {
  return { metadata: undefined, sandboxId: "", sandboxName: "", serviceName: "", targetPort: 0, domain: false };
}

export const ServiceEndpoint: MessageFns<ServiceEndpoint> = {
  encode(message: ServiceEndpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.sandboxId !== "") {
      writer.uint32(18).string(message.sandboxId);
    }
    if (message.sandboxName !== "") {
      writer.uint32(26).string(message.sandboxName);
    }
    if (message.serviceName !== "") {
      writer.uint32(34).string(message.serviceName);
    }
    if (message.targetPort !== 0) {
      writer.uint32(40).uint32(message.targetPort);
    }
    if (message.domain !== false) {
      writer.uint32(48).bool(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceEndpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceEndpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = ObjectMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sandboxName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serviceName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.targetPort = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.domain = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceEndpoint {
    return {
      metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      sandboxName: isSet(object.sandboxName)
        ? globalThis.String(object.sandboxName)
        : isSet(object.sandbox_name)
        ? globalThis.String(object.sandbox_name)
        : "",
      serviceName: isSet(object.serviceName)
        ? globalThis.String(object.serviceName)
        : isSet(object.service_name)
        ? globalThis.String(object.service_name)
        : "",
      targetPort: isSet(object.targetPort)
        ? globalThis.Number(object.targetPort)
        : isSet(object.target_port)
        ? globalThis.Number(object.target_port)
        : 0,
      domain: isSet(object.domain) ? globalThis.Boolean(object.domain) : false,
    };
  },

  toJSON(message: ServiceEndpoint): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = ObjectMeta.toJSON(message.metadata);
    }
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.sandboxName !== "") {
      obj.sandboxName = message.sandboxName;
    }
    if (message.serviceName !== "") {
      obj.serviceName = message.serviceName;
    }
    if (message.targetPort !== 0) {
      obj.targetPort = Math.round(message.targetPort);
    }
    if (message.domain !== false) {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceEndpoint>, I>>(base?: I): ServiceEndpoint {
    return ServiceEndpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceEndpoint>, I>>(object: I): ServiceEndpoint {
    const message = createBaseServiceEndpoint();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ObjectMeta.fromPartial(object.metadata)
      : undefined;
    message.sandboxId = object.sandboxId ?? "";
    message.sandboxName = object.sandboxName ?? "";
    message.serviceName = object.serviceName ?? "";
    message.targetPort = object.targetPort ?? 0;
    message.domain = object.domain ?? false;
    return message;
  },
};

function createBaseServiceEndpointResponse(): ServiceEndpointResponse {
  return { endpoint: undefined, url: "" };
}

export const ServiceEndpointResponse: MessageFns<ServiceEndpointResponse> = {
  encode(message: ServiceEndpointResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpoint !== undefined) {
      ServiceEndpoint.encode(message.endpoint, writer.uint32(10).fork()).join();
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceEndpointResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceEndpointResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpoint = ServiceEndpoint.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceEndpointResponse {
    return {
      endpoint: isSet(object.endpoint) ? ServiceEndpoint.fromJSON(object.endpoint) : undefined,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: ServiceEndpointResponse): unknown {
    const obj: any = {};
    if (message.endpoint !== undefined) {
      obj.endpoint = ServiceEndpoint.toJSON(message.endpoint);
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceEndpointResponse>, I>>(base?: I): ServiceEndpointResponse {
    return ServiceEndpointResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceEndpointResponse>, I>>(object: I): ServiceEndpointResponse {
    const message = createBaseServiceEndpointResponse();
    message.endpoint = (object.endpoint !== undefined && object.endpoint !== null)
      ? ServiceEndpoint.fromPartial(object.endpoint)
      : undefined;
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseRevokeSshSessionRequest(): RevokeSshSessionRequest {
  return { token: "" };
}

export const RevokeSshSessionRequest: MessageFns<RevokeSshSessionRequest> = {
  encode(message: RevokeSshSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeSshSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeSshSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeSshSessionRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: RevokeSshSessionRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeSshSessionRequest>, I>>(base?: I): RevokeSshSessionRequest {
    return RevokeSshSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeSshSessionRequest>, I>>(object: I): RevokeSshSessionRequest {
    const message = createBaseRevokeSshSessionRequest();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseRevokeSshSessionResponse(): RevokeSshSessionResponse {
  return { revoked: false };
}

export const RevokeSshSessionResponse: MessageFns<RevokeSshSessionResponse> = {
  encode(message: RevokeSshSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revoked !== false) {
      writer.uint32(8).bool(message.revoked);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeSshSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeSshSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revoked = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeSshSessionResponse {
    return { revoked: isSet(object.revoked) ? globalThis.Boolean(object.revoked) : false };
  },

  toJSON(message: RevokeSshSessionResponse): unknown {
    const obj: any = {};
    if (message.revoked !== false) {
      obj.revoked = message.revoked;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevokeSshSessionResponse>, I>>(base?: I): RevokeSshSessionResponse {
    return RevokeSshSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevokeSshSessionResponse>, I>>(object: I): RevokeSshSessionResponse {
    const message = createBaseRevokeSshSessionResponse();
    message.revoked = object.revoked ?? false;
    return message;
  },
};

function createBaseExecSandboxRequest(): ExecSandboxRequest {
  return {
    sandboxId: "",
    command: [],
    workdir: "",
    environment: {},
    timeoutSeconds: 0,
    stdin: Buffer.alloc(0),
    tty: false,
    cols: 0,
    rows: 0,
  };
}

export const ExecSandboxRequest: MessageFns<ExecSandboxRequest> = {
  encode(message: ExecSandboxRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    for (const v of message.command) {
      writer.uint32(18).string(v!);
    }
    if (message.workdir !== "") {
      writer.uint32(26).string(message.workdir);
    }
    globalThis.Object.entries(message.environment).forEach(([key, value]: [string, string]) => {
      ExecSandboxRequest_EnvironmentEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.timeoutSeconds !== 0) {
      writer.uint32(40).uint32(message.timeoutSeconds);
    }
    if (message.stdin.length !== 0) {
      writer.uint32(50).bytes(message.stdin);
    }
    if (message.tty !== false) {
      writer.uint32(56).bool(message.tty);
    }
    if (message.cols !== 0) {
      writer.uint32(64).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(72).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workdir = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ExecSandboxRequest_EnvironmentEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.environment[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timeoutSeconds = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stdin = Buffer.from(reader.bytes());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tty = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      command: globalThis.Array.isArray(object?.command) ? object.command.map((e: any) => globalThis.String(e)) : [],
      workdir: isSet(object.workdir) ? globalThis.String(object.workdir) : "",
      environment: isObject(object.environment)
        ? (globalThis.Object.entries(object.environment) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      timeoutSeconds: isSet(object.timeoutSeconds)
        ? globalThis.Number(object.timeoutSeconds)
        : isSet(object.timeout_seconds)
        ? globalThis.Number(object.timeout_seconds)
        : 0,
      stdin: isSet(object.stdin) ? Buffer.from(bytesFromBase64(object.stdin)) : Buffer.alloc(0),
      tty: isSet(object.tty) ? globalThis.Boolean(object.tty) : false,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: ExecSandboxRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.workdir !== "") {
      obj.workdir = message.workdir;
    }
    if (message.environment) {
      const entries = globalThis.Object.entries(message.environment) as [string, string][];
      if (entries.length > 0) {
        obj.environment = {};
        entries.forEach(([k, v]) => {
          obj.environment[k] = v;
        });
      }
    }
    if (message.timeoutSeconds !== 0) {
      obj.timeoutSeconds = Math.round(message.timeoutSeconds);
    }
    if (message.stdin.length !== 0) {
      obj.stdin = base64FromBytes(message.stdin);
    }
    if (message.tty !== false) {
      obj.tty = message.tty;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxRequest>, I>>(base?: I): ExecSandboxRequest {
    return ExecSandboxRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxRequest>, I>>(object: I): ExecSandboxRequest {
    const message = createBaseExecSandboxRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.command = object.command?.map((e) => e) || [];
    message.workdir = object.workdir ?? "";
    message.environment = (globalThis.Object.entries(object.environment ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.timeoutSeconds = object.timeoutSeconds ?? 0;
    message.stdin = object.stdin ?? Buffer.alloc(0);
    message.tty = object.tty ?? false;
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBaseExecSandboxRequest_EnvironmentEntry(): ExecSandboxRequest_EnvironmentEntry {
  return { key: "", value: "" };
}

export const ExecSandboxRequest_EnvironmentEntry: MessageFns<ExecSandboxRequest_EnvironmentEntry> = {
  encode(message: ExecSandboxRequest_EnvironmentEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxRequest_EnvironmentEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxRequest_EnvironmentEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxRequest_EnvironmentEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ExecSandboxRequest_EnvironmentEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxRequest_EnvironmentEntry>, I>>(
    base?: I,
  ): ExecSandboxRequest_EnvironmentEntry {
    return ExecSandboxRequest_EnvironmentEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxRequest_EnvironmentEntry>, I>>(
    object: I,
  ): ExecSandboxRequest_EnvironmentEntry {
    const message = createBaseExecSandboxRequest_EnvironmentEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseExecSandboxStdout(): ExecSandboxStdout {
  return { data: Buffer.alloc(0) };
}

export const ExecSandboxStdout: MessageFns<ExecSandboxStdout> = {
  encode(message: ExecSandboxStdout, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxStdout {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxStdout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxStdout {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: ExecSandboxStdout): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxStdout>, I>>(base?: I): ExecSandboxStdout {
    return ExecSandboxStdout.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxStdout>, I>>(object: I): ExecSandboxStdout {
    const message = createBaseExecSandboxStdout();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseExecSandboxStderr(): ExecSandboxStderr {
  return { data: Buffer.alloc(0) };
}

export const ExecSandboxStderr: MessageFns<ExecSandboxStderr> = {
  encode(message: ExecSandboxStderr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxStderr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxStderr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxStderr {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: ExecSandboxStderr): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxStderr>, I>>(base?: I): ExecSandboxStderr {
    return ExecSandboxStderr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxStderr>, I>>(object: I): ExecSandboxStderr {
    const message = createBaseExecSandboxStderr();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseExecSandboxExit(): ExecSandboxExit {
  return { exitCode: 0 };
}

export const ExecSandboxExit: MessageFns<ExecSandboxExit> = {
  encode(message: ExecSandboxExit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).int32(message.exitCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxExit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxExit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxExit {
    return {
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
    };
  },

  toJSON(message: ExecSandboxExit): unknown {
    const obj: any = {};
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxExit>, I>>(base?: I): ExecSandboxExit {
    return ExecSandboxExit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxExit>, I>>(object: I): ExecSandboxExit {
    const message = createBaseExecSandboxExit();
    message.exitCode = object.exitCode ?? 0;
    return message;
  },
};

function createBaseExecSandboxEvent(): ExecSandboxEvent {
  return { payload: undefined };
}

export const ExecSandboxEvent: MessageFns<ExecSandboxEvent> = {
  encode(message: ExecSandboxEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "stdout":
        ExecSandboxStdout.encode(message.payload.stdout, writer.uint32(10).fork()).join();
        break;
      case "stderr":
        ExecSandboxStderr.encode(message.payload.stderr, writer.uint32(18).fork()).join();
        break;
      case "exit":
        ExecSandboxExit.encode(message.payload.exit, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "stdout", stdout: ExecSandboxStdout.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "stderr", stderr: ExecSandboxStderr.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "exit", exit: ExecSandboxExit.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxEvent {
    return {
      payload: isSet(object.stdout)
        ? { $case: "stdout", stdout: ExecSandboxStdout.fromJSON(object.stdout) }
        : isSet(object.stderr)
        ? { $case: "stderr", stderr: ExecSandboxStderr.fromJSON(object.stderr) }
        : isSet(object.exit)
        ? { $case: "exit", exit: ExecSandboxExit.fromJSON(object.exit) }
        : undefined,
    };
  },

  toJSON(message: ExecSandboxEvent): unknown {
    const obj: any = {};
    if (message.payload?.$case === "stdout") {
      obj.stdout = ExecSandboxStdout.toJSON(message.payload.stdout);
    } else if (message.payload?.$case === "stderr") {
      obj.stderr = ExecSandboxStderr.toJSON(message.payload.stderr);
    } else if (message.payload?.$case === "exit") {
      obj.exit = ExecSandboxExit.toJSON(message.payload.exit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxEvent>, I>>(base?: I): ExecSandboxEvent {
    return ExecSandboxEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxEvent>, I>>(object: I): ExecSandboxEvent {
    const message = createBaseExecSandboxEvent();
    switch (object.payload?.$case) {
      case "stdout": {
        if (object.payload?.stdout !== undefined && object.payload?.stdout !== null) {
          message.payload = { $case: "stdout", stdout: ExecSandboxStdout.fromPartial(object.payload.stdout) };
        }
        break;
      }
      case "stderr": {
        if (object.payload?.stderr !== undefined && object.payload?.stderr !== null) {
          message.payload = { $case: "stderr", stderr: ExecSandboxStderr.fromPartial(object.payload.stderr) };
        }
        break;
      }
      case "exit": {
        if (object.payload?.exit !== undefined && object.payload?.exit !== null) {
          message.payload = { $case: "exit", exit: ExecSandboxExit.fromPartial(object.payload.exit) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseTcpForwardInit(): TcpForwardInit {
  return { sandboxId: "", serviceId: "", target: undefined, authorizationToken: "" };
}

export const TcpForwardInit: MessageFns<TcpForwardInit> = {
  encode(message: TcpForwardInit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.serviceId !== "") {
      writer.uint32(34).string(message.serviceId);
    }
    switch (message.target?.$case) {
      case "ssh":
        SshRelayTarget.encode(message.target.ssh, writer.uint32(42).fork()).join();
        break;
      case "tcp":
        TcpRelayTarget.encode(message.target.tcp, writer.uint32(50).fork()).join();
        break;
    }
    if (message.authorizationToken !== "") {
      writer.uint32(58).string(message.authorizationToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TcpForwardInit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTcpForwardInit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serviceId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.target = { $case: "ssh", ssh: SshRelayTarget.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.target = { $case: "tcp", tcp: TcpRelayTarget.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.authorizationToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TcpForwardInit {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      serviceId: isSet(object.serviceId)
        ? globalThis.String(object.serviceId)
        : isSet(object.service_id)
        ? globalThis.String(object.service_id)
        : "",
      target: isSet(object.ssh)
        ? { $case: "ssh", ssh: SshRelayTarget.fromJSON(object.ssh) }
        : isSet(object.tcp)
        ? { $case: "tcp", tcp: TcpRelayTarget.fromJSON(object.tcp) }
        : undefined,
      authorizationToken: isSet(object.authorizationToken)
        ? globalThis.String(object.authorizationToken)
        : isSet(object.authorization_token)
        ? globalThis.String(object.authorization_token)
        : "",
    };
  },

  toJSON(message: TcpForwardInit): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.serviceId !== "") {
      obj.serviceId = message.serviceId;
    }
    if (message.target?.$case === "ssh") {
      obj.ssh = SshRelayTarget.toJSON(message.target.ssh);
    } else if (message.target?.$case === "tcp") {
      obj.tcp = TcpRelayTarget.toJSON(message.target.tcp);
    }
    if (message.authorizationToken !== "") {
      obj.authorizationToken = message.authorizationToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TcpForwardInit>, I>>(base?: I): TcpForwardInit {
    return TcpForwardInit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TcpForwardInit>, I>>(object: I): TcpForwardInit {
    const message = createBaseTcpForwardInit();
    message.sandboxId = object.sandboxId ?? "";
    message.serviceId = object.serviceId ?? "";
    switch (object.target?.$case) {
      case "ssh": {
        if (object.target?.ssh !== undefined && object.target?.ssh !== null) {
          message.target = { $case: "ssh", ssh: SshRelayTarget.fromPartial(object.target.ssh) };
        }
        break;
      }
      case "tcp": {
        if (object.target?.tcp !== undefined && object.target?.tcp !== null) {
          message.target = { $case: "tcp", tcp: TcpRelayTarget.fromPartial(object.target.tcp) };
        }
        break;
      }
    }
    message.authorizationToken = object.authorizationToken ?? "";
    return message;
  },
};

function createBaseTcpForwardFrame(): TcpForwardFrame {
  return { payload: undefined };
}

export const TcpForwardFrame: MessageFns<TcpForwardFrame> = {
  encode(message: TcpForwardFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "init":
        TcpForwardInit.encode(message.payload.init, writer.uint32(10).fork()).join();
        break;
      case "data":
        writer.uint32(18).bytes(message.payload.data);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TcpForwardFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTcpForwardFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "init", init: TcpForwardInit.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "data", data: Buffer.from(reader.bytes()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TcpForwardFrame {
    return {
      payload: isSet(object.init)
        ? { $case: "init", init: TcpForwardInit.fromJSON(object.init) }
        : isSet(object.data)
        ? { $case: "data", data: Buffer.from(bytesFromBase64(object.data)) }
        : undefined,
    };
  },

  toJSON(message: TcpForwardFrame): unknown {
    const obj: any = {};
    if (message.payload?.$case === "init") {
      obj.init = TcpForwardInit.toJSON(message.payload.init);
    } else if (message.payload?.$case === "data") {
      obj.data = base64FromBytes(message.payload.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TcpForwardFrame>, I>>(base?: I): TcpForwardFrame {
    return TcpForwardFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TcpForwardFrame>, I>>(object: I): TcpForwardFrame {
    const message = createBaseTcpForwardFrame();
    switch (object.payload?.$case) {
      case "init": {
        if (object.payload?.init !== undefined && object.payload?.init !== null) {
          message.payload = { $case: "init", init: TcpForwardInit.fromPartial(object.payload.init) };
        }
        break;
      }
      case "data": {
        if (object.payload?.data !== undefined && object.payload?.data !== null) {
          message.payload = { $case: "data", data: object.payload.data };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseExecSandboxInput(): ExecSandboxInput {
  return { payload: undefined };
}

export const ExecSandboxInput: MessageFns<ExecSandboxInput> = {
  encode(message: ExecSandboxInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "start":
        ExecSandboxRequest.encode(message.payload.start, writer.uint32(10).fork()).join();
        break;
      case "stdin":
        writer.uint32(18).bytes(message.payload.stdin);
        break;
      case "resize":
        ExecSandboxWindowResize.encode(message.payload.resize, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "start", start: ExecSandboxRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "stdin", stdin: Buffer.from(reader.bytes()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "resize", resize: ExecSandboxWindowResize.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxInput {
    return {
      payload: isSet(object.start)
        ? { $case: "start", start: ExecSandboxRequest.fromJSON(object.start) }
        : isSet(object.stdin)
        ? { $case: "stdin", stdin: Buffer.from(bytesFromBase64(object.stdin)) }
        : isSet(object.resize)
        ? { $case: "resize", resize: ExecSandboxWindowResize.fromJSON(object.resize) }
        : undefined,
    };
  },

  toJSON(message: ExecSandboxInput): unknown {
    const obj: any = {};
    if (message.payload?.$case === "start") {
      obj.start = ExecSandboxRequest.toJSON(message.payload.start);
    } else if (message.payload?.$case === "stdin") {
      obj.stdin = base64FromBytes(message.payload.stdin);
    } else if (message.payload?.$case === "resize") {
      obj.resize = ExecSandboxWindowResize.toJSON(message.payload.resize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxInput>, I>>(base?: I): ExecSandboxInput {
    return ExecSandboxInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxInput>, I>>(object: I): ExecSandboxInput {
    const message = createBaseExecSandboxInput();
    switch (object.payload?.$case) {
      case "start": {
        if (object.payload?.start !== undefined && object.payload?.start !== null) {
          message.payload = { $case: "start", start: ExecSandboxRequest.fromPartial(object.payload.start) };
        }
        break;
      }
      case "stdin": {
        if (object.payload?.stdin !== undefined && object.payload?.stdin !== null) {
          message.payload = { $case: "stdin", stdin: object.payload.stdin };
        }
        break;
      }
      case "resize": {
        if (object.payload?.resize !== undefined && object.payload?.resize !== null) {
          message.payload = { $case: "resize", resize: ExecSandboxWindowResize.fromPartial(object.payload.resize) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseExecSandboxWindowResize(): ExecSandboxWindowResize {
  return { cols: 0, rows: 0 };
}

export const ExecSandboxWindowResize: MessageFns<ExecSandboxWindowResize> = {
  encode(message: ExecSandboxWindowResize, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cols !== 0) {
      writer.uint32(8).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(16).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecSandboxWindowResize {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecSandboxWindowResize();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecSandboxWindowResize {
    return {
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: ExecSandboxWindowResize): unknown {
    const obj: any = {};
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecSandboxWindowResize>, I>>(base?: I): ExecSandboxWindowResize {
    return ExecSandboxWindowResize.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecSandboxWindowResize>, I>>(object: I): ExecSandboxWindowResize {
    const message = createBaseExecSandboxWindowResize();
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBaseSshSession(): SshSession {
  return { metadata: undefined, sandboxId: "", token: "", expiresAtMs: 0, revoked: false };
}

export const SshSession: MessageFns<SshSession> = {
  encode(message: SshSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.sandboxId !== "") {
      writer.uint32(18).string(message.sandboxId);
    }
    if (message.token !== "") {
      writer.uint32(26).string(message.token);
    }
    if (message.expiresAtMs !== 0) {
      writer.uint32(32).int64(message.expiresAtMs);
    }
    if (message.revoked !== false) {
      writer.uint32(40).bool(message.revoked);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SshSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSshSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = ObjectMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.expiresAtMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.revoked = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SshSession {
    return {
      metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      expiresAtMs: isSet(object.expiresAtMs)
        ? globalThis.Number(object.expiresAtMs)
        : isSet(object.expires_at_ms)
        ? globalThis.Number(object.expires_at_ms)
        : 0,
      revoked: isSet(object.revoked) ? globalThis.Boolean(object.revoked) : false,
    };
  },

  toJSON(message: SshSession): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = ObjectMeta.toJSON(message.metadata);
    }
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.expiresAtMs !== 0) {
      obj.expiresAtMs = Math.round(message.expiresAtMs);
    }
    if (message.revoked !== false) {
      obj.revoked = message.revoked;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SshSession>, I>>(base?: I): SshSession {
    return SshSession.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SshSession>, I>>(object: I): SshSession {
    const message = createBaseSshSession();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ObjectMeta.fromPartial(object.metadata)
      : undefined;
    message.sandboxId = object.sandboxId ?? "";
    message.token = object.token ?? "";
    message.expiresAtMs = object.expiresAtMs ?? 0;
    message.revoked = object.revoked ?? false;
    return message;
  },
};

function createBaseWatchSandboxRequest(): WatchSandboxRequest {
  return {
    id: "",
    followStatus: false,
    followLogs: false,
    followEvents: false,
    logTailLines: 0,
    eventTail: 0,
    stopOnTerminal: false,
    logSinceMs: 0,
    logSources: [],
    logMinLevel: "",
  };
}

export const WatchSandboxRequest: MessageFns<WatchSandboxRequest> = {
  encode(message: WatchSandboxRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.followStatus !== false) {
      writer.uint32(16).bool(message.followStatus);
    }
    if (message.followLogs !== false) {
      writer.uint32(24).bool(message.followLogs);
    }
    if (message.followEvents !== false) {
      writer.uint32(32).bool(message.followEvents);
    }
    if (message.logTailLines !== 0) {
      writer.uint32(40).uint32(message.logTailLines);
    }
    if (message.eventTail !== 0) {
      writer.uint32(48).uint32(message.eventTail);
    }
    if (message.stopOnTerminal !== false) {
      writer.uint32(56).bool(message.stopOnTerminal);
    }
    if (message.logSinceMs !== 0) {
      writer.uint32(64).int64(message.logSinceMs);
    }
    for (const v of message.logSources) {
      writer.uint32(74).string(v!);
    }
    if (message.logMinLevel !== "") {
      writer.uint32(82).string(message.logMinLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WatchSandboxRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatchSandboxRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.followStatus = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.followLogs = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.followEvents = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.logTailLines = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.eventTail = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.stopOnTerminal = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.logSinceMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.logSources.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.logMinLevel = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WatchSandboxRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      followStatus: isSet(object.followStatus)
        ? globalThis.Boolean(object.followStatus)
        : isSet(object.follow_status)
        ? globalThis.Boolean(object.follow_status)
        : false,
      followLogs: isSet(object.followLogs)
        ? globalThis.Boolean(object.followLogs)
        : isSet(object.follow_logs)
        ? globalThis.Boolean(object.follow_logs)
        : false,
      followEvents: isSet(object.followEvents)
        ? globalThis.Boolean(object.followEvents)
        : isSet(object.follow_events)
        ? globalThis.Boolean(object.follow_events)
        : false,
      logTailLines: isSet(object.logTailLines)
        ? globalThis.Number(object.logTailLines)
        : isSet(object.log_tail_lines)
        ? globalThis.Number(object.log_tail_lines)
        : 0,
      eventTail: isSet(object.eventTail)
        ? globalThis.Number(object.eventTail)
        : isSet(object.event_tail)
        ? globalThis.Number(object.event_tail)
        : 0,
      stopOnTerminal: isSet(object.stopOnTerminal)
        ? globalThis.Boolean(object.stopOnTerminal)
        : isSet(object.stop_on_terminal)
        ? globalThis.Boolean(object.stop_on_terminal)
        : false,
      logSinceMs: isSet(object.logSinceMs)
        ? globalThis.Number(object.logSinceMs)
        : isSet(object.log_since_ms)
        ? globalThis.Number(object.log_since_ms)
        : 0,
      logSources: globalThis.Array.isArray(object?.logSources)
        ? object.logSources.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.log_sources)
        ? object.log_sources.map((e: any) => globalThis.String(e))
        : [],
      logMinLevel: isSet(object.logMinLevel)
        ? globalThis.String(object.logMinLevel)
        : isSet(object.log_min_level)
        ? globalThis.String(object.log_min_level)
        : "",
    };
  },

  toJSON(message: WatchSandboxRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.followStatus !== false) {
      obj.followStatus = message.followStatus;
    }
    if (message.followLogs !== false) {
      obj.followLogs = message.followLogs;
    }
    if (message.followEvents !== false) {
      obj.followEvents = message.followEvents;
    }
    if (message.logTailLines !== 0) {
      obj.logTailLines = Math.round(message.logTailLines);
    }
    if (message.eventTail !== 0) {
      obj.eventTail = Math.round(message.eventTail);
    }
    if (message.stopOnTerminal !== false) {
      obj.stopOnTerminal = message.stopOnTerminal;
    }
    if (message.logSinceMs !== 0) {
      obj.logSinceMs = Math.round(message.logSinceMs);
    }
    if (message.logSources?.length) {
      obj.logSources = message.logSources;
    }
    if (message.logMinLevel !== "") {
      obj.logMinLevel = message.logMinLevel;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WatchSandboxRequest>, I>>(base?: I): WatchSandboxRequest {
    return WatchSandboxRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WatchSandboxRequest>, I>>(object: I): WatchSandboxRequest {
    const message = createBaseWatchSandboxRequest();
    message.id = object.id ?? "";
    message.followStatus = object.followStatus ?? false;
    message.followLogs = object.followLogs ?? false;
    message.followEvents = object.followEvents ?? false;
    message.logTailLines = object.logTailLines ?? 0;
    message.eventTail = object.eventTail ?? 0;
    message.stopOnTerminal = object.stopOnTerminal ?? false;
    message.logSinceMs = object.logSinceMs ?? 0;
    message.logSources = object.logSources?.map((e) => e) || [];
    message.logMinLevel = object.logMinLevel ?? "";
    return message;
  },
};

function createBaseSandboxStreamEvent(): SandboxStreamEvent {
  return { payload: undefined };
}

export const SandboxStreamEvent: MessageFns<SandboxStreamEvent> = {
  encode(message: SandboxStreamEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "sandbox":
        Sandbox.encode(message.payload.sandbox, writer.uint32(10).fork()).join();
        break;
      case "log":
        SandboxLogLine.encode(message.payload.log, writer.uint32(18).fork()).join();
        break;
      case "event":
        PlatformEvent.encode(message.payload.event, writer.uint32(26).fork()).join();
        break;
      case "warning":
        SandboxStreamWarning.encode(message.payload.warning, writer.uint32(34).fork()).join();
        break;
      case "draftPolicyUpdate":
        DraftPolicyUpdate.encode(message.payload.draftPolicyUpdate, writer.uint32(42).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxStreamEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxStreamEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "sandbox", sandbox: Sandbox.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "log", log: SandboxLogLine.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "event", event: PlatformEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = { $case: "warning", warning: SandboxStreamWarning.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = {
            $case: "draftPolicyUpdate",
            draftPolicyUpdate: DraftPolicyUpdate.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxStreamEvent {
    return {
      payload: isSet(object.sandbox)
        ? { $case: "sandbox", sandbox: Sandbox.fromJSON(object.sandbox) }
        : isSet(object.log)
        ? { $case: "log", log: SandboxLogLine.fromJSON(object.log) }
        : isSet(object.event)
        ? { $case: "event", event: PlatformEvent.fromJSON(object.event) }
        : isSet(object.warning)
        ? { $case: "warning", warning: SandboxStreamWarning.fromJSON(object.warning) }
        : isSet(object.draftPolicyUpdate)
        ? { $case: "draftPolicyUpdate", draftPolicyUpdate: DraftPolicyUpdate.fromJSON(object.draftPolicyUpdate) }
        : isSet(object.draft_policy_update)
        ? { $case: "draftPolicyUpdate", draftPolicyUpdate: DraftPolicyUpdate.fromJSON(object.draft_policy_update) }
        : undefined,
    };
  },

  toJSON(message: SandboxStreamEvent): unknown {
    const obj: any = {};
    if (message.payload?.$case === "sandbox") {
      obj.sandbox = Sandbox.toJSON(message.payload.sandbox);
    } else if (message.payload?.$case === "log") {
      obj.log = SandboxLogLine.toJSON(message.payload.log);
    } else if (message.payload?.$case === "event") {
      obj.event = PlatformEvent.toJSON(message.payload.event);
    } else if (message.payload?.$case === "warning") {
      obj.warning = SandboxStreamWarning.toJSON(message.payload.warning);
    } else if (message.payload?.$case === "draftPolicyUpdate") {
      obj.draftPolicyUpdate = DraftPolicyUpdate.toJSON(message.payload.draftPolicyUpdate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxStreamEvent>, I>>(base?: I): SandboxStreamEvent {
    return SandboxStreamEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxStreamEvent>, I>>(object: I): SandboxStreamEvent {
    const message = createBaseSandboxStreamEvent();
    switch (object.payload?.$case) {
      case "sandbox": {
        if (object.payload?.sandbox !== undefined && object.payload?.sandbox !== null) {
          message.payload = { $case: "sandbox", sandbox: Sandbox.fromPartial(object.payload.sandbox) };
        }
        break;
      }
      case "log": {
        if (object.payload?.log !== undefined && object.payload?.log !== null) {
          message.payload = { $case: "log", log: SandboxLogLine.fromPartial(object.payload.log) };
        }
        break;
      }
      case "event": {
        if (object.payload?.event !== undefined && object.payload?.event !== null) {
          message.payload = { $case: "event", event: PlatformEvent.fromPartial(object.payload.event) };
        }
        break;
      }
      case "warning": {
        if (object.payload?.warning !== undefined && object.payload?.warning !== null) {
          message.payload = { $case: "warning", warning: SandboxStreamWarning.fromPartial(object.payload.warning) };
        }
        break;
      }
      case "draftPolicyUpdate": {
        if (object.payload?.draftPolicyUpdate !== undefined && object.payload?.draftPolicyUpdate !== null) {
          message.payload = {
            $case: "draftPolicyUpdate",
            draftPolicyUpdate: DraftPolicyUpdate.fromPartial(object.payload.draftPolicyUpdate),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseSandboxLogLine(): SandboxLogLine {
  return { sandboxId: "", timestampMs: 0, level: "", target: "", message: "", source: "", fields: {} };
}

export const SandboxLogLine: MessageFns<SandboxLogLine> = {
  encode(message: SandboxLogLine, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.timestampMs !== 0) {
      writer.uint32(16).int64(message.timestampMs);
    }
    if (message.level !== "") {
      writer.uint32(26).string(message.level);
    }
    if (message.target !== "") {
      writer.uint32(34).string(message.target);
    }
    if (message.message !== "") {
      writer.uint32(42).string(message.message);
    }
    if (message.source !== "") {
      writer.uint32(50).string(message.source);
    }
    globalThis.Object.entries(message.fields).forEach(([key, value]: [string, string]) => {
      SandboxLogLine_FieldsEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxLogLine {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxLogLine();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestampMs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.level = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.target = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = SandboxLogLine_FieldsEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.fields[entry7.key] = entry7.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxLogLine {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      timestampMs: isSet(object.timestampMs)
        ? globalThis.Number(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? globalThis.Number(object.timestamp_ms)
        : 0,
      level: isSet(object.level) ? globalThis.String(object.level) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      fields: isObject(object.fields)
        ? (globalThis.Object.entries(object.fields) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: SandboxLogLine): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.timestampMs !== 0) {
      obj.timestampMs = Math.round(message.timestampMs);
    }
    if (message.level !== "") {
      obj.level = message.level;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.fields) {
      const entries = globalThis.Object.entries(message.fields) as [string, string][];
      if (entries.length > 0) {
        obj.fields = {};
        entries.forEach(([k, v]) => {
          obj.fields[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxLogLine>, I>>(base?: I): SandboxLogLine {
    return SandboxLogLine.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxLogLine>, I>>(object: I): SandboxLogLine {
    const message = createBaseSandboxLogLine();
    message.sandboxId = object.sandboxId ?? "";
    message.timestampMs = object.timestampMs ?? 0;
    message.level = object.level ?? "";
    message.target = object.target ?? "";
    message.message = object.message ?? "";
    message.source = object.source ?? "";
    message.fields = (globalThis.Object.entries(object.fields ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseSandboxLogLine_FieldsEntry(): SandboxLogLine_FieldsEntry {
  return { key: "", value: "" };
}

export const SandboxLogLine_FieldsEntry: MessageFns<SandboxLogLine_FieldsEntry> = {
  encode(message: SandboxLogLine_FieldsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxLogLine_FieldsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxLogLine_FieldsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxLogLine_FieldsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SandboxLogLine_FieldsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxLogLine_FieldsEntry>, I>>(base?: I): SandboxLogLine_FieldsEntry {
    return SandboxLogLine_FieldsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxLogLine_FieldsEntry>, I>>(object: I): SandboxLogLine_FieldsEntry {
    const message = createBaseSandboxLogLine_FieldsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSandboxStreamWarning(): SandboxStreamWarning {
  return { message: "" };
}

export const SandboxStreamWarning: MessageFns<SandboxStreamWarning> = {
  encode(message: SandboxStreamWarning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxStreamWarning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxStreamWarning();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxStreamWarning {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: SandboxStreamWarning): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxStreamWarning>, I>>(base?: I): SandboxStreamWarning {
    return SandboxStreamWarning.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxStreamWarning>, I>>(object: I): SandboxStreamWarning {
    const message = createBaseSandboxStreamWarning();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseCreateProviderRequest(): CreateProviderRequest {
  return { provider: undefined };
}

export const CreateProviderRequest: MessageFns<CreateProviderRequest> = {
  encode(message: CreateProviderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== undefined) {
      Provider.encode(message.provider, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProviderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProviderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = Provider.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProviderRequest {
    return { provider: isSet(object.provider) ? Provider.fromJSON(object.provider) : undefined };
  },

  toJSON(message: CreateProviderRequest): unknown {
    const obj: any = {};
    if (message.provider !== undefined) {
      obj.provider = Provider.toJSON(message.provider);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProviderRequest>, I>>(base?: I): CreateProviderRequest {
    return CreateProviderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProviderRequest>, I>>(object: I): CreateProviderRequest {
    const message = createBaseCreateProviderRequest();
    message.provider = (object.provider !== undefined && object.provider !== null)
      ? Provider.fromPartial(object.provider)
      : undefined;
    return message;
  },
};

function createBaseGetProviderRequest(): GetProviderRequest {
  return { name: "" };
}

export const GetProviderRequest: MessageFns<GetProviderRequest> = {
  encode(message: GetProviderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProviderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProviderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProviderRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetProviderRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProviderRequest>, I>>(base?: I): GetProviderRequest {
    return GetProviderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProviderRequest>, I>>(object: I): GetProviderRequest {
    const message = createBaseGetProviderRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseListProvidersRequest(): ListProvidersRequest {
  return { limit: 0, offset: 0 };
}

export const ListProvidersRequest: MessageFns<ListProvidersRequest> = {
  encode(message: ListProvidersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limit !== 0) {
      writer.uint32(8).uint32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProvidersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProvidersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProvidersRequest {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: ListProvidersRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProvidersRequest>, I>>(base?: I): ListProvidersRequest {
    return ListProvidersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProvidersRequest>, I>>(object: I): ListProvidersRequest {
    const message = createBaseListProvidersRequest();
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseUpdateProviderRequest(): UpdateProviderRequest {
  return { provider: undefined, credentialExpiresAtMs: {} };
}

export const UpdateProviderRequest: MessageFns<UpdateProviderRequest> = {
  encode(message: UpdateProviderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== undefined) {
      Provider.encode(message.provider, writer.uint32(10).fork()).join();
    }
    globalThis.Object.entries(message.credentialExpiresAtMs).forEach(([key, value]: [string, number]) => {
      UpdateProviderRequest_CredentialExpiresAtMsEntry.encode({ key: key as any, value }, writer.uint32(18).fork())
        .join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateProviderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateProviderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = Provider.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = UpdateProviderRequest_CredentialExpiresAtMsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.credentialExpiresAtMs[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateProviderRequest {
    return {
      provider: isSet(object.provider) ? Provider.fromJSON(object.provider) : undefined,
      credentialExpiresAtMs: isObject(object.credentialExpiresAtMs)
        ? (globalThis.Object.entries(object.credentialExpiresAtMs) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.credential_expires_at_ms)
        ? (globalThis.Object.entries(object.credential_expires_at_ms) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: UpdateProviderRequest): unknown {
    const obj: any = {};
    if (message.provider !== undefined) {
      obj.provider = Provider.toJSON(message.provider);
    }
    if (message.credentialExpiresAtMs) {
      const entries = globalThis.Object.entries(message.credentialExpiresAtMs) as [string, number][];
      if (entries.length > 0) {
        obj.credentialExpiresAtMs = {};
        entries.forEach(([k, v]) => {
          obj.credentialExpiresAtMs[k] = Math.round(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateProviderRequest>, I>>(base?: I): UpdateProviderRequest {
    return UpdateProviderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateProviderRequest>, I>>(object: I): UpdateProviderRequest {
    const message = createBaseUpdateProviderRequest();
    message.provider = (object.provider !== undefined && object.provider !== null)
      ? Provider.fromPartial(object.provider)
      : undefined;
    message.credentialExpiresAtMs =
      (globalThis.Object.entries(object.credentialExpiresAtMs ?? {}) as [string, number][]).reduce(
        (acc: { [key: string]: number }, [key, value]: [string, number]) => {
          if (value !== undefined) {
            acc[key] = globalThis.Number(value);
          }
          return acc;
        },
        {},
      );
    return message;
  },
};

function createBaseUpdateProviderRequest_CredentialExpiresAtMsEntry(): UpdateProviderRequest_CredentialExpiresAtMsEntry {
  return { key: "", value: 0 };
}

export const UpdateProviderRequest_CredentialExpiresAtMsEntry: MessageFns<
  UpdateProviderRequest_CredentialExpiresAtMsEntry
> = {
  encode(
    message: UpdateProviderRequest_CredentialExpiresAtMsEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateProviderRequest_CredentialExpiresAtMsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateProviderRequest_CredentialExpiresAtMsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateProviderRequest_CredentialExpiresAtMsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: UpdateProviderRequest_CredentialExpiresAtMsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateProviderRequest_CredentialExpiresAtMsEntry>, I>>(
    base?: I,
  ): UpdateProviderRequest_CredentialExpiresAtMsEntry {
    return UpdateProviderRequest_CredentialExpiresAtMsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateProviderRequest_CredentialExpiresAtMsEntry>, I>>(
    object: I,
  ): UpdateProviderRequest_CredentialExpiresAtMsEntry {
    const message = createBaseUpdateProviderRequest_CredentialExpiresAtMsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseDeleteProviderRequest(): DeleteProviderRequest {
  return { name: "" };
}

export const DeleteProviderRequest: MessageFns<DeleteProviderRequest> = {
  encode(message: DeleteProviderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProviderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProviderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProviderRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: DeleteProviderRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteProviderRequest>, I>>(base?: I): DeleteProviderRequest {
    return DeleteProviderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteProviderRequest>, I>>(object: I): DeleteProviderRequest {
    const message = createBaseDeleteProviderRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseProviderResponse(): ProviderResponse {
  return { provider: undefined };
}

export const ProviderResponse: MessageFns<ProviderResponse> = {
  encode(message: ProviderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== undefined) {
      Provider.encode(message.provider, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = Provider.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderResponse {
    return { provider: isSet(object.provider) ? Provider.fromJSON(object.provider) : undefined };
  },

  toJSON(message: ProviderResponse): unknown {
    const obj: any = {};
    if (message.provider !== undefined) {
      obj.provider = Provider.toJSON(message.provider);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderResponse>, I>>(base?: I): ProviderResponse {
    return ProviderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderResponse>, I>>(object: I): ProviderResponse {
    const message = createBaseProviderResponse();
    message.provider = (object.provider !== undefined && object.provider !== null)
      ? Provider.fromPartial(object.provider)
      : undefined;
    return message;
  },
};

function createBaseListProvidersResponse(): ListProvidersResponse {
  return { providers: [] };
}

export const ListProvidersResponse: MessageFns<ListProvidersResponse> = {
  encode(message: ListProvidersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.providers) {
      Provider.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProvidersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProvidersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providers.push(Provider.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProvidersResponse {
    return {
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => Provider.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProvidersResponse): unknown {
    const obj: any = {};
    if (message.providers?.length) {
      obj.providers = message.providers.map((e) => Provider.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProvidersResponse>, I>>(base?: I): ListProvidersResponse {
    return ListProvidersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProvidersResponse>, I>>(object: I): ListProvidersResponse {
    const message = createBaseListProvidersResponse();
    message.providers = object.providers?.map((e) => Provider.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListProviderProfilesRequest(): ListProviderProfilesRequest {
  return { limit: 0, offset: 0 };
}

export const ListProviderProfilesRequest: MessageFns<ListProviderProfilesRequest> = {
  encode(message: ListProviderProfilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limit !== 0) {
      writer.uint32(8).uint32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProviderProfilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProviderProfilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProviderProfilesRequest {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: ListProviderProfilesRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProviderProfilesRequest>, I>>(base?: I): ListProviderProfilesRequest {
    return ListProviderProfilesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProviderProfilesRequest>, I>>(object: I): ListProviderProfilesRequest {
    const message = createBaseListProviderProfilesRequest();
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseGetProviderProfileRequest(): GetProviderProfileRequest {
  return { id: "" };
}

export const GetProviderProfileRequest: MessageFns<GetProviderProfileRequest> = {
  encode(message: GetProviderProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProviderProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProviderProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProviderProfileRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: GetProviderProfileRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProviderProfileRequest>, I>>(base?: I): GetProviderProfileRequest {
    return GetProviderProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProviderProfileRequest>, I>>(object: I): GetProviderProfileRequest {
    const message = createBaseGetProviderProfileRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseProviderProfileImportItem(): ProviderProfileImportItem {
  return { profile: undefined, source: "" };
}

export const ProviderProfileImportItem: MessageFns<ProviderProfileImportItem> = {
  encode(message: ProviderProfileImportItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      ProviderProfile.encode(message.profile, writer.uint32(10).fork()).join();
    }
    if (message.source !== "") {
      writer.uint32(18).string(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderProfileImportItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderProfileImportItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = ProviderProfile.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderProfileImportItem {
    return {
      profile: isSet(object.profile) ? ProviderProfile.fromJSON(object.profile) : undefined,
      source: isSet(object.source) ? globalThis.String(object.source) : "",
    };
  },

  toJSON(message: ProviderProfileImportItem): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = ProviderProfile.toJSON(message.profile);
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderProfileImportItem>, I>>(base?: I): ProviderProfileImportItem {
    return ProviderProfileImportItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderProfileImportItem>, I>>(object: I): ProviderProfileImportItem {
    const message = createBaseProviderProfileImportItem();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? ProviderProfile.fromPartial(object.profile)
      : undefined;
    message.source = object.source ?? "";
    return message;
  },
};

function createBaseProviderProfileDiagnostic(): ProviderProfileDiagnostic {
  return { source: "", profileId: "", field: "", message: "", severity: "" };
}

export const ProviderProfileDiagnostic: MessageFns<ProviderProfileDiagnostic> = {
  encode(message: ProviderProfileDiagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.profileId !== "") {
      writer.uint32(18).string(message.profileId);
    }
    if (message.field !== "") {
      writer.uint32(26).string(message.field);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    if (message.severity !== "") {
      writer.uint32(42).string(message.severity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderProfileDiagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderProfileDiagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.profileId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.field = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.severity = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderProfileDiagnostic {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      profileId: isSet(object.profileId)
        ? globalThis.String(object.profileId)
        : isSet(object.profile_id)
        ? globalThis.String(object.profile_id)
        : "",
      field: isSet(object.field) ? globalThis.String(object.field) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      severity: isSet(object.severity) ? globalThis.String(object.severity) : "",
    };
  },

  toJSON(message: ProviderProfileDiagnostic): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.profileId !== "") {
      obj.profileId = message.profileId;
    }
    if (message.field !== "") {
      obj.field = message.field;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.severity !== "") {
      obj.severity = message.severity;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderProfileDiagnostic>, I>>(base?: I): ProviderProfileDiagnostic {
    return ProviderProfileDiagnostic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderProfileDiagnostic>, I>>(object: I): ProviderProfileDiagnostic {
    const message = createBaseProviderProfileDiagnostic();
    message.source = object.source ?? "";
    message.profileId = object.profileId ?? "";
    message.field = object.field ?? "";
    message.message = object.message ?? "";
    message.severity = object.severity ?? "";
    return message;
  },
};

function createBaseProviderProfileCredential(): ProviderProfileCredential {
  return {
    name: "",
    description: "",
    envVars: [],
    required: false,
    authStyle: "",
    headerName: "",
    queryParam: "",
    refresh: undefined,
  };
}

export const ProviderProfileCredential: MessageFns<ProviderProfileCredential> = {
  encode(message: ProviderProfileCredential, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    for (const v of message.envVars) {
      writer.uint32(26).string(v!);
    }
    if (message.required !== false) {
      writer.uint32(32).bool(message.required);
    }
    if (message.authStyle !== "") {
      writer.uint32(42).string(message.authStyle);
    }
    if (message.headerName !== "") {
      writer.uint32(50).string(message.headerName);
    }
    if (message.queryParam !== "") {
      writer.uint32(58).string(message.queryParam);
    }
    if (message.refresh !== undefined) {
      ProviderCredentialRefresh.encode(message.refresh, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderProfileCredential {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderProfileCredential();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.envVars.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.authStyle = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.headerName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.queryParam = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.refresh = ProviderCredentialRefresh.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderProfileCredential {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      envVars: globalThis.Array.isArray(object?.envVars)
        ? object.envVars.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.env_vars)
        ? object.env_vars.map((e: any) => globalThis.String(e))
        : [],
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
      authStyle: isSet(object.authStyle)
        ? globalThis.String(object.authStyle)
        : isSet(object.auth_style)
        ? globalThis.String(object.auth_style)
        : "",
      headerName: isSet(object.headerName)
        ? globalThis.String(object.headerName)
        : isSet(object.header_name)
        ? globalThis.String(object.header_name)
        : "",
      queryParam: isSet(object.queryParam)
        ? globalThis.String(object.queryParam)
        : isSet(object.query_param)
        ? globalThis.String(object.query_param)
        : "",
      refresh: isSet(object.refresh) ? ProviderCredentialRefresh.fromJSON(object.refresh) : undefined,
    };
  },

  toJSON(message: ProviderProfileCredential): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.envVars?.length) {
      obj.envVars = message.envVars;
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    if (message.authStyle !== "") {
      obj.authStyle = message.authStyle;
    }
    if (message.headerName !== "") {
      obj.headerName = message.headerName;
    }
    if (message.queryParam !== "") {
      obj.queryParam = message.queryParam;
    }
    if (message.refresh !== undefined) {
      obj.refresh = ProviderCredentialRefresh.toJSON(message.refresh);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderProfileCredential>, I>>(base?: I): ProviderProfileCredential {
    return ProviderProfileCredential.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderProfileCredential>, I>>(object: I): ProviderProfileCredential {
    const message = createBaseProviderProfileCredential();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.envVars = object.envVars?.map((e) => e) || [];
    message.required = object.required ?? false;
    message.authStyle = object.authStyle ?? "";
    message.headerName = object.headerName ?? "";
    message.queryParam = object.queryParam ?? "";
    message.refresh = (object.refresh !== undefined && object.refresh !== null)
      ? ProviderCredentialRefresh.fromPartial(object.refresh)
      : undefined;
    return message;
  },
};

function createBaseProviderCredentialRefreshMaterial(): ProviderCredentialRefreshMaterial {
  return { name: "", description: "", required: false, secret: false };
}

export const ProviderCredentialRefreshMaterial: MessageFns<ProviderCredentialRefreshMaterial> = {
  encode(message: ProviderCredentialRefreshMaterial, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.required !== false) {
      writer.uint32(24).bool(message.required);
    }
    if (message.secret !== false) {
      writer.uint32(32).bool(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderCredentialRefreshMaterial {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderCredentialRefreshMaterial();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.secret = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderCredentialRefreshMaterial {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
      secret: isSet(object.secret) ? globalThis.Boolean(object.secret) : false,
    };
  },

  toJSON(message: ProviderCredentialRefreshMaterial): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    if (message.secret !== false) {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderCredentialRefreshMaterial>, I>>(
    base?: I,
  ): ProviderCredentialRefreshMaterial {
    return ProviderCredentialRefreshMaterial.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderCredentialRefreshMaterial>, I>>(
    object: I,
  ): ProviderCredentialRefreshMaterial {
    const message = createBaseProviderCredentialRefreshMaterial();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.required = object.required ?? false;
    message.secret = object.secret ?? false;
    return message;
  },
};

function createBaseProviderCredentialRefresh(): ProviderCredentialRefresh {
  return { strategy: 0, tokenUrl: "", scopes: [], refreshBeforeSeconds: 0, maxLifetimeSeconds: 0, material: [] };
}

export const ProviderCredentialRefresh: MessageFns<ProviderCredentialRefresh> = {
  encode(message: ProviderCredentialRefresh, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.strategy !== 0) {
      writer.uint32(8).int32(message.strategy);
    }
    if (message.tokenUrl !== "") {
      writer.uint32(18).string(message.tokenUrl);
    }
    for (const v of message.scopes) {
      writer.uint32(26).string(v!);
    }
    if (message.refreshBeforeSeconds !== 0) {
      writer.uint32(32).int64(message.refreshBeforeSeconds);
    }
    if (message.maxLifetimeSeconds !== 0) {
      writer.uint32(40).int64(message.maxLifetimeSeconds);
    }
    for (const v of message.material) {
      ProviderCredentialRefreshMaterial.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderCredentialRefresh {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderCredentialRefresh();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.strategy = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tokenUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scopes.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.refreshBeforeSeconds = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxLifetimeSeconds = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.material.push(ProviderCredentialRefreshMaterial.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderCredentialRefresh {
    return {
      strategy: isSet(object.strategy) ? providerCredentialRefreshStrategyFromJSON(object.strategy) : 0,
      tokenUrl: isSet(object.tokenUrl)
        ? globalThis.String(object.tokenUrl)
        : isSet(object.token_url)
        ? globalThis.String(object.token_url)
        : "",
      scopes: globalThis.Array.isArray(object?.scopes) ? object.scopes.map((e: any) => globalThis.String(e)) : [],
      refreshBeforeSeconds: isSet(object.refreshBeforeSeconds)
        ? globalThis.Number(object.refreshBeforeSeconds)
        : isSet(object.refresh_before_seconds)
        ? globalThis.Number(object.refresh_before_seconds)
        : 0,
      maxLifetimeSeconds: isSet(object.maxLifetimeSeconds)
        ? globalThis.Number(object.maxLifetimeSeconds)
        : isSet(object.max_lifetime_seconds)
        ? globalThis.Number(object.max_lifetime_seconds)
        : 0,
      material: globalThis.Array.isArray(object?.material)
        ? object.material.map((e: any) => ProviderCredentialRefreshMaterial.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ProviderCredentialRefresh): unknown {
    const obj: any = {};
    if (message.strategy !== 0) {
      obj.strategy = providerCredentialRefreshStrategyToJSON(message.strategy);
    }
    if (message.tokenUrl !== "") {
      obj.tokenUrl = message.tokenUrl;
    }
    if (message.scopes?.length) {
      obj.scopes = message.scopes;
    }
    if (message.refreshBeforeSeconds !== 0) {
      obj.refreshBeforeSeconds = Math.round(message.refreshBeforeSeconds);
    }
    if (message.maxLifetimeSeconds !== 0) {
      obj.maxLifetimeSeconds = Math.round(message.maxLifetimeSeconds);
    }
    if (message.material?.length) {
      obj.material = message.material.map((e) => ProviderCredentialRefreshMaterial.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderCredentialRefresh>, I>>(base?: I): ProviderCredentialRefresh {
    return ProviderCredentialRefresh.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderCredentialRefresh>, I>>(object: I): ProviderCredentialRefresh {
    const message = createBaseProviderCredentialRefresh();
    message.strategy = object.strategy ?? 0;
    message.tokenUrl = object.tokenUrl ?? "";
    message.scopes = object.scopes?.map((e) => e) || [];
    message.refreshBeforeSeconds = object.refreshBeforeSeconds ?? 0;
    message.maxLifetimeSeconds = object.maxLifetimeSeconds ?? 0;
    message.material = object.material?.map((e) => ProviderCredentialRefreshMaterial.fromPartial(e)) || [];
    return message;
  },
};

function createBaseProviderCredentialRefreshStatus(): ProviderCredentialRefreshStatus {
  return {
    providerName: "",
    providerId: "",
    credentialKey: "",
    strategy: 0,
    status: "",
    expiresAtMs: 0,
    nextRefreshAtMs: 0,
    lastRefreshAtMs: 0,
    lastError: "",
  };
}

export const ProviderCredentialRefreshStatus: MessageFns<ProviderCredentialRefreshStatus> = {
  encode(message: ProviderCredentialRefreshStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.providerName !== "") {
      writer.uint32(10).string(message.providerName);
    }
    if (message.providerId !== "") {
      writer.uint32(18).string(message.providerId);
    }
    if (message.credentialKey !== "") {
      writer.uint32(26).string(message.credentialKey);
    }
    if (message.strategy !== 0) {
      writer.uint32(32).int32(message.strategy);
    }
    if (message.status !== "") {
      writer.uint32(42).string(message.status);
    }
    if (message.expiresAtMs !== 0) {
      writer.uint32(48).int64(message.expiresAtMs);
    }
    if (message.nextRefreshAtMs !== 0) {
      writer.uint32(56).int64(message.nextRefreshAtMs);
    }
    if (message.lastRefreshAtMs !== 0) {
      writer.uint32(64).int64(message.lastRefreshAtMs);
    }
    if (message.lastError !== "") {
      writer.uint32(74).string(message.lastError);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderCredentialRefreshStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderCredentialRefreshStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providerName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.credentialKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.strategy = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expiresAtMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.nextRefreshAtMs = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.lastRefreshAtMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lastError = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderCredentialRefreshStatus {
    return {
      providerName: isSet(object.providerName)
        ? globalThis.String(object.providerName)
        : isSet(object.provider_name)
        ? globalThis.String(object.provider_name)
        : "",
      providerId: isSet(object.providerId)
        ? globalThis.String(object.providerId)
        : isSet(object.provider_id)
        ? globalThis.String(object.provider_id)
        : "",
      credentialKey: isSet(object.credentialKey)
        ? globalThis.String(object.credentialKey)
        : isSet(object.credential_key)
        ? globalThis.String(object.credential_key)
        : "",
      strategy: isSet(object.strategy) ? providerCredentialRefreshStrategyFromJSON(object.strategy) : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      expiresAtMs: isSet(object.expiresAtMs)
        ? globalThis.Number(object.expiresAtMs)
        : isSet(object.expires_at_ms)
        ? globalThis.Number(object.expires_at_ms)
        : 0,
      nextRefreshAtMs: isSet(object.nextRefreshAtMs)
        ? globalThis.Number(object.nextRefreshAtMs)
        : isSet(object.next_refresh_at_ms)
        ? globalThis.Number(object.next_refresh_at_ms)
        : 0,
      lastRefreshAtMs: isSet(object.lastRefreshAtMs)
        ? globalThis.Number(object.lastRefreshAtMs)
        : isSet(object.last_refresh_at_ms)
        ? globalThis.Number(object.last_refresh_at_ms)
        : 0,
      lastError: isSet(object.lastError)
        ? globalThis.String(object.lastError)
        : isSet(object.last_error)
        ? globalThis.String(object.last_error)
        : "",
    };
  },

  toJSON(message: ProviderCredentialRefreshStatus): unknown {
    const obj: any = {};
    if (message.providerName !== "") {
      obj.providerName = message.providerName;
    }
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.credentialKey !== "") {
      obj.credentialKey = message.credentialKey;
    }
    if (message.strategy !== 0) {
      obj.strategy = providerCredentialRefreshStrategyToJSON(message.strategy);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.expiresAtMs !== 0) {
      obj.expiresAtMs = Math.round(message.expiresAtMs);
    }
    if (message.nextRefreshAtMs !== 0) {
      obj.nextRefreshAtMs = Math.round(message.nextRefreshAtMs);
    }
    if (message.lastRefreshAtMs !== 0) {
      obj.lastRefreshAtMs = Math.round(message.lastRefreshAtMs);
    }
    if (message.lastError !== "") {
      obj.lastError = message.lastError;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderCredentialRefreshStatus>, I>>(base?: I): ProviderCredentialRefreshStatus {
    return ProviderCredentialRefreshStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderCredentialRefreshStatus>, I>>(
    object: I,
  ): ProviderCredentialRefreshStatus {
    const message = createBaseProviderCredentialRefreshStatus();
    message.providerName = object.providerName ?? "";
    message.providerId = object.providerId ?? "";
    message.credentialKey = object.credentialKey ?? "";
    message.strategy = object.strategy ?? 0;
    message.status = object.status ?? "";
    message.expiresAtMs = object.expiresAtMs ?? 0;
    message.nextRefreshAtMs = object.nextRefreshAtMs ?? 0;
    message.lastRefreshAtMs = object.lastRefreshAtMs ?? 0;
    message.lastError = object.lastError ?? "";
    return message;
  },
};

function createBaseStoredProviderCredentialRefreshState(): StoredProviderCredentialRefreshState {
  return {
    metadata: undefined,
    providerId: "",
    providerName: "",
    credentialKey: "",
    strategy: 0,
    material: {},
    secretMaterialKeys: [],
    expiresAtMs: 0,
    nextRefreshAtMs: 0,
    lastRefreshAtMs: 0,
    status: "",
    lastError: "",
    tokenUrl: "",
    scopes: [],
    refreshBeforeSeconds: 0,
    maxLifetimeSeconds: 0,
  };
}

export const StoredProviderCredentialRefreshState: MessageFns<StoredProviderCredentialRefreshState> = {
  encode(message: StoredProviderCredentialRefreshState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.providerId !== "") {
      writer.uint32(18).string(message.providerId);
    }
    if (message.providerName !== "") {
      writer.uint32(26).string(message.providerName);
    }
    if (message.credentialKey !== "") {
      writer.uint32(34).string(message.credentialKey);
    }
    if (message.strategy !== 0) {
      writer.uint32(40).int32(message.strategy);
    }
    globalThis.Object.entries(message.material).forEach(([key, value]: [string, string]) => {
      StoredProviderCredentialRefreshState_MaterialEntry.encode({ key: key as any, value }, writer.uint32(50).fork())
        .join();
    });
    for (const v of message.secretMaterialKeys) {
      writer.uint32(58).string(v!);
    }
    if (message.expiresAtMs !== 0) {
      writer.uint32(64).int64(message.expiresAtMs);
    }
    if (message.nextRefreshAtMs !== 0) {
      writer.uint32(72).int64(message.nextRefreshAtMs);
    }
    if (message.lastRefreshAtMs !== 0) {
      writer.uint32(80).int64(message.lastRefreshAtMs);
    }
    if (message.status !== "") {
      writer.uint32(90).string(message.status);
    }
    if (message.lastError !== "") {
      writer.uint32(98).string(message.lastError);
    }
    if (message.tokenUrl !== "") {
      writer.uint32(106).string(message.tokenUrl);
    }
    for (const v of message.scopes) {
      writer.uint32(114).string(v!);
    }
    if (message.refreshBeforeSeconds !== 0) {
      writer.uint32(120).int64(message.refreshBeforeSeconds);
    }
    if (message.maxLifetimeSeconds !== 0) {
      writer.uint32(128).int64(message.maxLifetimeSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredProviderCredentialRefreshState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredProviderCredentialRefreshState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = ObjectMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.providerName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.credentialKey = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.strategy = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = StoredProviderCredentialRefreshState_MaterialEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.material[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.secretMaterialKeys.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.expiresAtMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.nextRefreshAtMs = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.lastRefreshAtMs = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.lastError = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.tokenUrl = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.scopes.push(reader.string());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.refreshBeforeSeconds = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.maxLifetimeSeconds = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredProviderCredentialRefreshState {
    return {
      metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
      providerId: isSet(object.providerId)
        ? globalThis.String(object.providerId)
        : isSet(object.provider_id)
        ? globalThis.String(object.provider_id)
        : "",
      providerName: isSet(object.providerName)
        ? globalThis.String(object.providerName)
        : isSet(object.provider_name)
        ? globalThis.String(object.provider_name)
        : "",
      credentialKey: isSet(object.credentialKey)
        ? globalThis.String(object.credentialKey)
        : isSet(object.credential_key)
        ? globalThis.String(object.credential_key)
        : "",
      strategy: isSet(object.strategy) ? providerCredentialRefreshStrategyFromJSON(object.strategy) : 0,
      material: isObject(object.material)
        ? (globalThis.Object.entries(object.material) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      secretMaterialKeys: globalThis.Array.isArray(object?.secretMaterialKeys)
        ? object.secretMaterialKeys.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.secret_material_keys)
        ? object.secret_material_keys.map((e: any) => globalThis.String(e))
        : [],
      expiresAtMs: isSet(object.expiresAtMs)
        ? globalThis.Number(object.expiresAtMs)
        : isSet(object.expires_at_ms)
        ? globalThis.Number(object.expires_at_ms)
        : 0,
      nextRefreshAtMs: isSet(object.nextRefreshAtMs)
        ? globalThis.Number(object.nextRefreshAtMs)
        : isSet(object.next_refresh_at_ms)
        ? globalThis.Number(object.next_refresh_at_ms)
        : 0,
      lastRefreshAtMs: isSet(object.lastRefreshAtMs)
        ? globalThis.Number(object.lastRefreshAtMs)
        : isSet(object.last_refresh_at_ms)
        ? globalThis.Number(object.last_refresh_at_ms)
        : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      lastError: isSet(object.lastError)
        ? globalThis.String(object.lastError)
        : isSet(object.last_error)
        ? globalThis.String(object.last_error)
        : "",
      tokenUrl: isSet(object.tokenUrl)
        ? globalThis.String(object.tokenUrl)
        : isSet(object.token_url)
        ? globalThis.String(object.token_url)
        : "",
      scopes: globalThis.Array.isArray(object?.scopes)
        ? object.scopes.map((e: any) => globalThis.String(e))
        : [],
      refreshBeforeSeconds: isSet(object.refreshBeforeSeconds)
        ? globalThis.Number(object.refreshBeforeSeconds)
        : isSet(object.refresh_before_seconds)
        ? globalThis.Number(object.refresh_before_seconds)
        : 0,
      maxLifetimeSeconds: isSet(object.maxLifetimeSeconds)
        ? globalThis.Number(object.maxLifetimeSeconds)
        : isSet(object.max_lifetime_seconds)
        ? globalThis.Number(object.max_lifetime_seconds)
        : 0,
    };
  },

  toJSON(message: StoredProviderCredentialRefreshState): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = ObjectMeta.toJSON(message.metadata);
    }
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.providerName !== "") {
      obj.providerName = message.providerName;
    }
    if (message.credentialKey !== "") {
      obj.credentialKey = message.credentialKey;
    }
    if (message.strategy !== 0) {
      obj.strategy = providerCredentialRefreshStrategyToJSON(message.strategy);
    }
    if (message.material) {
      const entries = globalThis.Object.entries(message.material) as [string, string][];
      if (entries.length > 0) {
        obj.material = {};
        entries.forEach(([k, v]) => {
          obj.material[k] = v;
        });
      }
    }
    if (message.secretMaterialKeys?.length) {
      obj.secretMaterialKeys = message.secretMaterialKeys;
    }
    if (message.expiresAtMs !== 0) {
      obj.expiresAtMs = Math.round(message.expiresAtMs);
    }
    if (message.nextRefreshAtMs !== 0) {
      obj.nextRefreshAtMs = Math.round(message.nextRefreshAtMs);
    }
    if (message.lastRefreshAtMs !== 0) {
      obj.lastRefreshAtMs = Math.round(message.lastRefreshAtMs);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.lastError !== "") {
      obj.lastError = message.lastError;
    }
    if (message.tokenUrl !== "") {
      obj.tokenUrl = message.tokenUrl;
    }
    if (message.scopes?.length) {
      obj.scopes = message.scopes;
    }
    if (message.refreshBeforeSeconds !== 0) {
      obj.refreshBeforeSeconds = Math.round(message.refreshBeforeSeconds);
    }
    if (message.maxLifetimeSeconds !== 0) {
      obj.maxLifetimeSeconds = Math.round(message.maxLifetimeSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredProviderCredentialRefreshState>, I>>(
    base?: I,
  ): StoredProviderCredentialRefreshState {
    return StoredProviderCredentialRefreshState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredProviderCredentialRefreshState>, I>>(
    object: I,
  ): StoredProviderCredentialRefreshState {
    const message = createBaseStoredProviderCredentialRefreshState();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ObjectMeta.fromPartial(object.metadata)
      : undefined;
    message.providerId = object.providerId ?? "";
    message.providerName = object.providerName ?? "";
    message.credentialKey = object.credentialKey ?? "";
    message.strategy = object.strategy ?? 0;
    message.material = (globalThis.Object.entries(object.material ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.secretMaterialKeys = object.secretMaterialKeys?.map((e) => e) || [];
    message.expiresAtMs = object.expiresAtMs ?? 0;
    message.nextRefreshAtMs = object.nextRefreshAtMs ?? 0;
    message.lastRefreshAtMs = object.lastRefreshAtMs ?? 0;
    message.status = object.status ?? "";
    message.lastError = object.lastError ?? "";
    message.tokenUrl = object.tokenUrl ?? "";
    message.scopes = object.scopes?.map((e) => e) || [];
    message.refreshBeforeSeconds = object.refreshBeforeSeconds ?? 0;
    message.maxLifetimeSeconds = object.maxLifetimeSeconds ?? 0;
    return message;
  },
};

function createBaseStoredProviderCredentialRefreshState_MaterialEntry(): StoredProviderCredentialRefreshState_MaterialEntry {
  return { key: "", value: "" };
}

export const StoredProviderCredentialRefreshState_MaterialEntry: MessageFns<
  StoredProviderCredentialRefreshState_MaterialEntry
> = {
  encode(
    message: StoredProviderCredentialRefreshState_MaterialEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredProviderCredentialRefreshState_MaterialEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredProviderCredentialRefreshState_MaterialEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredProviderCredentialRefreshState_MaterialEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: StoredProviderCredentialRefreshState_MaterialEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredProviderCredentialRefreshState_MaterialEntry>, I>>(
    base?: I,
  ): StoredProviderCredentialRefreshState_MaterialEntry {
    return StoredProviderCredentialRefreshState_MaterialEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredProviderCredentialRefreshState_MaterialEntry>, I>>(
    object: I,
  ): StoredProviderCredentialRefreshState_MaterialEntry {
    const message = createBaseStoredProviderCredentialRefreshState_MaterialEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetProviderRefreshStatusRequest(): GetProviderRefreshStatusRequest {
  return { provider: "", credentialKey: "" };
}

export const GetProviderRefreshStatusRequest: MessageFns<GetProviderRefreshStatusRequest> = {
  encode(message: GetProviderRefreshStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.credentialKey !== "") {
      writer.uint32(18).string(message.credentialKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProviderRefreshStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProviderRefreshStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentialKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProviderRefreshStatusRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      credentialKey: isSet(object.credentialKey)
        ? globalThis.String(object.credentialKey)
        : isSet(object.credential_key)
        ? globalThis.String(object.credential_key)
        : "",
    };
  },

  toJSON(message: GetProviderRefreshStatusRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.credentialKey !== "") {
      obj.credentialKey = message.credentialKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProviderRefreshStatusRequest>, I>>(base?: I): GetProviderRefreshStatusRequest {
    return GetProviderRefreshStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProviderRefreshStatusRequest>, I>>(
    object: I,
  ): GetProviderRefreshStatusRequest {
    const message = createBaseGetProviderRefreshStatusRequest();
    message.provider = object.provider ?? "";
    message.credentialKey = object.credentialKey ?? "";
    return message;
  },
};

function createBaseGetProviderRefreshStatusResponse(): GetProviderRefreshStatusResponse {
  return { credentials: [] };
}

export const GetProviderRefreshStatusResponse: MessageFns<GetProviderRefreshStatusResponse> = {
  encode(message: GetProviderRefreshStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.credentials) {
      ProviderCredentialRefreshStatus.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProviderRefreshStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProviderRefreshStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credentials.push(ProviderCredentialRefreshStatus.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProviderRefreshStatusResponse {
    return {
      credentials: globalThis.Array.isArray(object?.credentials)
        ? object.credentials.map((e: any) => ProviderCredentialRefreshStatus.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetProviderRefreshStatusResponse): unknown {
    const obj: any = {};
    if (message.credentials?.length) {
      obj.credentials = message.credentials.map((e) => ProviderCredentialRefreshStatus.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProviderRefreshStatusResponse>, I>>(
    base?: I,
  ): GetProviderRefreshStatusResponse {
    return GetProviderRefreshStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProviderRefreshStatusResponse>, I>>(
    object: I,
  ): GetProviderRefreshStatusResponse {
    const message = createBaseGetProviderRefreshStatusResponse();
    message.credentials = object.credentials?.map((e) => ProviderCredentialRefreshStatus.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConfigureProviderRefreshRequest(): ConfigureProviderRefreshRequest {
  return { provider: "", credentialKey: "", strategy: 0, material: {}, secretMaterialKeys: [], expiresAtMs: undefined };
}

export const ConfigureProviderRefreshRequest: MessageFns<ConfigureProviderRefreshRequest> = {
  encode(message: ConfigureProviderRefreshRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.credentialKey !== "") {
      writer.uint32(18).string(message.credentialKey);
    }
    if (message.strategy !== 0) {
      writer.uint32(24).int32(message.strategy);
    }
    globalThis.Object.entries(message.material).forEach(([key, value]: [string, string]) => {
      ConfigureProviderRefreshRequest_MaterialEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    for (const v of message.secretMaterialKeys) {
      writer.uint32(42).string(v!);
    }
    if (message.expiresAtMs !== undefined) {
      writer.uint32(48).int64(message.expiresAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigureProviderRefreshRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigureProviderRefreshRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentialKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.strategy = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ConfigureProviderRefreshRequest_MaterialEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.material[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secretMaterialKeys.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expiresAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigureProviderRefreshRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      credentialKey: isSet(object.credentialKey)
        ? globalThis.String(object.credentialKey)
        : isSet(object.credential_key)
        ? globalThis.String(object.credential_key)
        : "",
      strategy: isSet(object.strategy) ? providerCredentialRefreshStrategyFromJSON(object.strategy) : 0,
      material: isObject(object.material)
        ? (globalThis.Object.entries(object.material) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      secretMaterialKeys: globalThis.Array.isArray(object?.secretMaterialKeys)
        ? object.secretMaterialKeys.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.secret_material_keys)
        ? object.secret_material_keys.map((e: any) => globalThis.String(e))
        : [],
      expiresAtMs: isSet(object.expiresAtMs)
        ? globalThis.Number(object.expiresAtMs)
        : isSet(object.expires_at_ms)
        ? globalThis.Number(object.expires_at_ms)
        : undefined,
    };
  },

  toJSON(message: ConfigureProviderRefreshRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.credentialKey !== "") {
      obj.credentialKey = message.credentialKey;
    }
    if (message.strategy !== 0) {
      obj.strategy = providerCredentialRefreshStrategyToJSON(message.strategy);
    }
    if (message.material) {
      const entries = globalThis.Object.entries(message.material) as [string, string][];
      if (entries.length > 0) {
        obj.material = {};
        entries.forEach(([k, v]) => {
          obj.material[k] = v;
        });
      }
    }
    if (message.secretMaterialKeys?.length) {
      obj.secretMaterialKeys = message.secretMaterialKeys;
    }
    if (message.expiresAtMs !== undefined) {
      obj.expiresAtMs = Math.round(message.expiresAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigureProviderRefreshRequest>, I>>(base?: I): ConfigureProviderRefreshRequest {
    return ConfigureProviderRefreshRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigureProviderRefreshRequest>, I>>(
    object: I,
  ): ConfigureProviderRefreshRequest {
    const message = createBaseConfigureProviderRefreshRequest();
    message.provider = object.provider ?? "";
    message.credentialKey = object.credentialKey ?? "";
    message.strategy = object.strategy ?? 0;
    message.material = (globalThis.Object.entries(object.material ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.secretMaterialKeys = object.secretMaterialKeys?.map((e) => e) || [];
    message.expiresAtMs = object.expiresAtMs ?? undefined;
    return message;
  },
};

function createBaseConfigureProviderRefreshRequest_MaterialEntry(): ConfigureProviderRefreshRequest_MaterialEntry {
  return { key: "", value: "" };
}

export const ConfigureProviderRefreshRequest_MaterialEntry: MessageFns<ConfigureProviderRefreshRequest_MaterialEntry> =
  {
    encode(
      message: ConfigureProviderRefreshRequest_MaterialEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== "") {
        writer.uint32(18).string(message.value);
      }
      return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ConfigureProviderRefreshRequest_MaterialEntry {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfigureProviderRefreshRequest_MaterialEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): ConfigureProviderRefreshRequest_MaterialEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? globalThis.String(object.value) : "",
      };
    },

    toJSON(message: ConfigureProviderRefreshRequest_MaterialEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== "") {
        obj.value = message.value;
      }
      return obj;
    },

    create<I extends Exact<DeepPartial<ConfigureProviderRefreshRequest_MaterialEntry>, I>>(
      base?: I,
    ): ConfigureProviderRefreshRequest_MaterialEntry {
      return ConfigureProviderRefreshRequest_MaterialEntry.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ConfigureProviderRefreshRequest_MaterialEntry>, I>>(
      object: I,
    ): ConfigureProviderRefreshRequest_MaterialEntry {
      const message = createBaseConfigureProviderRefreshRequest_MaterialEntry();
      message.key = object.key ?? "";
      message.value = object.value ?? "";
      return message;
    },
  };

function createBaseConfigureProviderRefreshResponse(): ConfigureProviderRefreshResponse {
  return { status: undefined };
}

export const ConfigureProviderRefreshResponse: MessageFns<ConfigureProviderRefreshResponse> = {
  encode(message: ConfigureProviderRefreshResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ProviderCredentialRefreshStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigureProviderRefreshResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigureProviderRefreshResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ProviderCredentialRefreshStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigureProviderRefreshResponse {
    return { status: isSet(object.status) ? ProviderCredentialRefreshStatus.fromJSON(object.status) : undefined };
  },

  toJSON(message: ConfigureProviderRefreshResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = ProviderCredentialRefreshStatus.toJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigureProviderRefreshResponse>, I>>(
    base?: I,
  ): ConfigureProviderRefreshResponse {
    return ConfigureProviderRefreshResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigureProviderRefreshResponse>, I>>(
    object: I,
  ): ConfigureProviderRefreshResponse {
    const message = createBaseConfigureProviderRefreshResponse();
    message.status = (object.status !== undefined && object.status !== null)
      ? ProviderCredentialRefreshStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseRotateProviderCredentialRequest(): RotateProviderCredentialRequest {
  return { provider: "", credentialKey: "" };
}

export const RotateProviderCredentialRequest: MessageFns<RotateProviderCredentialRequest> = {
  encode(message: RotateProviderCredentialRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.credentialKey !== "") {
      writer.uint32(18).string(message.credentialKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateProviderCredentialRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateProviderCredentialRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentialKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateProviderCredentialRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      credentialKey: isSet(object.credentialKey)
        ? globalThis.String(object.credentialKey)
        : isSet(object.credential_key)
        ? globalThis.String(object.credential_key)
        : "",
    };
  },

  toJSON(message: RotateProviderCredentialRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.credentialKey !== "") {
      obj.credentialKey = message.credentialKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RotateProviderCredentialRequest>, I>>(base?: I): RotateProviderCredentialRequest {
    return RotateProviderCredentialRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RotateProviderCredentialRequest>, I>>(
    object: I,
  ): RotateProviderCredentialRequest {
    const message = createBaseRotateProviderCredentialRequest();
    message.provider = object.provider ?? "";
    message.credentialKey = object.credentialKey ?? "";
    return message;
  },
};

function createBaseRotateProviderCredentialResponse(): RotateProviderCredentialResponse {
  return { status: undefined };
}

export const RotateProviderCredentialResponse: MessageFns<RotateProviderCredentialResponse> = {
  encode(message: RotateProviderCredentialResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ProviderCredentialRefreshStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateProviderCredentialResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateProviderCredentialResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ProviderCredentialRefreshStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateProviderCredentialResponse {
    return { status: isSet(object.status) ? ProviderCredentialRefreshStatus.fromJSON(object.status) : undefined };
  },

  toJSON(message: RotateProviderCredentialResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = ProviderCredentialRefreshStatus.toJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RotateProviderCredentialResponse>, I>>(
    base?: I,
  ): RotateProviderCredentialResponse {
    return RotateProviderCredentialResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RotateProviderCredentialResponse>, I>>(
    object: I,
  ): RotateProviderCredentialResponse {
    const message = createBaseRotateProviderCredentialResponse();
    message.status = (object.status !== undefined && object.status !== null)
      ? ProviderCredentialRefreshStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseDeleteProviderRefreshRequest(): DeleteProviderRefreshRequest {
  return { provider: "", credentialKey: "" };
}

export const DeleteProviderRefreshRequest: MessageFns<DeleteProviderRefreshRequest> = {
  encode(message: DeleteProviderRefreshRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.credentialKey !== "") {
      writer.uint32(18).string(message.credentialKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProviderRefreshRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProviderRefreshRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentialKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProviderRefreshRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      credentialKey: isSet(object.credentialKey)
        ? globalThis.String(object.credentialKey)
        : isSet(object.credential_key)
        ? globalThis.String(object.credential_key)
        : "",
    };
  },

  toJSON(message: DeleteProviderRefreshRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.credentialKey !== "") {
      obj.credentialKey = message.credentialKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteProviderRefreshRequest>, I>>(base?: I): DeleteProviderRefreshRequest {
    return DeleteProviderRefreshRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteProviderRefreshRequest>, I>>(object: I): DeleteProviderRefreshRequest {
    const message = createBaseDeleteProviderRefreshRequest();
    message.provider = object.provider ?? "";
    message.credentialKey = object.credentialKey ?? "";
    return message;
  },
};

function createBaseDeleteProviderRefreshResponse(): DeleteProviderRefreshResponse {
  return { deleted: false };
}

export const DeleteProviderRefreshResponse: MessageFns<DeleteProviderRefreshResponse> = {
  encode(message: DeleteProviderRefreshResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== false) {
      writer.uint32(8).bool(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProviderRefreshResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProviderRefreshResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProviderRefreshResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false };
  },

  toJSON(message: DeleteProviderRefreshResponse): unknown {
    const obj: any = {};
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteProviderRefreshResponse>, I>>(base?: I): DeleteProviderRefreshResponse {
    return DeleteProviderRefreshResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteProviderRefreshResponse>, I>>(
    object: I,
  ): DeleteProviderRefreshResponse {
    const message = createBaseDeleteProviderRefreshResponse();
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseProviderProfile(): ProviderProfile {
  return {
    id: "",
    displayName: "",
    description: "",
    category: 0,
    credentials: [],
    endpoints: [],
    binaries: [],
    inferenceCapable: false,
  };
}

export const ProviderProfile: MessageFns<ProviderProfile> = {
  encode(message: ProviderProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.category !== 0) {
      writer.uint32(32).int32(message.category);
    }
    for (const v of message.credentials) {
      ProviderProfileCredential.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.endpoints) {
      NetworkEndpoint.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.binaries) {
      NetworkBinary.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.inferenceCapable !== false) {
      writer.uint32(64).bool(message.inferenceCapable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.credentials.push(ProviderProfileCredential.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endpoints.push(NetworkEndpoint.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.binaries.push(NetworkBinary.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.inferenceCapable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderProfile {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      category: isSet(object.category) ? providerProfileCategoryFromJSON(object.category) : 0,
      credentials: globalThis.Array.isArray(object?.credentials)
        ? object.credentials.map((e: any) => ProviderProfileCredential.fromJSON(e))
        : [],
      endpoints: globalThis.Array.isArray(object?.endpoints)
        ? object.endpoints.map((e: any) => NetworkEndpoint.fromJSON(e))
        : [],
      binaries: globalThis.Array.isArray(object?.binaries)
        ? object.binaries.map((e: any) => NetworkBinary.fromJSON(e))
        : [],
      inferenceCapable: isSet(object.inferenceCapable)
        ? globalThis.Boolean(object.inferenceCapable)
        : isSet(object.inference_capable)
        ? globalThis.Boolean(object.inference_capable)
        : false,
    };
  },

  toJSON(message: ProviderProfile): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.category !== 0) {
      obj.category = providerProfileCategoryToJSON(message.category);
    }
    if (message.credentials?.length) {
      obj.credentials = message.credentials.map((e) => ProviderProfileCredential.toJSON(e));
    }
    if (message.endpoints?.length) {
      obj.endpoints = message.endpoints.map((e) => NetworkEndpoint.toJSON(e));
    }
    if (message.binaries?.length) {
      obj.binaries = message.binaries.map((e) => NetworkBinary.toJSON(e));
    }
    if (message.inferenceCapable !== false) {
      obj.inferenceCapable = message.inferenceCapable;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderProfile>, I>>(base?: I): ProviderProfile {
    return ProviderProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderProfile>, I>>(object: I): ProviderProfile {
    const message = createBaseProviderProfile();
    message.id = object.id ?? "";
    message.displayName = object.displayName ?? "";
    message.description = object.description ?? "";
    message.category = object.category ?? 0;
    message.credentials = object.credentials?.map((e) => ProviderProfileCredential.fromPartial(e)) || [];
    message.endpoints = object.endpoints?.map((e) => NetworkEndpoint.fromPartial(e)) || [];
    message.binaries = object.binaries?.map((e) => NetworkBinary.fromPartial(e)) || [];
    message.inferenceCapable = object.inferenceCapable ?? false;
    return message;
  },
};

function createBaseStoredProviderProfile(): StoredProviderProfile {
  return { metadata: undefined, profile: undefined };
}

export const StoredProviderProfile: MessageFns<StoredProviderProfile> = {
  encode(message: StoredProviderProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.profile !== undefined) {
      ProviderProfile.encode(message.profile, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredProviderProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredProviderProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = ObjectMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.profile = ProviderProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredProviderProfile {
    return {
      metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
      profile: isSet(object.profile) ? ProviderProfile.fromJSON(object.profile) : undefined,
    };
  },

  toJSON(message: StoredProviderProfile): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = ObjectMeta.toJSON(message.metadata);
    }
    if (message.profile !== undefined) {
      obj.profile = ProviderProfile.toJSON(message.profile);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredProviderProfile>, I>>(base?: I): StoredProviderProfile {
    return StoredProviderProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredProviderProfile>, I>>(object: I): StoredProviderProfile {
    const message = createBaseStoredProviderProfile();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ObjectMeta.fromPartial(object.metadata)
      : undefined;
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? ProviderProfile.fromPartial(object.profile)
      : undefined;
    return message;
  },
};

function createBaseProviderProfileResponse(): ProviderProfileResponse {
  return { profile: undefined };
}

export const ProviderProfileResponse: MessageFns<ProviderProfileResponse> = {
  encode(message: ProviderProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      ProviderProfile.encode(message.profile, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = ProviderProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderProfileResponse {
    return { profile: isSet(object.profile) ? ProviderProfile.fromJSON(object.profile) : undefined };
  },

  toJSON(message: ProviderProfileResponse): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = ProviderProfile.toJSON(message.profile);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderProfileResponse>, I>>(base?: I): ProviderProfileResponse {
    return ProviderProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderProfileResponse>, I>>(object: I): ProviderProfileResponse {
    const message = createBaseProviderProfileResponse();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? ProviderProfile.fromPartial(object.profile)
      : undefined;
    return message;
  },
};

function createBaseListProviderProfilesResponse(): ListProviderProfilesResponse {
  return { profiles: [] };
}

export const ListProviderProfilesResponse: MessageFns<ListProviderProfilesResponse> = {
  encode(message: ListProviderProfilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.profiles) {
      ProviderProfile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProviderProfilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProviderProfilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profiles.push(ProviderProfile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProviderProfilesResponse {
    return {
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => ProviderProfile.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProviderProfilesResponse): unknown {
    const obj: any = {};
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => ProviderProfile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProviderProfilesResponse>, I>>(base?: I): ListProviderProfilesResponse {
    return ListProviderProfilesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProviderProfilesResponse>, I>>(object: I): ListProviderProfilesResponse {
    const message = createBaseListProviderProfilesResponse();
    message.profiles = object.profiles?.map((e) => ProviderProfile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImportProviderProfilesRequest(): ImportProviderProfilesRequest {
  return { profiles: [] };
}

export const ImportProviderProfilesRequest: MessageFns<ImportProviderProfilesRequest> = {
  encode(message: ImportProviderProfilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.profiles) {
      ProviderProfileImportItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportProviderProfilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportProviderProfilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profiles.push(ProviderProfileImportItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportProviderProfilesRequest {
    return {
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => ProviderProfileImportItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ImportProviderProfilesRequest): unknown {
    const obj: any = {};
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => ProviderProfileImportItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportProviderProfilesRequest>, I>>(base?: I): ImportProviderProfilesRequest {
    return ImportProviderProfilesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportProviderProfilesRequest>, I>>(
    object: I,
  ): ImportProviderProfilesRequest {
    const message = createBaseImportProviderProfilesRequest();
    message.profiles = object.profiles?.map((e) => ProviderProfileImportItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImportProviderProfilesResponse(): ImportProviderProfilesResponse {
  return { diagnostics: [], profiles: [], imported: false };
}

export const ImportProviderProfilesResponse: MessageFns<ImportProviderProfilesResponse> = {
  encode(message: ImportProviderProfilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.diagnostics) {
      ProviderProfileDiagnostic.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.profiles) {
      ProviderProfile.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.imported !== false) {
      writer.uint32(24).bool(message.imported);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportProviderProfilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportProviderProfilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.diagnostics.push(ProviderProfileDiagnostic.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.profiles.push(ProviderProfile.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.imported = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportProviderProfilesResponse {
    return {
      diagnostics: globalThis.Array.isArray(object?.diagnostics)
        ? object.diagnostics.map((e: any) => ProviderProfileDiagnostic.fromJSON(e))
        : [],
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => ProviderProfile.fromJSON(e))
        : [],
      imported: isSet(object.imported) ? globalThis.Boolean(object.imported) : false,
    };
  },

  toJSON(message: ImportProviderProfilesResponse): unknown {
    const obj: any = {};
    if (message.diagnostics?.length) {
      obj.diagnostics = message.diagnostics.map((e) => ProviderProfileDiagnostic.toJSON(e));
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => ProviderProfile.toJSON(e));
    }
    if (message.imported !== false) {
      obj.imported = message.imported;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportProviderProfilesResponse>, I>>(base?: I): ImportProviderProfilesResponse {
    return ImportProviderProfilesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportProviderProfilesResponse>, I>>(
    object: I,
  ): ImportProviderProfilesResponse {
    const message = createBaseImportProviderProfilesResponse();
    message.diagnostics = object.diagnostics?.map((e) => ProviderProfileDiagnostic.fromPartial(e)) || [];
    message.profiles = object.profiles?.map((e) => ProviderProfile.fromPartial(e)) || [];
    message.imported = object.imported ?? false;
    return message;
  },
};

function createBaseLintProviderProfilesRequest(): LintProviderProfilesRequest {
  return { profiles: [] };
}

export const LintProviderProfilesRequest: MessageFns<LintProviderProfilesRequest> = {
  encode(message: LintProviderProfilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.profiles) {
      ProviderProfileImportItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LintProviderProfilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLintProviderProfilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profiles.push(ProviderProfileImportItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LintProviderProfilesRequest {
    return {
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => ProviderProfileImportItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: LintProviderProfilesRequest): unknown {
    const obj: any = {};
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => ProviderProfileImportItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LintProviderProfilesRequest>, I>>(base?: I): LintProviderProfilesRequest {
    return LintProviderProfilesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LintProviderProfilesRequest>, I>>(object: I): LintProviderProfilesRequest {
    const message = createBaseLintProviderProfilesRequest();
    message.profiles = object.profiles?.map((e) => ProviderProfileImportItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLintProviderProfilesResponse(): LintProviderProfilesResponse {
  return { diagnostics: [], valid: false };
}

export const LintProviderProfilesResponse: MessageFns<LintProviderProfilesResponse> = {
  encode(message: LintProviderProfilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.diagnostics) {
      ProviderProfileDiagnostic.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.valid !== false) {
      writer.uint32(16).bool(message.valid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LintProviderProfilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLintProviderProfilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.diagnostics.push(ProviderProfileDiagnostic.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LintProviderProfilesResponse {
    return {
      diagnostics: globalThis.Array.isArray(object?.diagnostics)
        ? object.diagnostics.map((e: any) => ProviderProfileDiagnostic.fromJSON(e))
        : [],
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
    };
  },

  toJSON(message: LintProviderProfilesResponse): unknown {
    const obj: any = {};
    if (message.diagnostics?.length) {
      obj.diagnostics = message.diagnostics.map((e) => ProviderProfileDiagnostic.toJSON(e));
    }
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LintProviderProfilesResponse>, I>>(base?: I): LintProviderProfilesResponse {
    return LintProviderProfilesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LintProviderProfilesResponse>, I>>(object: I): LintProviderProfilesResponse {
    const message = createBaseLintProviderProfilesResponse();
    message.diagnostics = object.diagnostics?.map((e) => ProviderProfileDiagnostic.fromPartial(e)) || [];
    message.valid = object.valid ?? false;
    return message;
  },
};

function createBaseDeleteProviderResponse(): DeleteProviderResponse {
  return { deleted: false };
}

export const DeleteProviderResponse: MessageFns<DeleteProviderResponse> = {
  encode(message: DeleteProviderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== false) {
      writer.uint32(8).bool(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProviderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProviderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProviderResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false };
  },

  toJSON(message: DeleteProviderResponse): unknown {
    const obj: any = {};
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteProviderResponse>, I>>(base?: I): DeleteProviderResponse {
    return DeleteProviderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteProviderResponse>, I>>(object: I): DeleteProviderResponse {
    const message = createBaseDeleteProviderResponse();
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseDeleteProviderProfileRequest(): DeleteProviderProfileRequest {
  return { id: "" };
}

export const DeleteProviderProfileRequest: MessageFns<DeleteProviderProfileRequest> = {
  encode(message: DeleteProviderProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProviderProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProviderProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProviderProfileRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: DeleteProviderProfileRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteProviderProfileRequest>, I>>(base?: I): DeleteProviderProfileRequest {
    return DeleteProviderProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteProviderProfileRequest>, I>>(object: I): DeleteProviderProfileRequest {
    const message = createBaseDeleteProviderProfileRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseDeleteProviderProfileResponse(): DeleteProviderProfileResponse {
  return { deleted: false };
}

export const DeleteProviderProfileResponse: MessageFns<DeleteProviderProfileResponse> = {
  encode(message: DeleteProviderProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deleted !== false) {
      writer.uint32(8).bool(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProviderProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProviderProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProviderProfileResponse {
    return { deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false };
  },

  toJSON(message: DeleteProviderProfileResponse): unknown {
    const obj: any = {};
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteProviderProfileResponse>, I>>(base?: I): DeleteProviderProfileResponse {
    return DeleteProviderProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteProviderProfileResponse>, I>>(
    object: I,
  ): DeleteProviderProfileResponse {
    const message = createBaseDeleteProviderProfileResponse();
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseGetSandboxProviderEnvironmentRequest(): GetSandboxProviderEnvironmentRequest {
  return { sandboxId: "" };
}

export const GetSandboxProviderEnvironmentRequest: MessageFns<GetSandboxProviderEnvironmentRequest> = {
  encode(message: GetSandboxProviderEnvironmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxProviderEnvironmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxProviderEnvironmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxProviderEnvironmentRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
    };
  },

  toJSON(message: GetSandboxProviderEnvironmentRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentRequest>, I>>(
    base?: I,
  ): GetSandboxProviderEnvironmentRequest {
    return GetSandboxProviderEnvironmentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentRequest>, I>>(
    object: I,
  ): GetSandboxProviderEnvironmentRequest {
    const message = createBaseGetSandboxProviderEnvironmentRequest();
    message.sandboxId = object.sandboxId ?? "";
    return message;
  },
};

function createBaseGetSandboxProviderEnvironmentResponse(): GetSandboxProviderEnvironmentResponse {
  return { environment: {}, providerEnvRevision: 0, credentialExpiresAtMs: {} };
}

export const GetSandboxProviderEnvironmentResponse: MessageFns<GetSandboxProviderEnvironmentResponse> = {
  encode(message: GetSandboxProviderEnvironmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.environment).forEach(([key, value]: [string, string]) => {
      GetSandboxProviderEnvironmentResponse_EnvironmentEntry.encode(
        { key: key as any, value },
        writer.uint32(10).fork(),
      ).join();
    });
    if (message.providerEnvRevision !== 0) {
      writer.uint32(16).uint64(message.providerEnvRevision);
    }
    globalThis.Object.entries(message.credentialExpiresAtMs).forEach(([key, value]: [string, number]) => {
      GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry.encode(
        { key: key as any, value },
        writer.uint32(26).fork(),
      ).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxProviderEnvironmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxProviderEnvironmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GetSandboxProviderEnvironmentResponse_EnvironmentEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.environment[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.providerEnvRevision = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry3.value !== undefined) {
            message.credentialExpiresAtMs[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxProviderEnvironmentResponse {
    return {
      environment: isObject(object.environment)
        ? (globalThis.Object.entries(object.environment) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      providerEnvRevision: isSet(object.providerEnvRevision)
        ? globalThis.Number(object.providerEnvRevision)
        : isSet(object.provider_env_revision)
        ? globalThis.Number(object.provider_env_revision)
        : 0,
      credentialExpiresAtMs: isObject(object.credentialExpiresAtMs)
        ? (globalThis.Object.entries(object.credentialExpiresAtMs) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.credential_expires_at_ms)
        ? (globalThis.Object.entries(object.credential_expires_at_ms) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: GetSandboxProviderEnvironmentResponse): unknown {
    const obj: any = {};
    if (message.environment) {
      const entries = globalThis.Object.entries(message.environment) as [string, string][];
      if (entries.length > 0) {
        obj.environment = {};
        entries.forEach(([k, v]) => {
          obj.environment[k] = v;
        });
      }
    }
    if (message.providerEnvRevision !== 0) {
      obj.providerEnvRevision = Math.round(message.providerEnvRevision);
    }
    if (message.credentialExpiresAtMs) {
      const entries = globalThis.Object.entries(message.credentialExpiresAtMs) as [string, number][];
      if (entries.length > 0) {
        obj.credentialExpiresAtMs = {};
        entries.forEach(([k, v]) => {
          obj.credentialExpiresAtMs[k] = Math.round(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentResponse>, I>>(
    base?: I,
  ): GetSandboxProviderEnvironmentResponse {
    return GetSandboxProviderEnvironmentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentResponse>, I>>(
    object: I,
  ): GetSandboxProviderEnvironmentResponse {
    const message = createBaseGetSandboxProviderEnvironmentResponse();
    message.environment = (globalThis.Object.entries(object.environment ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.providerEnvRevision = object.providerEnvRevision ?? 0;
    message.credentialExpiresAtMs =
      (globalThis.Object.entries(object.credentialExpiresAtMs ?? {}) as [string, number][]).reduce(
        (acc: { [key: string]: number }, [key, value]: [string, number]) => {
          if (value !== undefined) {
            acc[key] = globalThis.Number(value);
          }
          return acc;
        },
        {},
      );
    return message;
  },
};

function createBaseGetSandboxProviderEnvironmentResponse_EnvironmentEntry(): GetSandboxProviderEnvironmentResponse_EnvironmentEntry {
  return { key: "", value: "" };
}

export const GetSandboxProviderEnvironmentResponse_EnvironmentEntry: MessageFns<
  GetSandboxProviderEnvironmentResponse_EnvironmentEntry
> = {
  encode(
    message: GetSandboxProviderEnvironmentResponse_EnvironmentEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxProviderEnvironmentResponse_EnvironmentEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxProviderEnvironmentResponse_EnvironmentEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxProviderEnvironmentResponse_EnvironmentEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GetSandboxProviderEnvironmentResponse_EnvironmentEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentResponse_EnvironmentEntry>, I>>(
    base?: I,
  ): GetSandboxProviderEnvironmentResponse_EnvironmentEntry {
    return GetSandboxProviderEnvironmentResponse_EnvironmentEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentResponse_EnvironmentEntry>, I>>(
    object: I,
  ): GetSandboxProviderEnvironmentResponse_EnvironmentEntry {
    const message = createBaseGetSandboxProviderEnvironmentResponse_EnvironmentEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry(): GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry {
  return { key: "", value: 0 };
}

export const GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry: MessageFns<
  GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry
> = {
  encode(
    message: GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry>, I>>(
    base?: I,
  ): GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry {
    return GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry>, I>>(
    object: I,
  ): GetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry {
    const message = createBaseGetSandboxProviderEnvironmentResponse_CredentialExpiresAtMsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseUpdateConfigRequest(): UpdateConfigRequest {
  return {
    name: "",
    policy: undefined,
    settingKey: "",
    settingValue: undefined,
    deleteSetting: false,
    global: false,
    mergeOperations: [],
    expectedResourceVersion: 0,
  };
}

export const UpdateConfigRequest: MessageFns<UpdateConfigRequest> = {
  encode(message: UpdateConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.policy !== undefined) {
      SandboxPolicy.encode(message.policy, writer.uint32(18).fork()).join();
    }
    if (message.settingKey !== "") {
      writer.uint32(26).string(message.settingKey);
    }
    if (message.settingValue !== undefined) {
      SettingValue.encode(message.settingValue, writer.uint32(34).fork()).join();
    }
    if (message.deleteSetting !== false) {
      writer.uint32(40).bool(message.deleteSetting);
    }
    if (message.global !== false) {
      writer.uint32(48).bool(message.global);
    }
    for (const v of message.mergeOperations) {
      PolicyMergeOperation.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.expectedResourceVersion !== 0) {
      writer.uint32(64).uint64(message.expectedResourceVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policy = SandboxPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.settingKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.settingValue = SettingValue.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.deleteSetting = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.global = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.mergeOperations.push(PolicyMergeOperation.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.expectedResourceVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateConfigRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      policy: isSet(object.policy) ? SandboxPolicy.fromJSON(object.policy) : undefined,
      settingKey: isSet(object.settingKey)
        ? globalThis.String(object.settingKey)
        : isSet(object.setting_key)
        ? globalThis.String(object.setting_key)
        : "",
      settingValue: isSet(object.settingValue)
        ? SettingValue.fromJSON(object.settingValue)
        : isSet(object.setting_value)
        ? SettingValue.fromJSON(object.setting_value)
        : undefined,
      deleteSetting: isSet(object.deleteSetting)
        ? globalThis.Boolean(object.deleteSetting)
        : isSet(object.delete_setting)
        ? globalThis.Boolean(object.delete_setting)
        : false,
      global: isSet(object.global) ? globalThis.Boolean(object.global) : false,
      mergeOperations: globalThis.Array.isArray(object?.mergeOperations)
        ? object.mergeOperations.map((e: any) => PolicyMergeOperation.fromJSON(e))
        : globalThis.Array.isArray(object?.merge_operations)
        ? object.merge_operations.map((e: any) => PolicyMergeOperation.fromJSON(e))
        : [],
      expectedResourceVersion: isSet(object.expectedResourceVersion)
        ? globalThis.Number(object.expectedResourceVersion)
        : isSet(object.expected_resource_version)
        ? globalThis.Number(object.expected_resource_version)
        : 0,
    };
  },

  toJSON(message: UpdateConfigRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.policy !== undefined) {
      obj.policy = SandboxPolicy.toJSON(message.policy);
    }
    if (message.settingKey !== "") {
      obj.settingKey = message.settingKey;
    }
    if (message.settingValue !== undefined) {
      obj.settingValue = SettingValue.toJSON(message.settingValue);
    }
    if (message.deleteSetting !== false) {
      obj.deleteSetting = message.deleteSetting;
    }
    if (message.global !== false) {
      obj.global = message.global;
    }
    if (message.mergeOperations?.length) {
      obj.mergeOperations = message.mergeOperations.map((e) => PolicyMergeOperation.toJSON(e));
    }
    if (message.expectedResourceVersion !== 0) {
      obj.expectedResourceVersion = Math.round(message.expectedResourceVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateConfigRequest>, I>>(base?: I): UpdateConfigRequest {
    return UpdateConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateConfigRequest>, I>>(object: I): UpdateConfigRequest {
    const message = createBaseUpdateConfigRequest();
    message.name = object.name ?? "";
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? SandboxPolicy.fromPartial(object.policy)
      : undefined;
    message.settingKey = object.settingKey ?? "";
    message.settingValue = (object.settingValue !== undefined && object.settingValue !== null)
      ? SettingValue.fromPartial(object.settingValue)
      : undefined;
    message.deleteSetting = object.deleteSetting ?? false;
    message.global = object.global ?? false;
    message.mergeOperations = object.mergeOperations?.map((e) => PolicyMergeOperation.fromPartial(e)) || [];
    message.expectedResourceVersion = object.expectedResourceVersion ?? 0;
    return message;
  },
};

function createBasePolicyMergeOperation(): PolicyMergeOperation {
  return { operation: undefined };
}

export const PolicyMergeOperation: MessageFns<PolicyMergeOperation> = {
  encode(message: PolicyMergeOperation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.operation?.$case) {
      case "addRule":
        AddNetworkRule.encode(message.operation.addRule, writer.uint32(10).fork()).join();
        break;
      case "removeEndpoint":
        RemoveNetworkEndpoint.encode(message.operation.removeEndpoint, writer.uint32(18).fork()).join();
        break;
      case "removeRule":
        RemoveNetworkRule.encode(message.operation.removeRule, writer.uint32(26).fork()).join();
        break;
      case "addDenyRules":
        AddDenyRules.encode(message.operation.addDenyRules, writer.uint32(34).fork()).join();
        break;
      case "addAllowRules":
        AddAllowRules.encode(message.operation.addAllowRules, writer.uint32(42).fork()).join();
        break;
      case "removeBinary":
        RemoveNetworkBinary.encode(message.operation.removeBinary, writer.uint32(50).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyMergeOperation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyMergeOperation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operation = { $case: "addRule", addRule: AddNetworkRule.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operation = {
            $case: "removeEndpoint",
            removeEndpoint: RemoveNetworkEndpoint.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.operation = { $case: "removeRule", removeRule: RemoveNetworkRule.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.operation = { $case: "addDenyRules", addDenyRules: AddDenyRules.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.operation = { $case: "addAllowRules", addAllowRules: AddAllowRules.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.operation = {
            $case: "removeBinary",
            removeBinary: RemoveNetworkBinary.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyMergeOperation {
    return {
      operation: isSet(object.addRule)
        ? { $case: "addRule", addRule: AddNetworkRule.fromJSON(object.addRule) }
        : isSet(object.add_rule)
        ? { $case: "addRule", addRule: AddNetworkRule.fromJSON(object.add_rule) }
        : isSet(object.removeEndpoint)
        ? { $case: "removeEndpoint", removeEndpoint: RemoveNetworkEndpoint.fromJSON(object.removeEndpoint) }
        : isSet(object.remove_endpoint)
        ? { $case: "removeEndpoint", removeEndpoint: RemoveNetworkEndpoint.fromJSON(object.remove_endpoint) }
        : isSet(object.removeRule)
        ? { $case: "removeRule", removeRule: RemoveNetworkRule.fromJSON(object.removeRule) }
        : isSet(object.remove_rule)
        ? { $case: "removeRule", removeRule: RemoveNetworkRule.fromJSON(object.remove_rule) }
        : isSet(object.addDenyRules)
        ? { $case: "addDenyRules", addDenyRules: AddDenyRules.fromJSON(object.addDenyRules) }
        : isSet(object.add_deny_rules)
        ? { $case: "addDenyRules", addDenyRules: AddDenyRules.fromJSON(object.add_deny_rules) }
        : isSet(object.addAllowRules)
        ? { $case: "addAllowRules", addAllowRules: AddAllowRules.fromJSON(object.addAllowRules) }
        : isSet(object.add_allow_rules)
        ? { $case: "addAllowRules", addAllowRules: AddAllowRules.fromJSON(object.add_allow_rules) }
        : isSet(object.removeBinary)
        ? { $case: "removeBinary", removeBinary: RemoveNetworkBinary.fromJSON(object.removeBinary) }
        : isSet(object.remove_binary)
        ? { $case: "removeBinary", removeBinary: RemoveNetworkBinary.fromJSON(object.remove_binary) }
        : undefined,
    };
  },

  toJSON(message: PolicyMergeOperation): unknown {
    const obj: any = {};
    if (message.operation?.$case === "addRule") {
      obj.addRule = AddNetworkRule.toJSON(message.operation.addRule);
    } else if (message.operation?.$case === "removeEndpoint") {
      obj.removeEndpoint = RemoveNetworkEndpoint.toJSON(message.operation.removeEndpoint);
    } else if (message.operation?.$case === "removeRule") {
      obj.removeRule = RemoveNetworkRule.toJSON(message.operation.removeRule);
    } else if (message.operation?.$case === "addDenyRules") {
      obj.addDenyRules = AddDenyRules.toJSON(message.operation.addDenyRules);
    } else if (message.operation?.$case === "addAllowRules") {
      obj.addAllowRules = AddAllowRules.toJSON(message.operation.addAllowRules);
    } else if (message.operation?.$case === "removeBinary") {
      obj.removeBinary = RemoveNetworkBinary.toJSON(message.operation.removeBinary);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyMergeOperation>, I>>(base?: I): PolicyMergeOperation {
    return PolicyMergeOperation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyMergeOperation>, I>>(object: I): PolicyMergeOperation {
    const message = createBasePolicyMergeOperation();
    switch (object.operation?.$case) {
      case "addRule": {
        if (object.operation?.addRule !== undefined && object.operation?.addRule !== null) {
          message.operation = { $case: "addRule", addRule: AddNetworkRule.fromPartial(object.operation.addRule) };
        }
        break;
      }
      case "removeEndpoint": {
        if (object.operation?.removeEndpoint !== undefined && object.operation?.removeEndpoint !== null) {
          message.operation = {
            $case: "removeEndpoint",
            removeEndpoint: RemoveNetworkEndpoint.fromPartial(object.operation.removeEndpoint),
          };
        }
        break;
      }
      case "removeRule": {
        if (object.operation?.removeRule !== undefined && object.operation?.removeRule !== null) {
          message.operation = {
            $case: "removeRule",
            removeRule: RemoveNetworkRule.fromPartial(object.operation.removeRule),
          };
        }
        break;
      }
      case "addDenyRules": {
        if (object.operation?.addDenyRules !== undefined && object.operation?.addDenyRules !== null) {
          message.operation = {
            $case: "addDenyRules",
            addDenyRules: AddDenyRules.fromPartial(object.operation.addDenyRules),
          };
        }
        break;
      }
      case "addAllowRules": {
        if (object.operation?.addAllowRules !== undefined && object.operation?.addAllowRules !== null) {
          message.operation = {
            $case: "addAllowRules",
            addAllowRules: AddAllowRules.fromPartial(object.operation.addAllowRules),
          };
        }
        break;
      }
      case "removeBinary": {
        if (object.operation?.removeBinary !== undefined && object.operation?.removeBinary !== null) {
          message.operation = {
            $case: "removeBinary",
            removeBinary: RemoveNetworkBinary.fromPartial(object.operation.removeBinary),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseAddNetworkRule(): AddNetworkRule {
  return { ruleName: "", rule: undefined };
}

export const AddNetworkRule: MessageFns<AddNetworkRule> = {
  encode(message: AddNetworkRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ruleName !== "") {
      writer.uint32(10).string(message.ruleName);
    }
    if (message.rule !== undefined) {
      NetworkPolicyRule.encode(message.rule, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddNetworkRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddNetworkRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rule = NetworkPolicyRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddNetworkRule {
    return {
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
      rule: isSet(object.rule) ? NetworkPolicyRule.fromJSON(object.rule) : undefined,
    };
  },

  toJSON(message: AddNetworkRule): unknown {
    const obj: any = {};
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    if (message.rule !== undefined) {
      obj.rule = NetworkPolicyRule.toJSON(message.rule);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddNetworkRule>, I>>(base?: I): AddNetworkRule {
    return AddNetworkRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddNetworkRule>, I>>(object: I): AddNetworkRule {
    const message = createBaseAddNetworkRule();
    message.ruleName = object.ruleName ?? "";
    message.rule = (object.rule !== undefined && object.rule !== null)
      ? NetworkPolicyRule.fromPartial(object.rule)
      : undefined;
    return message;
  },
};

function createBaseRemoveNetworkEndpoint(): RemoveNetworkEndpoint {
  return { ruleName: "", host: "", port: 0 };
}

export const RemoveNetworkEndpoint: MessageFns<RemoveNetworkEndpoint> = {
  encode(message: RemoveNetworkEndpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ruleName !== "") {
      writer.uint32(10).string(message.ruleName);
    }
    if (message.host !== "") {
      writer.uint32(18).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(24).uint32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveNetworkEndpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveNetworkEndpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveNetworkEndpoint {
    return {
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: RemoveNetworkEndpoint): unknown {
    const obj: any = {};
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveNetworkEndpoint>, I>>(base?: I): RemoveNetworkEndpoint {
    return RemoveNetworkEndpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveNetworkEndpoint>, I>>(object: I): RemoveNetworkEndpoint {
    const message = createBaseRemoveNetworkEndpoint();
    message.ruleName = object.ruleName ?? "";
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseRemoveNetworkRule(): RemoveNetworkRule {
  return { ruleName: "" };
}

export const RemoveNetworkRule: MessageFns<RemoveNetworkRule> = {
  encode(message: RemoveNetworkRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ruleName !== "") {
      writer.uint32(10).string(message.ruleName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveNetworkRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveNetworkRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveNetworkRule {
    return {
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
    };
  },

  toJSON(message: RemoveNetworkRule): unknown {
    const obj: any = {};
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveNetworkRule>, I>>(base?: I): RemoveNetworkRule {
    return RemoveNetworkRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveNetworkRule>, I>>(object: I): RemoveNetworkRule {
    const message = createBaseRemoveNetworkRule();
    message.ruleName = object.ruleName ?? "";
    return message;
  },
};

function createBaseAddDenyRules(): AddDenyRules {
  return { host: "", port: 0, denyRules: [] };
}

export const AddDenyRules: MessageFns<AddDenyRules> = {
  encode(message: AddDenyRules, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(16).uint32(message.port);
    }
    for (const v of message.denyRules) {
      L7DenyRule.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddDenyRules {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddDenyRules();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.denyRules.push(L7DenyRule.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddDenyRules {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      denyRules: globalThis.Array.isArray(object?.denyRules)
        ? object.denyRules.map((e: any) => L7DenyRule.fromJSON(e))
        : globalThis.Array.isArray(object?.deny_rules)
        ? object.deny_rules.map((e: any) => L7DenyRule.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AddDenyRules): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.denyRules?.length) {
      obj.denyRules = message.denyRules.map((e) => L7DenyRule.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddDenyRules>, I>>(base?: I): AddDenyRules {
    return AddDenyRules.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddDenyRules>, I>>(object: I): AddDenyRules {
    const message = createBaseAddDenyRules();
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.denyRules = object.denyRules?.map((e) => L7DenyRule.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAddAllowRules(): AddAllowRules {
  return { host: "", port: 0, rules: [] };
}

export const AddAllowRules: MessageFns<AddAllowRules> = {
  encode(message: AddAllowRules, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(16).uint32(message.port);
    }
    for (const v of message.rules) {
      L7Rule.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddAllowRules {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddAllowRules();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rules.push(L7Rule.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddAllowRules {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => L7Rule.fromJSON(e)) : [],
    };
  },

  toJSON(message: AddAllowRules): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => L7Rule.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddAllowRules>, I>>(base?: I): AddAllowRules {
    return AddAllowRules.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddAllowRules>, I>>(object: I): AddAllowRules {
    const message = createBaseAddAllowRules();
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.rules = object.rules?.map((e) => L7Rule.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRemoveNetworkBinary(): RemoveNetworkBinary {
  return { ruleName: "", binaryPath: "" };
}

export const RemoveNetworkBinary: MessageFns<RemoveNetworkBinary> = {
  encode(message: RemoveNetworkBinary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ruleName !== "") {
      writer.uint32(10).string(message.ruleName);
    }
    if (message.binaryPath !== "") {
      writer.uint32(18).string(message.binaryPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveNetworkBinary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveNetworkBinary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.binaryPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveNetworkBinary {
    return {
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
      binaryPath: isSet(object.binaryPath)
        ? globalThis.String(object.binaryPath)
        : isSet(object.binary_path)
        ? globalThis.String(object.binary_path)
        : "",
    };
  },

  toJSON(message: RemoveNetworkBinary): unknown {
    const obj: any = {};
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    if (message.binaryPath !== "") {
      obj.binaryPath = message.binaryPath;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveNetworkBinary>, I>>(base?: I): RemoveNetworkBinary {
    return RemoveNetworkBinary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveNetworkBinary>, I>>(object: I): RemoveNetworkBinary {
    const message = createBaseRemoveNetworkBinary();
    message.ruleName = object.ruleName ?? "";
    message.binaryPath = object.binaryPath ?? "";
    return message;
  },
};

function createBaseUpdateConfigResponse(): UpdateConfigResponse {
  return { version: 0, policyHash: "", settingsRevision: 0, deleted: false };
}

export const UpdateConfigResponse: MessageFns<UpdateConfigResponse> = {
  encode(message: UpdateConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.policyHash !== "") {
      writer.uint32(18).string(message.policyHash);
    }
    if (message.settingsRevision !== 0) {
      writer.uint32(24).uint64(message.settingsRevision);
    }
    if (message.deleted !== false) {
      writer.uint32(32).bool(message.deleted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.settingsRevision = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateConfigResponse {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
      settingsRevision: isSet(object.settingsRevision)
        ? globalThis.Number(object.settingsRevision)
        : isSet(object.settings_revision)
        ? globalThis.Number(object.settings_revision)
        : 0,
      deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false,
    };
  },

  toJSON(message: UpdateConfigResponse): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    if (message.settingsRevision !== 0) {
      obj.settingsRevision = Math.round(message.settingsRevision);
    }
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateConfigResponse>, I>>(base?: I): UpdateConfigResponse {
    return UpdateConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateConfigResponse>, I>>(object: I): UpdateConfigResponse {
    const message = createBaseUpdateConfigResponse();
    message.version = object.version ?? 0;
    message.policyHash = object.policyHash ?? "";
    message.settingsRevision = object.settingsRevision ?? 0;
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseGetSandboxPolicyStatusRequest(): GetSandboxPolicyStatusRequest {
  return { name: "", version: 0, global: false };
}

export const GetSandboxPolicyStatusRequest: MessageFns<GetSandboxPolicyStatusRequest> = {
  encode(message: GetSandboxPolicyStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.version !== 0) {
      writer.uint32(16).uint32(message.version);
    }
    if (message.global !== false) {
      writer.uint32(24).bool(message.global);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxPolicyStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxPolicyStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.global = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxPolicyStatusRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      global: isSet(object.global) ? globalThis.Boolean(object.global) : false,
    };
  },

  toJSON(message: GetSandboxPolicyStatusRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.global !== false) {
      obj.global = message.global;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxPolicyStatusRequest>, I>>(base?: I): GetSandboxPolicyStatusRequest {
    return GetSandboxPolicyStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxPolicyStatusRequest>, I>>(
    object: I,
  ): GetSandboxPolicyStatusRequest {
    const message = createBaseGetSandboxPolicyStatusRequest();
    message.name = object.name ?? "";
    message.version = object.version ?? 0;
    message.global = object.global ?? false;
    return message;
  },
};

function createBaseGetSandboxPolicyStatusResponse(): GetSandboxPolicyStatusResponse {
  return { revision: undefined, activeVersion: 0 };
}

export const GetSandboxPolicyStatusResponse: MessageFns<GetSandboxPolicyStatusResponse> = {
  encode(message: GetSandboxPolicyStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revision !== undefined) {
      SandboxPolicyRevision.encode(message.revision, writer.uint32(10).fork()).join();
    }
    if (message.activeVersion !== 0) {
      writer.uint32(16).uint32(message.activeVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxPolicyStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxPolicyStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.revision = SandboxPolicyRevision.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.activeVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxPolicyStatusResponse {
    return {
      revision: isSet(object.revision) ? SandboxPolicyRevision.fromJSON(object.revision) : undefined,
      activeVersion: isSet(object.activeVersion)
        ? globalThis.Number(object.activeVersion)
        : isSet(object.active_version)
        ? globalThis.Number(object.active_version)
        : 0,
    };
  },

  toJSON(message: GetSandboxPolicyStatusResponse): unknown {
    const obj: any = {};
    if (message.revision !== undefined) {
      obj.revision = SandboxPolicyRevision.toJSON(message.revision);
    }
    if (message.activeVersion !== 0) {
      obj.activeVersion = Math.round(message.activeVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxPolicyStatusResponse>, I>>(base?: I): GetSandboxPolicyStatusResponse {
    return GetSandboxPolicyStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxPolicyStatusResponse>, I>>(
    object: I,
  ): GetSandboxPolicyStatusResponse {
    const message = createBaseGetSandboxPolicyStatusResponse();
    message.revision = (object.revision !== undefined && object.revision !== null)
      ? SandboxPolicyRevision.fromPartial(object.revision)
      : undefined;
    message.activeVersion = object.activeVersion ?? 0;
    return message;
  },
};

function createBaseListSandboxPoliciesRequest(): ListSandboxPoliciesRequest {
  return { name: "", limit: 0, offset: 0, global: false };
}

export const ListSandboxPoliciesRequest: MessageFns<ListSandboxPoliciesRequest> = {
  encode(message: ListSandboxPoliciesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(24).uint32(message.offset);
    }
    if (message.global !== false) {
      writer.uint32(32).bool(message.global);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSandboxPoliciesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSandboxPoliciesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.global = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSandboxPoliciesRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      global: isSet(object.global) ? globalThis.Boolean(object.global) : false,
    };
  },

  toJSON(message: ListSandboxPoliciesRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.global !== false) {
      obj.global = message.global;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSandboxPoliciesRequest>, I>>(base?: I): ListSandboxPoliciesRequest {
    return ListSandboxPoliciesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSandboxPoliciesRequest>, I>>(object: I): ListSandboxPoliciesRequest {
    const message = createBaseListSandboxPoliciesRequest();
    message.name = object.name ?? "";
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.global = object.global ?? false;
    return message;
  },
};

function createBaseListSandboxPoliciesResponse(): ListSandboxPoliciesResponse {
  return { revisions: [] };
}

export const ListSandboxPoliciesResponse: MessageFns<ListSandboxPoliciesResponse> = {
  encode(message: ListSandboxPoliciesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.revisions) {
      SandboxPolicyRevision.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSandboxPoliciesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSandboxPoliciesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.revisions.push(SandboxPolicyRevision.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSandboxPoliciesResponse {
    return {
      revisions: globalThis.Array.isArray(object?.revisions)
        ? object.revisions.map((e: any) => SandboxPolicyRevision.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListSandboxPoliciesResponse): unknown {
    const obj: any = {};
    if (message.revisions?.length) {
      obj.revisions = message.revisions.map((e) => SandboxPolicyRevision.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSandboxPoliciesResponse>, I>>(base?: I): ListSandboxPoliciesResponse {
    return ListSandboxPoliciesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSandboxPoliciesResponse>, I>>(object: I): ListSandboxPoliciesResponse {
    const message = createBaseListSandboxPoliciesResponse();
    message.revisions = object.revisions?.map((e) => SandboxPolicyRevision.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReportPolicyStatusRequest(): ReportPolicyStatusRequest {
  return { sandboxId: "", version: 0, status: 0, loadError: "" };
}

export const ReportPolicyStatusRequest: MessageFns<ReportPolicyStatusRequest> = {
  encode(message: ReportPolicyStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.version !== 0) {
      writer.uint32(16).uint32(message.version);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.loadError !== "") {
      writer.uint32(34).string(message.loadError);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportPolicyStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportPolicyStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.loadError = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportPolicyStatusRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      status: isSet(object.status) ? policyStatusFromJSON(object.status) : 0,
      loadError: isSet(object.loadError)
        ? globalThis.String(object.loadError)
        : isSet(object.load_error)
        ? globalThis.String(object.load_error)
        : "",
    };
  },

  toJSON(message: ReportPolicyStatusRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.status !== 0) {
      obj.status = policyStatusToJSON(message.status);
    }
    if (message.loadError !== "") {
      obj.loadError = message.loadError;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportPolicyStatusRequest>, I>>(base?: I): ReportPolicyStatusRequest {
    return ReportPolicyStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportPolicyStatusRequest>, I>>(object: I): ReportPolicyStatusRequest {
    const message = createBaseReportPolicyStatusRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.version = object.version ?? 0;
    message.status = object.status ?? 0;
    message.loadError = object.loadError ?? "";
    return message;
  },
};

function createBaseReportPolicyStatusResponse(): ReportPolicyStatusResponse {
  return {};
}

export const ReportPolicyStatusResponse: MessageFns<ReportPolicyStatusResponse> = {
  encode(_: ReportPolicyStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportPolicyStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportPolicyStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ReportPolicyStatusResponse {
    return {};
  },

  toJSON(_: ReportPolicyStatusResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportPolicyStatusResponse>, I>>(base?: I): ReportPolicyStatusResponse {
    return ReportPolicyStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportPolicyStatusResponse>, I>>(_: I): ReportPolicyStatusResponse {
    const message = createBaseReportPolicyStatusResponse();
    return message;
  },
};

function createBaseSandboxPolicyRevision(): SandboxPolicyRevision {
  return { version: 0, policyHash: "", status: 0, loadError: "", createdAtMs: 0, loadedAtMs: 0, policy: undefined };
}

export const SandboxPolicyRevision: MessageFns<SandboxPolicyRevision> = {
  encode(message: SandboxPolicyRevision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.policyHash !== "") {
      writer.uint32(18).string(message.policyHash);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.loadError !== "") {
      writer.uint32(34).string(message.loadError);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(40).int64(message.createdAtMs);
    }
    if (message.loadedAtMs !== 0) {
      writer.uint32(48).int64(message.loadedAtMs);
    }
    if (message.policy !== undefined) {
      SandboxPolicy.encode(message.policy, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxPolicyRevision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxPolicyRevision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.loadError = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.loadedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.policy = SandboxPolicy.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxPolicyRevision {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
      status: isSet(object.status) ? policyStatusFromJSON(object.status) : 0,
      loadError: isSet(object.loadError)
        ? globalThis.String(object.loadError)
        : isSet(object.load_error)
        ? globalThis.String(object.load_error)
        : "",
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      loadedAtMs: isSet(object.loadedAtMs)
        ? globalThis.Number(object.loadedAtMs)
        : isSet(object.loaded_at_ms)
        ? globalThis.Number(object.loaded_at_ms)
        : 0,
      policy: isSet(object.policy) ? SandboxPolicy.fromJSON(object.policy) : undefined,
    };
  },

  toJSON(message: SandboxPolicyRevision): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    if (message.status !== 0) {
      obj.status = policyStatusToJSON(message.status);
    }
    if (message.loadError !== "") {
      obj.loadError = message.loadError;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.loadedAtMs !== 0) {
      obj.loadedAtMs = Math.round(message.loadedAtMs);
    }
    if (message.policy !== undefined) {
      obj.policy = SandboxPolicy.toJSON(message.policy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxPolicyRevision>, I>>(base?: I): SandboxPolicyRevision {
    return SandboxPolicyRevision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxPolicyRevision>, I>>(object: I): SandboxPolicyRevision {
    const message = createBaseSandboxPolicyRevision();
    message.version = object.version ?? 0;
    message.policyHash = object.policyHash ?? "";
    message.status = object.status ?? 0;
    message.loadError = object.loadError ?? "";
    message.createdAtMs = object.createdAtMs ?? 0;
    message.loadedAtMs = object.loadedAtMs ?? 0;
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? SandboxPolicy.fromPartial(object.policy)
      : undefined;
    return message;
  },
};

function createBaseGetSandboxLogsRequest(): GetSandboxLogsRequest {
  return { sandboxId: "", lines: 0, sinceMs: 0, sources: [], minLevel: "" };
}

export const GetSandboxLogsRequest: MessageFns<GetSandboxLogsRequest> = {
  encode(message: GetSandboxLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.lines !== 0) {
      writer.uint32(16).uint32(message.lines);
    }
    if (message.sinceMs !== 0) {
      writer.uint32(24).int64(message.sinceMs);
    }
    for (const v of message.sources) {
      writer.uint32(34).string(v!);
    }
    if (message.minLevel !== "") {
      writer.uint32(42).string(message.minLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lines = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sinceMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sources.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.minLevel = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxLogsRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      lines: isSet(object.lines) ? globalThis.Number(object.lines) : 0,
      sinceMs: isSet(object.sinceMs)
        ? globalThis.Number(object.sinceMs)
        : isSet(object.since_ms)
        ? globalThis.Number(object.since_ms)
        : 0,
      sources: globalThis.Array.isArray(object?.sources) ? object.sources.map((e: any) => globalThis.String(e)) : [],
      minLevel: isSet(object.minLevel)
        ? globalThis.String(object.minLevel)
        : isSet(object.min_level)
        ? globalThis.String(object.min_level)
        : "",
    };
  },

  toJSON(message: GetSandboxLogsRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.lines !== 0) {
      obj.lines = Math.round(message.lines);
    }
    if (message.sinceMs !== 0) {
      obj.sinceMs = Math.round(message.sinceMs);
    }
    if (message.sources?.length) {
      obj.sources = message.sources;
    }
    if (message.minLevel !== "") {
      obj.minLevel = message.minLevel;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxLogsRequest>, I>>(base?: I): GetSandboxLogsRequest {
    return GetSandboxLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxLogsRequest>, I>>(object: I): GetSandboxLogsRequest {
    const message = createBaseGetSandboxLogsRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.lines = object.lines ?? 0;
    message.sinceMs = object.sinceMs ?? 0;
    message.sources = object.sources?.map((e) => e) || [];
    message.minLevel = object.minLevel ?? "";
    return message;
  },
};

function createBasePushSandboxLogsRequest(): PushSandboxLogsRequest {
  return { sandboxId: "", logs: [] };
}

export const PushSandboxLogsRequest: MessageFns<PushSandboxLogsRequest> = {
  encode(message: PushSandboxLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    for (const v of message.logs) {
      SandboxLogLine.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushSandboxLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushSandboxLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logs.push(SandboxLogLine.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushSandboxLogsRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      logs: globalThis.Array.isArray(object?.logs) ? object.logs.map((e: any) => SandboxLogLine.fromJSON(e)) : [],
    };
  },

  toJSON(message: PushSandboxLogsRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => SandboxLogLine.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushSandboxLogsRequest>, I>>(base?: I): PushSandboxLogsRequest {
    return PushSandboxLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushSandboxLogsRequest>, I>>(object: I): PushSandboxLogsRequest {
    const message = createBasePushSandboxLogsRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.logs = object.logs?.map((e) => SandboxLogLine.fromPartial(e)) || [];
    return message;
  },
};

function createBasePushSandboxLogsResponse(): PushSandboxLogsResponse {
  return {};
}

export const PushSandboxLogsResponse: MessageFns<PushSandboxLogsResponse> = {
  encode(_: PushSandboxLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushSandboxLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushSandboxLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PushSandboxLogsResponse {
    return {};
  },

  toJSON(_: PushSandboxLogsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PushSandboxLogsResponse>, I>>(base?: I): PushSandboxLogsResponse {
    return PushSandboxLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushSandboxLogsResponse>, I>>(_: I): PushSandboxLogsResponse {
    const message = createBasePushSandboxLogsResponse();
    return message;
  },
};

function createBaseGetSandboxLogsResponse(): GetSandboxLogsResponse {
  return { logs: [], bufferTotal: 0 };
}

export const GetSandboxLogsResponse: MessageFns<GetSandboxLogsResponse> = {
  encode(message: GetSandboxLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.logs) {
      SandboxLogLine.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.bufferTotal !== 0) {
      writer.uint32(16).uint32(message.bufferTotal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSandboxLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSandboxLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logs.push(SandboxLogLine.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bufferTotal = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSandboxLogsResponse {
    return {
      logs: globalThis.Array.isArray(object?.logs) ? object.logs.map((e: any) => SandboxLogLine.fromJSON(e)) : [],
      bufferTotal: isSet(object.bufferTotal)
        ? globalThis.Number(object.bufferTotal)
        : isSet(object.buffer_total)
        ? globalThis.Number(object.buffer_total)
        : 0,
    };
  },

  toJSON(message: GetSandboxLogsResponse): unknown {
    const obj: any = {};
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => SandboxLogLine.toJSON(e));
    }
    if (message.bufferTotal !== 0) {
      obj.bufferTotal = Math.round(message.bufferTotal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSandboxLogsResponse>, I>>(base?: I): GetSandboxLogsResponse {
    return GetSandboxLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSandboxLogsResponse>, I>>(object: I): GetSandboxLogsResponse {
    const message = createBaseGetSandboxLogsResponse();
    message.logs = object.logs?.map((e) => SandboxLogLine.fromPartial(e)) || [];
    message.bufferTotal = object.bufferTotal ?? 0;
    return message;
  },
};

function createBaseSupervisorMessage(): SupervisorMessage {
  return { payload: undefined };
}

export const SupervisorMessage: MessageFns<SupervisorMessage> = {
  encode(message: SupervisorMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "hello":
        SupervisorHello.encode(message.payload.hello, writer.uint32(10).fork()).join();
        break;
      case "heartbeat":
        SupervisorHeartbeat.encode(message.payload.heartbeat, writer.uint32(18).fork()).join();
        break;
      case "relayOpenResult":
        RelayOpenResult.encode(message.payload.relayOpenResult, writer.uint32(26).fork()).join();
        break;
      case "relayClose":
        RelayClose.encode(message.payload.relayClose, writer.uint32(34).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SupervisorMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSupervisorMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "hello", hello: SupervisorHello.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "heartbeat", heartbeat: SupervisorHeartbeat.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = {
            $case: "relayOpenResult",
            relayOpenResult: RelayOpenResult.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = { $case: "relayClose", relayClose: RelayClose.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SupervisorMessage {
    return {
      payload: isSet(object.hello)
        ? { $case: "hello", hello: SupervisorHello.fromJSON(object.hello) }
        : isSet(object.heartbeat)
        ? { $case: "heartbeat", heartbeat: SupervisorHeartbeat.fromJSON(object.heartbeat) }
        : isSet(object.relayOpenResult)
        ? { $case: "relayOpenResult", relayOpenResult: RelayOpenResult.fromJSON(object.relayOpenResult) }
        : isSet(object.relay_open_result)
        ? { $case: "relayOpenResult", relayOpenResult: RelayOpenResult.fromJSON(object.relay_open_result) }
        : isSet(object.relayClose)
        ? { $case: "relayClose", relayClose: RelayClose.fromJSON(object.relayClose) }
        : isSet(object.relay_close)
        ? { $case: "relayClose", relayClose: RelayClose.fromJSON(object.relay_close) }
        : undefined,
    };
  },

  toJSON(message: SupervisorMessage): unknown {
    const obj: any = {};
    if (message.payload?.$case === "hello") {
      obj.hello = SupervisorHello.toJSON(message.payload.hello);
    } else if (message.payload?.$case === "heartbeat") {
      obj.heartbeat = SupervisorHeartbeat.toJSON(message.payload.heartbeat);
    } else if (message.payload?.$case === "relayOpenResult") {
      obj.relayOpenResult = RelayOpenResult.toJSON(message.payload.relayOpenResult);
    } else if (message.payload?.$case === "relayClose") {
      obj.relayClose = RelayClose.toJSON(message.payload.relayClose);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SupervisorMessage>, I>>(base?: I): SupervisorMessage {
    return SupervisorMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SupervisorMessage>, I>>(object: I): SupervisorMessage {
    const message = createBaseSupervisorMessage();
    switch (object.payload?.$case) {
      case "hello": {
        if (object.payload?.hello !== undefined && object.payload?.hello !== null) {
          message.payload = { $case: "hello", hello: SupervisorHello.fromPartial(object.payload.hello) };
        }
        break;
      }
      case "heartbeat": {
        if (object.payload?.heartbeat !== undefined && object.payload?.heartbeat !== null) {
          message.payload = {
            $case: "heartbeat",
            heartbeat: SupervisorHeartbeat.fromPartial(object.payload.heartbeat),
          };
        }
        break;
      }
      case "relayOpenResult": {
        if (object.payload?.relayOpenResult !== undefined && object.payload?.relayOpenResult !== null) {
          message.payload = {
            $case: "relayOpenResult",
            relayOpenResult: RelayOpenResult.fromPartial(object.payload.relayOpenResult),
          };
        }
        break;
      }
      case "relayClose": {
        if (object.payload?.relayClose !== undefined && object.payload?.relayClose !== null) {
          message.payload = { $case: "relayClose", relayClose: RelayClose.fromPartial(object.payload.relayClose) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseGatewayMessage(): GatewayMessage {
  return { payload: undefined };
}

export const GatewayMessage: MessageFns<GatewayMessage> = {
  encode(message: GatewayMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "sessionAccepted":
        SessionAccepted.encode(message.payload.sessionAccepted, writer.uint32(10).fork()).join();
        break;
      case "sessionRejected":
        SessionRejected.encode(message.payload.sessionRejected, writer.uint32(18).fork()).join();
        break;
      case "heartbeat":
        GatewayHeartbeat.encode(message.payload.heartbeat, writer.uint32(26).fork()).join();
        break;
      case "relayOpen":
        RelayOpen.encode(message.payload.relayOpen, writer.uint32(34).fork()).join();
        break;
      case "relayClose":
        RelayClose.encode(message.payload.relayClose, writer.uint32(42).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GatewayMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGatewayMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = {
            $case: "sessionAccepted",
            sessionAccepted: SessionAccepted.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = {
            $case: "sessionRejected",
            sessionRejected: SessionRejected.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "heartbeat", heartbeat: GatewayHeartbeat.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = { $case: "relayOpen", relayOpen: RelayOpen.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = { $case: "relayClose", relayClose: RelayClose.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GatewayMessage {
    return {
      payload: isSet(object.sessionAccepted)
        ? { $case: "sessionAccepted", sessionAccepted: SessionAccepted.fromJSON(object.sessionAccepted) }
        : isSet(object.session_accepted)
        ? { $case: "sessionAccepted", sessionAccepted: SessionAccepted.fromJSON(object.session_accepted) }
        : isSet(object.sessionRejected)
        ? { $case: "sessionRejected", sessionRejected: SessionRejected.fromJSON(object.sessionRejected) }
        : isSet(object.session_rejected)
        ? { $case: "sessionRejected", sessionRejected: SessionRejected.fromJSON(object.session_rejected) }
        : isSet(object.heartbeat)
        ? { $case: "heartbeat", heartbeat: GatewayHeartbeat.fromJSON(object.heartbeat) }
        : isSet(object.relayOpen)
        ? { $case: "relayOpen", relayOpen: RelayOpen.fromJSON(object.relayOpen) }
        : isSet(object.relay_open)
        ? { $case: "relayOpen", relayOpen: RelayOpen.fromJSON(object.relay_open) }
        : isSet(object.relayClose)
        ? { $case: "relayClose", relayClose: RelayClose.fromJSON(object.relayClose) }
        : isSet(object.relay_close)
        ? { $case: "relayClose", relayClose: RelayClose.fromJSON(object.relay_close) }
        : undefined,
    };
  },

  toJSON(message: GatewayMessage): unknown {
    const obj: any = {};
    if (message.payload?.$case === "sessionAccepted") {
      obj.sessionAccepted = SessionAccepted.toJSON(message.payload.sessionAccepted);
    } else if (message.payload?.$case === "sessionRejected") {
      obj.sessionRejected = SessionRejected.toJSON(message.payload.sessionRejected);
    } else if (message.payload?.$case === "heartbeat") {
      obj.heartbeat = GatewayHeartbeat.toJSON(message.payload.heartbeat);
    } else if (message.payload?.$case === "relayOpen") {
      obj.relayOpen = RelayOpen.toJSON(message.payload.relayOpen);
    } else if (message.payload?.$case === "relayClose") {
      obj.relayClose = RelayClose.toJSON(message.payload.relayClose);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GatewayMessage>, I>>(base?: I): GatewayMessage {
    return GatewayMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GatewayMessage>, I>>(object: I): GatewayMessage {
    const message = createBaseGatewayMessage();
    switch (object.payload?.$case) {
      case "sessionAccepted": {
        if (object.payload?.sessionAccepted !== undefined && object.payload?.sessionAccepted !== null) {
          message.payload = {
            $case: "sessionAccepted",
            sessionAccepted: SessionAccepted.fromPartial(object.payload.sessionAccepted),
          };
        }
        break;
      }
      case "sessionRejected": {
        if (object.payload?.sessionRejected !== undefined && object.payload?.sessionRejected !== null) {
          message.payload = {
            $case: "sessionRejected",
            sessionRejected: SessionRejected.fromPartial(object.payload.sessionRejected),
          };
        }
        break;
      }
      case "heartbeat": {
        if (object.payload?.heartbeat !== undefined && object.payload?.heartbeat !== null) {
          message.payload = { $case: "heartbeat", heartbeat: GatewayHeartbeat.fromPartial(object.payload.heartbeat) };
        }
        break;
      }
      case "relayOpen": {
        if (object.payload?.relayOpen !== undefined && object.payload?.relayOpen !== null) {
          message.payload = { $case: "relayOpen", relayOpen: RelayOpen.fromPartial(object.payload.relayOpen) };
        }
        break;
      }
      case "relayClose": {
        if (object.payload?.relayClose !== undefined && object.payload?.relayClose !== null) {
          message.payload = { $case: "relayClose", relayClose: RelayClose.fromPartial(object.payload.relayClose) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseSupervisorHello(): SupervisorHello {
  return { sandboxId: "", instanceId: "" };
}

export const SupervisorHello: MessageFns<SupervisorHello> = {
  encode(message: SupervisorHello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.instanceId !== "") {
      writer.uint32(18).string(message.instanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SupervisorHello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSupervisorHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SupervisorHello {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
    };
  },

  toJSON(message: SupervisorHello): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SupervisorHello>, I>>(base?: I): SupervisorHello {
    return SupervisorHello.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SupervisorHello>, I>>(object: I): SupervisorHello {
    const message = createBaseSupervisorHello();
    message.sandboxId = object.sandboxId ?? "";
    message.instanceId = object.instanceId ?? "";
    return message;
  },
};

function createBaseSessionAccepted(): SessionAccepted {
  return { sessionId: "", heartbeatIntervalSecs: 0 };
}

export const SessionAccepted: MessageFns<SessionAccepted> = {
  encode(message: SessionAccepted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.heartbeatIntervalSecs !== 0) {
      writer.uint32(16).uint32(message.heartbeatIntervalSecs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionAccepted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionAccepted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.heartbeatIntervalSecs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionAccepted {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      heartbeatIntervalSecs: isSet(object.heartbeatIntervalSecs)
        ? globalThis.Number(object.heartbeatIntervalSecs)
        : isSet(object.heartbeat_interval_secs)
        ? globalThis.Number(object.heartbeat_interval_secs)
        : 0,
    };
  },

  toJSON(message: SessionAccepted): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.heartbeatIntervalSecs !== 0) {
      obj.heartbeatIntervalSecs = Math.round(message.heartbeatIntervalSecs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionAccepted>, I>>(base?: I): SessionAccepted {
    return SessionAccepted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionAccepted>, I>>(object: I): SessionAccepted {
    const message = createBaseSessionAccepted();
    message.sessionId = object.sessionId ?? "";
    message.heartbeatIntervalSecs = object.heartbeatIntervalSecs ?? 0;
    return message;
  },
};

function createBaseSessionRejected(): SessionRejected {
  return { reason: "" };
}

export const SessionRejected: MessageFns<SessionRejected> = {
  encode(message: SessionRejected, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionRejected {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionRejected();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionRejected {
    return { reason: isSet(object.reason) ? globalThis.String(object.reason) : "" };
  },

  toJSON(message: SessionRejected): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionRejected>, I>>(base?: I): SessionRejected {
    return SessionRejected.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionRejected>, I>>(object: I): SessionRejected {
    const message = createBaseSessionRejected();
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseSupervisorHeartbeat(): SupervisorHeartbeat {
  return {};
}

export const SupervisorHeartbeat: MessageFns<SupervisorHeartbeat> = {
  encode(_: SupervisorHeartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SupervisorHeartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSupervisorHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SupervisorHeartbeat {
    return {};
  },

  toJSON(_: SupervisorHeartbeat): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<SupervisorHeartbeat>, I>>(base?: I): SupervisorHeartbeat {
    return SupervisorHeartbeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SupervisorHeartbeat>, I>>(_: I): SupervisorHeartbeat {
    const message = createBaseSupervisorHeartbeat();
    return message;
  },
};

function createBaseGatewayHeartbeat(): GatewayHeartbeat {
  return {};
}

export const GatewayHeartbeat: MessageFns<GatewayHeartbeat> = {
  encode(_: GatewayHeartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GatewayHeartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGatewayHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GatewayHeartbeat {
    return {};
  },

  toJSON(_: GatewayHeartbeat): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GatewayHeartbeat>, I>>(base?: I): GatewayHeartbeat {
    return GatewayHeartbeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GatewayHeartbeat>, I>>(_: I): GatewayHeartbeat {
    const message = createBaseGatewayHeartbeat();
    return message;
  },
};

function createBaseRelayOpen(): RelayOpen {
  return { channelId: "", target: undefined, serviceId: "" };
}

export const RelayOpen: MessageFns<RelayOpen> = {
  encode(message: RelayOpen, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    switch (message.target?.$case) {
      case "ssh":
        SshRelayTarget.encode(message.target.ssh, writer.uint32(18).fork()).join();
        break;
      case "tcp":
        TcpRelayTarget.encode(message.target.tcp, writer.uint32(26).fork()).join();
        break;
    }
    if (message.serviceId !== "") {
      writer.uint32(42).string(message.serviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayOpen {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelayOpen();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = { $case: "ssh", ssh: SshRelayTarget.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.target = { $case: "tcp", tcp: TcpRelayTarget.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RelayOpen {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      target: isSet(object.ssh)
        ? { $case: "ssh", ssh: SshRelayTarget.fromJSON(object.ssh) }
        : isSet(object.tcp)
        ? { $case: "tcp", tcp: TcpRelayTarget.fromJSON(object.tcp) }
        : undefined,
      serviceId: isSet(object.serviceId)
        ? globalThis.String(object.serviceId)
        : isSet(object.service_id)
        ? globalThis.String(object.service_id)
        : "",
    };
  },

  toJSON(message: RelayOpen): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.target?.$case === "ssh") {
      obj.ssh = SshRelayTarget.toJSON(message.target.ssh);
    } else if (message.target?.$case === "tcp") {
      obj.tcp = TcpRelayTarget.toJSON(message.target.tcp);
    }
    if (message.serviceId !== "") {
      obj.serviceId = message.serviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RelayOpen>, I>>(base?: I): RelayOpen {
    return RelayOpen.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RelayOpen>, I>>(object: I): RelayOpen {
    const message = createBaseRelayOpen();
    message.channelId = object.channelId ?? "";
    switch (object.target?.$case) {
      case "ssh": {
        if (object.target?.ssh !== undefined && object.target?.ssh !== null) {
          message.target = { $case: "ssh", ssh: SshRelayTarget.fromPartial(object.target.ssh) };
        }
        break;
      }
      case "tcp": {
        if (object.target?.tcp !== undefined && object.target?.tcp !== null) {
          message.target = { $case: "tcp", tcp: TcpRelayTarget.fromPartial(object.target.tcp) };
        }
        break;
      }
    }
    message.serviceId = object.serviceId ?? "";
    return message;
  },
};

function createBaseSshRelayTarget(): SshRelayTarget {
  return {};
}

export const SshRelayTarget: MessageFns<SshRelayTarget> = {
  encode(_: SshRelayTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SshRelayTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSshRelayTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SshRelayTarget {
    return {};
  },

  toJSON(_: SshRelayTarget): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<SshRelayTarget>, I>>(base?: I): SshRelayTarget {
    return SshRelayTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SshRelayTarget>, I>>(_: I): SshRelayTarget {
    const message = createBaseSshRelayTarget();
    return message;
  },
};

function createBaseTcpRelayTarget(): TcpRelayTarget {
  return { host: "", port: 0 };
}

export const TcpRelayTarget: MessageFns<TcpRelayTarget> = {
  encode(message: TcpRelayTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(16).uint32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TcpRelayTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTcpRelayTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TcpRelayTarget {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: TcpRelayTarget): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TcpRelayTarget>, I>>(base?: I): TcpRelayTarget {
    return TcpRelayTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TcpRelayTarget>, I>>(object: I): TcpRelayTarget {
    const message = createBaseTcpRelayTarget();
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseRelayInit(): RelayInit {
  return { channelId: "" };
}

export const RelayInit: MessageFns<RelayInit> = {
  encode(message: RelayInit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayInit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelayInit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RelayInit {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
    };
  },

  toJSON(message: RelayInit): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RelayInit>, I>>(base?: I): RelayInit {
    return RelayInit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RelayInit>, I>>(object: I): RelayInit {
    const message = createBaseRelayInit();
    message.channelId = object.channelId ?? "";
    return message;
  },
};

function createBaseRelayFrame(): RelayFrame {
  return { payload: undefined };
}

export const RelayFrame: MessageFns<RelayFrame> = {
  encode(message: RelayFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "init":
        RelayInit.encode(message.payload.init, writer.uint32(10).fork()).join();
        break;
      case "data":
        writer.uint32(18).bytes(message.payload.data);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelayFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "init", init: RelayInit.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "data", data: Buffer.from(reader.bytes()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RelayFrame {
    return {
      payload: isSet(object.init)
        ? { $case: "init", init: RelayInit.fromJSON(object.init) }
        : isSet(object.data)
        ? { $case: "data", data: Buffer.from(bytesFromBase64(object.data)) }
        : undefined,
    };
  },

  toJSON(message: RelayFrame): unknown {
    const obj: any = {};
    if (message.payload?.$case === "init") {
      obj.init = RelayInit.toJSON(message.payload.init);
    } else if (message.payload?.$case === "data") {
      obj.data = base64FromBytes(message.payload.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RelayFrame>, I>>(base?: I): RelayFrame {
    return RelayFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RelayFrame>, I>>(object: I): RelayFrame {
    const message = createBaseRelayFrame();
    switch (object.payload?.$case) {
      case "init": {
        if (object.payload?.init !== undefined && object.payload?.init !== null) {
          message.payload = { $case: "init", init: RelayInit.fromPartial(object.payload.init) };
        }
        break;
      }
      case "data": {
        if (object.payload?.data !== undefined && object.payload?.data !== null) {
          message.payload = { $case: "data", data: object.payload.data };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseRelayOpenResult(): RelayOpenResult {
  return { channelId: "", success: false, error: "" };
}

export const RelayOpenResult: MessageFns<RelayOpenResult> = {
  encode(message: RelayOpenResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(26).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayOpenResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelayOpenResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RelayOpenResult {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: RelayOpenResult): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RelayOpenResult>, I>>(base?: I): RelayOpenResult {
    return RelayOpenResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RelayOpenResult>, I>>(object: I): RelayOpenResult {
    const message = createBaseRelayOpenResult();
    message.channelId = object.channelId ?? "";
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseRelayClose(): RelayClose {
  return { channelId: "", reason: "" };
}

export const RelayClose: MessageFns<RelayClose> = {
  encode(message: RelayClose, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayClose {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelayClose();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RelayClose {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: RelayClose): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RelayClose>, I>>(base?: I): RelayClose {
    return RelayClose.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RelayClose>, I>>(object: I): RelayClose {
    const message = createBaseRelayClose();
    message.channelId = object.channelId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseL7RequestSample(): L7RequestSample {
  return { method: "", path: "", decision: "", count: 0 };
}

export const L7RequestSample: MessageFns<L7RequestSample> = {
  encode(message: L7RequestSample, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.decision !== "") {
      writer.uint32(26).string(message.decision);
    }
    if (message.count !== 0) {
      writer.uint32(32).uint32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): L7RequestSample {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseL7RequestSample();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.decision = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): L7RequestSample {
    return {
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      decision: isSet(object.decision) ? globalThis.String(object.decision) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: L7RequestSample): unknown {
    const obj: any = {};
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.decision !== "") {
      obj.decision = message.decision;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<L7RequestSample>, I>>(base?: I): L7RequestSample {
    return L7RequestSample.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<L7RequestSample>, I>>(object: I): L7RequestSample {
    const message = createBaseL7RequestSample();
    message.method = object.method ?? "";
    message.path = object.path ?? "";
    message.decision = object.decision ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseDenialSummary(): DenialSummary {
  return {
    sandboxId: "",
    host: "",
    port: 0,
    binary: "",
    ancestors: [],
    denyReason: "",
    firstSeenMs: 0,
    lastSeenMs: 0,
    count: 0,
    suppressedCount: 0,
    totalCount: 0,
    sampleCmdlines: [],
    binarySha256: "",
    persistent: false,
    denialStage: "",
    l7RequestSamples: [],
    l7InspectionActive: false,
  };
}

export const DenialSummary: MessageFns<DenialSummary> = {
  encode(message: DenialSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.host !== "") {
      writer.uint32(18).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(24).uint32(message.port);
    }
    if (message.binary !== "") {
      writer.uint32(34).string(message.binary);
    }
    for (const v of message.ancestors) {
      writer.uint32(42).string(v!);
    }
    if (message.denyReason !== "") {
      writer.uint32(50).string(message.denyReason);
    }
    if (message.firstSeenMs !== 0) {
      writer.uint32(56).int64(message.firstSeenMs);
    }
    if (message.lastSeenMs !== 0) {
      writer.uint32(64).int64(message.lastSeenMs);
    }
    if (message.count !== 0) {
      writer.uint32(72).uint32(message.count);
    }
    if (message.suppressedCount !== 0) {
      writer.uint32(80).uint32(message.suppressedCount);
    }
    if (message.totalCount !== 0) {
      writer.uint32(88).uint32(message.totalCount);
    }
    for (const v of message.sampleCmdlines) {
      writer.uint32(98).string(v!);
    }
    if (message.binarySha256 !== "") {
      writer.uint32(106).string(message.binarySha256);
    }
    if (message.persistent !== false) {
      writer.uint32(112).bool(message.persistent);
    }
    if (message.denialStage !== "") {
      writer.uint32(122).string(message.denialStage);
    }
    for (const v of message.l7RequestSamples) {
      L7RequestSample.encode(v!, writer.uint32(130).fork()).join();
    }
    if (message.l7InspectionActive !== false) {
      writer.uint32(136).bool(message.l7InspectionActive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DenialSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDenialSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.binary = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ancestors.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.denyReason = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.firstSeenMs = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.lastSeenMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.count = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.suppressedCount = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.totalCount = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.sampleCmdlines.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.binarySha256 = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.persistent = reader.bool();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.denialStage = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.l7RequestSamples.push(L7RequestSample.decode(reader, reader.uint32()));
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.l7InspectionActive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DenialSummary {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      binary: isSet(object.binary) ? globalThis.String(object.binary) : "",
      ancestors: globalThis.Array.isArray(object?.ancestors)
        ? object.ancestors.map((e: any) => globalThis.String(e))
        : [],
      denyReason: isSet(object.denyReason)
        ? globalThis.String(object.denyReason)
        : isSet(object.deny_reason)
        ? globalThis.String(object.deny_reason)
        : "",
      firstSeenMs: isSet(object.firstSeenMs)
        ? globalThis.Number(object.firstSeenMs)
        : isSet(object.first_seen_ms)
        ? globalThis.Number(object.first_seen_ms)
        : 0,
      lastSeenMs: isSet(object.lastSeenMs)
        ? globalThis.Number(object.lastSeenMs)
        : isSet(object.last_seen_ms)
        ? globalThis.Number(object.last_seen_ms)
        : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      suppressedCount: isSet(object.suppressedCount)
        ? globalThis.Number(object.suppressedCount)
        : isSet(object.suppressed_count)
        ? globalThis.Number(object.suppressed_count)
        : 0,
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
      sampleCmdlines: globalThis.Array.isArray(object?.sampleCmdlines)
        ? object.sampleCmdlines.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.sample_cmdlines)
        ? object.sample_cmdlines.map((e: any) => globalThis.String(e))
        : [],
      binarySha256: isSet(object.binarySha256)
        ? globalThis.String(object.binarySha256)
        : isSet(object.binary_sha256)
        ? globalThis.String(object.binary_sha256)
        : "",
      persistent: isSet(object.persistent) ? globalThis.Boolean(object.persistent) : false,
      denialStage: isSet(object.denialStage)
        ? globalThis.String(object.denialStage)
        : isSet(object.denial_stage)
        ? globalThis.String(object.denial_stage)
        : "",
      l7RequestSamples: globalThis.Array.isArray(object?.l7RequestSamples)
        ? object.l7RequestSamples.map((e: any) => L7RequestSample.fromJSON(e))
        : globalThis.Array.isArray(object?.l7_request_samples)
        ? object.l7_request_samples.map((e: any) => L7RequestSample.fromJSON(e))
        : [],
      l7InspectionActive: isSet(object.l7InspectionActive)
        ? globalThis.Boolean(object.l7InspectionActive)
        : isSet(object.l7_inspection_active)
        ? globalThis.Boolean(object.l7_inspection_active)
        : false,
    };
  },

  toJSON(message: DenialSummary): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.binary !== "") {
      obj.binary = message.binary;
    }
    if (message.ancestors?.length) {
      obj.ancestors = message.ancestors;
    }
    if (message.denyReason !== "") {
      obj.denyReason = message.denyReason;
    }
    if (message.firstSeenMs !== 0) {
      obj.firstSeenMs = Math.round(message.firstSeenMs);
    }
    if (message.lastSeenMs !== 0) {
      obj.lastSeenMs = Math.round(message.lastSeenMs);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.suppressedCount !== 0) {
      obj.suppressedCount = Math.round(message.suppressedCount);
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    if (message.sampleCmdlines?.length) {
      obj.sampleCmdlines = message.sampleCmdlines;
    }
    if (message.binarySha256 !== "") {
      obj.binarySha256 = message.binarySha256;
    }
    if (message.persistent !== false) {
      obj.persistent = message.persistent;
    }
    if (message.denialStage !== "") {
      obj.denialStage = message.denialStage;
    }
    if (message.l7RequestSamples?.length) {
      obj.l7RequestSamples = message.l7RequestSamples.map((e) => L7RequestSample.toJSON(e));
    }
    if (message.l7InspectionActive !== false) {
      obj.l7InspectionActive = message.l7InspectionActive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DenialSummary>, I>>(base?: I): DenialSummary {
    return DenialSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DenialSummary>, I>>(object: I): DenialSummary {
    const message = createBaseDenialSummary();
    message.sandboxId = object.sandboxId ?? "";
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.binary = object.binary ?? "";
    message.ancestors = object.ancestors?.map((e) => e) || [];
    message.denyReason = object.denyReason ?? "";
    message.firstSeenMs = object.firstSeenMs ?? 0;
    message.lastSeenMs = object.lastSeenMs ?? 0;
    message.count = object.count ?? 0;
    message.suppressedCount = object.suppressedCount ?? 0;
    message.totalCount = object.totalCount ?? 0;
    message.sampleCmdlines = object.sampleCmdlines?.map((e) => e) || [];
    message.binarySha256 = object.binarySha256 ?? "";
    message.persistent = object.persistent ?? false;
    message.denialStage = object.denialStage ?? "";
    message.l7RequestSamples = object.l7RequestSamples?.map((e) => L7RequestSample.fromPartial(e)) || [];
    message.l7InspectionActive = object.l7InspectionActive ?? false;
    return message;
  },
};

function createBasePolicyChunk(): PolicyChunk {
  return {
    id: "",
    status: "",
    ruleName: "",
    proposedRule: undefined,
    rationale: "",
    securityNotes: "",
    confidence: 0,
    denialSummaryIds: [],
    createdAtMs: 0,
    decidedAtMs: 0,
    stage: "",
    supersedesChunkId: "",
    hitCount: 0,
    firstSeenMs: 0,
    lastSeenMs: 0,
    binary: "",
    validationResult: "",
    rejectionReason: "",
  };
}

export const PolicyChunk: MessageFns<PolicyChunk> = {
  encode(message: PolicyChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.ruleName !== "") {
      writer.uint32(26).string(message.ruleName);
    }
    if (message.proposedRule !== undefined) {
      NetworkPolicyRule.encode(message.proposedRule, writer.uint32(34).fork()).join();
    }
    if (message.rationale !== "") {
      writer.uint32(42).string(message.rationale);
    }
    if (message.securityNotes !== "") {
      writer.uint32(50).string(message.securityNotes);
    }
    if (message.confidence !== 0) {
      writer.uint32(61).float(message.confidence);
    }
    for (const v of message.denialSummaryIds) {
      writer.uint32(66).string(v!);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(72).int64(message.createdAtMs);
    }
    if (message.decidedAtMs !== 0) {
      writer.uint32(80).int64(message.decidedAtMs);
    }
    if (message.stage !== "") {
      writer.uint32(90).string(message.stage);
    }
    if (message.supersedesChunkId !== "") {
      writer.uint32(98).string(message.supersedesChunkId);
    }
    if (message.hitCount !== 0) {
      writer.uint32(104).int32(message.hitCount);
    }
    if (message.firstSeenMs !== 0) {
      writer.uint32(112).int64(message.firstSeenMs);
    }
    if (message.lastSeenMs !== 0) {
      writer.uint32(120).int64(message.lastSeenMs);
    }
    if (message.binary !== "") {
      writer.uint32(130).string(message.binary);
    }
    if (message.validationResult !== "") {
      writer.uint32(138).string(message.validationResult);
    }
    if (message.rejectionReason !== "") {
      writer.uint32(146).string(message.rejectionReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.proposedRule = NetworkPolicyRule.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.rationale = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.securityNotes = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.denialSummaryIds.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.decidedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.stage = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.supersedesChunkId = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.hitCount = reader.int32();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.firstSeenMs = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.lastSeenMs = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.binary = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.validationResult = reader.string();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.rejectionReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyChunk {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
      proposedRule: isSet(object.proposedRule)
        ? NetworkPolicyRule.fromJSON(object.proposedRule)
        : isSet(object.proposed_rule)
        ? NetworkPolicyRule.fromJSON(object.proposed_rule)
        : undefined,
      rationale: isSet(object.rationale) ? globalThis.String(object.rationale) : "",
      securityNotes: isSet(object.securityNotes)
        ? globalThis.String(object.securityNotes)
        : isSet(object.security_notes)
        ? globalThis.String(object.security_notes)
        : "",
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      denialSummaryIds: globalThis.Array.isArray(object?.denialSummaryIds)
        ? object.denialSummaryIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.denial_summary_ids)
        ? object.denial_summary_ids.map((e: any) => globalThis.String(e))
        : [],
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      decidedAtMs: isSet(object.decidedAtMs)
        ? globalThis.Number(object.decidedAtMs)
        : isSet(object.decided_at_ms)
        ? globalThis.Number(object.decided_at_ms)
        : 0,
      stage: isSet(object.stage) ? globalThis.String(object.stage) : "",
      supersedesChunkId: isSet(object.supersedesChunkId)
        ? globalThis.String(object.supersedesChunkId)
        : isSet(object.supersedes_chunk_id)
        ? globalThis.String(object.supersedes_chunk_id)
        : "",
      hitCount: isSet(object.hitCount)
        ? globalThis.Number(object.hitCount)
        : isSet(object.hit_count)
        ? globalThis.Number(object.hit_count)
        : 0,
      firstSeenMs: isSet(object.firstSeenMs)
        ? globalThis.Number(object.firstSeenMs)
        : isSet(object.first_seen_ms)
        ? globalThis.Number(object.first_seen_ms)
        : 0,
      lastSeenMs: isSet(object.lastSeenMs)
        ? globalThis.Number(object.lastSeenMs)
        : isSet(object.last_seen_ms)
        ? globalThis.Number(object.last_seen_ms)
        : 0,
      binary: isSet(object.binary) ? globalThis.String(object.binary) : "",
      validationResult: isSet(object.validationResult)
        ? globalThis.String(object.validationResult)
        : isSet(object.validation_result)
        ? globalThis.String(object.validation_result)
        : "",
      rejectionReason: isSet(object.rejectionReason)
        ? globalThis.String(object.rejectionReason)
        : isSet(object.rejection_reason)
        ? globalThis.String(object.rejection_reason)
        : "",
    };
  },

  toJSON(message: PolicyChunk): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    if (message.proposedRule !== undefined) {
      obj.proposedRule = NetworkPolicyRule.toJSON(message.proposedRule);
    }
    if (message.rationale !== "") {
      obj.rationale = message.rationale;
    }
    if (message.securityNotes !== "") {
      obj.securityNotes = message.securityNotes;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.denialSummaryIds?.length) {
      obj.denialSummaryIds = message.denialSummaryIds;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.decidedAtMs !== 0) {
      obj.decidedAtMs = Math.round(message.decidedAtMs);
    }
    if (message.stage !== "") {
      obj.stage = message.stage;
    }
    if (message.supersedesChunkId !== "") {
      obj.supersedesChunkId = message.supersedesChunkId;
    }
    if (message.hitCount !== 0) {
      obj.hitCount = Math.round(message.hitCount);
    }
    if (message.firstSeenMs !== 0) {
      obj.firstSeenMs = Math.round(message.firstSeenMs);
    }
    if (message.lastSeenMs !== 0) {
      obj.lastSeenMs = Math.round(message.lastSeenMs);
    }
    if (message.binary !== "") {
      obj.binary = message.binary;
    }
    if (message.validationResult !== "") {
      obj.validationResult = message.validationResult;
    }
    if (message.rejectionReason !== "") {
      obj.rejectionReason = message.rejectionReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyChunk>, I>>(base?: I): PolicyChunk {
    return PolicyChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyChunk>, I>>(object: I): PolicyChunk {
    const message = createBasePolicyChunk();
    message.id = object.id ?? "";
    message.status = object.status ?? "";
    message.ruleName = object.ruleName ?? "";
    message.proposedRule = (object.proposedRule !== undefined && object.proposedRule !== null)
      ? NetworkPolicyRule.fromPartial(object.proposedRule)
      : undefined;
    message.rationale = object.rationale ?? "";
    message.securityNotes = object.securityNotes ?? "";
    message.confidence = object.confidence ?? 0;
    message.denialSummaryIds = object.denialSummaryIds?.map((e) => e) || [];
    message.createdAtMs = object.createdAtMs ?? 0;
    message.decidedAtMs = object.decidedAtMs ?? 0;
    message.stage = object.stage ?? "";
    message.supersedesChunkId = object.supersedesChunkId ?? "";
    message.hitCount = object.hitCount ?? 0;
    message.firstSeenMs = object.firstSeenMs ?? 0;
    message.lastSeenMs = object.lastSeenMs ?? 0;
    message.binary = object.binary ?? "";
    message.validationResult = object.validationResult ?? "";
    message.rejectionReason = object.rejectionReason ?? "";
    return message;
  },
};

function createBaseDraftPolicyUpdate(): DraftPolicyUpdate {
  return { draftVersion: 0, newChunks: 0, totalPending: 0, summary: "" };
}

export const DraftPolicyUpdate: MessageFns<DraftPolicyUpdate> = {
  encode(message: DraftPolicyUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.draftVersion !== 0) {
      writer.uint32(8).uint64(message.draftVersion);
    }
    if (message.newChunks !== 0) {
      writer.uint32(16).uint32(message.newChunks);
    }
    if (message.totalPending !== 0) {
      writer.uint32(24).uint32(message.totalPending);
    }
    if (message.summary !== "") {
      writer.uint32(34).string(message.summary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DraftPolicyUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDraftPolicyUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.draftVersion = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.newChunks = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalPending = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DraftPolicyUpdate {
    return {
      draftVersion: isSet(object.draftVersion)
        ? globalThis.Number(object.draftVersion)
        : isSet(object.draft_version)
        ? globalThis.Number(object.draft_version)
        : 0,
      newChunks: isSet(object.newChunks)
        ? globalThis.Number(object.newChunks)
        : isSet(object.new_chunks)
        ? globalThis.Number(object.new_chunks)
        : 0,
      totalPending: isSet(object.totalPending)
        ? globalThis.Number(object.totalPending)
        : isSet(object.total_pending)
        ? globalThis.Number(object.total_pending)
        : 0,
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
    };
  },

  toJSON(message: DraftPolicyUpdate): unknown {
    const obj: any = {};
    if (message.draftVersion !== 0) {
      obj.draftVersion = Math.round(message.draftVersion);
    }
    if (message.newChunks !== 0) {
      obj.newChunks = Math.round(message.newChunks);
    }
    if (message.totalPending !== 0) {
      obj.totalPending = Math.round(message.totalPending);
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DraftPolicyUpdate>, I>>(base?: I): DraftPolicyUpdate {
    return DraftPolicyUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DraftPolicyUpdate>, I>>(object: I): DraftPolicyUpdate {
    const message = createBaseDraftPolicyUpdate();
    message.draftVersion = object.draftVersion ?? 0;
    message.newChunks = object.newChunks ?? 0;
    message.totalPending = object.totalPending ?? 0;
    message.summary = object.summary ?? "";
    return message;
  },
};

function createBaseSubmitPolicyAnalysisRequest(): SubmitPolicyAnalysisRequest {
  return { summaries: [], proposedChunks: [], analysisMode: "", name: "" };
}

export const SubmitPolicyAnalysisRequest: MessageFns<SubmitPolicyAnalysisRequest> = {
  encode(message: SubmitPolicyAnalysisRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.summaries) {
      DenialSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.proposedChunks) {
      PolicyChunk.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.analysisMode !== "") {
      writer.uint32(26).string(message.analysisMode);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitPolicyAnalysisRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitPolicyAnalysisRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.summaries.push(DenialSummary.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.proposedChunks.push(PolicyChunk.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.analysisMode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitPolicyAnalysisRequest {
    return {
      summaries: globalThis.Array.isArray(object?.summaries)
        ? object.summaries.map((e: any) => DenialSummary.fromJSON(e))
        : [],
      proposedChunks: globalThis.Array.isArray(object?.proposedChunks)
        ? object.proposedChunks.map((e: any) => PolicyChunk.fromJSON(e))
        : globalThis.Array.isArray(object?.proposed_chunks)
        ? object.proposed_chunks.map((e: any) => PolicyChunk.fromJSON(e))
        : [],
      analysisMode: isSet(object.analysisMode)
        ? globalThis.String(object.analysisMode)
        : isSet(object.analysis_mode)
        ? globalThis.String(object.analysis_mode)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: SubmitPolicyAnalysisRequest): unknown {
    const obj: any = {};
    if (message.summaries?.length) {
      obj.summaries = message.summaries.map((e) => DenialSummary.toJSON(e));
    }
    if (message.proposedChunks?.length) {
      obj.proposedChunks = message.proposedChunks.map((e) => PolicyChunk.toJSON(e));
    }
    if (message.analysisMode !== "") {
      obj.analysisMode = message.analysisMode;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitPolicyAnalysisRequest>, I>>(base?: I): SubmitPolicyAnalysisRequest {
    return SubmitPolicyAnalysisRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitPolicyAnalysisRequest>, I>>(object: I): SubmitPolicyAnalysisRequest {
    const message = createBaseSubmitPolicyAnalysisRequest();
    message.summaries = object.summaries?.map((e) => DenialSummary.fromPartial(e)) || [];
    message.proposedChunks = object.proposedChunks?.map((e) => PolicyChunk.fromPartial(e)) || [];
    message.analysisMode = object.analysisMode ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseSubmitPolicyAnalysisResponse(): SubmitPolicyAnalysisResponse {
  return { acceptedChunks: 0, rejectedChunks: 0, rejectionReasons: [], acceptedChunkIds: [] };
}

export const SubmitPolicyAnalysisResponse: MessageFns<SubmitPolicyAnalysisResponse> = {
  encode(message: SubmitPolicyAnalysisResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acceptedChunks !== 0) {
      writer.uint32(8).uint32(message.acceptedChunks);
    }
    if (message.rejectedChunks !== 0) {
      writer.uint32(16).uint32(message.rejectedChunks);
    }
    for (const v of message.rejectionReasons) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.acceptedChunkIds) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitPolicyAnalysisResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitPolicyAnalysisResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.acceptedChunks = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rejectedChunks = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rejectionReasons.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.acceptedChunkIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitPolicyAnalysisResponse {
    return {
      acceptedChunks: isSet(object.acceptedChunks)
        ? globalThis.Number(object.acceptedChunks)
        : isSet(object.accepted_chunks)
        ? globalThis.Number(object.accepted_chunks)
        : 0,
      rejectedChunks: isSet(object.rejectedChunks)
        ? globalThis.Number(object.rejectedChunks)
        : isSet(object.rejected_chunks)
        ? globalThis.Number(object.rejected_chunks)
        : 0,
      rejectionReasons: globalThis.Array.isArray(object?.rejectionReasons)
        ? object.rejectionReasons.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rejection_reasons)
        ? object.rejection_reasons.map((e: any) => globalThis.String(e))
        : [],
      acceptedChunkIds: globalThis.Array.isArray(object?.acceptedChunkIds)
        ? object.acceptedChunkIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.accepted_chunk_ids)
        ? object.accepted_chunk_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SubmitPolicyAnalysisResponse): unknown {
    const obj: any = {};
    if (message.acceptedChunks !== 0) {
      obj.acceptedChunks = Math.round(message.acceptedChunks);
    }
    if (message.rejectedChunks !== 0) {
      obj.rejectedChunks = Math.round(message.rejectedChunks);
    }
    if (message.rejectionReasons?.length) {
      obj.rejectionReasons = message.rejectionReasons;
    }
    if (message.acceptedChunkIds?.length) {
      obj.acceptedChunkIds = message.acceptedChunkIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitPolicyAnalysisResponse>, I>>(base?: I): SubmitPolicyAnalysisResponse {
    return SubmitPolicyAnalysisResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitPolicyAnalysisResponse>, I>>(object: I): SubmitPolicyAnalysisResponse {
    const message = createBaseSubmitPolicyAnalysisResponse();
    message.acceptedChunks = object.acceptedChunks ?? 0;
    message.rejectedChunks = object.rejectedChunks ?? 0;
    message.rejectionReasons = object.rejectionReasons?.map((e) => e) || [];
    message.acceptedChunkIds = object.acceptedChunkIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetDraftPolicyRequest(): GetDraftPolicyRequest {
  return { name: "", statusFilter: "" };
}

export const GetDraftPolicyRequest: MessageFns<GetDraftPolicyRequest> = {
  encode(message: GetDraftPolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.statusFilter !== "") {
      writer.uint32(18).string(message.statusFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDraftPolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDraftPolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.statusFilter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDraftPolicyRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      statusFilter: isSet(object.statusFilter)
        ? globalThis.String(object.statusFilter)
        : isSet(object.status_filter)
        ? globalThis.String(object.status_filter)
        : "",
    };
  },

  toJSON(message: GetDraftPolicyRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.statusFilter !== "") {
      obj.statusFilter = message.statusFilter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDraftPolicyRequest>, I>>(base?: I): GetDraftPolicyRequest {
    return GetDraftPolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDraftPolicyRequest>, I>>(object: I): GetDraftPolicyRequest {
    const message = createBaseGetDraftPolicyRequest();
    message.name = object.name ?? "";
    message.statusFilter = object.statusFilter ?? "";
    return message;
  },
};

function createBaseGetDraftPolicyResponse(): GetDraftPolicyResponse {
  return { chunks: [], rollingSummary: "", draftVersion: 0, lastAnalyzedAtMs: 0 };
}

export const GetDraftPolicyResponse: MessageFns<GetDraftPolicyResponse> = {
  encode(message: GetDraftPolicyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.chunks) {
      PolicyChunk.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.rollingSummary !== "") {
      writer.uint32(18).string(message.rollingSummary);
    }
    if (message.draftVersion !== 0) {
      writer.uint32(24).uint64(message.draftVersion);
    }
    if (message.lastAnalyzedAtMs !== 0) {
      writer.uint32(32).int64(message.lastAnalyzedAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDraftPolicyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDraftPolicyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chunks.push(PolicyChunk.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rollingSummary = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.draftVersion = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastAnalyzedAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDraftPolicyResponse {
    return {
      chunks: globalThis.Array.isArray(object?.chunks) ? object.chunks.map((e: any) => PolicyChunk.fromJSON(e)) : [],
      rollingSummary: isSet(object.rollingSummary)
        ? globalThis.String(object.rollingSummary)
        : isSet(object.rolling_summary)
        ? globalThis.String(object.rolling_summary)
        : "",
      draftVersion: isSet(object.draftVersion)
        ? globalThis.Number(object.draftVersion)
        : isSet(object.draft_version)
        ? globalThis.Number(object.draft_version)
        : 0,
      lastAnalyzedAtMs: isSet(object.lastAnalyzedAtMs)
        ? globalThis.Number(object.lastAnalyzedAtMs)
        : isSet(object.last_analyzed_at_ms)
        ? globalThis.Number(object.last_analyzed_at_ms)
        : 0,
    };
  },

  toJSON(message: GetDraftPolicyResponse): unknown {
    const obj: any = {};
    if (message.chunks?.length) {
      obj.chunks = message.chunks.map((e) => PolicyChunk.toJSON(e));
    }
    if (message.rollingSummary !== "") {
      obj.rollingSummary = message.rollingSummary;
    }
    if (message.draftVersion !== 0) {
      obj.draftVersion = Math.round(message.draftVersion);
    }
    if (message.lastAnalyzedAtMs !== 0) {
      obj.lastAnalyzedAtMs = Math.round(message.lastAnalyzedAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDraftPolicyResponse>, I>>(base?: I): GetDraftPolicyResponse {
    return GetDraftPolicyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDraftPolicyResponse>, I>>(object: I): GetDraftPolicyResponse {
    const message = createBaseGetDraftPolicyResponse();
    message.chunks = object.chunks?.map((e) => PolicyChunk.fromPartial(e)) || [];
    message.rollingSummary = object.rollingSummary ?? "";
    message.draftVersion = object.draftVersion ?? 0;
    message.lastAnalyzedAtMs = object.lastAnalyzedAtMs ?? 0;
    return message;
  },
};

function createBaseApproveDraftChunkRequest(): ApproveDraftChunkRequest {
  return { name: "", chunkId: "" };
}

export const ApproveDraftChunkRequest: MessageFns<ApproveDraftChunkRequest> = {
  encode(message: ApproveDraftChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.chunkId !== "") {
      writer.uint32(18).string(message.chunkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApproveDraftChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproveDraftChunkRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproveDraftChunkRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      chunkId: isSet(object.chunkId)
        ? globalThis.String(object.chunkId)
        : isSet(object.chunk_id)
        ? globalThis.String(object.chunk_id)
        : "",
    };
  },

  toJSON(message: ApproveDraftChunkRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.chunkId !== "") {
      obj.chunkId = message.chunkId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApproveDraftChunkRequest>, I>>(base?: I): ApproveDraftChunkRequest {
    return ApproveDraftChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApproveDraftChunkRequest>, I>>(object: I): ApproveDraftChunkRequest {
    const message = createBaseApproveDraftChunkRequest();
    message.name = object.name ?? "";
    message.chunkId = object.chunkId ?? "";
    return message;
  },
};

function createBaseApproveDraftChunkResponse(): ApproveDraftChunkResponse {
  return { policyVersion: 0, policyHash: "" };
}

export const ApproveDraftChunkResponse: MessageFns<ApproveDraftChunkResponse> = {
  encode(message: ApproveDraftChunkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policyVersion !== 0) {
      writer.uint32(8).uint32(message.policyVersion);
    }
    if (message.policyHash !== "") {
      writer.uint32(18).string(message.policyHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApproveDraftChunkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproveDraftChunkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.policyVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproveDraftChunkResponse {
    return {
      policyVersion: isSet(object.policyVersion)
        ? globalThis.Number(object.policyVersion)
        : isSet(object.policy_version)
        ? globalThis.Number(object.policy_version)
        : 0,
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
    };
  },

  toJSON(message: ApproveDraftChunkResponse): unknown {
    const obj: any = {};
    if (message.policyVersion !== 0) {
      obj.policyVersion = Math.round(message.policyVersion);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApproveDraftChunkResponse>, I>>(base?: I): ApproveDraftChunkResponse {
    return ApproveDraftChunkResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApproveDraftChunkResponse>, I>>(object: I): ApproveDraftChunkResponse {
    const message = createBaseApproveDraftChunkResponse();
    message.policyVersion = object.policyVersion ?? 0;
    message.policyHash = object.policyHash ?? "";
    return message;
  },
};

function createBaseRejectDraftChunkRequest(): RejectDraftChunkRequest {
  return { name: "", chunkId: "", reason: "" };
}

export const RejectDraftChunkRequest: MessageFns<RejectDraftChunkRequest> = {
  encode(message: RejectDraftChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.chunkId !== "") {
      writer.uint32(18).string(message.chunkId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RejectDraftChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectDraftChunkRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RejectDraftChunkRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      chunkId: isSet(object.chunkId)
        ? globalThis.String(object.chunkId)
        : isSet(object.chunk_id)
        ? globalThis.String(object.chunk_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: RejectDraftChunkRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.chunkId !== "") {
      obj.chunkId = message.chunkId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RejectDraftChunkRequest>, I>>(base?: I): RejectDraftChunkRequest {
    return RejectDraftChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RejectDraftChunkRequest>, I>>(object: I): RejectDraftChunkRequest {
    const message = createBaseRejectDraftChunkRequest();
    message.name = object.name ?? "";
    message.chunkId = object.chunkId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseRejectDraftChunkResponse(): RejectDraftChunkResponse {
  return {};
}

export const RejectDraftChunkResponse: MessageFns<RejectDraftChunkResponse> = {
  encode(_: RejectDraftChunkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RejectDraftChunkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectDraftChunkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RejectDraftChunkResponse {
    return {};
  },

  toJSON(_: RejectDraftChunkResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RejectDraftChunkResponse>, I>>(base?: I): RejectDraftChunkResponse {
    return RejectDraftChunkResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RejectDraftChunkResponse>, I>>(_: I): RejectDraftChunkResponse {
    const message = createBaseRejectDraftChunkResponse();
    return message;
  },
};

function createBaseApproveAllDraftChunksRequest(): ApproveAllDraftChunksRequest {
  return { name: "", includeSecurityFlagged: false };
}

export const ApproveAllDraftChunksRequest: MessageFns<ApproveAllDraftChunksRequest> = {
  encode(message: ApproveAllDraftChunksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.includeSecurityFlagged !== false) {
      writer.uint32(16).bool(message.includeSecurityFlagged);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApproveAllDraftChunksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproveAllDraftChunksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.includeSecurityFlagged = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproveAllDraftChunksRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      includeSecurityFlagged: isSet(object.includeSecurityFlagged)
        ? globalThis.Boolean(object.includeSecurityFlagged)
        : isSet(object.include_security_flagged)
        ? globalThis.Boolean(object.include_security_flagged)
        : false,
    };
  },

  toJSON(message: ApproveAllDraftChunksRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.includeSecurityFlagged !== false) {
      obj.includeSecurityFlagged = message.includeSecurityFlagged;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApproveAllDraftChunksRequest>, I>>(base?: I): ApproveAllDraftChunksRequest {
    return ApproveAllDraftChunksRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApproveAllDraftChunksRequest>, I>>(object: I): ApproveAllDraftChunksRequest {
    const message = createBaseApproveAllDraftChunksRequest();
    message.name = object.name ?? "";
    message.includeSecurityFlagged = object.includeSecurityFlagged ?? false;
    return message;
  },
};

function createBaseApproveAllDraftChunksResponse(): ApproveAllDraftChunksResponse {
  return { policyVersion: 0, policyHash: "", chunksApproved: 0, chunksSkipped: 0 };
}

export const ApproveAllDraftChunksResponse: MessageFns<ApproveAllDraftChunksResponse> = {
  encode(message: ApproveAllDraftChunksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policyVersion !== 0) {
      writer.uint32(8).uint32(message.policyVersion);
    }
    if (message.policyHash !== "") {
      writer.uint32(18).string(message.policyHash);
    }
    if (message.chunksApproved !== 0) {
      writer.uint32(24).uint32(message.chunksApproved);
    }
    if (message.chunksSkipped !== 0) {
      writer.uint32(32).uint32(message.chunksSkipped);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApproveAllDraftChunksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproveAllDraftChunksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.policyVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.chunksApproved = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.chunksSkipped = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproveAllDraftChunksResponse {
    return {
      policyVersion: isSet(object.policyVersion)
        ? globalThis.Number(object.policyVersion)
        : isSet(object.policy_version)
        ? globalThis.Number(object.policy_version)
        : 0,
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
      chunksApproved: isSet(object.chunksApproved)
        ? globalThis.Number(object.chunksApproved)
        : isSet(object.chunks_approved)
        ? globalThis.Number(object.chunks_approved)
        : 0,
      chunksSkipped: isSet(object.chunksSkipped)
        ? globalThis.Number(object.chunksSkipped)
        : isSet(object.chunks_skipped)
        ? globalThis.Number(object.chunks_skipped)
        : 0,
    };
  },

  toJSON(message: ApproveAllDraftChunksResponse): unknown {
    const obj: any = {};
    if (message.policyVersion !== 0) {
      obj.policyVersion = Math.round(message.policyVersion);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    if (message.chunksApproved !== 0) {
      obj.chunksApproved = Math.round(message.chunksApproved);
    }
    if (message.chunksSkipped !== 0) {
      obj.chunksSkipped = Math.round(message.chunksSkipped);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApproveAllDraftChunksResponse>, I>>(base?: I): ApproveAllDraftChunksResponse {
    return ApproveAllDraftChunksResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApproveAllDraftChunksResponse>, I>>(
    object: I,
  ): ApproveAllDraftChunksResponse {
    const message = createBaseApproveAllDraftChunksResponse();
    message.policyVersion = object.policyVersion ?? 0;
    message.policyHash = object.policyHash ?? "";
    message.chunksApproved = object.chunksApproved ?? 0;
    message.chunksSkipped = object.chunksSkipped ?? 0;
    return message;
  },
};

function createBaseEditDraftChunkRequest(): EditDraftChunkRequest {
  return { name: "", chunkId: "", proposedRule: undefined };
}

export const EditDraftChunkRequest: MessageFns<EditDraftChunkRequest> = {
  encode(message: EditDraftChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.chunkId !== "") {
      writer.uint32(18).string(message.chunkId);
    }
    if (message.proposedRule !== undefined) {
      NetworkPolicyRule.encode(message.proposedRule, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EditDraftChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEditDraftChunkRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.proposedRule = NetworkPolicyRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EditDraftChunkRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      chunkId: isSet(object.chunkId)
        ? globalThis.String(object.chunkId)
        : isSet(object.chunk_id)
        ? globalThis.String(object.chunk_id)
        : "",
      proposedRule: isSet(object.proposedRule)
        ? NetworkPolicyRule.fromJSON(object.proposedRule)
        : isSet(object.proposed_rule)
        ? NetworkPolicyRule.fromJSON(object.proposed_rule)
        : undefined,
    };
  },

  toJSON(message: EditDraftChunkRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.chunkId !== "") {
      obj.chunkId = message.chunkId;
    }
    if (message.proposedRule !== undefined) {
      obj.proposedRule = NetworkPolicyRule.toJSON(message.proposedRule);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EditDraftChunkRequest>, I>>(base?: I): EditDraftChunkRequest {
    return EditDraftChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EditDraftChunkRequest>, I>>(object: I): EditDraftChunkRequest {
    const message = createBaseEditDraftChunkRequest();
    message.name = object.name ?? "";
    message.chunkId = object.chunkId ?? "";
    message.proposedRule = (object.proposedRule !== undefined && object.proposedRule !== null)
      ? NetworkPolicyRule.fromPartial(object.proposedRule)
      : undefined;
    return message;
  },
};

function createBaseEditDraftChunkResponse(): EditDraftChunkResponse {
  return {};
}

export const EditDraftChunkResponse: MessageFns<EditDraftChunkResponse> = {
  encode(_: EditDraftChunkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EditDraftChunkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEditDraftChunkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): EditDraftChunkResponse {
    return {};
  },

  toJSON(_: EditDraftChunkResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<EditDraftChunkResponse>, I>>(base?: I): EditDraftChunkResponse {
    return EditDraftChunkResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EditDraftChunkResponse>, I>>(_: I): EditDraftChunkResponse {
    const message = createBaseEditDraftChunkResponse();
    return message;
  },
};

function createBaseUndoDraftChunkRequest(): UndoDraftChunkRequest {
  return { name: "", chunkId: "" };
}

export const UndoDraftChunkRequest: MessageFns<UndoDraftChunkRequest> = {
  encode(message: UndoDraftChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.chunkId !== "") {
      writer.uint32(18).string(message.chunkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UndoDraftChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUndoDraftChunkRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UndoDraftChunkRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      chunkId: isSet(object.chunkId)
        ? globalThis.String(object.chunkId)
        : isSet(object.chunk_id)
        ? globalThis.String(object.chunk_id)
        : "",
    };
  },

  toJSON(message: UndoDraftChunkRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.chunkId !== "") {
      obj.chunkId = message.chunkId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UndoDraftChunkRequest>, I>>(base?: I): UndoDraftChunkRequest {
    return UndoDraftChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UndoDraftChunkRequest>, I>>(object: I): UndoDraftChunkRequest {
    const message = createBaseUndoDraftChunkRequest();
    message.name = object.name ?? "";
    message.chunkId = object.chunkId ?? "";
    return message;
  },
};

function createBaseUndoDraftChunkResponse(): UndoDraftChunkResponse {
  return { policyVersion: 0, policyHash: "" };
}

export const UndoDraftChunkResponse: MessageFns<UndoDraftChunkResponse> = {
  encode(message: UndoDraftChunkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policyVersion !== 0) {
      writer.uint32(8).uint32(message.policyVersion);
    }
    if (message.policyHash !== "") {
      writer.uint32(18).string(message.policyHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UndoDraftChunkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUndoDraftChunkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.policyVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UndoDraftChunkResponse {
    return {
      policyVersion: isSet(object.policyVersion)
        ? globalThis.Number(object.policyVersion)
        : isSet(object.policy_version)
        ? globalThis.Number(object.policy_version)
        : 0,
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
    };
  },

  toJSON(message: UndoDraftChunkResponse): unknown {
    const obj: any = {};
    if (message.policyVersion !== 0) {
      obj.policyVersion = Math.round(message.policyVersion);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UndoDraftChunkResponse>, I>>(base?: I): UndoDraftChunkResponse {
    return UndoDraftChunkResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UndoDraftChunkResponse>, I>>(object: I): UndoDraftChunkResponse {
    const message = createBaseUndoDraftChunkResponse();
    message.policyVersion = object.policyVersion ?? 0;
    message.policyHash = object.policyHash ?? "";
    return message;
  },
};

function createBaseClearDraftChunksRequest(): ClearDraftChunksRequest {
  return { name: "" };
}

export const ClearDraftChunksRequest: MessageFns<ClearDraftChunksRequest> = {
  encode(message: ClearDraftChunksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearDraftChunksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearDraftChunksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClearDraftChunksRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: ClearDraftChunksRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClearDraftChunksRequest>, I>>(base?: I): ClearDraftChunksRequest {
    return ClearDraftChunksRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClearDraftChunksRequest>, I>>(object: I): ClearDraftChunksRequest {
    const message = createBaseClearDraftChunksRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseClearDraftChunksResponse(): ClearDraftChunksResponse {
  return { chunksCleared: 0 };
}

export const ClearDraftChunksResponse: MessageFns<ClearDraftChunksResponse> = {
  encode(message: ClearDraftChunksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chunksCleared !== 0) {
      writer.uint32(8).uint32(message.chunksCleared);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearDraftChunksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearDraftChunksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.chunksCleared = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClearDraftChunksResponse {
    return {
      chunksCleared: isSet(object.chunksCleared)
        ? globalThis.Number(object.chunksCleared)
        : isSet(object.chunks_cleared)
        ? globalThis.Number(object.chunks_cleared)
        : 0,
    };
  },

  toJSON(message: ClearDraftChunksResponse): unknown {
    const obj: any = {};
    if (message.chunksCleared !== 0) {
      obj.chunksCleared = Math.round(message.chunksCleared);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClearDraftChunksResponse>, I>>(base?: I): ClearDraftChunksResponse {
    return ClearDraftChunksResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClearDraftChunksResponse>, I>>(object: I): ClearDraftChunksResponse {
    const message = createBaseClearDraftChunksResponse();
    message.chunksCleared = object.chunksCleared ?? 0;
    return message;
  },
};

function createBaseGetDraftHistoryRequest(): GetDraftHistoryRequest {
  return { name: "" };
}

export const GetDraftHistoryRequest: MessageFns<GetDraftHistoryRequest> = {
  encode(message: GetDraftHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDraftHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDraftHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDraftHistoryRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetDraftHistoryRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDraftHistoryRequest>, I>>(base?: I): GetDraftHistoryRequest {
    return GetDraftHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDraftHistoryRequest>, I>>(object: I): GetDraftHistoryRequest {
    const message = createBaseGetDraftHistoryRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseDraftHistoryEntry(): DraftHistoryEntry {
  return { timestampMs: 0, eventType: "", description: "", chunkId: "" };
}

export const DraftHistoryEntry: MessageFns<DraftHistoryEntry> = {
  encode(message: DraftHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestampMs !== 0) {
      writer.uint32(8).int64(message.timestampMs);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.chunkId !== "") {
      writer.uint32(34).string(message.chunkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DraftHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDraftHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestampMs = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DraftHistoryEntry {
    return {
      timestampMs: isSet(object.timestampMs)
        ? globalThis.Number(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? globalThis.Number(object.timestamp_ms)
        : 0,
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      chunkId: isSet(object.chunkId)
        ? globalThis.String(object.chunkId)
        : isSet(object.chunk_id)
        ? globalThis.String(object.chunk_id)
        : "",
    };
  },

  toJSON(message: DraftHistoryEntry): unknown {
    const obj: any = {};
    if (message.timestampMs !== 0) {
      obj.timestampMs = Math.round(message.timestampMs);
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.chunkId !== "") {
      obj.chunkId = message.chunkId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DraftHistoryEntry>, I>>(base?: I): DraftHistoryEntry {
    return DraftHistoryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DraftHistoryEntry>, I>>(object: I): DraftHistoryEntry {
    const message = createBaseDraftHistoryEntry();
    message.timestampMs = object.timestampMs ?? 0;
    message.eventType = object.eventType ?? "";
    message.description = object.description ?? "";
    message.chunkId = object.chunkId ?? "";
    return message;
  },
};

function createBaseGetDraftHistoryResponse(): GetDraftHistoryResponse {
  return { entries: [] };
}

export const GetDraftHistoryResponse: MessageFns<GetDraftHistoryResponse> = {
  encode(message: GetDraftHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      DraftHistoryEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDraftHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDraftHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(DraftHistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDraftHistoryResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => DraftHistoryEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetDraftHistoryResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => DraftHistoryEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDraftHistoryResponse>, I>>(base?: I): GetDraftHistoryResponse {
    return GetDraftHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDraftHistoryResponse>, I>>(object: I): GetDraftHistoryResponse {
    const message = createBaseGetDraftHistoryResponse();
    message.entries = object.entries?.map((e) => DraftHistoryEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBasePolicyRevisionPayload(): PolicyRevisionPayload {
  return { policy: undefined, hash: "", loadError: "", loadedAtMs: 0 };
}

export const PolicyRevisionPayload: MessageFns<PolicyRevisionPayload> = {
  encode(message: PolicyRevisionPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policy !== undefined) {
      SandboxPolicy.encode(message.policy, writer.uint32(10).fork()).join();
    }
    if (message.hash !== "") {
      writer.uint32(18).string(message.hash);
    }
    if (message.loadError !== "") {
      writer.uint32(26).string(message.loadError);
    }
    if (message.loadedAtMs !== 0) {
      writer.uint32(32).int64(message.loadedAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyRevisionPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyRevisionPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.policy = SandboxPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.loadError = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.loadedAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyRevisionPayload {
    return {
      policy: isSet(object.policy) ? SandboxPolicy.fromJSON(object.policy) : undefined,
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      loadError: isSet(object.loadError)
        ? globalThis.String(object.loadError)
        : isSet(object.load_error)
        ? globalThis.String(object.load_error)
        : "",
      loadedAtMs: isSet(object.loadedAtMs)
        ? globalThis.Number(object.loadedAtMs)
        : isSet(object.loaded_at_ms)
        ? globalThis.Number(object.loaded_at_ms)
        : 0,
    };
  },

  toJSON(message: PolicyRevisionPayload): unknown {
    const obj: any = {};
    if (message.policy !== undefined) {
      obj.policy = SandboxPolicy.toJSON(message.policy);
    }
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.loadError !== "") {
      obj.loadError = message.loadError;
    }
    if (message.loadedAtMs !== 0) {
      obj.loadedAtMs = Math.round(message.loadedAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyRevisionPayload>, I>>(base?: I): PolicyRevisionPayload {
    return PolicyRevisionPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyRevisionPayload>, I>>(object: I): PolicyRevisionPayload {
    const message = createBasePolicyRevisionPayload();
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? SandboxPolicy.fromPartial(object.policy)
      : undefined;
    message.hash = object.hash ?? "";
    message.loadError = object.loadError ?? "";
    message.loadedAtMs = object.loadedAtMs ?? 0;
    return message;
  },
};

function createBaseDraftChunkPayload(): DraftChunkPayload {
  return {
    ruleName: "",
    proposedRule: undefined,
    rationale: "",
    securityNotes: "",
    confidence: 0,
    decidedAtMs: 0,
    host: "",
    port: 0,
    binary: "",
    draftVersion: 0,
    validationResult: "",
    rejectionReason: "",
  };
}

export const DraftChunkPayload: MessageFns<DraftChunkPayload> = {
  encode(message: DraftChunkPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ruleName !== "") {
      writer.uint32(10).string(message.ruleName);
    }
    if (message.proposedRule !== undefined) {
      NetworkPolicyRule.encode(message.proposedRule, writer.uint32(18).fork()).join();
    }
    if (message.rationale !== "") {
      writer.uint32(26).string(message.rationale);
    }
    if (message.securityNotes !== "") {
      writer.uint32(34).string(message.securityNotes);
    }
    if (message.confidence !== 0) {
      writer.uint32(45).float(message.confidence);
    }
    if (message.decidedAtMs !== 0) {
      writer.uint32(48).int64(message.decidedAtMs);
    }
    if (message.host !== "") {
      writer.uint32(58).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(64).int32(message.port);
    }
    if (message.binary !== "") {
      writer.uint32(74).string(message.binary);
    }
    if (message.draftVersion !== 0) {
      writer.uint32(80).int64(message.draftVersion);
    }
    if (message.validationResult !== "") {
      writer.uint32(90).string(message.validationResult);
    }
    if (message.rejectionReason !== "") {
      writer.uint32(98).string(message.rejectionReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DraftChunkPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDraftChunkPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.proposedRule = NetworkPolicyRule.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rationale = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.securityNotes = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.decidedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.binary = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.draftVersion = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.validationResult = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.rejectionReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DraftChunkPayload {
    return {
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
      proposedRule: isSet(object.proposedRule)
        ? NetworkPolicyRule.fromJSON(object.proposedRule)
        : isSet(object.proposed_rule)
        ? NetworkPolicyRule.fromJSON(object.proposed_rule)
        : undefined,
      rationale: isSet(object.rationale) ? globalThis.String(object.rationale) : "",
      securityNotes: isSet(object.securityNotes)
        ? globalThis.String(object.securityNotes)
        : isSet(object.security_notes)
        ? globalThis.String(object.security_notes)
        : "",
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      decidedAtMs: isSet(object.decidedAtMs)
        ? globalThis.Number(object.decidedAtMs)
        : isSet(object.decided_at_ms)
        ? globalThis.Number(object.decided_at_ms)
        : 0,
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      binary: isSet(object.binary) ? globalThis.String(object.binary) : "",
      draftVersion: isSet(object.draftVersion)
        ? globalThis.Number(object.draftVersion)
        : isSet(object.draft_version)
        ? globalThis.Number(object.draft_version)
        : 0,
      validationResult: isSet(object.validationResult)
        ? globalThis.String(object.validationResult)
        : isSet(object.validation_result)
        ? globalThis.String(object.validation_result)
        : "",
      rejectionReason: isSet(object.rejectionReason)
        ? globalThis.String(object.rejectionReason)
        : isSet(object.rejection_reason)
        ? globalThis.String(object.rejection_reason)
        : "",
    };
  },

  toJSON(message: DraftChunkPayload): unknown {
    const obj: any = {};
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    if (message.proposedRule !== undefined) {
      obj.proposedRule = NetworkPolicyRule.toJSON(message.proposedRule);
    }
    if (message.rationale !== "") {
      obj.rationale = message.rationale;
    }
    if (message.securityNotes !== "") {
      obj.securityNotes = message.securityNotes;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.decidedAtMs !== 0) {
      obj.decidedAtMs = Math.round(message.decidedAtMs);
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.binary !== "") {
      obj.binary = message.binary;
    }
    if (message.draftVersion !== 0) {
      obj.draftVersion = Math.round(message.draftVersion);
    }
    if (message.validationResult !== "") {
      obj.validationResult = message.validationResult;
    }
    if (message.rejectionReason !== "") {
      obj.rejectionReason = message.rejectionReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DraftChunkPayload>, I>>(base?: I): DraftChunkPayload {
    return DraftChunkPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DraftChunkPayload>, I>>(object: I): DraftChunkPayload {
    const message = createBaseDraftChunkPayload();
    message.ruleName = object.ruleName ?? "";
    message.proposedRule = (object.proposedRule !== undefined && object.proposedRule !== null)
      ? NetworkPolicyRule.fromPartial(object.proposedRule)
      : undefined;
    message.rationale = object.rationale ?? "";
    message.securityNotes = object.securityNotes ?? "";
    message.confidence = object.confidence ?? 0;
    message.decidedAtMs = object.decidedAtMs ?? 0;
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.binary = object.binary ?? "";
    message.draftVersion = object.draftVersion ?? 0;
    message.validationResult = object.validationResult ?? "";
    message.rejectionReason = object.rejectionReason ?? "";
    return message;
  },
};

function createBaseStoredPolicyRevision(): StoredPolicyRevision {
  return {
    id: "",
    sandboxId: "",
    version: 0,
    policyPayload: Buffer.alloc(0),
    policyHash: "",
    status: "",
    loadError: undefined,
    createdAtMs: 0,
    loadedAtMs: undefined,
  };
}

export const StoredPolicyRevision: MessageFns<StoredPolicyRevision> = {
  encode(message: StoredPolicyRevision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.sandboxId !== "") {
      writer.uint32(18).string(message.sandboxId);
    }
    if (message.version !== 0) {
      writer.uint32(24).int64(message.version);
    }
    if (message.policyPayload.length !== 0) {
      writer.uint32(34).bytes(message.policyPayload);
    }
    if (message.policyHash !== "") {
      writer.uint32(42).string(message.policyHash);
    }
    if (message.status !== "") {
      writer.uint32(50).string(message.status);
    }
    if (message.loadError !== undefined) {
      writer.uint32(58).string(message.loadError);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(64).int64(message.createdAtMs);
    }
    if (message.loadedAtMs !== undefined) {
      writer.uint32(72).int64(message.loadedAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredPolicyRevision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredPolicyRevision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.version = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.policyPayload = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.policyHash = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.loadError = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.loadedAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredPolicyRevision {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      policyPayload: isSet(object.policyPayload)
        ? Buffer.from(bytesFromBase64(object.policyPayload))
        : isSet(object.policy_payload)
        ? Buffer.from(bytesFromBase64(object.policy_payload))
        : Buffer.alloc(0),
      policyHash: isSet(object.policyHash)
        ? globalThis.String(object.policyHash)
        : isSet(object.policy_hash)
        ? globalThis.String(object.policy_hash)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      loadError: isSet(object.loadError)
        ? globalThis.String(object.loadError)
        : isSet(object.load_error)
        ? globalThis.String(object.load_error)
        : undefined,
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      loadedAtMs: isSet(object.loadedAtMs)
        ? globalThis.Number(object.loadedAtMs)
        : isSet(object.loaded_at_ms)
        ? globalThis.Number(object.loaded_at_ms)
        : undefined,
    };
  },

  toJSON(message: StoredPolicyRevision): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.policyPayload.length !== 0) {
      obj.policyPayload = base64FromBytes(message.policyPayload);
    }
    if (message.policyHash !== "") {
      obj.policyHash = message.policyHash;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.loadError !== undefined) {
      obj.loadError = message.loadError;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.loadedAtMs !== undefined) {
      obj.loadedAtMs = Math.round(message.loadedAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredPolicyRevision>, I>>(base?: I): StoredPolicyRevision {
    return StoredPolicyRevision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredPolicyRevision>, I>>(object: I): StoredPolicyRevision {
    const message = createBaseStoredPolicyRevision();
    message.id = object.id ?? "";
    message.sandboxId = object.sandboxId ?? "";
    message.version = object.version ?? 0;
    message.policyPayload = object.policyPayload ?? Buffer.alloc(0);
    message.policyHash = object.policyHash ?? "";
    message.status = object.status ?? "";
    message.loadError = object.loadError ?? undefined;
    message.createdAtMs = object.createdAtMs ?? 0;
    message.loadedAtMs = object.loadedAtMs ?? undefined;
    return message;
  },
};

function createBaseStoredDraftChunk(): StoredDraftChunk {
  return {
    id: "",
    sandboxId: "",
    draftVersion: 0,
    status: "",
    ruleName: "",
    proposedRule: Buffer.alloc(0),
    rationale: "",
    securityNotes: "",
    confidence: 0,
    createdAtMs: 0,
    decidedAtMs: undefined,
    host: "",
    port: 0,
    binary: "",
    hitCount: 0,
    firstSeenMs: 0,
    lastSeenMs: 0,
    validationResult: "",
    rejectionReason: "",
  };
}

export const StoredDraftChunk: MessageFns<StoredDraftChunk> = {
  encode(message: StoredDraftChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.sandboxId !== "") {
      writer.uint32(18).string(message.sandboxId);
    }
    if (message.draftVersion !== 0) {
      writer.uint32(24).int64(message.draftVersion);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    if (message.ruleName !== "") {
      writer.uint32(42).string(message.ruleName);
    }
    if (message.proposedRule.length !== 0) {
      writer.uint32(50).bytes(message.proposedRule);
    }
    if (message.rationale !== "") {
      writer.uint32(58).string(message.rationale);
    }
    if (message.securityNotes !== "") {
      writer.uint32(66).string(message.securityNotes);
    }
    if (message.confidence !== 0) {
      writer.uint32(73).double(message.confidence);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(80).int64(message.createdAtMs);
    }
    if (message.decidedAtMs !== undefined) {
      writer.uint32(88).int64(message.decidedAtMs);
    }
    if (message.host !== "") {
      writer.uint32(98).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(104).int32(message.port);
    }
    if (message.binary !== "") {
      writer.uint32(114).string(message.binary);
    }
    if (message.hitCount !== 0) {
      writer.uint32(120).int32(message.hitCount);
    }
    if (message.firstSeenMs !== 0) {
      writer.uint32(128).int64(message.firstSeenMs);
    }
    if (message.lastSeenMs !== 0) {
      writer.uint32(136).int64(message.lastSeenMs);
    }
    if (message.validationResult !== "") {
      writer.uint32(146).string(message.validationResult);
    }
    if (message.rejectionReason !== "") {
      writer.uint32(154).string(message.rejectionReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredDraftChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredDraftChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.draftVersion = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ruleName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.proposedRule = Buffer.from(reader.bytes());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.rationale = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.securityNotes = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.confidence = reader.double();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.decidedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.binary = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.hitCount = reader.int32();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.firstSeenMs = longToNumber(reader.int64());
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.lastSeenMs = longToNumber(reader.int64());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.validationResult = reader.string();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.rejectionReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredDraftChunk {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      draftVersion: isSet(object.draftVersion)
        ? globalThis.Number(object.draftVersion)
        : isSet(object.draft_version)
        ? globalThis.Number(object.draft_version)
        : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      ruleName: isSet(object.ruleName)
        ? globalThis.String(object.ruleName)
        : isSet(object.rule_name)
        ? globalThis.String(object.rule_name)
        : "",
      proposedRule: isSet(object.proposedRule)
        ? Buffer.from(bytesFromBase64(object.proposedRule))
        : isSet(object.proposed_rule)
        ? Buffer.from(bytesFromBase64(object.proposed_rule))
        : Buffer.alloc(0),
      rationale: isSet(object.rationale) ? globalThis.String(object.rationale) : "",
      securityNotes: isSet(object.securityNotes)
        ? globalThis.String(object.securityNotes)
        : isSet(object.security_notes)
        ? globalThis.String(object.security_notes)
        : "",
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      decidedAtMs: isSet(object.decidedAtMs)
        ? globalThis.Number(object.decidedAtMs)
        : isSet(object.decided_at_ms)
        ? globalThis.Number(object.decided_at_ms)
        : undefined,
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      binary: isSet(object.binary) ? globalThis.String(object.binary) : "",
      hitCount: isSet(object.hitCount)
        ? globalThis.Number(object.hitCount)
        : isSet(object.hit_count)
        ? globalThis.Number(object.hit_count)
        : 0,
      firstSeenMs: isSet(object.firstSeenMs)
        ? globalThis.Number(object.firstSeenMs)
        : isSet(object.first_seen_ms)
        ? globalThis.Number(object.first_seen_ms)
        : 0,
      lastSeenMs: isSet(object.lastSeenMs)
        ? globalThis.Number(object.lastSeenMs)
        : isSet(object.last_seen_ms)
        ? globalThis.Number(object.last_seen_ms)
        : 0,
      validationResult: isSet(object.validationResult)
        ? globalThis.String(object.validationResult)
        : isSet(object.validation_result)
        ? globalThis.String(object.validation_result)
        : "",
      rejectionReason: isSet(object.rejectionReason)
        ? globalThis.String(object.rejectionReason)
        : isSet(object.rejection_reason)
        ? globalThis.String(object.rejection_reason)
        : "",
    };
  },

  toJSON(message: StoredDraftChunk): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.draftVersion !== 0) {
      obj.draftVersion = Math.round(message.draftVersion);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.ruleName !== "") {
      obj.ruleName = message.ruleName;
    }
    if (message.proposedRule.length !== 0) {
      obj.proposedRule = base64FromBytes(message.proposedRule);
    }
    if (message.rationale !== "") {
      obj.rationale = message.rationale;
    }
    if (message.securityNotes !== "") {
      obj.securityNotes = message.securityNotes;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.decidedAtMs !== undefined) {
      obj.decidedAtMs = Math.round(message.decidedAtMs);
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.binary !== "") {
      obj.binary = message.binary;
    }
    if (message.hitCount !== 0) {
      obj.hitCount = Math.round(message.hitCount);
    }
    if (message.firstSeenMs !== 0) {
      obj.firstSeenMs = Math.round(message.firstSeenMs);
    }
    if (message.lastSeenMs !== 0) {
      obj.lastSeenMs = Math.round(message.lastSeenMs);
    }
    if (message.validationResult !== "") {
      obj.validationResult = message.validationResult;
    }
    if (message.rejectionReason !== "") {
      obj.rejectionReason = message.rejectionReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredDraftChunk>, I>>(base?: I): StoredDraftChunk {
    return StoredDraftChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredDraftChunk>, I>>(object: I): StoredDraftChunk {
    const message = createBaseStoredDraftChunk();
    message.id = object.id ?? "";
    message.sandboxId = object.sandboxId ?? "";
    message.draftVersion = object.draftVersion ?? 0;
    message.status = object.status ?? "";
    message.ruleName = object.ruleName ?? "";
    message.proposedRule = object.proposedRule ?? Buffer.alloc(0);
    message.rationale = object.rationale ?? "";
    message.securityNotes = object.securityNotes ?? "";
    message.confidence = object.confidence ?? 0;
    message.createdAtMs = object.createdAtMs ?? 0;
    message.decidedAtMs = object.decidedAtMs ?? undefined;
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.binary = object.binary ?? "";
    message.hitCount = object.hitCount ?? 0;
    message.firstSeenMs = object.firstSeenMs ?? 0;
    message.lastSeenMs = object.lastSeenMs ?? 0;
    message.validationResult = object.validationResult ?? "";
    message.rejectionReason = object.rejectionReason ?? "";
    return message;
  },
};

/**
 * OpenShell service provides sandbox, provider, and runtime management capabilities.
 *
 * Conventions:
 * - This file owns the public API resource model exposed to OpenShell clients.
 * - `Sandbox`, `SandboxSpec`, `SandboxStatus`, and `SandboxPhase` are gateway-owned
 *   public types. Internal compute drivers must not import or return them directly.
 * - The gateway translates internal compute-driver observations into these public
 *   resource messages before persisting or returning them to clients.
 */
export type OpenShellService = typeof OpenShellService;
export const OpenShellService = {
  /** Check the health of the service. */
  health: {
    path: "/openshell.v1.OpenShell/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HealthRequest): Buffer => Buffer.from(HealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthRequest => HealthRequest.decode(value),
    responseSerialize: (value: HealthResponse): Buffer => Buffer.from(HealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthResponse => HealthResponse.decode(value),
  },
  /** Create a new sandbox. */
  createSandbox: {
    path: "/openshell.v1.OpenShell/CreateSandbox" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateSandboxRequest): Buffer => Buffer.from(CreateSandboxRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateSandboxRequest => CreateSandboxRequest.decode(value),
    responseSerialize: (value: SandboxResponse): Buffer => Buffer.from(SandboxResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SandboxResponse => SandboxResponse.decode(value),
  },
  /** Fetch a sandbox by name. */
  getSandbox: {
    path: "/openshell.v1.OpenShell/GetSandbox" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSandboxRequest): Buffer => Buffer.from(GetSandboxRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSandboxRequest => GetSandboxRequest.decode(value),
    responseSerialize: (value: SandboxResponse): Buffer => Buffer.from(SandboxResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SandboxResponse => SandboxResponse.decode(value),
  },
  /** List sandboxes. */
  listSandboxes: {
    path: "/openshell.v1.OpenShell/ListSandboxes" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSandboxesRequest): Buffer => Buffer.from(ListSandboxesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSandboxesRequest => ListSandboxesRequest.decode(value),
    responseSerialize: (value: ListSandboxesResponse): Buffer =>
      Buffer.from(ListSandboxesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSandboxesResponse => ListSandboxesResponse.decode(value),
  },
  /** List provider records attached to a sandbox. */
  listSandboxProviders: {
    path: "/openshell.v1.OpenShell/ListSandboxProviders" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSandboxProvidersRequest): Buffer =>
      Buffer.from(ListSandboxProvidersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSandboxProvidersRequest => ListSandboxProvidersRequest.decode(value),
    responseSerialize: (value: ListSandboxProvidersResponse): Buffer =>
      Buffer.from(ListSandboxProvidersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSandboxProvidersResponse => ListSandboxProvidersResponse.decode(value),
  },
  /** Attach a provider record to an existing sandbox. */
  attachSandboxProvider: {
    path: "/openshell.v1.OpenShell/AttachSandboxProvider" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AttachSandboxProviderRequest): Buffer =>
      Buffer.from(AttachSandboxProviderRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AttachSandboxProviderRequest => AttachSandboxProviderRequest.decode(value),
    responseSerialize: (value: AttachSandboxProviderResponse): Buffer =>
      Buffer.from(AttachSandboxProviderResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AttachSandboxProviderResponse => AttachSandboxProviderResponse.decode(value),
  },
  /** Detach a provider record from an existing sandbox. */
  detachSandboxProvider: {
    path: "/openshell.v1.OpenShell/DetachSandboxProvider" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DetachSandboxProviderRequest): Buffer =>
      Buffer.from(DetachSandboxProviderRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DetachSandboxProviderRequest => DetachSandboxProviderRequest.decode(value),
    responseSerialize: (value: DetachSandboxProviderResponse): Buffer =>
      Buffer.from(DetachSandboxProviderResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DetachSandboxProviderResponse => DetachSandboxProviderResponse.decode(value),
  },
  /** Delete a sandbox by name. */
  deleteSandbox: {
    path: "/openshell.v1.OpenShell/DeleteSandbox" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteSandboxRequest): Buffer => Buffer.from(DeleteSandboxRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteSandboxRequest => DeleteSandboxRequest.decode(value),
    responseSerialize: (value: DeleteSandboxResponse): Buffer =>
      Buffer.from(DeleteSandboxResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteSandboxResponse => DeleteSandboxResponse.decode(value),
  },
  /** Create a short-lived SSH session for a sandbox. */
  createSshSession: {
    path: "/openshell.v1.OpenShell/CreateSshSession" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateSshSessionRequest): Buffer =>
      Buffer.from(CreateSshSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateSshSessionRequest => CreateSshSessionRequest.decode(value),
    responseSerialize: (value: CreateSshSessionResponse): Buffer =>
      Buffer.from(CreateSshSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateSshSessionResponse => CreateSshSessionResponse.decode(value),
  },
  /** Create or update a sandbox HTTP service endpoint for local routing. */
  exposeService: {
    path: "/openshell.v1.OpenShell/ExposeService" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ExposeServiceRequest): Buffer => Buffer.from(ExposeServiceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExposeServiceRequest => ExposeServiceRequest.decode(value),
    responseSerialize: (value: ServiceEndpointResponse): Buffer =>
      Buffer.from(ServiceEndpointResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ServiceEndpointResponse => ServiceEndpointResponse.decode(value),
  },
  /** Fetch one sandbox HTTP service endpoint. */
  getService: {
    path: "/openshell.v1.OpenShell/GetService" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetServiceRequest): Buffer => Buffer.from(GetServiceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetServiceRequest => GetServiceRequest.decode(value),
    responseSerialize: (value: ServiceEndpointResponse): Buffer =>
      Buffer.from(ServiceEndpointResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ServiceEndpointResponse => ServiceEndpointResponse.decode(value),
  },
  /** List sandbox HTTP service endpoints. */
  listServices: {
    path: "/openshell.v1.OpenShell/ListServices" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListServicesRequest): Buffer => Buffer.from(ListServicesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListServicesRequest => ListServicesRequest.decode(value),
    responseSerialize: (value: ListServicesResponse): Buffer =>
      Buffer.from(ListServicesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListServicesResponse => ListServicesResponse.decode(value),
  },
  /** Delete one sandbox HTTP service endpoint. */
  deleteService: {
    path: "/openshell.v1.OpenShell/DeleteService" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteServiceRequest): Buffer => Buffer.from(DeleteServiceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteServiceRequest => DeleteServiceRequest.decode(value),
    responseSerialize: (value: DeleteServiceResponse): Buffer =>
      Buffer.from(DeleteServiceResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteServiceResponse => DeleteServiceResponse.decode(value),
  },
  /** Revoke a previously issued SSH session. */
  revokeSshSession: {
    path: "/openshell.v1.OpenShell/RevokeSshSession" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RevokeSshSessionRequest): Buffer =>
      Buffer.from(RevokeSshSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RevokeSshSessionRequest => RevokeSshSessionRequest.decode(value),
    responseSerialize: (value: RevokeSshSessionResponse): Buffer =>
      Buffer.from(RevokeSshSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RevokeSshSessionResponse => RevokeSshSessionResponse.decode(value),
  },
  /** Execute a command in a ready sandbox and stream output. */
  execSandbox: {
    path: "/openshell.v1.OpenShell/ExecSandbox" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ExecSandboxRequest): Buffer => Buffer.from(ExecSandboxRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExecSandboxRequest => ExecSandboxRequest.decode(value),
    responseSerialize: (value: ExecSandboxEvent): Buffer => Buffer.from(ExecSandboxEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExecSandboxEvent => ExecSandboxEvent.decode(value),
  },
  /** Forward one CLI-side TCP connection to a loopback TCP target in a sandbox. */
  forwardTcp: {
    path: "/openshell.v1.OpenShell/ForwardTcp" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: TcpForwardFrame): Buffer => Buffer.from(TcpForwardFrame.encode(value).finish()),
    requestDeserialize: (value: Buffer): TcpForwardFrame => TcpForwardFrame.decode(value),
    responseSerialize: (value: TcpForwardFrame): Buffer => Buffer.from(TcpForwardFrame.encode(value).finish()),
    responseDeserialize: (value: Buffer): TcpForwardFrame => TcpForwardFrame.decode(value),
  },
  /**
   * Execute an interactive command with bidirectional stdin/stdout streaming.
   * The first client message MUST carry an ExecSandboxInput with the start
   * variant. Subsequent messages carry stdin bytes or window resize events.
   */
  execSandboxInteractive: {
    path: "/openshell.v1.OpenShell/ExecSandboxInteractive" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: ExecSandboxInput): Buffer => Buffer.from(ExecSandboxInput.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExecSandboxInput => ExecSandboxInput.decode(value),
    responseSerialize: (value: ExecSandboxEvent): Buffer => Buffer.from(ExecSandboxEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExecSandboxEvent => ExecSandboxEvent.decode(value),
  },
  /** Create a provider. */
  createProvider: {
    path: "/openshell.v1.OpenShell/CreateProvider" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateProviderRequest): Buffer =>
      Buffer.from(CreateProviderRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateProviderRequest => CreateProviderRequest.decode(value),
    responseSerialize: (value: ProviderResponse): Buffer => Buffer.from(ProviderResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ProviderResponse => ProviderResponse.decode(value),
  },
  /** Fetch a provider by name. */
  getProvider: {
    path: "/openshell.v1.OpenShell/GetProvider" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetProviderRequest): Buffer => Buffer.from(GetProviderRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetProviderRequest => GetProviderRequest.decode(value),
    responseSerialize: (value: ProviderResponse): Buffer => Buffer.from(ProviderResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ProviderResponse => ProviderResponse.decode(value),
  },
  /** List providers. */
  listProviders: {
    path: "/openshell.v1.OpenShell/ListProviders" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListProvidersRequest): Buffer => Buffer.from(ListProvidersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListProvidersRequest => ListProvidersRequest.decode(value),
    responseSerialize: (value: ListProvidersResponse): Buffer =>
      Buffer.from(ListProvidersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListProvidersResponse => ListProvidersResponse.decode(value),
  },
  /** List available provider type profiles. */
  listProviderProfiles: {
    path: "/openshell.v1.OpenShell/ListProviderProfiles" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListProviderProfilesRequest): Buffer =>
      Buffer.from(ListProviderProfilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListProviderProfilesRequest => ListProviderProfilesRequest.decode(value),
    responseSerialize: (value: ListProviderProfilesResponse): Buffer =>
      Buffer.from(ListProviderProfilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListProviderProfilesResponse => ListProviderProfilesResponse.decode(value),
  },
  /** Fetch one provider type profile by id. */
  getProviderProfile: {
    path: "/openshell.v1.OpenShell/GetProviderProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetProviderProfileRequest): Buffer =>
      Buffer.from(GetProviderProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetProviderProfileRequest => GetProviderProfileRequest.decode(value),
    responseSerialize: (value: ProviderProfileResponse): Buffer =>
      Buffer.from(ProviderProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ProviderProfileResponse => ProviderProfileResponse.decode(value),
  },
  /** Import custom provider type profiles. */
  importProviderProfiles: {
    path: "/openshell.v1.OpenShell/ImportProviderProfiles" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ImportProviderProfilesRequest): Buffer =>
      Buffer.from(ImportProviderProfilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ImportProviderProfilesRequest => ImportProviderProfilesRequest.decode(value),
    responseSerialize: (value: ImportProviderProfilesResponse): Buffer =>
      Buffer.from(ImportProviderProfilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ImportProviderProfilesResponse =>
      ImportProviderProfilesResponse.decode(value),
  },
  /** Validate provider type profiles without registering them. */
  lintProviderProfiles: {
    path: "/openshell.v1.OpenShell/LintProviderProfiles" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: LintProviderProfilesRequest): Buffer =>
      Buffer.from(LintProviderProfilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): LintProviderProfilesRequest => LintProviderProfilesRequest.decode(value),
    responseSerialize: (value: LintProviderProfilesResponse): Buffer =>
      Buffer.from(LintProviderProfilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): LintProviderProfilesResponse => LintProviderProfilesResponse.decode(value),
  },
  /** Update an existing provider by name. */
  updateProvider: {
    path: "/openshell.v1.OpenShell/UpdateProvider" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateProviderRequest): Buffer =>
      Buffer.from(UpdateProviderRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateProviderRequest => UpdateProviderRequest.decode(value),
    responseSerialize: (value: ProviderResponse): Buffer => Buffer.from(ProviderResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ProviderResponse => ProviderResponse.decode(value),
  },
  /** Fetch refresh status for one provider or provider credential. */
  getProviderRefreshStatus: {
    path: "/openshell.v1.OpenShell/GetProviderRefreshStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetProviderRefreshStatusRequest): Buffer =>
      Buffer.from(GetProviderRefreshStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetProviderRefreshStatusRequest =>
      GetProviderRefreshStatusRequest.decode(value),
    responseSerialize: (value: GetProviderRefreshStatusResponse): Buffer =>
      Buffer.from(GetProviderRefreshStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetProviderRefreshStatusResponse =>
      GetProviderRefreshStatusResponse.decode(value),
  },
  /** Configure gateway-owned refresh material for one provider credential. */
  configureProviderRefresh: {
    path: "/openshell.v1.OpenShell/ConfigureProviderRefresh" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ConfigureProviderRefreshRequest): Buffer =>
      Buffer.from(ConfigureProviderRefreshRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ConfigureProviderRefreshRequest =>
      ConfigureProviderRefreshRequest.decode(value),
    responseSerialize: (value: ConfigureProviderRefreshResponse): Buffer =>
      Buffer.from(ConfigureProviderRefreshResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ConfigureProviderRefreshResponse =>
      ConfigureProviderRefreshResponse.decode(value),
  },
  /** Record a gateway-owned refresh request for one provider credential. */
  rotateProviderCredential: {
    path: "/openshell.v1.OpenShell/RotateProviderCredential" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RotateProviderCredentialRequest): Buffer =>
      Buffer.from(RotateProviderCredentialRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RotateProviderCredentialRequest =>
      RotateProviderCredentialRequest.decode(value),
    responseSerialize: (value: RotateProviderCredentialResponse): Buffer =>
      Buffer.from(RotateProviderCredentialResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RotateProviderCredentialResponse =>
      RotateProviderCredentialResponse.decode(value),
  },
  /** Delete gateway-owned refresh configuration for one provider credential. */
  deleteProviderRefresh: {
    path: "/openshell.v1.OpenShell/DeleteProviderRefresh" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteProviderRefreshRequest): Buffer =>
      Buffer.from(DeleteProviderRefreshRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteProviderRefreshRequest => DeleteProviderRefreshRequest.decode(value),
    responseSerialize: (value: DeleteProviderRefreshResponse): Buffer =>
      Buffer.from(DeleteProviderRefreshResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteProviderRefreshResponse => DeleteProviderRefreshResponse.decode(value),
  },
  /** Delete a provider by name. */
  deleteProvider: {
    path: "/openshell.v1.OpenShell/DeleteProvider" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteProviderRequest): Buffer =>
      Buffer.from(DeleteProviderRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteProviderRequest => DeleteProviderRequest.decode(value),
    responseSerialize: (value: DeleteProviderResponse): Buffer =>
      Buffer.from(DeleteProviderResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteProviderResponse => DeleteProviderResponse.decode(value),
  },
  /** Delete a custom provider type profile by id. */
  deleteProviderProfile: {
    path: "/openshell.v1.OpenShell/DeleteProviderProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteProviderProfileRequest): Buffer =>
      Buffer.from(DeleteProviderProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteProviderProfileRequest => DeleteProviderProfileRequest.decode(value),
    responseSerialize: (value: DeleteProviderProfileResponse): Buffer =>
      Buffer.from(DeleteProviderProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteProviderProfileResponse => DeleteProviderProfileResponse.decode(value),
  },
  /** Get sandbox settings by id (called by sandbox entrypoint and poll loop). */
  getSandboxConfig: {
    path: "/openshell.v1.OpenShell/GetSandboxConfig" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSandboxConfigRequest): Buffer =>
      Buffer.from(GetSandboxConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSandboxConfigRequest => GetSandboxConfigRequest.decode(value),
    responseSerialize: (value: GetSandboxConfigResponse): Buffer =>
      Buffer.from(GetSandboxConfigResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetSandboxConfigResponse => GetSandboxConfigResponse.decode(value),
  },
  /** Get gateway-global settings. */
  getGatewayConfig: {
    path: "/openshell.v1.OpenShell/GetGatewayConfig" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetGatewayConfigRequest): Buffer =>
      Buffer.from(GetGatewayConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetGatewayConfigRequest => GetGatewayConfigRequest.decode(value),
    responseSerialize: (value: GetGatewayConfigResponse): Buffer =>
      Buffer.from(GetGatewayConfigResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetGatewayConfigResponse => GetGatewayConfigResponse.decode(value),
  },
  /** Update settings or policy at sandbox or global scope. */
  updateConfig: {
    path: "/openshell.v1.OpenShell/UpdateConfig" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateConfigRequest): Buffer => Buffer.from(UpdateConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateConfigRequest => UpdateConfigRequest.decode(value),
    responseSerialize: (value: UpdateConfigResponse): Buffer =>
      Buffer.from(UpdateConfigResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateConfigResponse => UpdateConfigResponse.decode(value),
  },
  /** Get the load status of a specific policy version. */
  getSandboxPolicyStatus: {
    path: "/openshell.v1.OpenShell/GetSandboxPolicyStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSandboxPolicyStatusRequest): Buffer =>
      Buffer.from(GetSandboxPolicyStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSandboxPolicyStatusRequest => GetSandboxPolicyStatusRequest.decode(value),
    responseSerialize: (value: GetSandboxPolicyStatusResponse): Buffer =>
      Buffer.from(GetSandboxPolicyStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetSandboxPolicyStatusResponse =>
      GetSandboxPolicyStatusResponse.decode(value),
  },
  /** List policy history for a sandbox. */
  listSandboxPolicies: {
    path: "/openshell.v1.OpenShell/ListSandboxPolicies" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSandboxPoliciesRequest): Buffer =>
      Buffer.from(ListSandboxPoliciesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSandboxPoliciesRequest => ListSandboxPoliciesRequest.decode(value),
    responseSerialize: (value: ListSandboxPoliciesResponse): Buffer =>
      Buffer.from(ListSandboxPoliciesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSandboxPoliciesResponse => ListSandboxPoliciesResponse.decode(value),
  },
  /** Report policy load result (called by sandbox after reload attempt). */
  reportPolicyStatus: {
    path: "/openshell.v1.OpenShell/ReportPolicyStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReportPolicyStatusRequest): Buffer =>
      Buffer.from(ReportPolicyStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReportPolicyStatusRequest => ReportPolicyStatusRequest.decode(value),
    responseSerialize: (value: ReportPolicyStatusResponse): Buffer =>
      Buffer.from(ReportPolicyStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReportPolicyStatusResponse => ReportPolicyStatusResponse.decode(value),
  },
  /** Get provider environment for a sandbox (called by sandbox supervisor at startup). */
  getSandboxProviderEnvironment: {
    path: "/openshell.v1.OpenShell/GetSandboxProviderEnvironment" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSandboxProviderEnvironmentRequest): Buffer =>
      Buffer.from(GetSandboxProviderEnvironmentRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSandboxProviderEnvironmentRequest =>
      GetSandboxProviderEnvironmentRequest.decode(value),
    responseSerialize: (value: GetSandboxProviderEnvironmentResponse): Buffer =>
      Buffer.from(GetSandboxProviderEnvironmentResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetSandboxProviderEnvironmentResponse =>
      GetSandboxProviderEnvironmentResponse.decode(value),
  },
  /** Fetch recent sandbox logs (one-shot). */
  getSandboxLogs: {
    path: "/openshell.v1.OpenShell/GetSandboxLogs" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSandboxLogsRequest): Buffer =>
      Buffer.from(GetSandboxLogsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSandboxLogsRequest => GetSandboxLogsRequest.decode(value),
    responseSerialize: (value: GetSandboxLogsResponse): Buffer =>
      Buffer.from(GetSandboxLogsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetSandboxLogsResponse => GetSandboxLogsResponse.decode(value),
  },
  /** Push sandbox supervisor logs to the server (client-streaming). */
  pushSandboxLogs: {
    path: "/openshell.v1.OpenShell/PushSandboxLogs" as const,
    requestStream: true as const,
    responseStream: false as const,
    requestSerialize: (value: PushSandboxLogsRequest): Buffer =>
      Buffer.from(PushSandboxLogsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PushSandboxLogsRequest => PushSandboxLogsRequest.decode(value),
    responseSerialize: (value: PushSandboxLogsResponse): Buffer =>
      Buffer.from(PushSandboxLogsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PushSandboxLogsResponse => PushSandboxLogsResponse.decode(value),
  },
  /**
   * Persistent supervisor-to-gateway session (bidirectional streaming).
   *
   * The supervisor opens this stream at startup and keeps it alive for the
   * sandbox lifetime. The gateway uses it to coordinate relay channels for
   * SSH connect, ExecSandbox, and targetable sandbox services. Raw service
   * bytes flow over RelayStream calls (separate HTTP/2 streams on the same
   * connection), not over this stream.
   */
  connectSupervisor: {
    path: "/openshell.v1.OpenShell/ConnectSupervisor" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: SupervisorMessage): Buffer => Buffer.from(SupervisorMessage.encode(value).finish()),
    requestDeserialize: (value: Buffer): SupervisorMessage => SupervisorMessage.decode(value),
    responseSerialize: (value: GatewayMessage): Buffer => Buffer.from(GatewayMessage.encode(value).finish()),
    responseDeserialize: (value: Buffer): GatewayMessage => GatewayMessage.decode(value),
  },
  /**
   * Raw byte relay between supervisor and gateway.
   *
   * The supervisor initiates this call after receiving a RelayOpen message
   * on its ConnectSupervisor stream. The first RelayFrame carries a
   * RelayInit with the channel_id to associate the new HTTP/2 stream with
   * the pending relay slot on the gateway. Subsequent frames carry raw bytes in either
   * direction between the gateway-side waiter (ForwardTcp / exec handler)
   * and the supervisor-side target bridge.
   *
   * This rides the same TCP+TLS+HTTP/2 connection as ConnectSupervisor —
   * no new TLS handshake, no reverse HTTP CONNECT.
   */
  relayStream: {
    path: "/openshell.v1.OpenShell/RelayStream" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: RelayFrame): Buffer => Buffer.from(RelayFrame.encode(value).finish()),
    requestDeserialize: (value: Buffer): RelayFrame => RelayFrame.decode(value),
    responseSerialize: (value: RelayFrame): Buffer => Buffer.from(RelayFrame.encode(value).finish()),
    responseDeserialize: (value: Buffer): RelayFrame => RelayFrame.decode(value),
  },
  /**
   * Watch a sandbox and stream updates.
   *
   * This stream can include:
   * - Sandbox status snapshots (phase/status)
   * - OpenShell server process logs correlated by sandbox_id
   * - Platform events correlated to the sandbox
   */
  watchSandbox: {
    path: "/openshell.v1.OpenShell/WatchSandbox" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: WatchSandboxRequest): Buffer => Buffer.from(WatchSandboxRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WatchSandboxRequest => WatchSandboxRequest.decode(value),
    responseSerialize: (value: SandboxStreamEvent): Buffer => Buffer.from(SandboxStreamEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer): SandboxStreamEvent => SandboxStreamEvent.decode(value),
  },
  /** Submit denial analysis results from sandbox (summaries + proposed chunks). */
  submitPolicyAnalysis: {
    path: "/openshell.v1.OpenShell/SubmitPolicyAnalysis" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SubmitPolicyAnalysisRequest): Buffer =>
      Buffer.from(SubmitPolicyAnalysisRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SubmitPolicyAnalysisRequest => SubmitPolicyAnalysisRequest.decode(value),
    responseSerialize: (value: SubmitPolicyAnalysisResponse): Buffer =>
      Buffer.from(SubmitPolicyAnalysisResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SubmitPolicyAnalysisResponse => SubmitPolicyAnalysisResponse.decode(value),
  },
  /** Get draft policy recommendations for a sandbox. */
  getDraftPolicy: {
    path: "/openshell.v1.OpenShell/GetDraftPolicy" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetDraftPolicyRequest): Buffer =>
      Buffer.from(GetDraftPolicyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetDraftPolicyRequest => GetDraftPolicyRequest.decode(value),
    responseSerialize: (value: GetDraftPolicyResponse): Buffer =>
      Buffer.from(GetDraftPolicyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetDraftPolicyResponse => GetDraftPolicyResponse.decode(value),
  },
  /** Approve a single draft policy chunk (merges into active policy). */
  approveDraftChunk: {
    path: "/openshell.v1.OpenShell/ApproveDraftChunk" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ApproveDraftChunkRequest): Buffer =>
      Buffer.from(ApproveDraftChunkRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ApproveDraftChunkRequest => ApproveDraftChunkRequest.decode(value),
    responseSerialize: (value: ApproveDraftChunkResponse): Buffer =>
      Buffer.from(ApproveDraftChunkResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ApproveDraftChunkResponse => ApproveDraftChunkResponse.decode(value),
  },
  /** Reject a single draft policy chunk. */
  rejectDraftChunk: {
    path: "/openshell.v1.OpenShell/RejectDraftChunk" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RejectDraftChunkRequest): Buffer =>
      Buffer.from(RejectDraftChunkRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RejectDraftChunkRequest => RejectDraftChunkRequest.decode(value),
    responseSerialize: (value: RejectDraftChunkResponse): Buffer =>
      Buffer.from(RejectDraftChunkResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RejectDraftChunkResponse => RejectDraftChunkResponse.decode(value),
  },
  /** Approve all pending draft chunks (skips security-flagged unless forced). */
  approveAllDraftChunks: {
    path: "/openshell.v1.OpenShell/ApproveAllDraftChunks" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ApproveAllDraftChunksRequest): Buffer =>
      Buffer.from(ApproveAllDraftChunksRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ApproveAllDraftChunksRequest => ApproveAllDraftChunksRequest.decode(value),
    responseSerialize: (value: ApproveAllDraftChunksResponse): Buffer =>
      Buffer.from(ApproveAllDraftChunksResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ApproveAllDraftChunksResponse => ApproveAllDraftChunksResponse.decode(value),
  },
  /** Edit a pending draft chunk in-place (e.g. narrow allowed_ips). */
  editDraftChunk: {
    path: "/openshell.v1.OpenShell/EditDraftChunk" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: EditDraftChunkRequest): Buffer =>
      Buffer.from(EditDraftChunkRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): EditDraftChunkRequest => EditDraftChunkRequest.decode(value),
    responseSerialize: (value: EditDraftChunkResponse): Buffer =>
      Buffer.from(EditDraftChunkResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): EditDraftChunkResponse => EditDraftChunkResponse.decode(value),
  },
  /** Reverse an approval (remove merged rule from active policy). */
  undoDraftChunk: {
    path: "/openshell.v1.OpenShell/UndoDraftChunk" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UndoDraftChunkRequest): Buffer =>
      Buffer.from(UndoDraftChunkRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UndoDraftChunkRequest => UndoDraftChunkRequest.decode(value),
    responseSerialize: (value: UndoDraftChunkResponse): Buffer =>
      Buffer.from(UndoDraftChunkResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UndoDraftChunkResponse => UndoDraftChunkResponse.decode(value),
  },
  /** Clear all pending draft chunks for a sandbox. */
  clearDraftChunks: {
    path: "/openshell.v1.OpenShell/ClearDraftChunks" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ClearDraftChunksRequest): Buffer =>
      Buffer.from(ClearDraftChunksRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ClearDraftChunksRequest => ClearDraftChunksRequest.decode(value),
    responseSerialize: (value: ClearDraftChunksResponse): Buffer =>
      Buffer.from(ClearDraftChunksResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ClearDraftChunksResponse => ClearDraftChunksResponse.decode(value),
  },
  /** Get decision history for a sandbox's draft policy. */
  getDraftHistory: {
    path: "/openshell.v1.OpenShell/GetDraftHistory" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetDraftHistoryRequest): Buffer =>
      Buffer.from(GetDraftHistoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetDraftHistoryRequest => GetDraftHistoryRequest.decode(value),
    responseSerialize: (value: GetDraftHistoryResponse): Buffer =>
      Buffer.from(GetDraftHistoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetDraftHistoryResponse => GetDraftHistoryResponse.decode(value),
  },
} as const;

export interface OpenShellServer extends UntypedServiceImplementation {
  /** Check the health of the service. */
  health: handleUnaryCall<HealthRequest, HealthResponse>;
  /** Create a new sandbox. */
  createSandbox: handleUnaryCall<CreateSandboxRequest, SandboxResponse>;
  /** Fetch a sandbox by name. */
  getSandbox: handleUnaryCall<GetSandboxRequest, SandboxResponse>;
  /** List sandboxes. */
  listSandboxes: handleUnaryCall<ListSandboxesRequest, ListSandboxesResponse>;
  /** List provider records attached to a sandbox. */
  listSandboxProviders: handleUnaryCall<ListSandboxProvidersRequest, ListSandboxProvidersResponse>;
  /** Attach a provider record to an existing sandbox. */
  attachSandboxProvider: handleUnaryCall<AttachSandboxProviderRequest, AttachSandboxProviderResponse>;
  /** Detach a provider record from an existing sandbox. */
  detachSandboxProvider: handleUnaryCall<DetachSandboxProviderRequest, DetachSandboxProviderResponse>;
  /** Delete a sandbox by name. */
  deleteSandbox: handleUnaryCall<DeleteSandboxRequest, DeleteSandboxResponse>;
  /** Create a short-lived SSH session for a sandbox. */
  createSshSession: handleUnaryCall<CreateSshSessionRequest, CreateSshSessionResponse>;
  /** Create or update a sandbox HTTP service endpoint for local routing. */
  exposeService: handleUnaryCall<ExposeServiceRequest, ServiceEndpointResponse>;
  /** Fetch one sandbox HTTP service endpoint. */
  getService: handleUnaryCall<GetServiceRequest, ServiceEndpointResponse>;
  /** List sandbox HTTP service endpoints. */
  listServices: handleUnaryCall<ListServicesRequest, ListServicesResponse>;
  /** Delete one sandbox HTTP service endpoint. */
  deleteService: handleUnaryCall<DeleteServiceRequest, DeleteServiceResponse>;
  /** Revoke a previously issued SSH session. */
  revokeSshSession: handleUnaryCall<RevokeSshSessionRequest, RevokeSshSessionResponse>;
  /** Execute a command in a ready sandbox and stream output. */
  execSandbox: handleServerStreamingCall<ExecSandboxRequest, ExecSandboxEvent>;
  /** Forward one CLI-side TCP connection to a loopback TCP target in a sandbox. */
  forwardTcp: handleBidiStreamingCall<TcpForwardFrame, TcpForwardFrame>;
  /**
   * Execute an interactive command with bidirectional stdin/stdout streaming.
   * The first client message MUST carry an ExecSandboxInput with the start
   * variant. Subsequent messages carry stdin bytes or window resize events.
   */
  execSandboxInteractive: handleBidiStreamingCall<ExecSandboxInput, ExecSandboxEvent>;
  /** Create a provider. */
  createProvider: handleUnaryCall<CreateProviderRequest, ProviderResponse>;
  /** Fetch a provider by name. */
  getProvider: handleUnaryCall<GetProviderRequest, ProviderResponse>;
  /** List providers. */
  listProviders: handleUnaryCall<ListProvidersRequest, ListProvidersResponse>;
  /** List available provider type profiles. */
  listProviderProfiles: handleUnaryCall<ListProviderProfilesRequest, ListProviderProfilesResponse>;
  /** Fetch one provider type profile by id. */
  getProviderProfile: handleUnaryCall<GetProviderProfileRequest, ProviderProfileResponse>;
  /** Import custom provider type profiles. */
  importProviderProfiles: handleUnaryCall<ImportProviderProfilesRequest, ImportProviderProfilesResponse>;
  /** Validate provider type profiles without registering them. */
  lintProviderProfiles: handleUnaryCall<LintProviderProfilesRequest, LintProviderProfilesResponse>;
  /** Update an existing provider by name. */
  updateProvider: handleUnaryCall<UpdateProviderRequest, ProviderResponse>;
  /** Fetch refresh status for one provider or provider credential. */
  getProviderRefreshStatus: handleUnaryCall<GetProviderRefreshStatusRequest, GetProviderRefreshStatusResponse>;
  /** Configure gateway-owned refresh material for one provider credential. */
  configureProviderRefresh: handleUnaryCall<ConfigureProviderRefreshRequest, ConfigureProviderRefreshResponse>;
  /** Record a gateway-owned refresh request for one provider credential. */
  rotateProviderCredential: handleUnaryCall<RotateProviderCredentialRequest, RotateProviderCredentialResponse>;
  /** Delete gateway-owned refresh configuration for one provider credential. */
  deleteProviderRefresh: handleUnaryCall<DeleteProviderRefreshRequest, DeleteProviderRefreshResponse>;
  /** Delete a provider by name. */
  deleteProvider: handleUnaryCall<DeleteProviderRequest, DeleteProviderResponse>;
  /** Delete a custom provider type profile by id. */
  deleteProviderProfile: handleUnaryCall<DeleteProviderProfileRequest, DeleteProviderProfileResponse>;
  /** Get sandbox settings by id (called by sandbox entrypoint and poll loop). */
  getSandboxConfig: handleUnaryCall<GetSandboxConfigRequest, GetSandboxConfigResponse>;
  /** Get gateway-global settings. */
  getGatewayConfig: handleUnaryCall<GetGatewayConfigRequest, GetGatewayConfigResponse>;
  /** Update settings or policy at sandbox or global scope. */
  updateConfig: handleUnaryCall<UpdateConfigRequest, UpdateConfigResponse>;
  /** Get the load status of a specific policy version. */
  getSandboxPolicyStatus: handleUnaryCall<GetSandboxPolicyStatusRequest, GetSandboxPolicyStatusResponse>;
  /** List policy history for a sandbox. */
  listSandboxPolicies: handleUnaryCall<ListSandboxPoliciesRequest, ListSandboxPoliciesResponse>;
  /** Report policy load result (called by sandbox after reload attempt). */
  reportPolicyStatus: handleUnaryCall<ReportPolicyStatusRequest, ReportPolicyStatusResponse>;
  /** Get provider environment for a sandbox (called by sandbox supervisor at startup). */
  getSandboxProviderEnvironment: handleUnaryCall<
    GetSandboxProviderEnvironmentRequest,
    GetSandboxProviderEnvironmentResponse
  >;
  /** Fetch recent sandbox logs (one-shot). */
  getSandboxLogs: handleUnaryCall<GetSandboxLogsRequest, GetSandboxLogsResponse>;
  /** Push sandbox supervisor logs to the server (client-streaming). */
  pushSandboxLogs: handleClientStreamingCall<PushSandboxLogsRequest, PushSandboxLogsResponse>;
  /**
   * Persistent supervisor-to-gateway session (bidirectional streaming).
   *
   * The supervisor opens this stream at startup and keeps it alive for the
   * sandbox lifetime. The gateway uses it to coordinate relay channels for
   * SSH connect, ExecSandbox, and targetable sandbox services. Raw service
   * bytes flow over RelayStream calls (separate HTTP/2 streams on the same
   * connection), not over this stream.
   */
  connectSupervisor: handleBidiStreamingCall<SupervisorMessage, GatewayMessage>;
  /**
   * Raw byte relay between supervisor and gateway.
   *
   * The supervisor initiates this call after receiving a RelayOpen message
   * on its ConnectSupervisor stream. The first RelayFrame carries a
   * RelayInit with the channel_id to associate the new HTTP/2 stream with
   * the pending relay slot on the gateway. Subsequent frames carry raw bytes in either
   * direction between the gateway-side waiter (ForwardTcp / exec handler)
   * and the supervisor-side target bridge.
   *
   * This rides the same TCP+TLS+HTTP/2 connection as ConnectSupervisor —
   * no new TLS handshake, no reverse HTTP CONNECT.
   */
  relayStream: handleBidiStreamingCall<RelayFrame, RelayFrame>;
  /**
   * Watch a sandbox and stream updates.
   *
   * This stream can include:
   * - Sandbox status snapshots (phase/status)
   * - OpenShell server process logs correlated by sandbox_id
   * - Platform events correlated to the sandbox
   */
  watchSandbox: handleServerStreamingCall<WatchSandboxRequest, SandboxStreamEvent>;
  /** Submit denial analysis results from sandbox (summaries + proposed chunks). */
  submitPolicyAnalysis: handleUnaryCall<SubmitPolicyAnalysisRequest, SubmitPolicyAnalysisResponse>;
  /** Get draft policy recommendations for a sandbox. */
  getDraftPolicy: handleUnaryCall<GetDraftPolicyRequest, GetDraftPolicyResponse>;
  /** Approve a single draft policy chunk (merges into active policy). */
  approveDraftChunk: handleUnaryCall<ApproveDraftChunkRequest, ApproveDraftChunkResponse>;
  /** Reject a single draft policy chunk. */
  rejectDraftChunk: handleUnaryCall<RejectDraftChunkRequest, RejectDraftChunkResponse>;
  /** Approve all pending draft chunks (skips security-flagged unless forced). */
  approveAllDraftChunks: handleUnaryCall<ApproveAllDraftChunksRequest, ApproveAllDraftChunksResponse>;
  /** Edit a pending draft chunk in-place (e.g. narrow allowed_ips). */
  editDraftChunk: handleUnaryCall<EditDraftChunkRequest, EditDraftChunkResponse>;
  /** Reverse an approval (remove merged rule from active policy). */
  undoDraftChunk: handleUnaryCall<UndoDraftChunkRequest, UndoDraftChunkResponse>;
  /** Clear all pending draft chunks for a sandbox. */
  clearDraftChunks: handleUnaryCall<ClearDraftChunksRequest, ClearDraftChunksResponse>;
  /** Get decision history for a sandbox's draft policy. */
  getDraftHistory: handleUnaryCall<GetDraftHistoryRequest, GetDraftHistoryResponse>;
}

export interface OpenShellClient extends Client {
  /** Check the health of the service. */
  health(
    request: HealthRequest,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  /** Create a new sandbox. */
  createSandbox(
    request: CreateSandboxRequest,
    callback: (error: ServiceError | null, response: SandboxResponse) => void,
  ): ClientUnaryCall;
  createSandbox(
    request: CreateSandboxRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SandboxResponse) => void,
  ): ClientUnaryCall;
  createSandbox(
    request: CreateSandboxRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SandboxResponse) => void,
  ): ClientUnaryCall;
  /** Fetch a sandbox by name. */
  getSandbox(
    request: GetSandboxRequest,
    callback: (error: ServiceError | null, response: SandboxResponse) => void,
  ): ClientUnaryCall;
  getSandbox(
    request: GetSandboxRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SandboxResponse) => void,
  ): ClientUnaryCall;
  getSandbox(
    request: GetSandboxRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SandboxResponse) => void,
  ): ClientUnaryCall;
  /** List sandboxes. */
  listSandboxes(
    request: ListSandboxesRequest,
    callback: (error: ServiceError | null, response: ListSandboxesResponse) => void,
  ): ClientUnaryCall;
  listSandboxes(
    request: ListSandboxesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSandboxesResponse) => void,
  ): ClientUnaryCall;
  listSandboxes(
    request: ListSandboxesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSandboxesResponse) => void,
  ): ClientUnaryCall;
  /** List provider records attached to a sandbox. */
  listSandboxProviders(
    request: ListSandboxProvidersRequest,
    callback: (error: ServiceError | null, response: ListSandboxProvidersResponse) => void,
  ): ClientUnaryCall;
  listSandboxProviders(
    request: ListSandboxProvidersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSandboxProvidersResponse) => void,
  ): ClientUnaryCall;
  listSandboxProviders(
    request: ListSandboxProvidersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSandboxProvidersResponse) => void,
  ): ClientUnaryCall;
  /** Attach a provider record to an existing sandbox. */
  attachSandboxProvider(
    request: AttachSandboxProviderRequest,
    callback: (error: ServiceError | null, response: AttachSandboxProviderResponse) => void,
  ): ClientUnaryCall;
  attachSandboxProvider(
    request: AttachSandboxProviderRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AttachSandboxProviderResponse) => void,
  ): ClientUnaryCall;
  attachSandboxProvider(
    request: AttachSandboxProviderRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AttachSandboxProviderResponse) => void,
  ): ClientUnaryCall;
  /** Detach a provider record from an existing sandbox. */
  detachSandboxProvider(
    request: DetachSandboxProviderRequest,
    callback: (error: ServiceError | null, response: DetachSandboxProviderResponse) => void,
  ): ClientUnaryCall;
  detachSandboxProvider(
    request: DetachSandboxProviderRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DetachSandboxProviderResponse) => void,
  ): ClientUnaryCall;
  detachSandboxProvider(
    request: DetachSandboxProviderRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DetachSandboxProviderResponse) => void,
  ): ClientUnaryCall;
  /** Delete a sandbox by name. */
  deleteSandbox(
    request: DeleteSandboxRequest,
    callback: (error: ServiceError | null, response: DeleteSandboxResponse) => void,
  ): ClientUnaryCall;
  deleteSandbox(
    request: DeleteSandboxRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteSandboxResponse) => void,
  ): ClientUnaryCall;
  deleteSandbox(
    request: DeleteSandboxRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteSandboxResponse) => void,
  ): ClientUnaryCall;
  /** Create a short-lived SSH session for a sandbox. */
  createSshSession(
    request: CreateSshSessionRequest,
    callback: (error: ServiceError | null, response: CreateSshSessionResponse) => void,
  ): ClientUnaryCall;
  createSshSession(
    request: CreateSshSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateSshSessionResponse) => void,
  ): ClientUnaryCall;
  createSshSession(
    request: CreateSshSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateSshSessionResponse) => void,
  ): ClientUnaryCall;
  /** Create or update a sandbox HTTP service endpoint for local routing. */
  exposeService(
    request: ExposeServiceRequest,
    callback: (error: ServiceError | null, response: ServiceEndpointResponse) => void,
  ): ClientUnaryCall;
  exposeService(
    request: ExposeServiceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ServiceEndpointResponse) => void,
  ): ClientUnaryCall;
  exposeService(
    request: ExposeServiceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ServiceEndpointResponse) => void,
  ): ClientUnaryCall;
  /** Fetch one sandbox HTTP service endpoint. */
  getService(
    request: GetServiceRequest,
    callback: (error: ServiceError | null, response: ServiceEndpointResponse) => void,
  ): ClientUnaryCall;
  getService(
    request: GetServiceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ServiceEndpointResponse) => void,
  ): ClientUnaryCall;
  getService(
    request: GetServiceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ServiceEndpointResponse) => void,
  ): ClientUnaryCall;
  /** List sandbox HTTP service endpoints. */
  listServices(
    request: ListServicesRequest,
    callback: (error: ServiceError | null, response: ListServicesResponse) => void,
  ): ClientUnaryCall;
  listServices(
    request: ListServicesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListServicesResponse) => void,
  ): ClientUnaryCall;
  listServices(
    request: ListServicesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListServicesResponse) => void,
  ): ClientUnaryCall;
  /** Delete one sandbox HTTP service endpoint. */
  deleteService(
    request: DeleteServiceRequest,
    callback: (error: ServiceError | null, response: DeleteServiceResponse) => void,
  ): ClientUnaryCall;
  deleteService(
    request: DeleteServiceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteServiceResponse) => void,
  ): ClientUnaryCall;
  deleteService(
    request: DeleteServiceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteServiceResponse) => void,
  ): ClientUnaryCall;
  /** Revoke a previously issued SSH session. */
  revokeSshSession(
    request: RevokeSshSessionRequest,
    callback: (error: ServiceError | null, response: RevokeSshSessionResponse) => void,
  ): ClientUnaryCall;
  revokeSshSession(
    request: RevokeSshSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RevokeSshSessionResponse) => void,
  ): ClientUnaryCall;
  revokeSshSession(
    request: RevokeSshSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RevokeSshSessionResponse) => void,
  ): ClientUnaryCall;
  /** Execute a command in a ready sandbox and stream output. */
  execSandbox(request: ExecSandboxRequest, options?: Partial<CallOptions>): ClientReadableStream<ExecSandboxEvent>;
  execSandbox(
    request: ExecSandboxRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<ExecSandboxEvent>;
  /** Forward one CLI-side TCP connection to a loopback TCP target in a sandbox. */
  forwardTcp(): ClientDuplexStream<TcpForwardFrame, TcpForwardFrame>;
  forwardTcp(options: Partial<CallOptions>): ClientDuplexStream<TcpForwardFrame, TcpForwardFrame>;
  forwardTcp(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<TcpForwardFrame, TcpForwardFrame>;
  /**
   * Execute an interactive command with bidirectional stdin/stdout streaming.
   * The first client message MUST carry an ExecSandboxInput with the start
   * variant. Subsequent messages carry stdin bytes or window resize events.
   */
  execSandboxInteractive(): ClientDuplexStream<ExecSandboxInput, ExecSandboxEvent>;
  execSandboxInteractive(options: Partial<CallOptions>): ClientDuplexStream<ExecSandboxInput, ExecSandboxEvent>;
  execSandboxInteractive(
    metadata: Metadata,
    options?: Partial<CallOptions>,
  ): ClientDuplexStream<ExecSandboxInput, ExecSandboxEvent>;
  /** Create a provider. */
  createProvider(
    request: CreateProviderRequest,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  createProvider(
    request: CreateProviderRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  createProvider(
    request: CreateProviderRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  /** Fetch a provider by name. */
  getProvider(
    request: GetProviderRequest,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  getProvider(
    request: GetProviderRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  getProvider(
    request: GetProviderRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  /** List providers. */
  listProviders(
    request: ListProvidersRequest,
    callback: (error: ServiceError | null, response: ListProvidersResponse) => void,
  ): ClientUnaryCall;
  listProviders(
    request: ListProvidersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListProvidersResponse) => void,
  ): ClientUnaryCall;
  listProviders(
    request: ListProvidersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListProvidersResponse) => void,
  ): ClientUnaryCall;
  /** List available provider type profiles. */
  listProviderProfiles(
    request: ListProviderProfilesRequest,
    callback: (error: ServiceError | null, response: ListProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  listProviderProfiles(
    request: ListProviderProfilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  listProviderProfiles(
    request: ListProviderProfilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  /** Fetch one provider type profile by id. */
  getProviderProfile(
    request: GetProviderProfileRequest,
    callback: (error: ServiceError | null, response: ProviderProfileResponse) => void,
  ): ClientUnaryCall;
  getProviderProfile(
    request: GetProviderProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ProviderProfileResponse) => void,
  ): ClientUnaryCall;
  getProviderProfile(
    request: GetProviderProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ProviderProfileResponse) => void,
  ): ClientUnaryCall;
  /** Import custom provider type profiles. */
  importProviderProfiles(
    request: ImportProviderProfilesRequest,
    callback: (error: ServiceError | null, response: ImportProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  importProviderProfiles(
    request: ImportProviderProfilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ImportProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  importProviderProfiles(
    request: ImportProviderProfilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ImportProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  /** Validate provider type profiles without registering them. */
  lintProviderProfiles(
    request: LintProviderProfilesRequest,
    callback: (error: ServiceError | null, response: LintProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  lintProviderProfiles(
    request: LintProviderProfilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LintProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  lintProviderProfiles(
    request: LintProviderProfilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LintProviderProfilesResponse) => void,
  ): ClientUnaryCall;
  /** Update an existing provider by name. */
  updateProvider(
    request: UpdateProviderRequest,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  updateProvider(
    request: UpdateProviderRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  updateProvider(
    request: UpdateProviderRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ProviderResponse) => void,
  ): ClientUnaryCall;
  /** Fetch refresh status for one provider or provider credential. */
  getProviderRefreshStatus(
    request: GetProviderRefreshStatusRequest,
    callback: (error: ServiceError | null, response: GetProviderRefreshStatusResponse) => void,
  ): ClientUnaryCall;
  getProviderRefreshStatus(
    request: GetProviderRefreshStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetProviderRefreshStatusResponse) => void,
  ): ClientUnaryCall;
  getProviderRefreshStatus(
    request: GetProviderRefreshStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetProviderRefreshStatusResponse) => void,
  ): ClientUnaryCall;
  /** Configure gateway-owned refresh material for one provider credential. */
  configureProviderRefresh(
    request: ConfigureProviderRefreshRequest,
    callback: (error: ServiceError | null, response: ConfigureProviderRefreshResponse) => void,
  ): ClientUnaryCall;
  configureProviderRefresh(
    request: ConfigureProviderRefreshRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ConfigureProviderRefreshResponse) => void,
  ): ClientUnaryCall;
  configureProviderRefresh(
    request: ConfigureProviderRefreshRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ConfigureProviderRefreshResponse) => void,
  ): ClientUnaryCall;
  /** Record a gateway-owned refresh request for one provider credential. */
  rotateProviderCredential(
    request: RotateProviderCredentialRequest,
    callback: (error: ServiceError | null, response: RotateProviderCredentialResponse) => void,
  ): ClientUnaryCall;
  rotateProviderCredential(
    request: RotateProviderCredentialRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RotateProviderCredentialResponse) => void,
  ): ClientUnaryCall;
  rotateProviderCredential(
    request: RotateProviderCredentialRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RotateProviderCredentialResponse) => void,
  ): ClientUnaryCall;
  /** Delete gateway-owned refresh configuration for one provider credential. */
  deleteProviderRefresh(
    request: DeleteProviderRefreshRequest,
    callback: (error: ServiceError | null, response: DeleteProviderRefreshResponse) => void,
  ): ClientUnaryCall;
  deleteProviderRefresh(
    request: DeleteProviderRefreshRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteProviderRefreshResponse) => void,
  ): ClientUnaryCall;
  deleteProviderRefresh(
    request: DeleteProviderRefreshRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteProviderRefreshResponse) => void,
  ): ClientUnaryCall;
  /** Delete a provider by name. */
  deleteProvider(
    request: DeleteProviderRequest,
    callback: (error: ServiceError | null, response: DeleteProviderResponse) => void,
  ): ClientUnaryCall;
  deleteProvider(
    request: DeleteProviderRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteProviderResponse) => void,
  ): ClientUnaryCall;
  deleteProvider(
    request: DeleteProviderRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteProviderResponse) => void,
  ): ClientUnaryCall;
  /** Delete a custom provider type profile by id. */
  deleteProviderProfile(
    request: DeleteProviderProfileRequest,
    callback: (error: ServiceError | null, response: DeleteProviderProfileResponse) => void,
  ): ClientUnaryCall;
  deleteProviderProfile(
    request: DeleteProviderProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteProviderProfileResponse) => void,
  ): ClientUnaryCall;
  deleteProviderProfile(
    request: DeleteProviderProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteProviderProfileResponse) => void,
  ): ClientUnaryCall;
  /** Get sandbox settings by id (called by sandbox entrypoint and poll loop). */
  getSandboxConfig(
    request: GetSandboxConfigRequest,
    callback: (error: ServiceError | null, response: GetSandboxConfigResponse) => void,
  ): ClientUnaryCall;
  getSandboxConfig(
    request: GetSandboxConfigRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetSandboxConfigResponse) => void,
  ): ClientUnaryCall;
  getSandboxConfig(
    request: GetSandboxConfigRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetSandboxConfigResponse) => void,
  ): ClientUnaryCall;
  /** Get gateway-global settings. */
  getGatewayConfig(
    request: GetGatewayConfigRequest,
    callback: (error: ServiceError | null, response: GetGatewayConfigResponse) => void,
  ): ClientUnaryCall;
  getGatewayConfig(
    request: GetGatewayConfigRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetGatewayConfigResponse) => void,
  ): ClientUnaryCall;
  getGatewayConfig(
    request: GetGatewayConfigRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetGatewayConfigResponse) => void,
  ): ClientUnaryCall;
  /** Update settings or policy at sandbox or global scope. */
  updateConfig(
    request: UpdateConfigRequest,
    callback: (error: ServiceError | null, response: UpdateConfigResponse) => void,
  ): ClientUnaryCall;
  updateConfig(
    request: UpdateConfigRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateConfigResponse) => void,
  ): ClientUnaryCall;
  updateConfig(
    request: UpdateConfigRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateConfigResponse) => void,
  ): ClientUnaryCall;
  /** Get the load status of a specific policy version. */
  getSandboxPolicyStatus(
    request: GetSandboxPolicyStatusRequest,
    callback: (error: ServiceError | null, response: GetSandboxPolicyStatusResponse) => void,
  ): ClientUnaryCall;
  getSandboxPolicyStatus(
    request: GetSandboxPolicyStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetSandboxPolicyStatusResponse) => void,
  ): ClientUnaryCall;
  getSandboxPolicyStatus(
    request: GetSandboxPolicyStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetSandboxPolicyStatusResponse) => void,
  ): ClientUnaryCall;
  /** List policy history for a sandbox. */
  listSandboxPolicies(
    request: ListSandboxPoliciesRequest,
    callback: (error: ServiceError | null, response: ListSandboxPoliciesResponse) => void,
  ): ClientUnaryCall;
  listSandboxPolicies(
    request: ListSandboxPoliciesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSandboxPoliciesResponse) => void,
  ): ClientUnaryCall;
  listSandboxPolicies(
    request: ListSandboxPoliciesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSandboxPoliciesResponse) => void,
  ): ClientUnaryCall;
  /** Report policy load result (called by sandbox after reload attempt). */
  reportPolicyStatus(
    request: ReportPolicyStatusRequest,
    callback: (error: ServiceError | null, response: ReportPolicyStatusResponse) => void,
  ): ClientUnaryCall;
  reportPolicyStatus(
    request: ReportPolicyStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ReportPolicyStatusResponse) => void,
  ): ClientUnaryCall;
  reportPolicyStatus(
    request: ReportPolicyStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ReportPolicyStatusResponse) => void,
  ): ClientUnaryCall;
  /** Get provider environment for a sandbox (called by sandbox supervisor at startup). */
  getSandboxProviderEnvironment(
    request: GetSandboxProviderEnvironmentRequest,
    callback: (error: ServiceError | null, response: GetSandboxProviderEnvironmentResponse) => void,
  ): ClientUnaryCall;
  getSandboxProviderEnvironment(
    request: GetSandboxProviderEnvironmentRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetSandboxProviderEnvironmentResponse) => void,
  ): ClientUnaryCall;
  getSandboxProviderEnvironment(
    request: GetSandboxProviderEnvironmentRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetSandboxProviderEnvironmentResponse) => void,
  ): ClientUnaryCall;
  /** Fetch recent sandbox logs (one-shot). */
  getSandboxLogs(
    request: GetSandboxLogsRequest,
    callback: (error: ServiceError | null, response: GetSandboxLogsResponse) => void,
  ): ClientUnaryCall;
  getSandboxLogs(
    request: GetSandboxLogsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetSandboxLogsResponse) => void,
  ): ClientUnaryCall;
  getSandboxLogs(
    request: GetSandboxLogsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetSandboxLogsResponse) => void,
  ): ClientUnaryCall;
  /** Push sandbox supervisor logs to the server (client-streaming). */
  pushSandboxLogs(
    callback: (error: ServiceError | null, response: PushSandboxLogsResponse) => void,
  ): ClientWritableStream<PushSandboxLogsRequest>;
  pushSandboxLogs(
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PushSandboxLogsResponse) => void,
  ): ClientWritableStream<PushSandboxLogsRequest>;
  pushSandboxLogs(
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PushSandboxLogsResponse) => void,
  ): ClientWritableStream<PushSandboxLogsRequest>;
  pushSandboxLogs(
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PushSandboxLogsResponse) => void,
  ): ClientWritableStream<PushSandboxLogsRequest>;
  /**
   * Persistent supervisor-to-gateway session (bidirectional streaming).
   *
   * The supervisor opens this stream at startup and keeps it alive for the
   * sandbox lifetime. The gateway uses it to coordinate relay channels for
   * SSH connect, ExecSandbox, and targetable sandbox services. Raw service
   * bytes flow over RelayStream calls (separate HTTP/2 streams on the same
   * connection), not over this stream.
   */
  connectSupervisor(): ClientDuplexStream<SupervisorMessage, GatewayMessage>;
  connectSupervisor(options: Partial<CallOptions>): ClientDuplexStream<SupervisorMessage, GatewayMessage>;
  connectSupervisor(
    metadata: Metadata,
    options?: Partial<CallOptions>,
  ): ClientDuplexStream<SupervisorMessage, GatewayMessage>;
  /**
   * Raw byte relay between supervisor and gateway.
   *
   * The supervisor initiates this call after receiving a RelayOpen message
   * on its ConnectSupervisor stream. The first RelayFrame carries a
   * RelayInit with the channel_id to associate the new HTTP/2 stream with
   * the pending relay slot on the gateway. Subsequent frames carry raw bytes in either
   * direction between the gateway-side waiter (ForwardTcp / exec handler)
   * and the supervisor-side target bridge.
   *
   * This rides the same TCP+TLS+HTTP/2 connection as ConnectSupervisor —
   * no new TLS handshake, no reverse HTTP CONNECT.
   */
  relayStream(): ClientDuplexStream<RelayFrame, RelayFrame>;
  relayStream(options: Partial<CallOptions>): ClientDuplexStream<RelayFrame, RelayFrame>;
  relayStream(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<RelayFrame, RelayFrame>;
  /**
   * Watch a sandbox and stream updates.
   *
   * This stream can include:
   * - Sandbox status snapshots (phase/status)
   * - OpenShell server process logs correlated by sandbox_id
   * - Platform events correlated to the sandbox
   */
  watchSandbox(request: WatchSandboxRequest, options?: Partial<CallOptions>): ClientReadableStream<SandboxStreamEvent>;
  watchSandbox(
    request: WatchSandboxRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<SandboxStreamEvent>;
  /** Submit denial analysis results from sandbox (summaries + proposed chunks). */
  submitPolicyAnalysis(
    request: SubmitPolicyAnalysisRequest,
    callback: (error: ServiceError | null, response: SubmitPolicyAnalysisResponse) => void,
  ): ClientUnaryCall;
  submitPolicyAnalysis(
    request: SubmitPolicyAnalysisRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SubmitPolicyAnalysisResponse) => void,
  ): ClientUnaryCall;
  submitPolicyAnalysis(
    request: SubmitPolicyAnalysisRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SubmitPolicyAnalysisResponse) => void,
  ): ClientUnaryCall;
  /** Get draft policy recommendations for a sandbox. */
  getDraftPolicy(
    request: GetDraftPolicyRequest,
    callback: (error: ServiceError | null, response: GetDraftPolicyResponse) => void,
  ): ClientUnaryCall;
  getDraftPolicy(
    request: GetDraftPolicyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetDraftPolicyResponse) => void,
  ): ClientUnaryCall;
  getDraftPolicy(
    request: GetDraftPolicyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetDraftPolicyResponse) => void,
  ): ClientUnaryCall;
  /** Approve a single draft policy chunk (merges into active policy). */
  approveDraftChunk(
    request: ApproveDraftChunkRequest,
    callback: (error: ServiceError | null, response: ApproveDraftChunkResponse) => void,
  ): ClientUnaryCall;
  approveDraftChunk(
    request: ApproveDraftChunkRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ApproveDraftChunkResponse) => void,
  ): ClientUnaryCall;
  approveDraftChunk(
    request: ApproveDraftChunkRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ApproveDraftChunkResponse) => void,
  ): ClientUnaryCall;
  /** Reject a single draft policy chunk. */
  rejectDraftChunk(
    request: RejectDraftChunkRequest,
    callback: (error: ServiceError | null, response: RejectDraftChunkResponse) => void,
  ): ClientUnaryCall;
  rejectDraftChunk(
    request: RejectDraftChunkRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RejectDraftChunkResponse) => void,
  ): ClientUnaryCall;
  rejectDraftChunk(
    request: RejectDraftChunkRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RejectDraftChunkResponse) => void,
  ): ClientUnaryCall;
  /** Approve all pending draft chunks (skips security-flagged unless forced). */
  approveAllDraftChunks(
    request: ApproveAllDraftChunksRequest,
    callback: (error: ServiceError | null, response: ApproveAllDraftChunksResponse) => void,
  ): ClientUnaryCall;
  approveAllDraftChunks(
    request: ApproveAllDraftChunksRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ApproveAllDraftChunksResponse) => void,
  ): ClientUnaryCall;
  approveAllDraftChunks(
    request: ApproveAllDraftChunksRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ApproveAllDraftChunksResponse) => void,
  ): ClientUnaryCall;
  /** Edit a pending draft chunk in-place (e.g. narrow allowed_ips). */
  editDraftChunk(
    request: EditDraftChunkRequest,
    callback: (error: ServiceError | null, response: EditDraftChunkResponse) => void,
  ): ClientUnaryCall;
  editDraftChunk(
    request: EditDraftChunkRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: EditDraftChunkResponse) => void,
  ): ClientUnaryCall;
  editDraftChunk(
    request: EditDraftChunkRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: EditDraftChunkResponse) => void,
  ): ClientUnaryCall;
  /** Reverse an approval (remove merged rule from active policy). */
  undoDraftChunk(
    request: UndoDraftChunkRequest,
    callback: (error: ServiceError | null, response: UndoDraftChunkResponse) => void,
  ): ClientUnaryCall;
  undoDraftChunk(
    request: UndoDraftChunkRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UndoDraftChunkResponse) => void,
  ): ClientUnaryCall;
  undoDraftChunk(
    request: UndoDraftChunkRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UndoDraftChunkResponse) => void,
  ): ClientUnaryCall;
  /** Clear all pending draft chunks for a sandbox. */
  clearDraftChunks(
    request: ClearDraftChunksRequest,
    callback: (error: ServiceError | null, response: ClearDraftChunksResponse) => void,
  ): ClientUnaryCall;
  clearDraftChunks(
    request: ClearDraftChunksRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ClearDraftChunksResponse) => void,
  ): ClientUnaryCall;
  clearDraftChunks(
    request: ClearDraftChunksRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ClearDraftChunksResponse) => void,
  ): ClientUnaryCall;
  /** Get decision history for a sandbox's draft policy. */
  getDraftHistory(
    request: GetDraftHistoryRequest,
    callback: (error: ServiceError | null, response: GetDraftHistoryResponse) => void,
  ): ClientUnaryCall;
  getDraftHistory(
    request: GetDraftHistoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetDraftHistoryResponse) => void,
  ): ClientUnaryCall;
  getDraftHistory(
    request: GetDraftHistoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetDraftHistoryResponse) => void,
  ): ClientUnaryCall;
}

export const OpenShellClient = makeGenericClientConstructor(OpenShellService, "openshell.v1.OpenShell") as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): OpenShellClient;
  service: typeof OpenShellService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
