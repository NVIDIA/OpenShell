name: Test Install Scripts

on:
  pull_request:
    paths:
      - 'install.sh'
      - 'install-dev.sh'
      - 'e2e/install/**'
      - 'e2e/install-dev/**'
      - '.github/workflows/test-install.yml'
  push:
    branches: [main]
    paths:
      - 'install.sh'
      - 'install-dev.sh'
      - 'e2e/install/**'
      - 'e2e/install-dev/**'
      - '.github/workflows/test-install.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-install:
    name: install.sh (${{ matrix.shell }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - shell: sh
            test: e2e/install/sh_test.sh
            run: sh e2e/install/sh_test.sh
          - shell: bash
            test: e2e/install/bash_test.sh
            run: bash e2e/install/bash_test.sh
          - shell: zsh
            test: e2e/install/zsh_test.sh
            run: zsh e2e/install/zsh_test.sh
            install: zsh
          - shell: fish
            test: e2e/install/fish_test.fish
            run: fish e2e/install/fish_test.fish
            install: fish
          - shell: install-dev-macos
            test: e2e/install-dev/macos_homebrew_test.sh
            run: sh e2e/install-dev/macos_homebrew_test.sh

    steps:
      - uses: actions/checkout@v6

      - name: Install ${{ matrix.shell }}
        if: matrix.install
        run: sudo apt-get update && sudo apt-get install -y ${{ matrix.install }}

      - name: Run tests (${{ matrix.shell }})
        run: ${{ matrix.run }}
